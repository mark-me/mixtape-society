var __index = {"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"Mixtape Society","text":""},{"location":"index.html#introduction","title":"Introduction","text":"<p>Mixtape Society is a private, self\u2011hosted web app to create and share beautiful, add-free music \u2014\u202fthe way mixtapes were exchanged on paper, but with today\u2019s digital convenience.</p>"},{"location":"index.html#why-mixtape-society","title":"\ud83c\udfaf Why Mixtape Society?","text":"<ul> <li>\u2705 No ads, no tracking, no accounts \u2013 pure privacy.</li> <li>\ud83d\udd17 Public share links and QR codes \u2013 anyone can listen without an account.</li> <li>\ud83d\udcfc Cassette\u2011style experience \u2013 nostalgic UI with modern tech.</li> </ul> <p>Quick start</p> <p>\ud83d\udc49 Read the full Getting Started guide</p>"},{"location":"index.html#the-looks","title":"\ud83d\ude0e The looks","text":""},{"location":"index.html#mixtape-editor-user-journey","title":"Mixtape editor user journey","text":"<ul> <li> <p> Login page</p> </li> <li> <p> Manage your mixtape library that's stored locally on your device. Browse, search, share and organize your mixtape collection with ease.</p> </li> <li> <p> Manage your music collection by reviewing statistics and monitoring filesystem changes.</p> </li> <li> <p> Create or edit mixtapes with a live search feature that highlights your tracks as you type</p> </li> <li> <p> Add custom cover art to your mixtapes for a unique look.</p> </li> <li> <p> Add liner notes to your mixtapes for a personal touch.</p> </li> <li> <p> Generate a shareable link or use a QR code for your mixtapes with ease.</p> </li> <li> <p>     Use a QR image to include in physical gift.</p> </li> </ul>"},{"location":"index.html#mixtape-receiver-user-journey","title":"Mixtape receiver user journey","text":"<ul> <li> <p> Receive your mixtape with a public link</p> </li> <li> <p> Listen to your mixtape with a sleek, cassette-inspired player that supports full seeking for FLAC, MP3, M4A, and more.</p> </li> <li> <p> Audio Cassette mode for a nostalgic cassette-inspired player.</p> </li> <li> <p>     Enjoy your mobile nostalgic walkman immersive experience.</p> </li> </ul>"},{"location":"index.html#get-involved","title":"\ud83d\udc65 Get Involved","text":"<p>Interested in where this is headed?</p> <ul> <li>\ud83d\udca1 Browse ideas being explored</li> <li>\ud83c\udfaf See what's planned next</li> <li>\ud83d\udcdd Read what's been shipped</li> <li>\ud83d\udcac Share thoughts on GitHub Discussions</li> <li>\ud83d\udc1b Report issues or open an issue</li> </ul> <p>Pull requests welcome. Still early days, but contributions appreciated.</p> <p>Personal use only</p> <p>This software is a tool for personal, non-commercial use with legally owned music files. No copyrighted music or artwork is included. You are solely responsible for the media you host and share.</p>"},{"location":"development/app.html","title":"Main App","text":""},{"location":"development/app.html#app","title":"App","text":"<p>The file <code>app.py</code> defines the main Flask application for the \"mixtape-society\" project. It is responsible for initializing the web server, configuring the application based on the environment, setting up core services (such as music collection management), and registering routes and blueprints for handling various web requests. The file serves as the entry point for the application, orchestrating authentication, static file serving, and integration with modular route handlers.</p>"},{"location":"development/app.html#highlevel-architecture","title":"\ud83c\udfdb\ufe0f High\u2011Level Architecture","text":"<p>Flask App (<code>create_app() \u2192 Flask()</code>)</p> Core Services Blueprints MusicCollectionUI auth MixtapeManager browser AudioCache play Logger editor Config (BaseConfig) og_cover <p>All routes are mounted under the appropriate URL prefixes (<code>/auth</code>, <code>/mixtapes</code>, <code>/play</code>, <code>/editor</code>, <code>/og</code>).</p>"},{"location":"development/app.html#configuration-environment-selection","title":"\u2699\ufe0f Configuration &amp; Environment Selection","text":"<p><code>app.py</code> determines the configuration class based on the <code>APP_ENV</code> environment variable:</p> <code>APP_ENV</code> value Config class used Typical purpose development DevelopmentConfig Local dev with verbose logging, SQLite DB in <code>data/dev</code>. test TestConfig CI / automated tests (in-memory DB). production ProductionConfig Deployed instance (read-only DB, stricter limits). unset DevelopmentConfig Default fallback. <p>The selected class is instantiated, its <code>ensure_dirs()</code> method creates required directories (<code>MIXTAPE_DIR</code>, <code>CACHE_DIR</code>, <code>LOGS</code>, etc.), and the instance is returned from <code>get_configuration()</code>.</p>"},{"location":"development/app.html#flask-application-creation-create_app-stepbystep","title":"\u2697\ufe0f Flask Application Creation (create_app) \u2013 Step\u2011by\u2011Step","text":"<ol> <li>Instantiate Flask</li> </ol> <pre><code>app = Flask(__name__)\napp.secret_key = config_cls.PASSWORD\napp.config.from_object(config_cls)\nCORS(app)  # adds Access\u2011Control\u2011Allow\u2011Origin: *\n</code></pre> <ol> <li>Rate limiting \u2013 flask_limiter with defaults <code>1000/day</code> and <code>300/hour</code>.</li> <li>Logging \u2013 Calls <code>logger_setup(config)</code> (creates <code>logs/app.log</code>), then mirrors Gunicorn\u2019s logger if running under Gunicorn.</li> <li>Core services<ul> <li><code>MusicCollectionUI</code> \u2013 watches the music root, builds the SQLite FTS5 DB, and exposes search/high\u2011level APIs.</li> <li><code>AudioCache</code> \u2013 instantiated with <code>app.config[\"AUDIO_CACHE_DIR\"]</code>. Stored on <code>app.audio_cache</code> for later blueprint access.</li> <li><code>MixtapeManager</code> \u2013 points at <code>config_cls.MIXTAPE_DIR</code> and receives the collection instance.</li> </ul> </li> <li>Error handler \u2013 Catches <code>DatabaseCorruptionError</code> and returns either JSON (for AJAX) or a rendered <code>database_error.html</code>.</li> <li><code>@app.before_request</code> \u2013 Checks whether an indexing job (<code>rebuilding</code> / <code>resyncing</code>) is in progress; if so, renders <code>indexing.html</code> for authenticated users.</li> <li>Route definitions \u2013 Landing page, indexing\u2011status JSON, collection\u2011stats JSON, resync trigger, robots.txt, cover serving, DB reset, health check.</li> <li>Context processors \u2013 Inject <code>app_version</code>, <code>now</code> (UTC), <code>is_authenticated</code>, and <code>is_indexing</code> into every template.</li> <li>Jinja filter \u2013 <code>to_datetime</code> parses ISO strings or custom formats for display.</li> <li>Blueprint registration \u2013 Auth, Browser, Play, Editor, OG Cover are attached with their respective URL prefixes.</li> <li>Return \u2013 The fully configured <code>app</code> object.</li> </ol>"},{"location":"development/app.html#core-services-dependency-wiring","title":"\ud83d\udd0c Core Services &amp; Dependency Wiring","text":"Service Construction Primary responsibilities MusicCollectionUI (<code>musiclib</code>) <code>MusicCollectionUI(music_root=config_cls.MUSIC_ROOT, db_path=config_cls.DB_PATH, logger=logger)</code> Scans the music folder, builds an SQLite DB with FTS5, provides <code>search_highlighting</code>, <code>get_artist_details</code>, <code>get_album_details</code>, <code>get_cover</code>, <code>count</code>, <code>get_collection_stats</code>. AudioCache (<code>audio_cache</code>) <code>AudioCache(cache_dir=app.config[\"AUDIO_CACHE_DIR\"], logger=logger)</code> Stores transcoded audio files, provides progress tracking via <code>ProgressTracker</code>. MixtapeManager (<code>mixtape_manager</code>) <code>MixtapeManager(path_mixtapes=config_cls.MIXTAPE_DIR, collection=collection)</code> Persists mixtape JSON files, handles slug generation, cover processing, and deletion. Logger <code>get_logger(name=__name__)</code> (via <code>logtools</code>) Centralised structured logging (<code>INFO</code>, <code>WARNING</code>, <code>ERROR</code>). <p>All services are singletons attached to the Flask app (or passed explicitly to blueprints) so that they share the same DB connections and caches.</p>"},{"location":"development/app.html#error-handling-database-corruption","title":"\u26a0\ufe0f Error Handling (Database Corruption)","text":"<pre><code>@app.errorhandler(DatabaseCorruptionError)\ndef handle_database_corruption(e):\n    logger.error(f\"Database corruption detected: {e}\")\n\n    # Detect AJAX request\n    is_ajax = (\n        request.headers.get('X-Requested-With') == 'XMLHttpRequest' or\n        request.headers.get('Accept', '').startswith('application/json') or\n        request.is_json\n    )\n\n    if is_ajax:\n        return jsonify({\n            \"error\": \"database_corrupted\",\n            \"message\": \"The music library database needs to be rebuilt.\",\n            \"requires_reset\": True\n        }), 500\n    else:\n        return render_template('database_error.html'), 500\n</code></pre> <p>If the request is AJAX (or <code>Accept: application/json</code>), a JSON payload is returned; otherwise the user sees a friendly <code>database_error.html</code> page.</p>"},{"location":"development/app.html#requestlevel-hooks","title":"\ud83d\udd17 Request\u2011Level Hooks","text":""},{"location":"development/app.html#indexing-guard-check_indexing_before_request","title":"Indexing Guard (<code>check_indexing_before_request</code>)","text":"<p>Runs before every request (except static assets, <code>/play</code>, <code>/indexing-status</code>, <code>/check-database-health</code>).</p> <ul> <li>If the user is authenticated and the indexing status file (<code>indexing_status.json</code>) reports <code>rebuilding</code> or <code>resyncing</code>, the request is short\u2011circuited to <code>indexing.html</code>.</li> </ul>"},{"location":"development/app.html#authentication-check_auth","title":"Authentication (<code>check_auth</code>)","text":"<p>Used by the guard and by the <code>@require_auth</code> decorator.</p> <ul> <li>If the user is not authenticated, the guard does nothing (letting the view decide whether to redirect)</li> </ul>"},{"location":"development/app.html#public-routes","title":"\ud83c\udf10 Public Routes","text":"Route Method Description <code>/</code> GET Landing page (<code>landing.html</code>). If indexing is active \u2192 <code>indexing.html</code>. If authenticated \u2192 redirect to <code>/mixtapes</code>. <code>/indexing-status</code> GET (exempt from rate limiting) Returns JSON with current indexing progress (used by the front-end polling). <code>/collection-stats</code> GET (auth) Returns JSON with <code>num_artists</code>, <code>num_albums</code>, <code>num_tracks</code>, <code>total_duration</code>, <code>last_added</code>. <code>/resync</code> POST (auth) Starts a background resync of the music library (spawns a daemon thread). Returns <code>{ success: true }</code> or an error. <code>/robots.txt</code> GET Disallows all crawlers (<code>User-agent: *\\nDisallow: /</code>). <code>/covers/&lt;filename&gt;</code> GET Serves cached album cover images from <code>DATA_ROOT/cache/covers</code>. Only <code>.jpg</code>/<code>.jpeg</code>/<code>.png</code> are allowed (security). <code>/reset-database</code> POST (auth) Deletes the SQLite DB files (<code>*.db</code>, <code>*-wal</code>, <code>*-shm</code>, <code>*-journal</code>), recreates the <code>MusicCollectionUI</code> (triggers a rebuild), and returns a JSON summary. <code>/check-database-health</code> GET (auth) Runs a quick <code>PRAGMA quick_check</code> on the DB and returns <code>{ healthy: true/false, error?, requires_reset? }</code>. Used by the front-end health monitor."},{"location":"development/app.html#template-context-processors","title":"\ud83c\udfa8 Template Context Processors","text":"Processor Injected Variable Use Cases <code>inject_version</code> <code>app_version</code> (from <code>utils.get_version()</code>) Displayed in the footer (<code>vX.Y.Z</code>). <code>inject_now</code> <code>now</code> (UTC datetime) Used for timestamps in templates (<code>{{ now }}</code>). <code>inject_auth_status</code> <code>is_authenticated</code> (bool) Conditional UI (login/logout links). <code>inject_indexing_status</code> <code>is_indexing</code> (bool) Disables UI elements while indexing runs. <p>These variables are available in every template without needing to pass them explicitly.</p>"},{"location":"development/app.html#jinja-filter-to_datetime","title":"\u23f3 Jinja Filter \u2013 <code>to_datetime</code>","text":"<pre><code>@app.template_filter(\"to_datetime\")\ndef to_datetime_filter(s, fmt=\"%Y-%m-%d %H:%M:%S\"):\n    if not s:\n        return None\n    try:\n        return datetime.strptime(s, fmt)\n    except ValueError:\n        return datetime.fromisoformat(s.replace(\"Z\", \"+00:00\"))\n</code></pre> <p>Converts a string timestamp (custom format or ISO\u20118601) into a <code>datetime</code> object for formatting (<code>{{ ts|to_datetime(\"%b %d, %Y\") }}</code>).</p>"},{"location":"development/app.html#blueprint-registration","title":"\ud83d\udcdc Blueprint Registration","text":"<pre><code>app.register_blueprint(\n    create_authentication_blueprint(logger=logger, limiter=limiter),\n    url_prefix=\"/auth\",\n)\napp.register_blueprint(\n    create_browser_blueprint(\n        mixtape_manager=mixtape_manager,\n        func_processing_status=get_indexing_status,\n        logger=logger,\n    ),\n    url_prefix=\"/mixtapes\",\n)\napp.register_blueprint(\n    create_play_blueprint(\n        mixtape_manager=mixtape_manager,\n        path_audio_cache=app.config[\"AUDIO_CACHE_DIR\"],\n        logger=logger,\n    ),\n    url_prefix=\"/play\",\n)\napp.register_blueprint(\n    create_editor_blueprint(collection=collection, logger=logger),\n    url_prefix=\"/editor\",\n)\napp.register_blueprint(\n    create_og_cover_blueprint(\n        path_logo=Path(__file__).parent / \"static\" / \"logo.svg\", logger=logger\n    ),\n    url_prefix=\"/og\",\n)\n</code></pre> <p>Each blueprint lives in <code>routes/</code> and encapsulates a coherent feature set (authentication, browsing, playback, editing, Open\u2011Graph cover generation).</p>"},{"location":"development/app.html#application-runner","title":"\ud83c\udfc3\u200d\u2642\ufe0f Application Runner","text":"<pre><code>def serve(debug: bool = True) -&gt; None:\n    app = create_app()\n    app.run(debug=debug, use_reloader=False, host=\"0.0.0.0\", port=5000)\n\nif __name__ == \"__main__\":\n    serve(debug=True)\n</code></pre> Running python <code>app.py</code> starts the development server on port\u202f5000 (accessible from any interface). In production you would typically use Gunicorn or another WSGI server."},{"location":"development/app.html#base-layout-basehtml","title":"\ud83d\uddc2\ufe0f Base Layout (<code>base.html</code>)","text":"<p><code>base.html</code> is the master template that all pages extend. It defines the global UI skeleton, navigation, theme handling, and reusable modals/toasts.</p>"},{"location":"development/app.html#html-skeleton-meta-tags","title":"HTML Skeleton &amp; Meta Tags","text":"<pre><code>&lt;!doctype html&gt;\n&lt;html lang=\"en\" data-bs-theme=\"auto\"&gt;\n&lt;head&gt;\n    &lt;meta charset=\"utf-8\"&gt;\n    &lt;meta name=\"viewport\" content=\"width=device-width, initial-scale=1\"&gt;\n    &lt;title&gt;{% block title %}Mixtape Society{% endblock %}&lt;/title&gt;\n\n    &lt;!-- Bootstrap CSS + Icons --&gt;\n    &lt;link href=\"https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css\" rel=\"stylesheet\"&gt;\n    &lt;link href=\"https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css\" rel=\"stylesheet\"&gt;\n\n    &lt;!-- Custom CSS (global + theme) --&gt;\n    &lt;link href=\"{{ url_for('static', filename='css/base.css') }}\" rel=\"stylesheet\"&gt;\n    {% block extra_css %}{% endblock %}\n&lt;/head&gt;\n&lt;body class=\"{% if is_authenticated %}authenticated{% endif %}\"&gt;\n</code></pre> <p>The <code>data-bs-theme=\"auto\"</code> attribute enables Bootstrap\u2019s built\u2011in dark\u2011mode auto\u2011switching (controlled by the Theme Switcher JS).</p>"},{"location":"development/app.html#navigation-bar","title":"Navigation Bar","text":"<pre><code>&lt;nav class=\"navbar navbar-expand-lg navbar-dark bg-dark sticky-top\"&gt;\n    &lt;div class=\"container-fluid\"&gt;\n        &lt;a class=\"navbar-brand\" href=\"{{ url_for('landing') }}\"&gt;\n            &lt;i class=\"bi bi-music-note-beamed me-2\"&gt;&lt;/i&gt;Mixtape Society\n        &lt;/a&gt;\n\n        &lt;!-- Theme switcher (buttons with data-theme attributes) --&gt;\n        &lt;div class=\"d-flex align-items-center\"&gt;\n            &lt;button class=\"btn btn-sm btn-outline-light me-2\" data-theme=\"light\"&gt;\n                &lt;i class=\"bi bi-sun\"&gt;&lt;/i&gt;\n            &lt;/button&gt;\n            &lt;button class=\"btn btn-sm btn-outline-light me-2\" data-theme=\"dark\"&gt;\n                &lt;i class=\"bi bi-moon-stars\"&gt;&lt;/i&gt;\n            &lt;/button&gt;\n            &lt;button class=\"btn btn-sm btn-outline-light\" data-theme=\"auto\"&gt;\n                &lt;i class=\"bi bi-circle-half\"&gt;&lt;/i&gt;\n            &lt;/button&gt;\n        &lt;/div&gt;\n\n        &lt;button class=\"navbar-toggler\" type=\"button\" data-bs-toggle=\"collapse\"\n                data-bs-target=\"#navbarSupportedContent\" aria-controls=\"navbarSupportedContent\"\n                aria-expanded=\"false\" aria-label=\"Toggle navigation\"&gt;\n            &lt;span class=\"navbar-toggler-icon\"&gt;&lt;/span&gt;\n        &lt;/button&gt;\n\n        &lt;div class=\"collapse navbar-collapse justify-content-end\" id=\"navbarSupportedContent\"&gt;\n            {% if is_authenticated %}\n                &lt;a class=\"nav-link text-light\" href=\"{{ url_for('logout') }}\"&gt;Logout&lt;/a&gt;\n            {% else %}\n                &lt;a class=\"nav-link text-light\" href=\"{{ url_for('login') }}\"&gt;Login&lt;/a&gt;\n            {% endif %}\n        &lt;/div&gt;\n    &lt;/div&gt;\n&lt;/nav&gt;\n</code></pre> <p>Provides quick theme switching (light / dark / auto) and shows Login/Logout depending on <code>is_authenticated</code>.</p>"},{"location":"development/app.html#main-content-slot","title":"Main Content Slot","text":"<pre><code>&lt;main class=\"container my-4\"&gt;\n    {% block content %}{% endblock %}\n&lt;/main&gt;\n</code></pre> <p>All page\u2011specific templates (<code>landing.html</code>, <code>browse_mixtapes.html</code>, <code>editor.html</code>, etc.) inject their markup here.</p>"},{"location":"development/app.html#common-modals","title":"Common Modals","text":""},{"location":"development/app.html#indexing-progress-modal","title":"Indexing Progress Modal","text":"<pre><code>&lt;div class=\"modal fade\" id=\"indexingModal\" tabindex=\"-1\" aria-hidden=\"true\" data-bs-backdrop=\"static\"&gt;\n    &lt;div class=\"modal-dialog modal-dialog-centered\"&gt;\n        &lt;div class=\"modal-content bg-dark text-white\"&gt;\n            &lt;div class=\"modal-header border-0\"&gt;\n                &lt;h5 class=\"modal-title\"&gt;&lt;i class=\"bi bi-hourglass-split me-2\"&gt;&lt;/i&gt;Indexing Library&lt;/h5&gt;\n            &lt;/div&gt;\n            &lt;div class=\"modal-body\"&gt;\n                &lt;p id=\"indexingStatusText\"&gt;Preparing\u2026&lt;/p&gt;\n                &lt;div class=\"progress\"&gt;\n                    &lt;div id=\"indexingProgressBar\" class=\"progress-bar progress-bar-striped progress-bar-animated\"\n                         role=\"progressbar\" style=\"width: 0%\"&gt;&lt;/div&gt;\n                &lt;/div&gt;\n            &lt;/div&gt;\n        &lt;/div&gt;\n    &lt;/div&gt;\n&lt;/div&gt;\n</code></pre> <p>Shown by the <code>check_indexing_before_request</code> hook when the library is rebuilding or resyncing.</p>"},{"location":"development/app.html#database-corruption-modal","title":"Database Corruption Modal","text":"<pre><code>&lt;div class=\"modal fade\" id=\"corruptionModal\" tabindex=\"-1\" aria-hidden=\"true\" data-bs-backdrop=\"static\"&gt;\n    &lt;div class=\"modal-dialog modal-dialog-centered\"&gt;\n        &lt;div class=\"modal-content bg-danger text-white\"&gt;\n            &lt;div class=\"modal-header border-0\"&gt;\n                &lt;h5 class=\"modal-title\"&gt;&lt;i class=\"bi bi-exclamation-triangle-fill me-2\"&gt;&lt;/i&gt;Database Corruption Detected&lt;/h5&gt;\n            &lt;/div&gt;\n            &lt;div class=\"modal-body\"&gt;\n                &lt;p&gt;The music\u2011library database appears to be corrupted and cannot be used.&lt;/p&gt;\n                &lt;p class=\"fw-bold\"&gt;You must reset the database to continue.&lt;/p&gt;\n                &lt;div class=\"d-flex justify-content-end\"&gt;\n                    &lt;button id=\"showResetConfirmBtn\" class=\"btn btn-light me-2\" data-bs-dismiss=\"modal\"&gt;\n                        Cancel\n                    &lt;/button&gt;\n                    &lt;button class=\"btn btn-outline-light\" data-bs-toggle=\"modal\"\n                            data-bs-target=\"#confirmResetModal\"&gt;\n                        Reset Database\n                    &lt;/button&gt;\n                &lt;/div&gt;\n            &lt;/div&gt;\n        &lt;/div&gt;\n    &lt;/div&gt;\n&lt;/div&gt;\n</code></pre> <p>The modal is static (cannot be dismissed by clicking outside) so the user must explicitly choose to reset or cancel.</p>"},{"location":"development/app.html#reset-confirmation-modal","title":"Reset Confirmation Modal","text":"<pre><code>&lt;div class=\"modal fade\" id=\"confirmResetModal\" tabindex=\"-1\" aria-hidden=\"true\" data-bs-backdrop=\"static\"&gt;\n    &lt;div class=\"modal-dialog modal-dialog-centered\"&gt;\n        &lt;div class=\"modal-content bg-dark text-white\"&gt;\n            &lt;div class=\"modal-header border-0\"&gt;\n                &lt;h5 class=\"modal-title\"&gt;&lt;i class=\"bi bi-tools me-2\"&gt;&lt;/i&gt;Confirm Database Reset&lt;/h5&gt;\n            &lt;/div&gt;\n            &lt;div class=\"modal-body\"&gt;\n                &lt;p&gt;This will permanently delete the current SQLite database and start a fresh rebuild.&lt;/p&gt;\n                &lt;p class=\"text-warning fw-bold\"&gt;All existing track metadata will be lost.&lt;/p&gt;\n                &lt;div class=\"d-flex justify-content-end\"&gt;\n                    &lt;button class=\"btn btn-secondary me-2\" data-bs-dismiss=\"modal\"&gt;Cancel&lt;/button&gt;\n                    &lt;button id=\"confirmResetBtn\" class=\"btn btn-danger\"&gt;Yes, Reset Now&lt;/button&gt;\n                &lt;/div&gt;\n            &lt;/div&gt;\n        &lt;/div&gt;\n    &lt;/div&gt;\n&lt;/div&gt;\n</code></pre> <p>Triggered from the Database Corruption modal. The JavaScript in <code>static/js/base/databaseCorruption.js</code> (or its production\u2011optimized variant) handles the POST to <code>/reset-database</code>, shows a loading overlay, and finally redirects to the landing page where the indexing progress is displayed.</p>"},{"location":"development/app.html#loading-overlay-used-during-reset","title":"Loading Overlay (used during reset)","text":"<pre><code>&lt;div id=\"resetLoadingOverlay\" class=\"d-none\"&gt;\n    &lt;div class=\"loading-spinner\"&gt;&lt;/div&gt;\n    &lt;p class=\"mt-3\"&gt;Resetting database\u2026 please wait.&lt;/p&gt;\n&lt;/div&gt;\n</code></pre> <p>The overlay is hidden (<code>d-none</code>) by default. The JS toggles <code>style.display = 'flex'</code> while the reset request is in flight, preventing any further interaction.</p>"},{"location":"development/app.html#toasts-user-feedback","title":"Toasts (User Feedback)","text":"<pre><code>&lt;!-- Copy\u2011to\u2011clipboard toast --&gt;\n&lt;div class=\"position-fixed bottom-0 end-0 p-3\" style=\"z-index: 1080;\"&gt;\n    &lt;div id=\"copyToast\" class=\"toast align-items-center text-bg-success border-0 shadow\" role=\"alert\"&gt;\n        &lt;div class=\"d-flex\"&gt;\n            &lt;div class=\"toast-body\"&gt;&lt;i class=\"bi bi-check2-circle me-2\"&gt;&lt;/i&gt;Public link copied!&lt;/div&gt;\n            &lt;button type=\"button\" class=\"btn-close btn-close-white me-2 m-auto\" data-bs-dismiss=\"toast\"&gt;&lt;/button&gt;\n        &lt;/div&gt;\n    &lt;/div&gt;\n&lt;/div&gt;\n\n&lt;!-- Delete success toast --&gt;\n&lt;div class=\"position-fixed bottom-0 end-0 p-3\" style=\"z-index: 1080;\"&gt;\n    &lt;div id=\"deleteSuccessToast\" class=\"toast align-items-center text-bg-danger border-0 shadow\" role=\"alert\"&gt;\n        &lt;div class=\"d-flex\"&gt;\n            &lt;div class=\"toast-body\"&gt;&lt;i class=\"bi bi-trash-fill me-2\"&gt;&lt;/i&gt;Mixtape deleted successfully&lt;/div&gt;\n            &lt;button type=\"button\" class=\"btn-close btn-close-white me-2 m-auto\" data-bs-dismiss=\"toast\"&gt;&lt;/button&gt;\n        &lt;/div&gt;\n    &lt;/div&gt;\n&lt;/div&gt;\n\n&lt;!-- Add track toast (used by the editor) --&gt;\n&lt;div class=\"position-fixed bottom-0 end-0 p-3\" style=\"z-index: 1080;\"&gt;\n    &lt;div id=\"addTrackToast\" class=\"toast align-items-center text-bg-success border-0 shadow\" role=\"alert\"&gt;\n        &lt;div class=\"d-flex\"&gt;\n            &lt;div class=\"toast-body\"&gt;&lt;i class=\"bi bi-plus-circle me-2\"&gt;&lt;/i&gt;Track added to mixtape!&lt;/div&gt;\n            &lt;button type=\"button\" class=\"btn-close btn-close-white me-2 m-auto\" data-bs-dismiss=\"toast\"&gt;&lt;/button&gt;\n        &lt;/div&gt;\n    &lt;/div&gt;\n&lt;/div&gt;\n\n&lt;!-- Remove track toast (used by the editor) --&gt;\n&lt;div class=\"position-fixed bottom-0 end-0 p-3\" style=\"z-index: 1080;\"&gt;\n    &lt;div id=\"removeTrackToast\" class=\"toast align-items-center text-bg-danger border-0 shadow\" role=\"alert\"&gt;\n        &lt;div class=\"d-flex\"&gt;\n            &lt;div class=\"toast-body\"&gt;&lt;i class=\"bi bi-trash-fill me-2\"&gt;&lt;/i&gt;Track removed from mixtape&lt;/div&gt;\n            &lt;button type=\"button\" class=\"btn-close btn-close-white me-2 m-auto\" data-bs-dismiss=\"toast\"&gt;&lt;/button&gt;\n        &lt;/div&gt;\n    &lt;/div&gt;\n&lt;/div&gt;\n</code></pre> <p>All toasts are Bootstrap 5 components. They are instantiated in the corresponding JavaScript modules (<code>copyToast.js</code>, <code>deleteMixtape.js</code>, <code>editor/ui.js</code>). The <code>position-fixed</code> placement ensures they appear in the bottom\u2011right corner on every page.</p>"},{"location":"development/app.html#footer","title":"Footer","text":"<pre><code>&lt;footer class=\"mt-5 py-4 border-top text-center bg-body-tertiary\"&gt;\n    &lt;div class=\"container\"&gt;\n        &lt;p class=\"mb-1\"&gt;\n            \u00a9 {{ now.year }} Mixtape Society \u2013 v{{ app_version }}\n            &lt;a href=\"https://github.com/yourorg/mixtape-society\" target=\"_blank\" class=\"text-decoration-none ms-2\"&gt;\n                &lt;i class=\"bi bi-github\"&gt;&lt;/i&gt;GitHub\n            &lt;/a&gt;\n        &lt;/p&gt;\n        &lt;p class=\"text-muted small mb-0\"&gt;\n            Theme by &lt;a href=\"https://bootswatch.com/\" target=\"_blank\" class=\"text-muted\"&gt;Bootswatch&lt;/a&gt;.\n            Icons from &lt;a href=\"https://icons.getbootstrap.com/\" target=\"_blank\" class=\"text-muted\"&gt;Bootstrap Icons&lt;/a&gt;.\n        &lt;/p&gt;\n    &lt;/div&gt;\n&lt;/footer&gt;\n</code></pre> <p><code>{{ now.year }}</code> comes from the inject_now context processor. <code>{{ app_version }}</code> is injected by inject_version. The footer is deliberately minimal to keep the focus on the main content.</p>"},{"location":"development/app.html#extra-css-js-includes","title":"Extra CSS / JS Includes","text":"<p>At the very end of <code>base.html</code> (just before <code>&lt;/body&gt;</code>):</p> <pre><code>&lt;!-- Bootstrap Bundle (includes Popper) --&gt;\n&lt;script src=\"https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js\"&gt;&lt;/script&gt;\n\n&lt;!-- Base JavaScript (theme switcher, collection stats, database\u2011corruption handling) --&gt;\n&lt;script type=\"module\" src=\"{{ url_for('static', filename='js/base/index.js') }}\"&gt;&lt;/script&gt;\n\n{% block extra_js %}{% endblock %}\n&lt;/body&gt;\n&lt;/html&gt;\n</code></pre> <p><code>extra_js</code> is a block that child templates can extend (e.g., the editor page loads its own modules). The base <code>index.js</code> pulls in the theme switcher, collection\u2011stats loader, and database\u2011corruption detection.</p>"},{"location":"development/app.html#api","title":"\ud83d\udd0c API","text":""},{"location":"development/app.html#src.app","title":"<code>src.app</code>","text":"<p>Functions:</p> Name Description <code>get_configuration</code> <p>Determines and returns the appropriate configuration class for the current environment.</p> <code>serve</code> <p>Starts the Flask application server.</p>"},{"location":"development/app.html#src.app.get_configuration","title":"<code>get_configuration()</code>","text":"<p>Determines and returns the appropriate configuration class for the current environment.</p> <p>Selects the configuration based on the APP_ENV environment variable, ensures necessary directories exist, and returns the configuration object.</p> <p>Returns:</p> Name Type Description <code>BaseConfig</code> <code>BaseConfig</code> <p>The configuration object for the current environment.</p> Source code in <code>src/app.py</code> <pre><code>def get_configuration() -&gt; BaseConfig:\n    \"\"\"\n    Determines and returns the appropriate configuration class for the current environment.\n\n    Selects the configuration based on the APP_ENV environment variable,\n    ensures necessary directories exist, and returns the configuration object.\n\n    Returns:\n        BaseConfig: The configuration object for the current environment.\n    \"\"\"\n    CONFIG_MAP = {\n        \"development\": DevelopmentConfig,\n        \"test\": TestConfig,\n        \"production\": ProductionConfig,\n    }\n\n    ENV = os.getenv(\"APP_ENV\", \"development\")\n\n    config = CONFIG_MAP.get(ENV, DevelopmentConfig)\n    config.ensure_dirs()\n    return config\n</code></pre>"},{"location":"development/app.html#src.app.serve","title":"<code>serve(debug=True)</code>","text":"<p>Starts the Flask application server.</p> <p>Runs the app on host 0.0.0.0 and port 5000, with debugging enabled or disabled based on the argument.</p> <p>Parameters:</p> Name Type Description Default <code>debug</code> <code>bool</code> <p>Whether to run the server in debug mode. Defaults to True.</p> <code>True</code> Source code in <code>src/app.py</code> <pre><code>def serve(debug: bool = True) -&gt; None:\n    \"\"\"\n    Starts the Flask application server.\n\n    Runs the app on host 0.0.0.0 and port 5000, with debugging enabled or disabled based on the argument.\n\n    Args:\n        debug: Whether to run the server in debug mode. Defaults to True.\n    \"\"\"\n    app = create_app()\n    app.run(debug=debug, use_reloader=False, host=\"0.0.0.0\", port=5000)\n</code></pre>"},{"location":"development/audio_caching.html","title":"Audio Caching","text":""},{"location":"development/audio_caching.html#audio-caching-system","title":"Audio Caching System","text":"<p>The audio\u2011caching subsystem automatically converts large lossless audio files (FLAC, WAV, AIFF, \u2026) into smaller MP3 streams, dramatically reducing bandwidth while preserving a pleasant listening experience.</p> <p>TL;DR \u2013 The cache turns a 40\u202fMB FLAC track into a ~5\u202fMB MP3 (\u2248\u202f87\u202f% bandwidth saving) and serves the MP3 via HTTP range requests.</p>"},{"location":"development/audio_caching.html#overview","title":"\ud83d\udcd6 Overview","text":"<p>When streaming lossless audio over the web, bandwidth quickly becomes a bottleneck:</p> Format Approx. size (4\u2011min track) Typical bitrate FLAC (original) 40\u201150\u202fMB ~1\u202f000\u202fkbps MP3 \u2013 High (256\u202fkbps) \u2248\u202f8\u202fMB 256\u202fkbps MP3 \u2013 Medium (192\u202fkbps) \u2248\u202f5\u202fMB 192\u202fkbps MP3 \u2013 Low (128\u202fkbps) \u2248\u202f3\u202fMB 128\u202fkbps <p>Result: A 4\u2011minute track drops from ~45\u202fMB to ~5\u202fMB (\u2248\u202f87\u202f% bandwidth reduction) with negligible audible loss for casual listening.</p> <p>The cache is transparent to the rest of the application:</p> <ul> <li>The UI requests a track \u2192 the Flask route asks <code>AudioCache</code> for a cached version.</li> <li>If a suitable MP3 exists, it is streamed via HTTP range requests.</li> <li>If not, the original file is streamed (or a background job creates the cache for the next request).</li> </ul>"},{"location":"development/audio_caching.html#architecture-overview","title":"\ud83c\udfdb\ufe0f Architecture Overview","text":"<pre><code>graph TD\n    A[\"AudioCache (core)\"] --&gt; B[\"Cache Path Generation\"]\n    A --&gt; C[\"Transcoding (ffmpeg)\"]\n    A --&gt; D[\"Cache Management (size, cleanup)\"]\n\n    E[CacheWorker] --&gt; A\n    E --&gt; F[\"ThreadPool (parallel batch)\"]\n    E --&gt; G[\"ProgressTracker (SSE)\"]\n\n    H[ProgressTracker] --&gt; I[\"Frontend (EventSource)\"]</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom  <ul> <li><code>audio_cache.py</code> \u2013 core logic (hash\u2011based filenames, transcoding, cache look\u2011ups).</li> <li><code>cache_worker.py</code> \u2013 batch processing, thread\u2011pool parallelism, progress callbacks.</li> <li><code>progress_tracker.py</code> \u2013 Server\u2011Sent Events (SSE) emitter that feeds the UI\u2019s \u201ccaching progress\u201d modal.</li> </ul>"},{"location":"development/audio_caching.html#key-features","title":"\u2728 Key Features","text":"Feature Description Automatic transcoding FLAC, WAV, AIFF, APE, ALAC \u2192 MP3 (high/medium/low). Multiple quality levels <code>high</code> (256 kbps), <code>medium</code> (192 kbps), <code>low</code> (128 kbps). Smart caching Only creates a cached file when the source is lossless and the cache is missing/out-of-date. Pre-caching on upload When a mixtape is saved, the system can generate caches automatically. Parallel batch processing Thread-pool (configurable workers) for fast bulk transcoding. Progress tracking Real-time SSE updates displayed in a Bootstrap modal. Cache management utilities Size calculation, age-based cleanup, full purge. Config-driven All knobs live in <code>src/config/config.py</code> (<code>AUDIO_CACHE_*</code>)."},{"location":"development/audio_caching.html#how-it-works-stepbystep","title":"\ud83d\udccb How It Works (Step\u2011by\u2011Step)","text":""},{"location":"development/audio_caching.html#cache-path-generation","title":"Cache Path Generation","text":"<pre><code>flowchart LR\n    A[Original file path] --&gt; B[Normalize &amp; resolve]\n    B --&gt; C[MD5 hash of full path]\n    C --&gt; D[Compose filename: `&lt;hash&gt;_&lt;quality&gt;_&lt;bitrate&gt;.mp3`]\n    D --&gt; E[\"Cache directory (`AUDIO_CACHE_DIR`)\"]</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom  <ul> <li>The hash guarantees collision\u2011free filenames, even for identically named tracks in different folders.</li> <li>Example:     Original: <code>/music/Radiohead/OK Computer/01 Airbag.flac</code> Hash: <code>a1b2c3\u2026</code> \u2192 Cache file <code>a1b2c3_medium_192k.mp3</code>.</li> </ul>"},{"location":"development/audio_caching.html#transcoding-flow","title":"Transcoding Flow","text":"<pre><code>sequenceDiagram\n    participant UI\n    participant Flask\n    participant CacheWorker\n    participant AudioCache\n    participant ffmpeg\n\n    UI-&gt;&gt;Flask: Request play (quality=medium)\n    Flask-&gt;&gt;AudioCache: get_cached_or_original()\n    alt Cached version exists\n        AudioCache--&gt;&gt;Flask: Return cached path\n    else No cache\n        AudioCache-&gt;&gt;CacheWorker: transcode_file()\n        CacheWorker-&gt;&gt;ffmpeg: Run ffmpeg command\n        ffmpeg--&gt;&gt;CacheWorker: MP3 file created\n        CacheWorker-&gt;&gt;AudioCache: Store in cache dir\n        AudioCache--&gt;&gt;Flask: Return newly cached path\n    end\n    Flask-&gt;&gt;UI: Stream MP3</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom  <ul> <li>If a cached file is present, it is served immediately.</li> <li>Otherwise the worker spawns ffmpeg, writes the MP3, and returns the new path.</li> </ul>"},{"location":"development/audio_caching.html#playback-flow","title":"Playback Flow","text":"<pre><code>graph LR\n    A[User clicks Play] --&gt; B{Quality selected?}\n    B --&gt;|Original| C[Serve original FLAC]\n    B --&gt;|High/Med/Low| D{Is source lossless?}\n    D --&gt;|No| C\n    D --&gt;|Yes| E{Cache exists?}\n    E --&gt;|Yes| F[Serve cached MP3]\n    E --&gt;|No| G[Log warning \u2192 fall back to original]\n    F --&gt; H[User streams small file]\n    C --&gt; I[User streams large file]</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom"},{"location":"development/audio_caching.html#api-reference","title":"\ud83d\udd0c API Reference","text":""},{"location":"development/audio_caching.html#audiocache-core","title":"AudioCache (core)","text":""},{"location":"development/audio_caching.html#src.audio_cache.audio_cache.AudioCache","title":"<code>src.audio_cache.audio_cache.AudioCache</code>","text":"<p>Manages audio file transcoding and caching for bandwidth optimization.</p> <p>Provides methods to check for cached versions, generate transcoded files, and manage the cache directory.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Initialize the AudioCache manager.</p> <code>clear_cache</code> <p>Clear cached files, optionally only those older than specified days.</p> <code>get_cache_path</code> <p>Generate a cache filename based on the original path and quality level.</p> <code>get_cache_size</code> <p>Calculate total size of the cache directory in bytes.</p> <code>get_cached_or_original</code> <p>Get the cached version if available, otherwise return original path.</p> <code>is_cached</code> <p>Check if a cached version exists and is up-to-date.</p> <code>precache_file</code> <p>Pre-generate cached versions at multiple quality levels.</p> <code>should_transcode</code> <p>Determine if a file should be transcoded based on its format.</p> <code>transcode_file</code> <p>Transcode an audio file to a cached version.</p> Source code in <code>src/audio_cache/audio_cache.py</code> <pre><code>class AudioCache:\n    \"\"\"\n    Manages audio file transcoding and caching for bandwidth optimization.\n\n    Provides methods to check for cached versions, generate transcoded files,\n    and manage the cache directory.\n    \"\"\"\n\n    def __init__(self, cache_dir: Path, logger: Logger | None = None):\n        \"\"\"\n        Initialize the AudioCache manager.\n\n        Args:\n            cache_dir: Directory where cached transcoded files will be stored.\n            logger: Optional logger for tracking operations.\n        \"\"\"\n        self.cache_dir = Path(cache_dir)\n        self.cache_dir.mkdir(parents=True, exist_ok=True)\n        self.logger = logger or NullLogger()\n\n    def _normalize_path(self, path: Path) -&gt; str:\n        \"\"\"\n        Normalize a path for consistent hashing.\n\n        This ensures that the same file always produces the same hash,\n        regardless of how the path was constructed.\n\n        Args:\n            path: Path to normalize\n\n        Returns:\n            Normalized path string for hashing\n        \"\"\"\n        try:\n            # Try to resolve to absolute path\n            normalized = path.resolve()\n            return str(normalized)\n        except Exception as e:\n            # If resolve fails (e.g., file doesn't exist yet), use absolute\n            self.logger.debug(f\"Path resolve failed for {path}, using absolute: {e}\")\n            return str(path.absolute())\n\n    def get_cache_path(\n        self, original_path: Path, quality: QualityLevel = \"medium\"\n    ) -&gt; Path:\n        \"\"\"\n        Generate a cache filename based on the original path and quality level.\n\n        Args:\n            original_path: Path to the original audio file.\n            quality: Quality level for transcoding (high, medium, low).\n\n        Returns:\n            Path to the cached file location.\n        \"\"\"\n        if quality == \"original\":\n            return original_path\n\n        # Create unique hash from the normalized file path\n        path_str = self._normalize_path(original_path)\n        path_hash = hashlib.md5(path_str.encode()).hexdigest()\n\n        # Get quality settings\n        settings = QUALITY_SETTINGS[quality]\n        bitrate = settings[\"bitrate\"]\n        ext = settings[\"format\"]\n\n        cache_filename = f\"{path_hash}_{quality}_{bitrate}.{ext}\"\n\n        # Log for debugging\n        self.logger.debug(\n            f\"Cache path generation: {original_path.name} -&gt; {cache_filename} \"\n            f\"(hash of: {path_str})\"\n        )\n\n        return self.cache_dir / cache_filename\n\n    def should_transcode(self, file_path: Path) -&gt; bool:\n        \"\"\"\n        Determine if a file should be transcoded based on its format.\n\n        Args:\n            file_path: Path to the audio file.\n\n        Returns:\n            True if the file should be transcoded, False otherwise.\n        \"\"\"\n        # Transcode lossless formats that are bandwidth-heavy\n        transcode_formats = {\".flac\", \".wav\", \".aiff\", \".ape\", \".alac\"}\n        return file_path.suffix.lower() in transcode_formats\n\n    def is_cached(self, original_path: Path, quality: QualityLevel = \"medium\") -&gt; bool:\n        \"\"\"\n        Check if a cached version exists and is up-to-date.\n\n        Args:\n            original_path: Path to the original audio file.\n            quality: Quality level to check.\n\n        Returns:\n            True if a valid cached version exists, False otherwise.\n        \"\"\"\n        if quality == \"original\" or not self.should_transcode(original_path):\n            return True\n\n        cache_path = self.get_cache_path(original_path, quality)\n\n        if not cache_path.exists():\n            self.logger.debug(f\"Cache miss: {cache_path.name} does not exist\")\n            return False\n\n        # Check if cache is newer than original\n        try:\n            cache_mtime = cache_path.stat().st_mtime\n\n            # Only check mtime if original file exists\n            if original_path.exists():\n                original_mtime = original_path.stat().st_mtime\n                if cache_mtime &lt; original_mtime:\n                    self.logger.debug(\n                        f\"Cache outdated: {cache_path.name} is older than source\"\n                    )\n                    return False\n\n            self.logger.debug(f\"Cache hit: {cache_path.name}\")\n            return True\n\n        except OSError as e:\n            self.logger.warning(f\"Error checking cache status for {cache_path}: {e}\")\n            return False\n\n    def transcode_file(\n        self,\n        original_path: Path,\n        quality: QualityLevel = \"medium\",\n        overwrite: bool = False\n    ) -&gt; Path:\n        \"\"\"\n        Transcode an audio file to a cached version.\n\n        Args:\n            original_path: Path to the original audio file.\n            quality: Quality level for transcoding.\n            overwrite: If True, regenerate cache even if it exists.\n\n        Returns:\n            Path to the transcoded file (or original if no transcoding needed).\n\n        Raises:\n            subprocess.CalledProcessError: If ffmpeg transcoding fails.\n            FileNotFoundError: If the original file doesn't exist.\n        \"\"\"\n        if quality == \"original\" or not self.should_transcode(original_path):\n            return original_path\n\n        if not original_path.exists():\n            raise FileNotFoundError(f\"Original file not found: {original_path}\")\n\n        cache_path = self.get_cache_path(original_path, quality)\n\n        # Check if we need to transcode\n        if not overwrite and self.is_cached(original_path, quality):\n            self.logger.debug(f\"Using existing cache: {cache_path}\")\n            return cache_path\n\n        # Get transcoding settings\n        settings = QUALITY_SETTINGS[quality]\n        bitrate = settings[\"bitrate\"]\n\n        self.logger.debug(f\"Transcoding {original_path.name} to {quality} quality ({bitrate})\")\n\n        # Build ffmpeg command\n        cmd = [\n            \"ffmpeg\",\n            \"-y\",  # Overwrite output file\n            \"-i\", str(original_path),\n            \"-vn\",  # No video\n            \"-ar\", \"44100\",  # Sample rate\n            \"-ac\", \"2\",  # Stereo\n            \"-b:a\", bitrate,  # Target bitrate\n            \"-map_metadata\", \"0\",  # Copy metadata\n            \"-id3v2_version\", \"3\",  # ID3v2.3 for better compatibility\n            str(cache_path),\n        ]\n\n        try:\n            subprocess.run(\n                cmd,\n                capture_output=True,\n                check=True,\n                text=True,\n            )\n            self.logger.debug(f\"Successfully cached: {cache_path}\")\n            return cache_path\n\n        except subprocess.CalledProcessError as e:\n            self.logger.error(f\"Transcoding failed for {original_path}: {e.stderr}\")\n            # Clean up partial file if it exists\n            if cache_path.exists():\n                cache_path.unlink()\n            raise\n\n    def get_cached_or_original(\n        self, original_path: Path, quality: QualityLevel = \"medium\"\n    ) -&gt; Path:\n        \"\"\"\n        Get the cached version if available, otherwise return original path.\n\n        This method does NOT generate a cache if it doesn't exist.\n        Use transcode_file() for that purpose.\n\n        Args:\n            original_path: Path to the original audio file.\n            quality: Quality level to retrieve.\n\n        Returns:\n            Path to cached version if available, otherwise original path.\n        \"\"\"\n        if quality == \"original\" or not self.should_transcode(original_path):\n            return original_path\n\n        cache_path = self.get_cache_path(original_path, quality)\n\n        return cache_path if self.is_cached(original_path, quality) else original_path\n\n    def precache_file(\n        self,\n        original_path: Path,\n        qualities: list[QualityLevel] = None\n    ) -&gt; dict[QualityLevel, Path]:\n        \"\"\"\n        Pre-generate cached versions at multiple quality levels.\n\n        Args:\n            original_path: Path to the original audio file.\n            qualities: List of quality levels to generate. Defaults to [\"medium\"].\n\n        Returns:\n            Dictionary mapping quality levels to their cached paths.\n        \"\"\"\n        if qualities is None:\n            qualities = [\"medium\"]\n\n        results = {}\n\n        for quality in qualities:\n            if quality == \"original\":\n                results[quality] = original_path\n                continue\n\n            try:\n                cached_path = self.transcode_file(original_path, quality)\n                results[quality] = cached_path\n            except Exception as e:\n                self.logger.error(f\"Failed to precache {quality} version: {e}\")\n                results[quality] = original_path\n\n        return results\n\n    def get_cache_size(self) -&gt; int:\n        \"\"\"\n        Calculate total size of the cache directory in bytes.\n\n        Returns:\n            Total size in bytes.\n        \"\"\"\n        total_size = sum(\n            file_path.stat().st_size\n            for file_path in self.cache_dir.rglob(\"*\")\n            if file_path.is_file()\n        )\n        return total_size\n\n    def clear_cache(self, older_than_days: int | None = None) -&gt; int:\n        \"\"\"\n        Clear cached files, optionally only those older than specified days.\n\n        Args:\n            older_than_days: If specified, only delete files older than this many days.\n\n        Returns:\n            Number of files deleted.\n        \"\"\"\n        import time\n\n        deleted_count = 0\n        current_time = time.time()\n\n        for file_path in self.cache_dir.rglob(\"*.mp3\"):\n            if file_path.is_file():\n                should_delete = True\n\n                if older_than_days is not None:\n                    file_age_days = (current_time - file_path.stat().st_mtime) / 86400\n                    should_delete = file_age_days &gt; older_than_days\n\n                if should_delete:\n                    try:\n                        file_path.unlink()\n                        deleted_count += 1\n                        self.logger.debug(f\"Deleted cached file: {file_path}\")\n                    except OSError as e:\n                        self.logger.error(f\"Failed to delete {file_path}: {e}\")\n\n        self.logger.info(f\"Cache cleanup: deleted {deleted_count} files\")\n        return deleted_count\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.audio_cache.AudioCache.__init__","title":"<code>__init__(cache_dir, logger=None)</code>","text":"<p>Initialize the AudioCache manager.</p> <p>Parameters:</p> Name Type Description Default <code>cache_dir</code> <code>Path</code> <p>Directory where cached transcoded files will be stored.</p> required <code>logger</code> <code>Logger | None</code> <p>Optional logger for tracking operations.</p> <code>None</code> Source code in <code>src/audio_cache/audio_cache.py</code> <pre><code>def __init__(self, cache_dir: Path, logger: Logger | None = None):\n    \"\"\"\n    Initialize the AudioCache manager.\n\n    Args:\n        cache_dir: Directory where cached transcoded files will be stored.\n        logger: Optional logger for tracking operations.\n    \"\"\"\n    self.cache_dir = Path(cache_dir)\n    self.cache_dir.mkdir(parents=True, exist_ok=True)\n    self.logger = logger or NullLogger()\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.audio_cache.AudioCache.clear_cache","title":"<code>clear_cache(older_than_days=None)</code>","text":"<p>Clear cached files, optionally only those older than specified days.</p> <p>Parameters:</p> Name Type Description Default <code>older_than_days</code> <code>int | None</code> <p>If specified, only delete files older than this many days.</p> <code>None</code> <p>Returns:</p> Type Description <code>int</code> <p>Number of files deleted.</p> Source code in <code>src/audio_cache/audio_cache.py</code> <pre><code>def clear_cache(self, older_than_days: int | None = None) -&gt; int:\n    \"\"\"\n    Clear cached files, optionally only those older than specified days.\n\n    Args:\n        older_than_days: If specified, only delete files older than this many days.\n\n    Returns:\n        Number of files deleted.\n    \"\"\"\n    import time\n\n    deleted_count = 0\n    current_time = time.time()\n\n    for file_path in self.cache_dir.rglob(\"*.mp3\"):\n        if file_path.is_file():\n            should_delete = True\n\n            if older_than_days is not None:\n                file_age_days = (current_time - file_path.stat().st_mtime) / 86400\n                should_delete = file_age_days &gt; older_than_days\n\n            if should_delete:\n                try:\n                    file_path.unlink()\n                    deleted_count += 1\n                    self.logger.debug(f\"Deleted cached file: {file_path}\")\n                except OSError as e:\n                    self.logger.error(f\"Failed to delete {file_path}: {e}\")\n\n    self.logger.info(f\"Cache cleanup: deleted {deleted_count} files\")\n    return deleted_count\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.audio_cache.AudioCache.get_cache_path","title":"<code>get_cache_path(original_path, quality='medium')</code>","text":"<p>Generate a cache filename based on the original path and quality level.</p> <p>Parameters:</p> Name Type Description Default <code>original_path</code> <code>Path</code> <p>Path to the original audio file.</p> required <code>quality</code> <code>QualityLevel</code> <p>Quality level for transcoding (high, medium, low).</p> <code>'medium'</code> <p>Returns:</p> Type Description <code>Path</code> <p>Path to the cached file location.</p> Source code in <code>src/audio_cache/audio_cache.py</code> <pre><code>def get_cache_path(\n    self, original_path: Path, quality: QualityLevel = \"medium\"\n) -&gt; Path:\n    \"\"\"\n    Generate a cache filename based on the original path and quality level.\n\n    Args:\n        original_path: Path to the original audio file.\n        quality: Quality level for transcoding (high, medium, low).\n\n    Returns:\n        Path to the cached file location.\n    \"\"\"\n    if quality == \"original\":\n        return original_path\n\n    # Create unique hash from the normalized file path\n    path_str = self._normalize_path(original_path)\n    path_hash = hashlib.md5(path_str.encode()).hexdigest()\n\n    # Get quality settings\n    settings = QUALITY_SETTINGS[quality]\n    bitrate = settings[\"bitrate\"]\n    ext = settings[\"format\"]\n\n    cache_filename = f\"{path_hash}_{quality}_{bitrate}.{ext}\"\n\n    # Log for debugging\n    self.logger.debug(\n        f\"Cache path generation: {original_path.name} -&gt; {cache_filename} \"\n        f\"(hash of: {path_str})\"\n    )\n\n    return self.cache_dir / cache_filename\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.audio_cache.AudioCache.get_cache_size","title":"<code>get_cache_size()</code>","text":"<p>Calculate total size of the cache directory in bytes.</p> <p>Returns:</p> Type Description <code>int</code> <p>Total size in bytes.</p> Source code in <code>src/audio_cache/audio_cache.py</code> <pre><code>def get_cache_size(self) -&gt; int:\n    \"\"\"\n    Calculate total size of the cache directory in bytes.\n\n    Returns:\n        Total size in bytes.\n    \"\"\"\n    total_size = sum(\n        file_path.stat().st_size\n        for file_path in self.cache_dir.rglob(\"*\")\n        if file_path.is_file()\n    )\n    return total_size\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.audio_cache.AudioCache.get_cached_or_original","title":"<code>get_cached_or_original(original_path, quality='medium')</code>","text":"<p>Get the cached version if available, otherwise return original path.</p> <p>This method does NOT generate a cache if it doesn't exist. Use transcode_file() for that purpose.</p> <p>Parameters:</p> Name Type Description Default <code>original_path</code> <code>Path</code> <p>Path to the original audio file.</p> required <code>quality</code> <code>QualityLevel</code> <p>Quality level to retrieve.</p> <code>'medium'</code> <p>Returns:</p> Type Description <code>Path</code> <p>Path to cached version if available, otherwise original path.</p> Source code in <code>src/audio_cache/audio_cache.py</code> <pre><code>def get_cached_or_original(\n    self, original_path: Path, quality: QualityLevel = \"medium\"\n) -&gt; Path:\n    \"\"\"\n    Get the cached version if available, otherwise return original path.\n\n    This method does NOT generate a cache if it doesn't exist.\n    Use transcode_file() for that purpose.\n\n    Args:\n        original_path: Path to the original audio file.\n        quality: Quality level to retrieve.\n\n    Returns:\n        Path to cached version if available, otherwise original path.\n    \"\"\"\n    if quality == \"original\" or not self.should_transcode(original_path):\n        return original_path\n\n    cache_path = self.get_cache_path(original_path, quality)\n\n    return cache_path if self.is_cached(original_path, quality) else original_path\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.audio_cache.AudioCache.is_cached","title":"<code>is_cached(original_path, quality='medium')</code>","text":"<p>Check if a cached version exists and is up-to-date.</p> <p>Parameters:</p> Name Type Description Default <code>original_path</code> <code>Path</code> <p>Path to the original audio file.</p> required <code>quality</code> <code>QualityLevel</code> <p>Quality level to check.</p> <code>'medium'</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if a valid cached version exists, False otherwise.</p> Source code in <code>src/audio_cache/audio_cache.py</code> <pre><code>def is_cached(self, original_path: Path, quality: QualityLevel = \"medium\") -&gt; bool:\n    \"\"\"\n    Check if a cached version exists and is up-to-date.\n\n    Args:\n        original_path: Path to the original audio file.\n        quality: Quality level to check.\n\n    Returns:\n        True if a valid cached version exists, False otherwise.\n    \"\"\"\n    if quality == \"original\" or not self.should_transcode(original_path):\n        return True\n\n    cache_path = self.get_cache_path(original_path, quality)\n\n    if not cache_path.exists():\n        self.logger.debug(f\"Cache miss: {cache_path.name} does not exist\")\n        return False\n\n    # Check if cache is newer than original\n    try:\n        cache_mtime = cache_path.stat().st_mtime\n\n        # Only check mtime if original file exists\n        if original_path.exists():\n            original_mtime = original_path.stat().st_mtime\n            if cache_mtime &lt; original_mtime:\n                self.logger.debug(\n                    f\"Cache outdated: {cache_path.name} is older than source\"\n                )\n                return False\n\n        self.logger.debug(f\"Cache hit: {cache_path.name}\")\n        return True\n\n    except OSError as e:\n        self.logger.warning(f\"Error checking cache status for {cache_path}: {e}\")\n        return False\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.audio_cache.AudioCache.precache_file","title":"<code>precache_file(original_path, qualities=None)</code>","text":"<p>Pre-generate cached versions at multiple quality levels.</p> <p>Parameters:</p> Name Type Description Default <code>original_path</code> <code>Path</code> <p>Path to the original audio file.</p> required <code>qualities</code> <code>list[QualityLevel]</code> <p>List of quality levels to generate. Defaults to [\"medium\"].</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[QualityLevel, Path]</code> <p>Dictionary mapping quality levels to their cached paths.</p> Source code in <code>src/audio_cache/audio_cache.py</code> <pre><code>def precache_file(\n    self,\n    original_path: Path,\n    qualities: list[QualityLevel] = None\n) -&gt; dict[QualityLevel, Path]:\n    \"\"\"\n    Pre-generate cached versions at multiple quality levels.\n\n    Args:\n        original_path: Path to the original audio file.\n        qualities: List of quality levels to generate. Defaults to [\"medium\"].\n\n    Returns:\n        Dictionary mapping quality levels to their cached paths.\n    \"\"\"\n    if qualities is None:\n        qualities = [\"medium\"]\n\n    results = {}\n\n    for quality in qualities:\n        if quality == \"original\":\n            results[quality] = original_path\n            continue\n\n        try:\n            cached_path = self.transcode_file(original_path, quality)\n            results[quality] = cached_path\n        except Exception as e:\n            self.logger.error(f\"Failed to precache {quality} version: {e}\")\n            results[quality] = original_path\n\n    return results\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.audio_cache.AudioCache.should_transcode","title":"<code>should_transcode(file_path)</code>","text":"<p>Determine if a file should be transcoded based on its format.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>Path</code> <p>Path to the audio file.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the file should be transcoded, False otherwise.</p> Source code in <code>src/audio_cache/audio_cache.py</code> <pre><code>def should_transcode(self, file_path: Path) -&gt; bool:\n    \"\"\"\n    Determine if a file should be transcoded based on its format.\n\n    Args:\n        file_path: Path to the audio file.\n\n    Returns:\n        True if the file should be transcoded, False otherwise.\n    \"\"\"\n    # Transcode lossless formats that are bandwidth-heavy\n    transcode_formats = {\".flac\", \".wav\", \".aiff\", \".ape\", \".alac\"}\n    return file_path.suffix.lower() in transcode_formats\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.audio_cache.AudioCache.transcode_file","title":"<code>transcode_file(original_path, quality='medium', overwrite=False)</code>","text":"<p>Transcode an audio file to a cached version.</p> <p>Parameters:</p> Name Type Description Default <code>original_path</code> <code>Path</code> <p>Path to the original audio file.</p> required <code>quality</code> <code>QualityLevel</code> <p>Quality level for transcoding.</p> <code>'medium'</code> <code>overwrite</code> <code>bool</code> <p>If True, regenerate cache even if it exists.</p> <code>False</code> <p>Returns:</p> Type Description <code>Path</code> <p>Path to the transcoded file (or original if no transcoding needed).</p> <p>Raises:</p> Type Description <code>CalledProcessError</code> <p>If ffmpeg transcoding fails.</p> <code>FileNotFoundError</code> <p>If the original file doesn't exist.</p> Source code in <code>src/audio_cache/audio_cache.py</code> <pre><code>def transcode_file(\n    self,\n    original_path: Path,\n    quality: QualityLevel = \"medium\",\n    overwrite: bool = False\n) -&gt; Path:\n    \"\"\"\n    Transcode an audio file to a cached version.\n\n    Args:\n        original_path: Path to the original audio file.\n        quality: Quality level for transcoding.\n        overwrite: If True, regenerate cache even if it exists.\n\n    Returns:\n        Path to the transcoded file (or original if no transcoding needed).\n\n    Raises:\n        subprocess.CalledProcessError: If ffmpeg transcoding fails.\n        FileNotFoundError: If the original file doesn't exist.\n    \"\"\"\n    if quality == \"original\" or not self.should_transcode(original_path):\n        return original_path\n\n    if not original_path.exists():\n        raise FileNotFoundError(f\"Original file not found: {original_path}\")\n\n    cache_path = self.get_cache_path(original_path, quality)\n\n    # Check if we need to transcode\n    if not overwrite and self.is_cached(original_path, quality):\n        self.logger.debug(f\"Using existing cache: {cache_path}\")\n        return cache_path\n\n    # Get transcoding settings\n    settings = QUALITY_SETTINGS[quality]\n    bitrate = settings[\"bitrate\"]\n\n    self.logger.debug(f\"Transcoding {original_path.name} to {quality} quality ({bitrate})\")\n\n    # Build ffmpeg command\n    cmd = [\n        \"ffmpeg\",\n        \"-y\",  # Overwrite output file\n        \"-i\", str(original_path),\n        \"-vn\",  # No video\n        \"-ar\", \"44100\",  # Sample rate\n        \"-ac\", \"2\",  # Stereo\n        \"-b:a\", bitrate,  # Target bitrate\n        \"-map_metadata\", \"0\",  # Copy metadata\n        \"-id3v2_version\", \"3\",  # ID3v2.3 for better compatibility\n        str(cache_path),\n    ]\n\n    try:\n        subprocess.run(\n            cmd,\n            capture_output=True,\n            check=True,\n            text=True,\n        )\n        self.logger.debug(f\"Successfully cached: {cache_path}\")\n        return cache_path\n\n    except subprocess.CalledProcessError as e:\n        self.logger.error(f\"Transcoding failed for {original_path}: {e.stderr}\")\n        # Clean up partial file if it exists\n        if cache_path.exists():\n            cache_path.unlink()\n        raise\n</code></pre>"},{"location":"development/audio_caching.html#cacheworker-batch-async","title":"CacheWorker (batch &amp; async)","text":""},{"location":"development/audio_caching.html#src.audio_cache.cache_worker.CacheWorker","title":"<code>src.audio_cache.cache_worker.CacheWorker</code>","text":"<p>Worker for pre-caching audio files in the background.</p> <p>Provides methods to cache individual files or entire mixtapes at specified quality levels using thread pools for parallel processing.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Initialize the cache worker.</p> <code>cache_mixtape</code> <p>Cache all audio files in a mixtape.</p> <code>cache_mixtape_async</code> <p>Cache all audio files in a mixtape using parallel processing.</p> <code>cache_single_file</code> <p>Cache a single audio file at specified quality levels.</p> <code>regenerate_outdated_cache</code> <p>Regenerate cached versions that are older than their source files.</p> <code>verify_mixtape_cache</code> <p>Verify which tracks in a mixtape have valid cached versions.</p> Source code in <code>src/audio_cache/cache_worker.py</code> <pre><code>class CacheWorker:\n    \"\"\"\n    Worker for pre-caching audio files in the background.\n\n    Provides methods to cache individual files or entire mixtapes at specified\n    quality levels using thread pools for parallel processing.\n    \"\"\"\n\n    def __init__(\n        self,\n        audio_cache: AudioCache,\n        logger: Logger | None = None,\n        max_workers: int = 4,\n    ):\n        \"\"\"\n        Initialize the cache worker.\n\n        Args:\n            audio_cache: AudioCache instance for transcoding operations.\n            logger: Optional logger for tracking operations.\n            max_workers: Maximum number of parallel transcoding threads.\n        \"\"\"\n        self.audio_cache = audio_cache\n        self.logger = logger or NullLogger()\n        self.max_workers = max_workers\n\n    def cache_single_file(\n        self,\n        file_path: Path,\n        qualities: list[QualityLevel] = None,\n    ) -&gt; dict[QualityLevel, bool]:\n        \"\"\"\n        Cache a single audio file at specified quality levels.\n\n        Args:\n            file_path: Path to the audio file.\n            qualities: List of quality levels to cache. Defaults to [\"medium\"].\n\n        Returns:\n            Dictionary mapping quality levels to success status.\n        \"\"\"\n        if qualities is None:\n            qualities = [\"medium\"]\n\n        results = {}\n\n        for quality in qualities:\n            if quality == \"original\":\n                results[quality] = True\n                continue\n\n            try:\n                self.audio_cache.transcode_file(file_path, quality)\n                results[quality] = True\n                self.logger.debug(f\"Cached {file_path.name} at {quality} quality\")\n            except Exception as e:\n                results[quality] = False\n                self.logger.error(f\"Failed to cache {file_path.name} at {quality}: {e}\")\n\n        return results\n\n    def cache_mixtape(\n        self,\n        track_paths: list[Path],\n        qualities: list[QualityLevel] = None,\n        progress_callback: Callable[[int, int], None] | None = None,\n    ) -&gt; dict[str, dict]:\n        \"\"\"\n        Cache all audio files in a mixtape.\n\n        Args:\n            track_paths: List of paths to audio files in the mixtape.\n            qualities: Quality levels to cache. Defaults to [\"medium\"].\n            progress_callback: Optional callback function(current, total) for progress updates.\n\n        Returns:\n            Dictionary with results for each file.\n        \"\"\"\n        if qualities is None:\n            qualities = [\"medium\"]\n\n        total_files = len(track_paths)\n        results = {}\n\n        self.logger.debug(\n            f\"Starting cache generation for {total_files} tracks at {qualities} quality levels\"\n        )\n\n        for idx, file_path in enumerate(track_paths, 1):\n            if not self.audio_cache.should_transcode(file_path):\n                self.logger.debug(f\"Skipping {file_path.name} (no transcoding needed)\")\n                results[str(file_path)] = {\"skipped\": True}\n                continue\n\n            file_results = self.cache_single_file(file_path, qualities)\n            results[str(file_path)] = file_results\n\n            if progress_callback:\n                progress_callback(idx, total_files)\n\n        self.logger.debug(f\"Cache generation complete for {total_files} tracks\")\n        return results\n\n    def cache_mixtape_async(\n        self,\n        track_paths: list[Path],\n        qualities: list[QualityLevel] = None,\n        progress_callback: Callable[[int, int], None] | None = None,\n    ) -&gt; dict[str, dict]:\n        \"\"\"\n        Cache all audio files in a mixtape using parallel processing.\n\n        Args:\n            track_paths: List of paths to audio files in the mixtape.\n            qualities: Quality levels to cache. Defaults to [\"medium\"].\n            progress_callback: Optional callback function(current, total) for progress updates.\n\n        Returns:\n            Dictionary with results for each file.\n        \"\"\"\n        if qualities is None:\n            qualities = [\"medium\"]\n\n        # Filter files that need transcoding\n        files_to_cache = [\n            path for path in track_paths if self.audio_cache.should_transcode(path)\n        ]\n\n        # Track skipped files (non-FLAC that don't need transcoding)\n        skipped_files = [\n            path for path in track_paths if not self.audio_cache.should_transcode(path)\n        ]\n\n        total_files = len(files_to_cache)\n        # Report skipped files via progress callback\n        if progress_callback and hasattr(progress_callback, 'track_skipped'):\n            for path in skipped_files:\n                progress_callback.track_skipped(path.name, reason=\"No transcoding needed (MP3/M4A/etc)\")\n\n        results = {\n            str(path): {\"skipped\": True, \"reason\": \"No transcoding needed\"}\n            for path in skipped_files\n        }\n        if total_files == 0:\n            self.logger.debug(f\"No files need transcoding ({len(skipped_files)} files skipped)\")\n            # Emit final progress if all files were skipped\n            if progress_callback and hasattr(progress_callback, '__call__'):\n                progress_callback(len(track_paths), len(track_paths))\n            return results\n\n        completed = 0\n\n        self.logger.debug(\n            f\"Starting parallel cache generation for {total_files} tracks \"\n            f\"at {qualities} quality levels (max workers: {self.max_workers})\"\n        )\n\n        with ThreadPoolExecutor(max_workers=self.max_workers) as executor:\n            # Submit all jobs\n            future_to_path = {\n                executor.submit(self.cache_single_file, path, qualities): path\n                for path in files_to_cache\n            }\n\n            # Process completed jobs\n            for future in as_completed(future_to_path):\n                path = future_to_path[future]\n                completed += 1\n\n                try:\n                    file_results = future.result()\n                    results[str(path)] = file_results\n                    self.logger.debug(\n                        f\"[{completed}/{total_files}] Cached {path.name}\"\n                    )\n                except Exception as e:\n                    results[str(path)] = {\"error\": str(e)}\n                    self.logger.error(f\"Failed to cache {path.name}: {e}\")\n\n                if progress_callback:\n                    progress_callback(completed, total_files)\n\n        self.logger.debug(f\"Parallel cache generation complete for {total_files} tracks\")\n        return results\n\n    def verify_mixtape_cache(\n        self, track_paths: list[Path], quality: QualityLevel = \"medium\"\n    ) -&gt; dict[str, bool]:\n        \"\"\"\n        Verify which tracks in a mixtape have valid cached versions.\n\n        Args:\n            track_paths: List of paths to audio files in the mixtape.\n            quality: Quality level to check.\n\n        Returns:\n            Dictionary mapping file paths to cache availability status.\n        \"\"\"\n        results = {\n            str(path): (\n                self.audio_cache.is_cached(path, quality) if self.audio_cache.should_transcode(path) else True\n            )\n            for path in track_paths\n        }\n        return results\n\n    def regenerate_outdated_cache(\n        self, track_paths: list[Path], qualities: list[QualityLevel] = None\n    ) -&gt; dict[str, dict]:\n        \"\"\"\n        Regenerate cached versions that are older than their source files.\n\n        Args:\n            track_paths: List of paths to audio files.\n            qualities: Quality levels to regenerate.\n\n        Returns:\n            Dictionary with regeneration results for each file.\n        \"\"\"\n        if qualities is None:\n            qualities = [\"medium\"]\n\n        files_to_regenerate = []\n\n        for path in track_paths:\n            if not self.audio_cache.should_transcode(path):\n                continue\n\n            # Check each quality level\n            for quality in qualities:\n                if not self.audio_cache.is_cached(path, quality):\n                    files_to_regenerate.append((path, quality))\n                    self.logger.debug(\n                        f\"Cache outdated or missing: {path.name} at {quality}\"\n                    )\n\n        if not files_to_regenerate:\n            self.logger.info(\"All caches are up-to-date\")\n            return {}\n\n        results = {}\n\n        for path, quality in files_to_regenerate:\n            try:\n                self.audio_cache.transcode_file(path, quality, overwrite=True)\n                key = f\"{path}_{quality}\"\n                results[key] = {\"success\": True}\n                self.logger.debug(f\"Regenerated cache: {path.name} at {quality}\")\n            except Exception as e:\n                key = f\"{path}_{quality}\"\n                results[key] = {\"success\": False, \"error\": str(e)}\n                self.logger.error(f\"Failed to regenerate {path.name}: {e}\")\n\n        return results\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.cache_worker.CacheWorker.__init__","title":"<code>__init__(audio_cache, logger=None, max_workers=4)</code>","text":"<p>Initialize the cache worker.</p> <p>Parameters:</p> Name Type Description Default <code>audio_cache</code> <code>AudioCache</code> <p>AudioCache instance for transcoding operations.</p> required <code>logger</code> <code>Logger | None</code> <p>Optional logger for tracking operations.</p> <code>None</code> <code>max_workers</code> <code>int</code> <p>Maximum number of parallel transcoding threads.</p> <code>4</code> Source code in <code>src/audio_cache/cache_worker.py</code> <pre><code>def __init__(\n    self,\n    audio_cache: AudioCache,\n    logger: Logger | None = None,\n    max_workers: int = 4,\n):\n    \"\"\"\n    Initialize the cache worker.\n\n    Args:\n        audio_cache: AudioCache instance for transcoding operations.\n        logger: Optional logger for tracking operations.\n        max_workers: Maximum number of parallel transcoding threads.\n    \"\"\"\n    self.audio_cache = audio_cache\n    self.logger = logger or NullLogger()\n    self.max_workers = max_workers\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.cache_worker.CacheWorker.cache_mixtape","title":"<code>cache_mixtape(track_paths, qualities=None, progress_callback=None)</code>","text":"<p>Cache all audio files in a mixtape.</p> <p>Parameters:</p> Name Type Description Default <code>track_paths</code> <code>list[Path]</code> <p>List of paths to audio files in the mixtape.</p> required <code>qualities</code> <code>list[QualityLevel]</code> <p>Quality levels to cache. Defaults to [\"medium\"].</p> <code>None</code> <code>progress_callback</code> <code>Callable[[int, int], None] | None</code> <p>Optional callback function(current, total) for progress updates.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, dict]</code> <p>Dictionary with results for each file.</p> Source code in <code>src/audio_cache/cache_worker.py</code> <pre><code>def cache_mixtape(\n    self,\n    track_paths: list[Path],\n    qualities: list[QualityLevel] = None,\n    progress_callback: Callable[[int, int], None] | None = None,\n) -&gt; dict[str, dict]:\n    \"\"\"\n    Cache all audio files in a mixtape.\n\n    Args:\n        track_paths: List of paths to audio files in the mixtape.\n        qualities: Quality levels to cache. Defaults to [\"medium\"].\n        progress_callback: Optional callback function(current, total) for progress updates.\n\n    Returns:\n        Dictionary with results for each file.\n    \"\"\"\n    if qualities is None:\n        qualities = [\"medium\"]\n\n    total_files = len(track_paths)\n    results = {}\n\n    self.logger.debug(\n        f\"Starting cache generation for {total_files} tracks at {qualities} quality levels\"\n    )\n\n    for idx, file_path in enumerate(track_paths, 1):\n        if not self.audio_cache.should_transcode(file_path):\n            self.logger.debug(f\"Skipping {file_path.name} (no transcoding needed)\")\n            results[str(file_path)] = {\"skipped\": True}\n            continue\n\n        file_results = self.cache_single_file(file_path, qualities)\n        results[str(file_path)] = file_results\n\n        if progress_callback:\n            progress_callback(idx, total_files)\n\n    self.logger.debug(f\"Cache generation complete for {total_files} tracks\")\n    return results\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.cache_worker.CacheWorker.cache_mixtape_async","title":"<code>cache_mixtape_async(track_paths, qualities=None, progress_callback=None)</code>","text":"<p>Cache all audio files in a mixtape using parallel processing.</p> <p>Parameters:</p> Name Type Description Default <code>track_paths</code> <code>list[Path]</code> <p>List of paths to audio files in the mixtape.</p> required <code>qualities</code> <code>list[QualityLevel]</code> <p>Quality levels to cache. Defaults to [\"medium\"].</p> <code>None</code> <code>progress_callback</code> <code>Callable[[int, int], None] | None</code> <p>Optional callback function(current, total) for progress updates.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, dict]</code> <p>Dictionary with results for each file.</p> Source code in <code>src/audio_cache/cache_worker.py</code> <pre><code>def cache_mixtape_async(\n    self,\n    track_paths: list[Path],\n    qualities: list[QualityLevel] = None,\n    progress_callback: Callable[[int, int], None] | None = None,\n) -&gt; dict[str, dict]:\n    \"\"\"\n    Cache all audio files in a mixtape using parallel processing.\n\n    Args:\n        track_paths: List of paths to audio files in the mixtape.\n        qualities: Quality levels to cache. Defaults to [\"medium\"].\n        progress_callback: Optional callback function(current, total) for progress updates.\n\n    Returns:\n        Dictionary with results for each file.\n    \"\"\"\n    if qualities is None:\n        qualities = [\"medium\"]\n\n    # Filter files that need transcoding\n    files_to_cache = [\n        path for path in track_paths if self.audio_cache.should_transcode(path)\n    ]\n\n    # Track skipped files (non-FLAC that don't need transcoding)\n    skipped_files = [\n        path for path in track_paths if not self.audio_cache.should_transcode(path)\n    ]\n\n    total_files = len(files_to_cache)\n    # Report skipped files via progress callback\n    if progress_callback and hasattr(progress_callback, 'track_skipped'):\n        for path in skipped_files:\n            progress_callback.track_skipped(path.name, reason=\"No transcoding needed (MP3/M4A/etc)\")\n\n    results = {\n        str(path): {\"skipped\": True, \"reason\": \"No transcoding needed\"}\n        for path in skipped_files\n    }\n    if total_files == 0:\n        self.logger.debug(f\"No files need transcoding ({len(skipped_files)} files skipped)\")\n        # Emit final progress if all files were skipped\n        if progress_callback and hasattr(progress_callback, '__call__'):\n            progress_callback(len(track_paths), len(track_paths))\n        return results\n\n    completed = 0\n\n    self.logger.debug(\n        f\"Starting parallel cache generation for {total_files} tracks \"\n        f\"at {qualities} quality levels (max workers: {self.max_workers})\"\n    )\n\n    with ThreadPoolExecutor(max_workers=self.max_workers) as executor:\n        # Submit all jobs\n        future_to_path = {\n            executor.submit(self.cache_single_file, path, qualities): path\n            for path in files_to_cache\n        }\n\n        # Process completed jobs\n        for future in as_completed(future_to_path):\n            path = future_to_path[future]\n            completed += 1\n\n            try:\n                file_results = future.result()\n                results[str(path)] = file_results\n                self.logger.debug(\n                    f\"[{completed}/{total_files}] Cached {path.name}\"\n                )\n            except Exception as e:\n                results[str(path)] = {\"error\": str(e)}\n                self.logger.error(f\"Failed to cache {path.name}: {e}\")\n\n            if progress_callback:\n                progress_callback(completed, total_files)\n\n    self.logger.debug(f\"Parallel cache generation complete for {total_files} tracks\")\n    return results\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.cache_worker.CacheWorker.cache_single_file","title":"<code>cache_single_file(file_path, qualities=None)</code>","text":"<p>Cache a single audio file at specified quality levels.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>Path</code> <p>Path to the audio file.</p> required <code>qualities</code> <code>list[QualityLevel]</code> <p>List of quality levels to cache. Defaults to [\"medium\"].</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[QualityLevel, bool]</code> <p>Dictionary mapping quality levels to success status.</p> Source code in <code>src/audio_cache/cache_worker.py</code> <pre><code>def cache_single_file(\n    self,\n    file_path: Path,\n    qualities: list[QualityLevel] = None,\n) -&gt; dict[QualityLevel, bool]:\n    \"\"\"\n    Cache a single audio file at specified quality levels.\n\n    Args:\n        file_path: Path to the audio file.\n        qualities: List of quality levels to cache. Defaults to [\"medium\"].\n\n    Returns:\n        Dictionary mapping quality levels to success status.\n    \"\"\"\n    if qualities is None:\n        qualities = [\"medium\"]\n\n    results = {}\n\n    for quality in qualities:\n        if quality == \"original\":\n            results[quality] = True\n            continue\n\n        try:\n            self.audio_cache.transcode_file(file_path, quality)\n            results[quality] = True\n            self.logger.debug(f\"Cached {file_path.name} at {quality} quality\")\n        except Exception as e:\n            results[quality] = False\n            self.logger.error(f\"Failed to cache {file_path.name} at {quality}: {e}\")\n\n    return results\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.cache_worker.CacheWorker.regenerate_outdated_cache","title":"<code>regenerate_outdated_cache(track_paths, qualities=None)</code>","text":"<p>Regenerate cached versions that are older than their source files.</p> <p>Parameters:</p> Name Type Description Default <code>track_paths</code> <code>list[Path]</code> <p>List of paths to audio files.</p> required <code>qualities</code> <code>list[QualityLevel]</code> <p>Quality levels to regenerate.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, dict]</code> <p>Dictionary with regeneration results for each file.</p> Source code in <code>src/audio_cache/cache_worker.py</code> <pre><code>def regenerate_outdated_cache(\n    self, track_paths: list[Path], qualities: list[QualityLevel] = None\n) -&gt; dict[str, dict]:\n    \"\"\"\n    Regenerate cached versions that are older than their source files.\n\n    Args:\n        track_paths: List of paths to audio files.\n        qualities: Quality levels to regenerate.\n\n    Returns:\n        Dictionary with regeneration results for each file.\n    \"\"\"\n    if qualities is None:\n        qualities = [\"medium\"]\n\n    files_to_regenerate = []\n\n    for path in track_paths:\n        if not self.audio_cache.should_transcode(path):\n            continue\n\n        # Check each quality level\n        for quality in qualities:\n            if not self.audio_cache.is_cached(path, quality):\n                files_to_regenerate.append((path, quality))\n                self.logger.debug(\n                    f\"Cache outdated or missing: {path.name} at {quality}\"\n                )\n\n    if not files_to_regenerate:\n        self.logger.info(\"All caches are up-to-date\")\n        return {}\n\n    results = {}\n\n    for path, quality in files_to_regenerate:\n        try:\n            self.audio_cache.transcode_file(path, quality, overwrite=True)\n            key = f\"{path}_{quality}\"\n            results[key] = {\"success\": True}\n            self.logger.debug(f\"Regenerated cache: {path.name} at {quality}\")\n        except Exception as e:\n            key = f\"{path}_{quality}\"\n            results[key] = {\"success\": False, \"error\": str(e)}\n            self.logger.error(f\"Failed to regenerate {path.name}: {e}\")\n\n    return results\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.cache_worker.CacheWorker.verify_mixtape_cache","title":"<code>verify_mixtape_cache(track_paths, quality='medium')</code>","text":"<p>Verify which tracks in a mixtape have valid cached versions.</p> <p>Parameters:</p> Name Type Description Default <code>track_paths</code> <code>list[Path]</code> <p>List of paths to audio files in the mixtape.</p> required <code>quality</code> <code>QualityLevel</code> <p>Quality level to check.</p> <code>'medium'</code> <p>Returns:</p> Type Description <code>dict[str, bool]</code> <p>Dictionary mapping file paths to cache availability status.</p> Source code in <code>src/audio_cache/cache_worker.py</code> <pre><code>def verify_mixtape_cache(\n    self, track_paths: list[Path], quality: QualityLevel = \"medium\"\n) -&gt; dict[str, bool]:\n    \"\"\"\n    Verify which tracks in a mixtape have valid cached versions.\n\n    Args:\n        track_paths: List of paths to audio files in the mixtape.\n        quality: Quality level to check.\n\n    Returns:\n        Dictionary mapping file paths to cache availability status.\n    \"\"\"\n    results = {\n        str(path): (\n            self.audio_cache.is_cached(path, quality) if self.audio_cache.should_transcode(path) else True\n        )\n        for path in track_paths\n    }\n    return results\n</code></pre>"},{"location":"development/audio_caching.html#convenience-scheduler","title":"Convenience Scheduler","text":""},{"location":"development/audio_caching.html#src.audio_cache.cache_worker.schedule_mixtape_caching","title":"<code>src.audio_cache.cache_worker.schedule_mixtape_caching(mixtape_tracks, music_root, audio_cache, logger=None, qualities=None, async_mode=True, progress_callback=None)</code>","text":"<p>Convenience function to schedule caching for a mixtape's tracks.</p> <p>Parameters:</p> Name Type Description Default <code>mixtape_tracks</code> <code>list[dict]</code> <p>List of track dictionaries with 'path' keys.</p> required <code>music_root</code> <code>Path</code> <p>Root directory for music files.</p> required <code>audio_cache</code> <code>AudioCache</code> <p>AudioCache instance.</p> required <code>logger</code> <code>Logger | None</code> <p>Optional logger.</p> <code>None</code> <code>qualities</code> <code>list[QualityLevel]</code> <p>Quality levels to cache. Defaults to [\"medium\"].</p> <code>None</code> <code>async_mode</code> <code>bool</code> <p>If True, use parallel processing.</p> <code>True</code> <code>progress_callback</code> <code>Callable[[int, int], None] | None</code> <p>Optional callback function(current, total) for progress updates.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict</code> <p>Dictionary with caching results.</p> Source code in <code>src/audio_cache/cache_worker.py</code> <pre><code>def schedule_mixtape_caching(\n    mixtape_tracks: list[dict],\n    music_root: Path,\n    audio_cache: AudioCache,\n    logger: Logger | None = None,\n    qualities: list[QualityLevel] = None,\n    async_mode: bool = True,\n    progress_callback: Callable[[int, int], None] | None = None,\n) -&gt; dict:\n    \"\"\"\n    Convenience function to schedule caching for a mixtape's tracks.\n\n    Args:\n        mixtape_tracks: List of track dictionaries with 'path' keys.\n        music_root: Root directory for music files.\n        audio_cache: AudioCache instance.\n        logger: Optional logger.\n        qualities: Quality levels to cache. Defaults to [\"medium\"].\n        async_mode: If True, use parallel processing.\n        progress_callback: Optional callback function(current, total) for progress updates.\n\n    Returns:\n        Dictionary with caching results.\n    \"\"\"\n    if qualities is None:\n        qualities = [\"medium\"]\n\n    # Convert track dictionaries to Path objects\n    track_paths = [music_root / track[\"path\"] for track in mixtape_tracks]\n\n    # Filter out non-existent files\n    valid_paths = [path for path in track_paths if path.exists()]\n\n    if len(valid_paths) &lt; len(track_paths):\n        missing = len(track_paths) - len(valid_paths)\n        if logger:\n            logger.warning(f\"{missing} track files not found, skipping\")\n\n    # Create worker and cache files\n    worker = CacheWorker(audio_cache, logger)\n\n    if async_mode:\n        return worker.cache_mixtape_async(valid_paths, qualities, progress_callback)\n    else:\n        return worker.cache_mixtape(valid_paths, qualities, progress_callback)\n</code></pre>"},{"location":"development/audio_caching.html#progress-tracker-sse","title":"Progress Tracker (SSE)","text":""},{"location":"development/audio_caching.html#src.audio_cache.progress_tracker.get_progress_tracker","title":"<code>src.audio_cache.progress_tracker.get_progress_tracker(logger=None)</code>","text":"<p>Get or create the global progress tracker instance.</p> Source code in <code>src/audio_cache/progress_tracker.py</code> <pre><code>def get_progress_tracker(logger: Logger | None = None) -&gt; ProgressTracker:\n    \"\"\"Get or create the global progress tracker instance.\"\"\"\n    global _progress_tracker\n    if _progress_tracker is None:\n        _progress_tracker = ProgressTracker(logger)\n    return _progress_tracker\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.progress_tracker.ProgressTracker","title":"<code>src.audio_cache.progress_tracker.ProgressTracker</code>","text":"<p>Tracks progress of long-running operations and broadcasts updates via SSE.</p> <p>Thread-safe implementation that allows multiple operations to report progress while clients listen for updates.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Initializes a new progress tracker with optional logging.</p> <code>cleanup_task</code> <p>Remove a task and its queue.</p> <code>create_task</code> <p>Create a new task for tracking.</p> <code>emit</code> <p>Emit a progress event.</p> <code>listen</code> <p>Generator that yields SSE-formatted progress events.</p> Source code in <code>src/audio_cache/progress_tracker.py</code> <pre><code>class ProgressTracker:\n    \"\"\"\n    Tracks progress of long-running operations and broadcasts updates via SSE.\n\n    Thread-safe implementation that allows multiple operations to report progress\n    while clients listen for updates.\n    \"\"\"\n\n    def __init__(self, logger: Logger | None = None):\n        \"\"\"Initializes a new progress tracker with optional logging.\n\n        Sets up internal, thread-safe queues for tracking task-specific progress events that can be streamed to clients.\n\n        Args:\n            logger: Optional logger instance used to record progress tracker activity.\n        \"\"\"\n        self.logger = logger or NullLogger()\n        self._queues: dict[str, Queue] = {}\n        self._lock = threading.Lock()\n\n    def create_task(self, task_id: str) -&gt; None:\n        \"\"\"\n        Create a new task for tracking.\n\n        Args:\n            task_id: Unique identifier for this task (e.g., mixtape slug)\n        \"\"\"\n        with self._lock:\n            if task_id not in self._queues:\n                self._queues[task_id] = Queue()\n                self.logger.debug(f\"Created progress task: {task_id}\")\n\n    def emit(\n        self,\n        task_id: str,\n        step: str,\n        status: ProgressStatus,\n        message: str,\n        current: int = 0,\n        total: int = 0\n    ) -&gt; None:\n        \"\"\"\n        Emit a progress event.\n\n        Args:\n            task_id: Task identifier\n            step: Name of the current step (e.g., \"saving\", \"caching_track\")\n            status: Current status\n            message: Human-readable message\n            current: Current progress count\n            total: Total items to process\n        \"\"\"\n        event = ProgressEvent(\n            task_id=task_id,\n            step=step,\n            status=status,\n            message=message,\n            current=current,\n            total=total\n        )\n\n        with self._lock:\n            if task_id in self._queues:\n                self._queues[task_id].put(event)\n                self.logger.debug(f\"[{task_id}] {step}: {message} ({current}/{total})\")\n\n    def listen(self, task_id: str, timeout: int = 300):\n        \"\"\"\n        Generator that yields SSE-formatted progress events.\n\n        Args:\n            task_id: Task identifier to listen to\n            timeout: Maximum time to wait for events (seconds)\n\n        Yields:\n            str: SSE-formatted event strings\n        \"\"\"\n        self.create_task(task_id)\n\n        start_time = time.time()\n\n        # Send initial connection event\n        yield f\"data: {json.dumps({'type': 'connected', 'task_id': task_id})}\\n\\n\"\n\n        while True:\n            # Check timeout\n            if time.time() - start_time &gt; timeout:\n                self.logger.warning(f\"Progress stream timeout for task: {task_id}\")\n                break\n\n            try:\n                with self._lock:\n                    queue = self._queues.get(task_id)\n\n                if queue is None:\n                    break\n\n                # Get event with short timeout to allow checking for completion\n                event = queue.get(timeout=1)\n\n                yield event.to_sse()\n\n                # If task is completed or failed, clean up and stop\n                if event.status in (ProgressStatus.COMPLETED, ProgressStatus.FAILED):\n                    self.logger.debug(f\"Task completed: {task_id}\")\n                    break\n\n            except Empty:\n                # Send keepalive\n                yield \": keepalive\\n\\n\"\n                continue\n\n        # Cleanup\n        self.cleanup_task(task_id)\n\n    def cleanup_task(self, task_id: str) -&gt; None:\n        \"\"\"\n        Remove a task and its queue.\n\n        Args:\n            task_id: Task identifier to clean up\n        \"\"\"\n        with self._lock:\n            if task_id in self._queues:\n                del self._queues[task_id]\n                self.logger.debug(f\"Cleaned up progress task: {task_id}\")\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.progress_tracker.ProgressTracker.__init__","title":"<code>__init__(logger=None)</code>","text":"<p>Initializes a new progress tracker with optional logging.</p> <p>Sets up internal, thread-safe queues for tracking task-specific progress events that can be streamed to clients.</p> <p>Parameters:</p> Name Type Description Default <code>logger</code> <code>Logger | None</code> <p>Optional logger instance used to record progress tracker activity.</p> <code>None</code> Source code in <code>src/audio_cache/progress_tracker.py</code> <pre><code>def __init__(self, logger: Logger | None = None):\n    \"\"\"Initializes a new progress tracker with optional logging.\n\n    Sets up internal, thread-safe queues for tracking task-specific progress events that can be streamed to clients.\n\n    Args:\n        logger: Optional logger instance used to record progress tracker activity.\n    \"\"\"\n    self.logger = logger or NullLogger()\n    self._queues: dict[str, Queue] = {}\n    self._lock = threading.Lock()\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.progress_tracker.ProgressTracker.cleanup_task","title":"<code>cleanup_task(task_id)</code>","text":"<p>Remove a task and its queue.</p> <p>Parameters:</p> Name Type Description Default <code>task_id</code> <code>str</code> <p>Task identifier to clean up</p> required Source code in <code>src/audio_cache/progress_tracker.py</code> <pre><code>def cleanup_task(self, task_id: str) -&gt; None:\n    \"\"\"\n    Remove a task and its queue.\n\n    Args:\n        task_id: Task identifier to clean up\n    \"\"\"\n    with self._lock:\n        if task_id in self._queues:\n            del self._queues[task_id]\n            self.logger.debug(f\"Cleaned up progress task: {task_id}\")\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.progress_tracker.ProgressTracker.create_task","title":"<code>create_task(task_id)</code>","text":"<p>Create a new task for tracking.</p> <p>Parameters:</p> Name Type Description Default <code>task_id</code> <code>str</code> <p>Unique identifier for this task (e.g., mixtape slug)</p> required Source code in <code>src/audio_cache/progress_tracker.py</code> <pre><code>def create_task(self, task_id: str) -&gt; None:\n    \"\"\"\n    Create a new task for tracking.\n\n    Args:\n        task_id: Unique identifier for this task (e.g., mixtape slug)\n    \"\"\"\n    with self._lock:\n        if task_id not in self._queues:\n            self._queues[task_id] = Queue()\n            self.logger.debug(f\"Created progress task: {task_id}\")\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.progress_tracker.ProgressTracker.emit","title":"<code>emit(task_id, step, status, message, current=0, total=0)</code>","text":"<p>Emit a progress event.</p> <p>Parameters:</p> Name Type Description Default <code>task_id</code> <code>str</code> <p>Task identifier</p> required <code>step</code> <code>str</code> <p>Name of the current step (e.g., \"saving\", \"caching_track\")</p> required <code>status</code> <code>ProgressStatus</code> <p>Current status</p> required <code>message</code> <code>str</code> <p>Human-readable message</p> required <code>current</code> <code>int</code> <p>Current progress count</p> <code>0</code> <code>total</code> <code>int</code> <p>Total items to process</p> <code>0</code> Source code in <code>src/audio_cache/progress_tracker.py</code> <pre><code>def emit(\n    self,\n    task_id: str,\n    step: str,\n    status: ProgressStatus,\n    message: str,\n    current: int = 0,\n    total: int = 0\n) -&gt; None:\n    \"\"\"\n    Emit a progress event.\n\n    Args:\n        task_id: Task identifier\n        step: Name of the current step (e.g., \"saving\", \"caching_track\")\n        status: Current status\n        message: Human-readable message\n        current: Current progress count\n        total: Total items to process\n    \"\"\"\n    event = ProgressEvent(\n        task_id=task_id,\n        step=step,\n        status=status,\n        message=message,\n        current=current,\n        total=total\n    )\n\n    with self._lock:\n        if task_id in self._queues:\n            self._queues[task_id].put(event)\n            self.logger.debug(f\"[{task_id}] {step}: {message} ({current}/{total})\")\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.progress_tracker.ProgressTracker.listen","title":"<code>listen(task_id, timeout=300)</code>","text":"<p>Generator that yields SSE-formatted progress events.</p> <p>Parameters:</p> Name Type Description Default <code>task_id</code> <code>str</code> <p>Task identifier to listen to</p> required <code>timeout</code> <code>int</code> <p>Maximum time to wait for events (seconds)</p> <code>300</code> <p>Yields:</p> Name Type Description <code>str</code> <p>SSE-formatted event strings</p> Source code in <code>src/audio_cache/progress_tracker.py</code> <pre><code>def listen(self, task_id: str, timeout: int = 300):\n    \"\"\"\n    Generator that yields SSE-formatted progress events.\n\n    Args:\n        task_id: Task identifier to listen to\n        timeout: Maximum time to wait for events (seconds)\n\n    Yields:\n        str: SSE-formatted event strings\n    \"\"\"\n    self.create_task(task_id)\n\n    start_time = time.time()\n\n    # Send initial connection event\n    yield f\"data: {json.dumps({'type': 'connected', 'task_id': task_id})}\\n\\n\"\n\n    while True:\n        # Check timeout\n        if time.time() - start_time &gt; timeout:\n            self.logger.warning(f\"Progress stream timeout for task: {task_id}\")\n            break\n\n        try:\n            with self._lock:\n                queue = self._queues.get(task_id)\n\n            if queue is None:\n                break\n\n            # Get event with short timeout to allow checking for completion\n            event = queue.get(timeout=1)\n\n            yield event.to_sse()\n\n            # If task is completed or failed, clean up and stop\n            if event.status in (ProgressStatus.COMPLETED, ProgressStatus.FAILED):\n                self.logger.debug(f\"Task completed: {task_id}\")\n                break\n\n        except Empty:\n            # Send keepalive\n            yield \": keepalive\\n\\n\"\n            continue\n\n    # Cleanup\n    self.cleanup_task(task_id)\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.progress_tracker.ProgressCallback","title":"<code>src.audio_cache.progress_tracker.ProgressCallback</code>","text":"<p>Callback wrapper for audio caching progress.</p> <p>Translates cache worker progress updates into SSE events.</p> <p>Methods:</p> Name Description <code>__call__</code> <p>Called by cache worker with progress updates.</p> <code>__init__</code> <p>Initialize the progress callback.</p> <code>track_cached</code> <p>Records that a track has been successfully cached.</p> <code>track_failed</code> <p>Records that caching a track has failed.</p> <code>track_skipped</code> <p>Records that a track was intentionally skipped during caching.</p> Source code in <code>src/audio_cache/progress_tracker.py</code> <pre><code>class ProgressCallback:\n    \"\"\"\n    Callback wrapper for audio caching progress.\n\n    Translates cache worker progress updates into SSE events.\n    \"\"\"\n\n    def __init__(self, task_id: str, tracker: ProgressTracker, total_tracks: int):\n        \"\"\"\n        Initialize the progress callback.\n\n        Args:\n            task_id: Task identifier\n            tracker: ProgressTracker instance\n            total_tracks: Total number of tracks to cache\n        \"\"\"\n        self.task_id = task_id\n        self.tracker = tracker\n        self.total_tracks = total_tracks\n        self.cached_count = 0\n        self.skipped_count = 0\n        self.failed_count = 0\n\n    def __call__(self, current: int, total: int) -&gt; None:\n        \"\"\"\n        Called by cache worker with progress updates.\n\n        Args:\n            current: Current file number\n            total: Total files to process\n        \"\"\"\n        self.tracker.emit(\n            task_id=self.task_id,\n            step=\"caching\",\n            status=ProgressStatus.IN_PROGRESS,\n            message=f\"Caching track {current} of {total}\",\n            current=current,\n            total=total\n        )\n\n    def track_cached(self, track_name: str) -&gt; None:\n        \"\"\"Records that a track has been successfully cached.\n\n        Increments the count of cached tracks and emits a progress event reflecting the updated completion state.\n\n        Args:\n            track_name: The display name or identifier of the cached track.\n        \"\"\"\n        self.cached_count += 1\n        self.tracker.emit(\n            task_id=self.task_id,\n            step=\"track_cached\",\n            status=ProgressStatus.IN_PROGRESS,\n            message=f\"\u2713 Cached: {track_name}\",\n            current=self.cached_count,\n            total=self.total_tracks\n        )\n\n    def track_skipped(self, track_name: str, reason: str = \"already cached\") -&gt; None:\n        \"\"\"Records that a track was intentionally skipped during caching.\n\n        Increments the skipped count and emits a progress event explaining why the track was not processed.\n\n        Args:\n            track_name: The display name or identifier of the skipped track.\n            reason: Human-readable explanation for why the track was skipped.\n        \"\"\"\n        self.skipped_count += 1\n        self.tracker.emit(\n            task_id=self.task_id,\n            step=\"track_skipped\",\n            status=ProgressStatus.SKIPPED,\n            message=f\"\u2298 Skipped: {track_name} ({reason})\",\n            current=self.cached_count + self.skipped_count,\n            total=self.total_tracks\n        )\n\n    def track_failed(self, track_name: str, error: str) -&gt; None:\n        \"\"\"Records that caching a track has failed.\n\n        Increments the failed count and emits a progress event describing the error that occurred.\n\n        Args:\n            track_name: The display name or identifier of the track that failed to cache.\n            error: Human-readable error description explaining the failure.\n        \"\"\"\n        self.failed_count += 1\n        self.tracker.emit(\n            task_id=self.task_id,\n            step=\"track_failed\",\n            status=ProgressStatus.FAILED,\n            message=f\"\u2717 Failed: {track_name} - {error}\",\n            current=self.cached_count + self.skipped_count + self.failed_count,\n            total=self.total_tracks\n        )\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.progress_tracker.ProgressCallback.__call__","title":"<code>__call__(current, total)</code>","text":"<p>Called by cache worker with progress updates.</p> <p>Parameters:</p> Name Type Description Default <code>current</code> <code>int</code> <p>Current file number</p> required <code>total</code> <code>int</code> <p>Total files to process</p> required Source code in <code>src/audio_cache/progress_tracker.py</code> <pre><code>def __call__(self, current: int, total: int) -&gt; None:\n    \"\"\"\n    Called by cache worker with progress updates.\n\n    Args:\n        current: Current file number\n        total: Total files to process\n    \"\"\"\n    self.tracker.emit(\n        task_id=self.task_id,\n        step=\"caching\",\n        status=ProgressStatus.IN_PROGRESS,\n        message=f\"Caching track {current} of {total}\",\n        current=current,\n        total=total\n    )\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.progress_tracker.ProgressCallback.__init__","title":"<code>__init__(task_id, tracker, total_tracks)</code>","text":"<p>Initialize the progress callback.</p> <p>Parameters:</p> Name Type Description Default <code>task_id</code> <code>str</code> <p>Task identifier</p> required <code>tracker</code> <code>ProgressTracker</code> <p>ProgressTracker instance</p> required <code>total_tracks</code> <code>int</code> <p>Total number of tracks to cache</p> required Source code in <code>src/audio_cache/progress_tracker.py</code> <pre><code>def __init__(self, task_id: str, tracker: ProgressTracker, total_tracks: int):\n    \"\"\"\n    Initialize the progress callback.\n\n    Args:\n        task_id: Task identifier\n        tracker: ProgressTracker instance\n        total_tracks: Total number of tracks to cache\n    \"\"\"\n    self.task_id = task_id\n    self.tracker = tracker\n    self.total_tracks = total_tracks\n    self.cached_count = 0\n    self.skipped_count = 0\n    self.failed_count = 0\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.progress_tracker.ProgressCallback.track_cached","title":"<code>track_cached(track_name)</code>","text":"<p>Records that a track has been successfully cached.</p> <p>Increments the count of cached tracks and emits a progress event reflecting the updated completion state.</p> <p>Parameters:</p> Name Type Description Default <code>track_name</code> <code>str</code> <p>The display name or identifier of the cached track.</p> required Source code in <code>src/audio_cache/progress_tracker.py</code> <pre><code>def track_cached(self, track_name: str) -&gt; None:\n    \"\"\"Records that a track has been successfully cached.\n\n    Increments the count of cached tracks and emits a progress event reflecting the updated completion state.\n\n    Args:\n        track_name: The display name or identifier of the cached track.\n    \"\"\"\n    self.cached_count += 1\n    self.tracker.emit(\n        task_id=self.task_id,\n        step=\"track_cached\",\n        status=ProgressStatus.IN_PROGRESS,\n        message=f\"\u2713 Cached: {track_name}\",\n        current=self.cached_count,\n        total=self.total_tracks\n    )\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.progress_tracker.ProgressCallback.track_failed","title":"<code>track_failed(track_name, error)</code>","text":"<p>Records that caching a track has failed.</p> <p>Increments the failed count and emits a progress event describing the error that occurred.</p> <p>Parameters:</p> Name Type Description Default <code>track_name</code> <code>str</code> <p>The display name or identifier of the track that failed to cache.</p> required <code>error</code> <code>str</code> <p>Human-readable error description explaining the failure.</p> required Source code in <code>src/audio_cache/progress_tracker.py</code> <pre><code>def track_failed(self, track_name: str, error: str) -&gt; None:\n    \"\"\"Records that caching a track has failed.\n\n    Increments the failed count and emits a progress event describing the error that occurred.\n\n    Args:\n        track_name: The display name or identifier of the track that failed to cache.\n        error: Human-readable error description explaining the failure.\n    \"\"\"\n    self.failed_count += 1\n    self.tracker.emit(\n        task_id=self.task_id,\n        step=\"track_failed\",\n        status=ProgressStatus.FAILED,\n        message=f\"\u2717 Failed: {track_name} - {error}\",\n        current=self.cached_count + self.skipped_count + self.failed_count,\n        total=self.total_tracks\n    )\n</code></pre>"},{"location":"development/audio_caching.html#src.audio_cache.progress_tracker.ProgressCallback.track_skipped","title":"<code>track_skipped(track_name, reason='already cached')</code>","text":"<p>Records that a track was intentionally skipped during caching.</p> <p>Increments the skipped count and emits a progress event explaining why the track was not processed.</p> <p>Parameters:</p> Name Type Description Default <code>track_name</code> <code>str</code> <p>The display name or identifier of the skipped track.</p> required <code>reason</code> <code>str</code> <p>Human-readable explanation for why the track was skipped.</p> <code>'already cached'</code> Source code in <code>src/audio_cache/progress_tracker.py</code> <pre><code>def track_skipped(self, track_name: str, reason: str = \"already cached\") -&gt; None:\n    \"\"\"Records that a track was intentionally skipped during caching.\n\n    Increments the skipped count and emits a progress event explaining why the track was not processed.\n\n    Args:\n        track_name: The display name or identifier of the skipped track.\n        reason: Human-readable explanation for why the track was skipped.\n    \"\"\"\n    self.skipped_count += 1\n    self.tracker.emit(\n        task_id=self.task_id,\n        step=\"track_skipped\",\n        status=ProgressStatus.SKIPPED,\n        message=f\"\u2298 Skipped: {track_name} ({reason})\",\n        current=self.cached_count + self.skipped_count,\n        total=self.total_tracks\n    )\n</code></pre>"},{"location":"development/audio_caching.html#configuration-options","title":"\ud83d\udee0\ufe0f Configuration Options","text":"Option Default Description <code>AUDIO_CACHE_DIR</code> <code>\"cache/audio\"</code> Directory where MP3 caches are stored (relative to DATA_ROOT). <code>AUDIO_CACHE_ENABLED</code> <code>True</code> Master switch \u2013 set to <code>False</code> to bypass the entire subsystem. <code>AUDIO_CACHE_DEFAULT_QUALITY</code> <code>\"medium\"</code> Quality used when a client does not specify one. <code>AUDIO_CACHE_MAX_WORKERS</code> <code>4</code> Number of parallel threads for batch transcoding. <code>AUDIO_CACHE_PRECACHE_ON_UPLOAD</code> <code>True</code> Auto-cache mixtape tracks when a mixtape is saved. <code>AUDIO_CACHE_PRECACHE_QUALITIES</code> <code>[\"medium\"]</code> List of qualities to pre-generate (e.g., <code>[\"low\", \"medium\", \"high\"]</code>). <p>These values are defined in <code>src/config/config.py</code> and can be overridden with environment variables (e.g., <code>AUDIO_CACHE_MAX_WORKERS=8</code>).</p>"},{"location":"development/audio_caching.html#progress-tracking-sse","title":"\u23f3 Progress Tracking (SSE)","text":"<p>The progress modal in the editor UI subscribes to the endpoint:</p> <pre><code>GET /editor/progress/&lt;slug&gt;\n</code></pre> <p>The server returns a Server\u2011Sent Events stream. Each event looks like:</p> <pre><code>{\n  \"task_id\": \"summer-vibes\",\n  \"step\": \"caching\",\n  \"status\": \"in_progress\",\n  \"message\": \"Caching track 3 of 15\",\n  \"current\": 3,\n  \"total\": 15,\n  \"timestamp\": \"2024-09-28T12:34:56.789012\"\n}\n</code></pre> <p>The modal updates the progress bar, logs messages, and shows a final summary when the <code>status</code> becomes <code>completed</code> or <code>failed</code>.</p> <p>Implementation note:<code>ProgressCallback.track_cached()</code>, <code>track_skipped()</code>, and <code>track_failed()</code> are called from <code>CacheWorker</code> to emit the above events.</p>"},{"location":"development/audio_caching.html#troubleshooting-faq","title":"\ud83d\udd27 Troubleshooting FAQ","text":"\u26a1\u202fCache Misses \u2013 \u201cWhy isn\u2019t my file being cached?\u201d  | Symptom                            | Check                                                      | Fix                                                                 | |------------------------------------|------------------------------------------------------------|---------------------------------------------------------------------| | Cache miss warning in logs         | `grep -i \"cache miss\" app.log`                             | Verify `AUDIO_CACHE_ENABLED=True` and that the file\u2019s suffix is in `should_transcode` (FLAC, WAV, AIFF, APE, ALAC). | | Cache file exists but not found    | `ls collection-data/cache/audio/`                          | Ensure the hash matches the current absolute path. If you moved the music folder, run `python debug_cache.py  ` (see debug_cache.py). | | Cache never generated              | `AUDIO_CACHE_PRECACHE_ON_UPLOAD=False`                     | Enable pre-caching or trigger it manually via `schedule_mixtape_caching`. | | ffmpeg not found                   | `ffmpeg -version`                                          | Install ffmpeg on the host (Ubuntu: `apt install ffmpeg`; Alpine: `apk add ffmpeg`). | | Permission denied on cache dir     | `ls -ld collection-data/cache/audio`                       | The Flask process must have write permission (owner UID = the container user). | | High CPU usage during batch caching| `top while caching`                                        | Reduce `AUDIO_CACHE_MAX_WORKERS` (e.g., `export AUDIO_CACHE_MAX_WORKERS=2`). | | Stale cache after source file change| Compare timestamps (`stat -c %Y file`)                      | Run `cache.clear_cache()` or set `overwrite=True` in `transcode_file`. |   \ud83d\uded1\u202fTranscoding Failures \u2013 \u201cffmpeg exited with error code 1\u201d  1. **Inspect the ffmpeg stderr** \u2013 it is logged by `AudioCache.transcode_file`. 2. Common culprits:    * **Corrupt source file** \u2013 try re\u2011encoding the source with `ffmpeg -i inut.flac -c copy output.flac`.    * **Unsupported codec** \u2013 ensure the source is a supported lossless format.    * **Insufficient** disk space \u2013 check free space on the cache volume. 3. Manual test:      <pre><code>ffmpeg -i \"/music/Artist/Album/BadTrack.flac\" -b:a 192k -y \"/tmp/test.mp3\"\n</code></pre>      If this works, the problem is likely in the path handling (hash mismatch).  4. **Fix path mismatches** \u2013 run **debug_cache.py** (see the script in the repo) to compare the hash generated by the app vs. the one you expect."},{"location":"development/common.html","title":"Common","text":""},{"location":"development/common.html#common","title":"Common","text":"<p>The <code>common</code> module contains stable, infrastructure-free abstractions that may be used by all layers.</p>"},{"location":"development/common.html#api","title":"API","text":""},{"location":"development/common.html#src.common.logging","title":"<code>src.common.logging</code>","text":"<p>Classes:</p> Name Description <code>Logger</code> <p>Protocol for logger objects supporting info, warning, and error messages.</p> <code>NullLogger</code> <p>A logger implementation that ignores all log messages.</p>"},{"location":"development/common.html#src.common.logging.Logger","title":"<code>Logger</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Protocol for logger objects supporting info, warning, and error messages.</p> <p>Any logger implementing this protocol must provide info, warning, and error methods that accept a string message.</p> Source code in <code>src/common/logging.py</code> <pre><code>class Logger(Protocol):\n    \"\"\"\n    Protocol for logger objects supporting info, warning, and error messages.\n\n    Any logger implementing this protocol must provide info, warning, and error methods that accept a string message.\n    \"\"\"\n    def info(self, msg: str, *args, **kwargs) -&gt; None: ...\n    def warning(self, msg: str, *args, **kwargs) -&gt; None: ...\n    def error(self, msg: str, *args, **kwargs) -&gt; None: ...\n    def exception(self, msg: str, *args, **kwargs) -&gt; None: ...\n</code></pre>"},{"location":"development/common.html#src.common.logging.NullLogger","title":"<code>NullLogger</code>","text":"<p>A logger implementation that ignores all log messages.</p> <p>This logger provides info, warning, and error methods that do nothing, useful for disabling logging in certain contexts.</p> Source code in <code>src/common/logging.py</code> <pre><code>class NullLogger:\n    \"\"\"\n    A logger implementation that ignores all log messages.\n\n    This logger provides info, warning, and error methods that do nothing, useful for disabling logging in certain contexts.\n    \"\"\"\n    def info(self, msg: str, *args, **kwargs) -&gt; None: pass\n    def warning(self, msg: str, *args, **kwargs) -&gt; None: pass\n    def error(self, msg: str, *args, **kwargs) -&gt; None: pass\n    def exception(self, msg: str, *args, **kwargs) -&gt; None: pass\n</code></pre>"},{"location":"development/configuration.html","title":"Configuration","text":""},{"location":"development/configuration.html#configuration","title":"Configuration","text":"<p>This document explains every configuration option used by the Mixtape\u202fSociety application, how the values are sourced (environment variables, .env file, or defaults), the directory layout that results, and the Docker\u2011specific considerations. The description directly to the implementation in <code>src/config/config.py</code>.</p>"},{"location":"development/configuration.html#how-configuration-is-loaded","title":"\ud83d\udce5 How Configuration Is Loaded","text":"<pre><code># src/config/config.py\nfrom pathlib import Path\nimport os\nfrom dotenv import load_dotenv\n\n# Load .env only when we are *not* in production.\nif os.getenv(\"APP_ENV\", \"development\") != \"production\":\n    project_root = Path(__file__).parent.parent.parent   # src/config \u2192 project root\n    load_dotenv(project_root / \".env\")\n</code></pre> <ul> <li>In development (or when <code>APP_ENV</code> is anything other than <code>production</code>) the file tries to read a <code>.env</code> file located at the project root.</li> <li>In production the <code>.env</code> file is ignored \u2013 all configuration must be supplied via real environment variables.</li> </ul> <p>After the optional <code>.env</code> load, the module defines a <code>BASE_DIR</code> (<code>src/</code>) and a hierarchy of configuration classes.</p>"},{"location":"development/configuration.html#core-paths-their-defaults","title":"\ud83d\udee4\ufe0f Core Paths &amp; Their Defaults","text":"Setting Default (when no env var) Meaning <code>MUSIC_ROOT</code> <code>/music</code> Absolute path to the read-only music library that the collection scanner watches. <code>DATA_ROOT</code> <code>../collection-data</code> (relative to the project root) Root folder writable by the app. All generated data lives underneath this directory. <code>DB_PATH</code> <code>&lt;DATA_ROOT&gt;/collection.db</code> SQLite database file that stores the indexed music metadata. <code>MIXTAPE_DIR</code> <code>&lt;DATA_ROOT&gt;/mixtapes</code> Folder that holds each mixtape\u2019s JSON file (<code>&lt;slug&gt;.json</code>). <code>COVER_DIR</code> <code>&lt;MIXTAPE_DIR&gt;/covers</code> Sub-folder for mixtape cover images (<code>&lt;slug&gt;.jpg</code>). <code>AUDIO_CACHE_DIR</code> <code>&lt;DATA_ROOT&gt;/cache/audio</code> Directory where transcoded audio files are cached for streaming. <p>Important</p> <p><code>DB_PATH</code>, <code>MIXTAPE_DIR</code>, <code>COVER_DIR</code>, and <code>AUDIO_CACHE_DIR</code> are derived from <code>DATA_ROOT</code>. You must not set them directly \u2013 change <code>DATA_ROOT</code> instead.</p>"},{"location":"development/configuration.html#environment-variables-full-list","title":"\ud83c\udf31 Environment Variables (full list)","text":"Variable Where it is read Default (if not set) Description <code>APP_ENV</code> <code>config.py</code> (top) <code>development</code> Determines which configuration class (<code>DevelopmentConfig</code>, <code>TestConfig</code>, <code>ProductionConfig</code>) is used. Accepted values: <code>development</code>, <code>test</code>, <code>production</code>. <code>MUSIC_ROOT</code> <code>BaseConfig.MUSIC_ROOT</code> <code>/music</code> Absolute path to the music collection. Must be readable by the process. <code>DATA_ROOT</code> <code>BaseConfig.DATA_ROOT</code> <code>../collection-data</code> (project-root relative) Writable directory for all app-generated files. <code>PASSWORD</code> <code>BaseConfig.PASSWORD</code> (and overridden in subclasses) <code>dev-password</code> (dev) / <code>test-password</code> (test) / must be set (prod) Login password for the web UI. <code>AUDIO_CACHE_ENABLED</code> <code>BaseConfig.AUDIO_CACHE_ENABLED</code> <code>True</code> Toggle the whole audio-caching subsystem on/off. <code>AUDIO_CACHE_DEFAULT_QUALITY</code> <code>BaseConfig.AUDIO_CACHE_DEFAULT_QUALITY</code> <code>medium</code> Default quality for cached audio files (<code>low</code>, <code>medium</code>, <code>high</code>). <code>AUDIO_CACHE_MAX_WORKERS</code> <code>BaseConfig.AUDIO_CACHE_MAX_WORKERS</code> <code>4</code> Number of worker threads used for background transcoding. <code>AUDIO_CACHE_PRECACHE_ON_UPLOAD</code> <code>BaseConfig.AUDIO_CACHE_PRECACHE_ON_UPLOAD</code> <code>True</code> When a mixtape is saved, automatically start caching its tracks. <code>AUDIO_CACHE_PRECACHE_QUALITIES</code> <code>BaseConfig.AUDIO_CACHE_PRECACHE_QUALITIES</code> <code>[\"medium\"]</code> List of qualities to precache (e.g., <code>[\"low\",\"medium\",\"high\"]</code>). <code>DEBUG</code> Set by subclass (<code>DevelopmentConfig.DEBUG = True</code>) <code>False</code> (prod) Enables Flask\u2019s debugger and auto-reload. <code>PYTHONPATH</code> / <code>PYTHON_VERSION</code> Not read by the app, but documented for developers. \u2013 Pinning to Python\u202f3.11+ via <code>.python-version</code> (used by <code>uv</code>). <p>All variables can be supplied via: * A <code>.env</code> file in the project root (development only). * Docker environment variables (<code>-e VAR=value</code> or <code>environment:</code> block in <code>docker-compose.yml</code>). * Direct export in the shell (<code>export VAR=value</code>).</p>"},{"location":"development/configuration.html#derived-paths-never-set-directly","title":"\ud83d\udd17 Derived Paths (never set directly)","text":"<pre><code>class BaseConfig:\n    DB_PATH = DATA_ROOT / \"collection.db\"\n    MIXTAPE_DIR = DATA_ROOT / \"mixtapes\"\n    COVER_DIR = MIXTAPE_DIR / \"covers\"\n    AUDIO_CACHE_DIR = DATA_ROOT / \"cache\" / \"audio\"\n</code></pre> <p>Because these are computed once when the class is defined, they automatically follow any change to <code>DATA_ROOT</code>. Changing <code>DB_PATH</code> or <code>MIXTAPE_DIR</code> manually would have no effect and could cause path mismatches.</p>"},{"location":"development/configuration.html#directory-creation-ensure_dirs","title":"\ud83d\udcc1 Directory Creation (<code>ensure_dirs</code>)","text":"<pre><code>@classmethod\ndef ensure_dirs(cls):\n    cls.DATA_ROOT.mkdir(parents=True, exist_ok=True)\n    cls.MIXTAPE_DIR.mkdir(parents=True, exist_ok=True)\n    cls.COVER_DIR.mkdir(parents=True, exist_ok=True)\n    cls.AUDIO_CACHE_DIR.mkdir(parents=True, exist_ok=True)\n</code></pre> <p>Called early in <code>app.py</code> (<code>config.ensure_dirs()</code>).</p> <p>It guarantees that all writable directories exist before any component tries to write files. If a directory already exists, <code>exist_ok=True</code> prevents an error.</p>"},{"location":"development/configuration.html#configuration-classes","title":"\ud83c\udff7\ufe0f Configuration Classes","text":"Class Inherits from Overrides / Special behaviour <code>BaseConfig</code> \u2013 Defines all defaults and <code>ensure_dirs</code>. <code>DevelopmentConfig</code> <code>BaseConfig</code> <code>DEBUG = True</code>; <code>PASSWORD</code> defaults to <code>\"dev-password\"</code> (or env var). <code>TestConfig</code> <code>BaseConfig</code> Uses a temporary <code>DATA_ROOT</code> (<code>/tmp/mixtape-test-data</code>) and a dummy <code>MUSIC_ROOT</code> (<code>/tmp/test-music</code>). Ideal for CI pipelines. <code>ProductionConfig</code> <code>BaseConfig</code> <code>DEBUG = False</code>; requires <code>PASSWORD</code> to be set via env var (no fallback). All other values are taken from the environment or defaults. <p><code>app.py</code> selects the class with:</p> <pre><code>env = os.getenv(\"APP_ENV\", \"development\")\nif env == \"production\":\n    config_cls = ProductionConfig\nelif env == \"test\":\n    config_cls = TestConfig\nelse:\n    config_cls = DevelopmentConfig\n</code></pre>"},{"location":"development/configuration.html#docker-integration","title":"\ud83d\udc33 Docker Integration","text":"<p>For more information look at the Docker Deployment documentation.</p>"},{"location":"development/configuration.html#typical-scenarios-what-youll-see-on-disk","title":"\ud83d\uddbc\ufe0f Typical Scenarios (what you\u2019ll see on disk)","text":"Scenario APP_ENV MUSIC_ROOT DATA_ROOT Resulting layout (host side) Local dev, no <code>.env</code> development /home/mark/Music (hard-coded default) ../collection-data (next to the repo) <code>collection-data/collection.db</code>, <code>collection-data/mixtapes/\u2026</code>, <code>collection-data/cache/audio/\u2026</code> Local dev + <code>.env</code> development Whatever you set in <code>.env</code> Whatever you set in <code>.env</code> Same as above, but paths reflect the values you placed in <code>.env</code>. Docker prod production /music (mounted read-only) /app/collection-data (mounted read-write) Inside the container the same layout as above; on the host you\u2019ll see the folder you bound to <code>/app/collection-data</code>. Automated tests test /tmp/test-music (created by the test harness) /tmp/mixtape-test-data Temporary directories that are cleaned up after the test suite finishes."},{"location":"development/configuration.html#bestpractice-recommendations","title":"\ud83d\udca1 Best\u2011Practice Recommendations","text":"<ol> <li>Never commit a <code>.env</code> containing secrets. Keep passwords out of source control.</li> <li>In production, set <code>APP_PASSWORD</code> (or any other secret) via the orchestration platform (Docker\u2011Compose <code>env_file</code>, Kubernetes secret, etc.). The app will refuse to start without it.</li> <li>Mount <code>DATA_ROOT</code> on persistent storage (a volume, bind\u2011mount, or cloud\u2011disk) so that the SQLite DB and cached audio survive container restarts.</li> <li>Keep <code>MUSIC_ROOT</code> read\u2011only \u2013 the scanner only needs to read files; mounting it as ro prevents accidental modifications from the container.</li> <li>Adjust audio\u2011caching workers (<code>AUDIO_CACHE_MAX_WORKERS</code>) according to the host CPU count; the default of 4 works well on most 4\u2011core machines.</li> <li>If you change <code>DATA_ROOT</code> after the app has started, you must delete the old DB and mixtape folder (or run a migration script) because the paths are baked into the SQLite DB.</li> <li>Run the test suite with <code>APP_ENV=test</code> (or simply <code>pytest</code> which forces <code>TestConfig</code>). This isolates filesystem side\u2011effects to <code>/tmp</code>.</li> </ol>"},{"location":"development/configuration.html#api","title":"\ud83d\udd0c API","text":""},{"location":"development/configuration.html#src.config.config","title":"<code>src.config.config</code>","text":"<p>Classes:</p> Name Description <code>BaseConfig</code> <p>Base configuration shared by all application environments.</p> <code>DevelopmentConfig</code> <p>Development configuration used when running the app locally.</p> <code>ProductionConfig</code> <p>Production configuration for running the app in deployed environments.</p> <code>TestConfig</code> <p>Test configuration tailored for running the application's automated tests.</p>"},{"location":"development/configuration.html#src.config.config.BaseConfig","title":"<code>BaseConfig</code>","text":"<p>Base configuration shared by all application environments.</p> <p>This configuration defines paths, credentials, and caching settings that control how the app accesses music, stores data, and manages audio cache.</p> <p>Methods:</p> Name Description <code>ensure_dirs</code> <p>Create all required data and cache directories if they do not exist.</p> Source code in <code>src/config/config.py</code> <pre><code>class BaseConfig:\n    \"\"\"Base configuration shared by all application environments.\n\n    This configuration defines paths, credentials, and caching settings that\n    control how the app accesses music, stores data, and manages audio cache.\n    \"\"\"\n    # 1. Music library location\n    MUSIC_ROOT = Path(os.getenv(\"MUSIC_ROOT\", \"/music\"))\n\n    # 2. NEW: Single data root for everything the app writes\n    DATA_ROOT = Path(\n        os.getenv(\n            \"DATA_ROOT\",\n            # Fallback only used in local dev if no .env and no env var\n            BASE_DIR.parent / \"collection-data\"\n        )\n    )\n\n    # Derived paths \u2014 never override these directly\n    DB_PATH = DATA_ROOT / \"collection.db\"\n    MIXTAPE_DIR = DATA_ROOT / \"mixtapes\"\n    COVER_DIR = MIXTAPE_DIR / \"covers\"\n\n    # Cache directory audio\n    AUDIO_CACHE_DIR = DATA_ROOT / \"cache\" / \"audio\"\n\n    # Keep password handling clean\n    PASSWORD = os.getenv(\"PASSWORD\", \"dev-password\")\n\n    # Audio caching settings\n    AUDIO_CACHE_ENABLED = True\n    AUDIO_CACHE_DEFAULT_QUALITY = \"medium\"\n    AUDIO_CACHE_MAX_WORKERS = 4\n    AUDIO_CACHE_PRECACHE_ON_UPLOAD = True\n    AUDIO_CACHE_PRECACHE_QUALITIES = [\"medium\"]  # Can add [\"low\", \"medium\", \"high\"]\n\n\n    @classmethod\n    def ensure_dirs(cls):\n        \"\"\"Create all required data and cache directories if they do not exist.\n\n        This method prepares the filesystem so the application can safely store\n        databases, mixtapes, covers, and cached audio files.\n        \"\"\"\n        cls.DATA_ROOT.mkdir(parents=True, exist_ok=True)\n        cls.MIXTAPE_DIR.mkdir(parents=True, exist_ok=True)\n        cls.COVER_DIR.mkdir(parents=True, exist_ok=True)\n        cls.AUDIO_CACHE_DIR.mkdir(parents=True, exist_ok=True)\n</code></pre>"},{"location":"development/configuration.html#src.config.config.BaseConfig.ensure_dirs","title":"<code>ensure_dirs()</code>  <code>classmethod</code>","text":"<p>Create all required data and cache directories if they do not exist.</p> <p>This method prepares the filesystem so the application can safely store databases, mixtapes, covers, and cached audio files.</p> Source code in <code>src/config/config.py</code> <pre><code>@classmethod\ndef ensure_dirs(cls):\n    \"\"\"Create all required data and cache directories if they do not exist.\n\n    This method prepares the filesystem so the application can safely store\n    databases, mixtapes, covers, and cached audio files.\n    \"\"\"\n    cls.DATA_ROOT.mkdir(parents=True, exist_ok=True)\n    cls.MIXTAPE_DIR.mkdir(parents=True, exist_ok=True)\n    cls.COVER_DIR.mkdir(parents=True, exist_ok=True)\n    cls.AUDIO_CACHE_DIR.mkdir(parents=True, exist_ok=True)\n</code></pre>"},{"location":"development/configuration.html#src.config.config.DevelopmentConfig","title":"<code>DevelopmentConfig</code>","text":"<p>               Bases: <code>BaseConfig</code></p> <p>Development configuration used when running the app locally.</p> <p>This configuration enables debugging and uses local defaults suitable for iterative development.</p> Source code in <code>src/config/config.py</code> <pre><code>class DevelopmentConfig(BaseConfig):\n    \"\"\"Development configuration used when running the app locally.\n\n    This configuration enables debugging and uses local defaults suitable for\n    iterative development.\n    \"\"\"\n    DEBUG = True\n    PASSWORD = os.getenv(\"PASSWORD\", \"dev-password\")\n</code></pre>"},{"location":"development/configuration.html#src.config.config.ProductionConfig","title":"<code>ProductionConfig</code>","text":"<p>               Bases: <code>BaseConfig</code></p> <p>Production configuration for running the app in deployed environments.</p> <p>This configuration relies solely on environment variables and disables development-focused settings like debugging.</p> Source code in <code>src/config/config.py</code> <pre><code>class ProductionConfig(BaseConfig):\n    \"\"\"Production configuration for running the app in deployed environments.\n\n    This configuration relies solely on environment variables and disables\n    development-focused settings like debugging.\n    \"\"\"\n    DEBUG = False\n    # In production we trust only environment variables (set via docker-compose)\n    PASSWORD = os.getenv(\"PASSWORD\")\n</code></pre>"},{"location":"development/configuration.html#src.config.config.TestConfig","title":"<code>TestConfig</code>","text":"<p>               Bases: <code>BaseConfig</code></p> <p>Test configuration tailored for running the application's automated tests.</p> <p>This configuration isolates test data and music paths to avoid affecting development or production environments.</p> Source code in <code>src/config/config.py</code> <pre><code>class TestConfig(BaseConfig):\n    \"\"\"Test configuration tailored for running the application's automated tests.\n\n    This configuration isolates test data and music paths to avoid affecting\n    development or production environments.\n    \"\"\"\n    PASSWORD = \"test-password\"\n    DATA_ROOT = Path(\"/tmp/mixtape-test-data\")  # isolated for tests\n    MUSIC_ROOT = Path(\"/tmp/test-music\")\n</code></pre>"},{"location":"development/docker.html","title":"Local Development (Docker)","text":""},{"location":"development/docker.html#local-development-with-docker","title":"Local Development with Docker","text":"<p>While uv is recommended for local dev (faster iteration), Docker is great for testing production-like environments.</p>"},{"location":"development/docker.html#building-the-image","title":"Building the Image","text":"<p>How to build the Docker image locally:</p> <pre><code>docker build -f docker/Dockerfile -t mixtape-society:dev .\n# Or with compose: docker compose -f docker/docker-compose.yml build\n</code></pre> <p>Run: <code>docker compose up</code> (mount volumes as needed). Note: No hot-reload\u2014use <code>uv run app.py</code> for that. Prod images are auto-built on main pushes via GitHub Actions. For full deployment, see Deployment.</p>"},{"location":"development/intro.html","title":"Project Overview","text":""},{"location":"development/intro.html#development-guide-mixtape-society","title":"Development Guide \u2013 Mixtape Society","text":"<p>Welcome to the Mixtape Society development guide. This section explains how the project is organized, how to set up a local development environment, how to run tests, and where to find the architectural documentation.</p>"},{"location":"development/intro.html#project-structure","title":"\ud83d\udcc2 Project Structure","text":"<ul> <li>\ud83d\udcc1 mixtape\u2011society/<ul> <li>\ud83d\udc77\u200d\u2640\ufe0f .github/<ul> <li>\u2699\ufe0f workflows/<ul> <li><code>docker-image.yml</code> \u2013 Build Docker images on GHCR</li> <li><code>docs.yml</code> \u2013 Publish GitHub Pages</li> </ul> </li> </ul> </li> <li>\ud83d\udc33 docker/ \u2013 Dockerfiles &amp; Compose configs</li> <li>\ud83d\udcda docs/ \u2013 MkDocs source (this folder)</li> <li>\ud83d\udcc2 src/<ul> <li><code>app.py</code> \u2013 Flask entry point</li> <li>\ud83d\udcc2 <code>audio_cache/</code> \u2013 Audio transcoding &amp; caching</li> <li><code>auth.py</code> \u2013 Authentication utilities</li> <li>\ud83d\udcc2 <code>common/</code> \u2013 Infrastructure\u2011free abstractions</li> <li>\ud83d\udcc2 <code>config/</code> \u2013 Environment\u2011variable handling</li> <li>\ud83d\udcc2 <code>logtools/</code> \u2013 Structured logging helpers</li> <li>\ud83d\udcc2 <code>mixtape_manager/</code> \u2013 Mixtape persistence layer</li> <li>\ud83d\udcc2 <code>musiclib/</code> \u2013 Music indexing (TinyTag + SQLite)</li> <li>\ud83d\udcc2 <code>routes/</code> \u2013 Flask blueprints (browser, editor, \u2026)</li> <li>\ud83d\udcc2 <code>static/</code><ul> <li>\ud83d\udcc2 <code>css/</code> \u2013 Stylesheets for Jinja2 templates</li> <li>\ud83d\udcc2 <code>js/</code> \u2013 Front\u2011end scripts</li> </ul> </li> <li>\ud83d\udcc2 <code>templates/</code> \u2013 Jinja2 views</li> <li>\ud83d\udcc2 <code>utilities/</code> \u2013 Miscellaneous helper functions</li> </ul> </li> <li>\ud83d\udcc4 <code>.gitignore</code> \u2013 Ignored files for Git</li> <li>\ud83d\udcc4 <code>LICENSE</code> \u2013 Project license</li> <li>\ud83d\udcc4 <code>mkdocs.yml</code> \u2013 MkDocs site configuration</li> <li>\ud83d\udcc4 <code>pyproject.toml</code> \u2013 Poetry\u2011style project metadata</li> <li>\ud83d\udcc4 <code>.python-version</code> \u2013 Python version pinning (3.13+)</li> <li>\ud83d\udcc4 <code>README.md</code> \u2013 Repository overview</li> <li>\ud83d\udcc4 <code>uv.lock</code> \u2013 uv\u2011managed lockfile</li> </ul> </li> </ul> <p>Tip</p> <p>The <code>src/</code> package is deliberately framework\u2011agnostic; core modules (<code>musiclib</code>, <code>mixtape_manager</code>, <code>audio_cache</code>) contain no Flask imports, making them easy to unit\u2011test.</p>"},{"location":"development/intro.html#prerequisites","title":"\ud83d\udee0\ufe0f Prerequisites","text":"Tool Minimum Version Install Command Python 3.13+ (pinned in <code>.python-version</code>) Use your system package manager or <code>pyenv</code> uv (modern Python package manager) Latest <code>curl -LsSf https://astral.sh/uv/install.sh | sh</code> Docker (optional, for containerised dev) Engine\u202f\u2265\u202f20.10 Follow Docker\u2019s official install guide <p>Why <code>uv</code>? It resolves and installs dependencies without a <code>requirements.txt</code>, speeds up virtual\u2011environment creation, and produces a deterministic <code>uv.lock</code>.</p>"},{"location":"development/intro.html#local-development-workflow-uv","title":"\ud83d\ude80 Local Development Workflow (uv)","text":"<pre><code># 1\ufe0f\u20e3 Install runtime dependencies\nuv sync\n\n# 2\ufe0f\u20e3 Install development\u2011only dependencies (testing, linting, docs)\nuv sync --extra dev\n\n# 3\ufe0f\u20e3 Run the Flask development server\nuv run python -m app\n\n# 4\ufe0f\u20e3 Add a new runtime dependency (updates pyproject.toml &amp; uv.lock)\nuv add &lt;package-name&gt;\n</code></pre> <p>All commands are executed from the repository root.</p> <p>The development server automatically reloads when you edit files (Flask\u2019s built\u2011in reloader is enabled because <code>APP_ENV=development</code>).</p>"},{"location":"development/intro.html#dockerbased-development","title":"\ud83d\udc33 Docker\u2011Based Development","text":"<p>The repository ships with a Docker Compose configuration for rapid iteration:</p> <pre><code># Build the development image\ndocker compose -f docker/docker-compose.yml build\n\n# Start the stack (Flask + optional reverse\u2011proxy)\ndocker compose -f docker/docker-compose.yml up -d\n</code></pre> <p>The compose file mounts the source code into the container, so you can edit files locally and see changes instantly (no hot\u2011reload inside the container, but the Flask dev server picks up changes).</p>"},{"location":"development/intro.html#useful-shortcuts","title":"Useful shortcuts","text":"Command What it does <code>docker compose logs -f mixtape</code> Tail the Flask logs. <code>docker compose exec mixtape bash</code> Drop into the container shell. <code>docker compose down -v</code> Tear down and delete the persisted volume (useful for a clean start)."},{"location":"development/intro.html#architectural-documentation","title":"\ud83c\udfdb\ufe0f Architectural Documentation","text":"<p>This section dives into the inner workings of Mixtape Society. Each page focuses on a specific subsystem, explains its responsibilities, and shows how it interacts with the rest of the project.</p>"},{"location":"development/intro.html#core-modules","title":"Core Modules","text":"Module What it does Music Library (<code>musiclib</code>) Handles scanning, indexing, and full\u2011text search of the audio collection (TinyTag + SQLite\u202fFTS5). Mixtape Manager Persists mixtape JSON files, manages cover images, and provides CRUD operations. Audio Caching Transcodes FLAC files to MP3/OGG on\u2011demand, stores them in a cache, and streams via HTTP range requests. Configuration Centralised environment\u2011variable handling, directory layout, and defaults. Utilities Helper functions (logging, image processing, misc tools)."},{"location":"development/intro.html#flask-application-entry-point","title":"Flask Application (Entry Point)","text":"<p>The <code>app.py</code> file wires everything together:</p> <ul> <li>Initializes the Flask app, CORS, and rate limiting.</li> <li>Sets up core services (<code>MusicCollectionUI</code>, <code>MixtapeManager</code>, <code>AudioCache</code>).</li> <li>Registers blueprints for auth, browser, play, editor, and OG\u2011cover routes.</li> <li>Provides global error handling (e.g., <code>DatabaseCorruptionError</code>).</li> </ul> <p>\ud83d\udcc4 Read the full walkthrough</p>"},{"location":"development/intro.html#blueprint-route-overview","title":"Blueprint / Route Overview","text":"Blueprint URL Prefix Primary Concern Authentication <code>/auth</code> Login / logout, session handling. Browser <code>/mixtapes</code> List, play, delete, and serve static mixtape files. Play <code>/play</code> Audio streaming endpoint (<code>/play/&lt;slug&gt;</code>), range requests, and public player page. Editor <code>/editor</code> Mixtape creation / editing UI, JSON save, cover generation, and background caching trigger. OG\u2011Cover <code>/og</code> Dynamic Open\u2011Graph images for social sharing. <p>Each blueprint has its own Markdown file that details the routes, request flow, and any special middleware:</p> <ul> <li>Browser UI &amp; mixtape management</li> <li>Editor UI, save logic, and progress modal.</li> <li>Streaming, MIME detection, and range handling.</li> <li>Session\u2011based login flow.</li> <li>OG image generation.</li> </ul>"},{"location":"development/intro.html#data-flow-summary","title":"\ud83d\udd00 Data Flow Summary","text":"<pre><code>flowchart TD\n    subgraph Client\n        UI[\"Web UI\"]\n    end\n\n    subgraph Server\n        A[\"Flask app (app.py)\"]\n        B[MusicCollectionUI]\n        C[MixtapeManager]\n        D[AudioCache]\n        E[Routes]\n    end\n\n    UI --&gt;|HTTP request| A\n    A --&gt;|calls| B\n    A --&gt;|calls| C\n    A --&gt;|calls| D\n    A --&gt;|dispatches to| E\n\n    B --&gt;|reads/writes| SQLite[(SQLite DB)]\n    C --&gt;|reads/writes| JSON[(Mixtape JSON files)]\n    D --&gt;|stores| Cache[(Cached audio files)]\n\n    style Client stroke:#333,stroke-width:1px\n    style Server stroke:#333,stroke-width:1px</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom  <p>The diagram illustrates how a request travels from the browser through the Flask entry point, into the core services, and finally reaches the persistent storage.</p>"},{"location":"development/intro.html#contributing-guidelines","title":"\ud83d\udee1\ufe0f Contributing Guidelines","text":"<ol> <li>Fork the repository and create a feature branch (<code>git checkout -b feature/awesome-feature</code>).</li> <li>Install dev dependencies (<code>uv sync --extra dev</code>).</li> <li>Lint &amp; format (<code>uv run ruff check . &amp;&amp; uv run ruff format .</code>).</li> <li>Commit with a clear, conventional\u2011commit\u2011style message.</li> <li>Open a Pull Request targeting <code>master</code>. Include:<ul> <li>A concise description of the change.</li> <li>Screenshots or diagrams if UI is affected.</li> <li>A reference to any related issue (Closes #123).</li> </ul> </li> </ol> <p>Tip</p> <p>Code Style \u2013 Follow the existing codebase: type hints everywhere, use <code>logtools.get_logger</code> for logging, and keep Flask\u2011specific code inside the <code>routes/</code> package.</p>"},{"location":"development/intro.html#where-to-go-next","title":"\ud83e\udded Where to Go Next?","text":"<ul> <li>Deep dive into a module \u2013 Click any link in the tables above.</li> <li>Explore the code \u2013 All source files live under <code>src/</code> (e.g., <code>src/musiclib/reader.py</code>).</li> <li>Contribute \u2013 Follow the guidelines in this page and submit a PR.</li> </ul> <p>Happy hacking! \ud83c\udfa7\ud83d\ude80</p>"},{"location":"development/mixtape_manager.html","title":"Mixtape Manager","text":""},{"location":"development/mixtape_manager.html#mixtape-manager","title":"Mixtape Manager","text":"<p>The <code>mixtape_manager</code> package defines the <code>MixtapeManager</code> class, This class acts as the core data access and management layer for mixtape-related features in the larger system, abstracting away file system operations and data consistency concerns.</p> <p>It is responsible for managing the storage, retrieval, and organization of mixtape data and their associated cover images on disk. It provides a high-level interface for saving, deleting, listing, and loading mixtape metadata, handling both JSON data and image files. The class ensures that mixtape data is consistently stored, cover images are processed from base64, and metadata is maintained for easy retrieval and display.</p>"},{"location":"development/mixtape_manager.html#architecture-overview","title":"\ud83c\udfdb\ufe0f Architecture Overview","text":"<pre><code>classDiagram\n    class MixtapeManager {\n        -Path path_mixtapes\n        -Path path_cover\n        -MusicCollection collection\n        -Logger _logger\n        +MixtapeManager(path_mixtapes, collection, logger=None)\n        +save(mixtape_data) str\n        +update(slug, updated_data) str\n        +get(slug) dict|None\n        +list_all() list[dict]\n        +delete(slug) None\n        -_sanitize_title(title) str\n        -_generate_unique_slug(base_slug, current_slug=None) str\n        -_save_with_slug(mixtape_data, title, slug) str\n        -_process_cover(cover_data, slug) str|None\n        -_cover_resize(image, new_width=1200) Image\n        -_find_by_client_id(client_id) dict|None\n        -_verify_against_collection(data) (dict, bool|None)\n        -_verify_mixtape_metadata(data) dict\n        -_normalize_timestamps(data) dict\n        -_convert_old_mixtape(data) dict\n    }</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom  <ul> <li>Storage root \u2013 <code>path_mixtapes</code> (provided by the caller).</li> <li>Cover sub\u2011directory \u2013 <code>path_cover = path_mixtapes / \"covers\"</code> (created automatically).</li> <li>Dependency \u2013 <code>Pillow</code> (<code>PIL.Image</code>) is required for cover\u2011image resizing.</li> </ul>"},{"location":"development/mixtape_manager.html#filesystem-layout-json-schema","title":"\ud83d\udcc2 Filesystem Layout &amp; JSON Schema","text":"<pre><code>/path/to/mixtapes/\n\u2502\n\u251c\u2500 my\u2011awesome\u2011mix.json          \u2190 Mixtape metadata (UTF\u20118 JSON)\n\u251c\u2500 another\u2011mix.json\n\u2502\n\u2514\u2500 covers/\n   \u251c\u2500 my\u2011awesome\u2011mix.jpg       \u2190 JPEG cover (max width 1200\u202fpx, quality\u202f100)\n   \u2514\u2500 another\u2011mix.jpg\n</code></pre>"},{"location":"development/mixtape_manager.html#minimal-json-structure-all-fields-are-optional-unless-noted","title":"Minimal JSON structure (all fields are optional unless noted)","text":"Field Type Description <code>title</code> <code>str</code> Human-readable title (used for slug generation). <code>client_id</code> <code>str</code> Optional identifier that ties a mixtape to a specific client/device. <code>created_at</code> ISO-8601 <code>str</code> Set on first save; auto-filled if missing. <code>updated_at</code> ISO-8601 <code>str</code> Refreshed on every save or update. <code>cover</code> <code>str</code> Relative path to the JPEG cover (<code>covers/&lt;slug&gt;.jpg</code>) or a data-URI (<code>data:image/...</code>). <code>liner_notes</code> <code>str</code> Free-form notes; defaults to <code>\"\"</code>. <code>tracks</code> <code>list[dict]</code> Each entry must contain at least <code>path</code> (relative to the music root). Other keys (<code>artist</code>, <code>album</code>, <code>track</code>, <code>duration</code>, <code>filename</code>, <code>cover</code>) are filled/validated on read. <code>slug</code> <code>str</code> (added on output) Filesystem-safe identifier derived from <code>title</code>, added by the manager on output."},{"location":"development/mixtape_manager.html#behavior-details","title":"\u2699\ufe0f Behavior Details","text":""},{"location":"development/mixtape_manager.html#slug-generation-uniqueness","title":"Slug generation &amp; uniqueness","text":"<ul> <li><code>_sanitize_title</code> removes any character that isn\u2019t alphanumeric, hyphen, underscore, or space, then strips surrounding whitespace.</li> <li><code>_generate_unique_slug</code> checks <code>path_mixtapes/*.json</code>. If a conflict exists it appends <code>-1</code>, <code>-2</code>, \u2026 until a free filename is found.</li> <li>When updating an existing mixtape you may pass <code>current_slug</code> so the method can reuse the same filename.</li> </ul>"},{"location":"development/mixtape_manager.html#clientid-reuse-on-save","title":"Client\u2011ID reuse on save","text":"<ul> <li>If <code>mixtape_data</code> contains a <code>client_id</code>, <code>save()</code> first calls <code>_find_by_client_id</code>.</li> <li>The first mixtape that matches that <code>client_id</code> is updated in\u2011place (the same slug is kept).</li> <li>If no match is found a brand\u2011new mixtape is created.</li> </ul>"},{"location":"development/mixtape_manager.html#timestamp-handling-legacy-migration","title":"Timestamp handling &amp; legacy migration","text":"<ul> <li>On first creation both <code>created_at</code> and <code>updated_at</code> are set to <code>datetime.now().isoformat()</code>.</li> <li>On every <code>update</code> only <code>updated_at</code> is refreshed.</li> <li><code>_normalize_timestamps</code> migrates a historic <code>saved_at</code> field to <code>updated_at</code> and guarantees that <code>created_at</code> and <code>updated_at</code> are present (fills missing values with <code>None</code> or the current time).</li> </ul>"},{"location":"development/mixtape_manager.html#cover-image-handling","title":"Cover image handling","text":"Helper What it does Important details <code>_process_cover(cover_data: str, slug: str) \u2192 str \\| None</code> Decodes a <code>data:image/...;base64,\u2026</code> string, resizes the image, saves it as a JPEG under <code>covers/&lt;slug&gt;.jpg</code>, and returns the relative path (<code>covers/&lt;slug&gt;.jpg</code>). Returns <code>None</code> on any error; the calling code keeps the original <code>cover</code> value. <code>_cover_resize(image: Image, new_width: int = 1200) \u2192 Image</code> Resizes the Pillow <code>Image</code> to a maximum width of <code>new_width</code> while preserving aspect ratio. Uses <code>Image.LANCZOS</code> for high-quality down-sampling. <code>_save_with_slug</code> Handles cover persistence during save operations. If <code>cover</code> starts with <code>data:image</code>, delegates to <code>_process_cover</code>. If it is a plain string (already a relative path), it is stored unchanged. Errors during cover processing are logged but do not abort the mixtape save. <ul> <li>The JPEG is saved with quality\u202f=\u202f100 (lossless as far as JPEG permits).</li> <li>The filename on disk is exactly <code>covers/&lt;slug&gt;.jpg</code> \u2013 the method builds the relative path string for the JSON field.</li> </ul>"},{"location":"development/mixtape_manager.html#verification-against-the-music-collection","title":"Verification against the music collection","text":"<ul> <li><code>get(slug)</code> loads the JSON, then calls <code>_verify_against_collection.</code></li> <li>For each track it fetches the canonical record from the injected MusicCollection (<code>self.collection.get_track(path=Path(track* [\"path\"]))</code>).</li> <li>Missing or stale fields (<code>artist</code>, <code>album</code>, <code>track</code>, <code>filename</code>, <code>duration</code>, <code>cover</code>) are refreshed.</li> <li>If the collection cannot be accessed (e.g., DB corruption) the method falls back to the raw JSON and logs a warning.</li> </ul>"},{"location":"development/mixtape_manager.html#legacy-field-conversion","title":"Legacy field conversion","text":"<ul> <li>Older mixtapes stored a track title under the key <code>title</code>. <code>_convert_old_mixtape</code> renames that key to <code>track</code> so the rest of the code can rely on a uniform schema.</li> </ul>"},{"location":"development/mixtape_manager.html#error-handling-robustness","title":"Error handling &amp; robustness","text":"<ul> <li>Corrupted JSON files \u2013 both <code>list_all</code> and <code>_find_by_client_id</code> catch json.JSONDecodeError / OSError, log a warning, and simply skip the offending file.</li> <li>Cover\u2011image failures \u2013 <code>_process_cover</code> catches any exception, logs an error, and returns None. The mixtape is still saved; the <code>cover</code> field will be omitted or retain its previous value.</li> <li>Missing slug on update \u2013 <code>update</code> raises <code>FileNotFoundError</code> if the target JSON does not exist, making the failure explicit to the caller.</li> </ul>"},{"location":"development/mixtape_manager.html#sequence-diagram","title":"\ud83d\udd01 Sequence Diagram","text":"<pre><code>sequenceDiagram\n    participant Client\n    participant MixtapeManager\n    participant Disk\n    participant MusicCollection\n\n    Client-&gt;&gt;MixtapeManager: save(mixtape_data)\n    MixtapeManager-&gt;&gt;Disk: Write JSON file (&lt;slug&gt;.json)\n    alt cover is a data\u2011uri\n        MixtapeManager-&gt;&gt;Disk: Decode + resize + JPEG \u2192 covers/&lt;slug&gt;.jpg\n    end\n    MixtapeManager--&gt;&gt;Client: slug\n\n    Client-&gt;&gt;MixtapeManager: get(slug)\n    MixtapeManager-&gt;&gt;Disk: Read JSON file\n    MixtapeManager-&gt;&gt;MusicCollection: get_track for each track\n    MusicCollection--&gt;&gt;MixtapeManager: track metadata\n    MixtapeManager--&gt;&gt;Client: enriched mixtape dict\n\n    Client-&gt;&gt;MixtapeManager: list_all()\n    MixtapeManager-&gt;&gt;Disk: glob *.json, read each\n    MixtapeManager--&gt;&gt;Client: sorted list of mixtapes\n\n    Client-&gt;&gt;MixtapeManager: delete(slug)\n    MixtapeManager-&gt;&gt;Disk: Delete JSON + optional cover\n    MixtapeManager--&gt;&gt;Client: (none)</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom"},{"location":"development/mixtape_manager.html#api","title":"\ud83d\udd0c API","text":""},{"location":"development/mixtape_manager.html#src.mixtape_manager.mixtape_manager.MixtapeManager","title":"<code>src.mixtape_manager.mixtape_manager.MixtapeManager</code>","text":"<p>Manages mixtape files and their associated cover images.</p> <p>Provides functionality to create, update, delete, list, and retrieve mixtapes stored on disk.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Initializes the MixtapeManager with paths for mixtapes and covers.</p> <code>delete</code> <p>Deletes a mixtape and its associated cover image by slug.</p> <code>get</code> <p>Retrieves a single mixtape by its slug if it exists and is valid.</p> <code>list_all</code> <p>Lists all mixtapes stored on disk.</p> <code>save</code> <p>Creates a new mixtape or updates an existing one based on client identity.</p> <code>update</code> <p>Updates an existing mixtape with new data while preserving its identity.</p> Source code in <code>src/mixtape_manager/mixtape_manager.py</code> <pre><code>class MixtapeManager:\n    \"\"\"Manages mixtape files and their associated cover images.\n\n    Provides functionality to create, update, delete, list, and retrieve mixtapes stored on disk.\n    \"\"\"\n\n    def __init__(\n        self,\n        path_mixtapes: Path,\n        collection: MusicCollection,\n        logger: Logger | None = None,\n    ) -&gt; None:\n        \"\"\"Initializes the MixtapeManager with paths for mixtapes and covers.\n\n        Sets up the directory structure for storing mixtape JSON files and cover images.\n\n        Args:\n            path_mixtapes: Path to the directory where mixtapes are stored.\n            collection: Access to the collection's metadata\n            logger: Optional logger instance for logging actions.\n        \"\"\"\n        self._logger: Logger = logger or NullLogger()\n        self.path_mixtapes: Path = path_mixtapes\n        self.path_cover: Path = path_mixtapes / \"covers\"\n        self.path_mixtapes.mkdir(exist_ok=True)\n        self.path_cover.mkdir(exist_ok=True)\n        self.collection = collection\n\n    def _sanitize_title(self, title: str) -&gt; str:\n        \"\"\"Convert title to a URL-safe slug.\"\"\"\n        # Convert to lowercase\n        slug = title.lower()\n\n        # Replace spaces and underscores with hyphens\n        slug = re.sub(r'[\\s_]+', '-', slug)\n\n        # Remove non-alphanumeric characters (except hyphens)\n        slug = re.sub(r'[^a-z0-9-]', '', slug)\n\n        # Remove duplicate hyphens\n        slug = re.sub(r'-+', '-', slug)\n\n        # Strip leading/trailing hyphens\n        slug = slug.strip('-')\n\n        return slug or \"untitled\"\n\n    def _generate_unique_slug(\n        self, base_slug: str, current_slug: Optional[str] = None\n    ) -&gt; str:\n        \"\"\"Generates a unique slug for a mixtape based on the provided base slug.\n\n        Ensures the slug does not conflict with existing mixtape files, allowing reuse of the current slug if updating.\n\n        Args:\n            base_slug: The base string to use for the slug.\n            current_slug: The current slug, if updating an existing mixtape.\n\n        Returns:\n            str: A unique slug string.\n        \"\"\"\n        if not base_slug:\n            base_slug = \"untitled\"\n\n        slug = base_slug\n        counter = 1\n        while True:\n            json_path = self.path_mixtapes / f\"{slug}.json\"\n            if not json_path.exists() or (current_slug and slug == current_slug):\n                return slug\n            slug = f\"{base_slug}-{counter}\"\n            counter += 1\n\n    def save(self, mixtape_data: dict) -&gt; str:\n        \"\"\"Creates a new mixtape or updates an existing one based on client identity.\n\n        Reuses an existing mixtape when a matching client_id is found, otherwise generates a fresh mixtape entry.\n\n        Args:\n            mixtape_data: Dictionary containing mixtape information, including optional client_id, title, and tracks.\n\n        Returns:\n            str: The slug of the created or updated mixtape.\n        \"\"\"\n        client_id = mixtape_data.get(\"client_id\")\n        now = datetime.now().isoformat()\n\n        if existing := self._find_by_client_id(client_id):\n            slug = existing[\"slug\"]\n            self._logger.info(\n                f\"Found existing mixtape for client_id {client_id}, updating slug {slug}\"\n            )\n            return self.update(slug, mixtape_data)\n\n        # New creation\n        title = mixtape_data.get(\"title\", \"Untitled Mixtape\")\n        base_slug = self._sanitize_title(title)\n        slug = self._generate_unique_slug(base_slug)\n\n        # Preserve the client_id in the saved data\n        if client_id:\n            mixtape_data[\"client_id\"] = client_id\n\n        # Set both timestamps on first save\n        mixtape_data[\"created_at\"] = now\n        mixtape_data[\"updated_at\"] = now\n\n        return self._save_with_slug(mixtape_data=mixtape_data, title=title, slug=slug)\n\n    def _find_by_client_id(self, client_id: str | None) -&gt; dict | None:\n        \"\"\"Finds the first mixtape associated with a given client identifier.\n\n        Scans stored mixtapes to locate a matching client_id and returns its data in a convenient format.\n\n        Args:\n            client_id: The identifier used to associate a client with a mixtape.\n\n        Returns:\n            dict | None: The mixtape data including its slug if found, otherwise None.\n        \"\"\"\n        if not client_id:\n            return None\n\n        for file in self.path_mixtapes.glob(\"*.json\"):\n            try:\n                with open(file, \"r\", encoding=\"utf-8\") as f:\n                    data = json.load(f)\n                if data.get(\"client_id\") == client_id:\n                    data[\"slug\"] = file.stem\n                    return data\n            except (json.JSONDecodeError, OSError) as e:\n                self._logger.warning(f\"Skipping corrupted mixtape file {file}: {e}\")\n                continue\n        return None\n\n    def update(self, slug: str, updated_data: dict) -&gt; str:\n        \"\"\"Updates an existing mixtape with new data while preserving its identity.\n\n        Maintains the original slug and important metadata so clients can safely refresh mixtape content.\n\n        Args:\n            slug: The slug of the mixtape to update.\n            updated_data: Dictionary containing fields to update on the mixtape.\n\n        Returns:\n            str: The slug of the updated mixtape.\n        \"\"\"\n        old_json_path = self.path_mixtapes / f\"{slug}.json\"\n        if not old_json_path.exists():\n            raise FileNotFoundError(f\"Mixtape with slug '{slug}' not found.\")\n\n        with open(old_json_path, \"r\", encoding=\"utf-8\") as f:\n            existing_data = json.load(f)\n\n        # Preserve client_id\n        if \"client_id\" not in updated_data and \"client_id\" in existing_data:\n            updated_data[\"client_id\"] = existing_data[\"client_id\"]\n\n        # Merge updates\n        existing_data.update(updated_data)\n\n        # Ensure required fields\n        existing_data[\"title\"] = updated_data.get(\n            \"title\", existing_data.get(\"title\", \"Untitled Mixtape\")\n        )\n        if \"liner_notes\" not in existing_data:\n            existing_data[\"liner_notes\"] = \"\"\n\n        # Only update updated_at\n        existing_data[\"updated_at\"] = datetime.now().isoformat()\n\n        return self._save_with_slug(\n            mixtape_data=existing_data, title=existing_data[\"title\"], slug=slug\n        )\n\n    def _save_with_slug(self, mixtape_data: dict, title: str, slug: str) -&gt; str:\n        \"\"\"Saves mixtape data and its cover image using a specific slug.\n\n        Persists the mixtape JSON file and optional cover image to disk so it can be retrieved later.\n\n        Args:\n            mixtape_data: The mixtape metadata and track information to be stored.\n            title: The human-readable title of the mixtape used for logging.\n            slug: The filesystem-safe identifier used as the mixtape filename.\n\n        Returns:\n            str: The slug under which the mixtape was saved.\n        \"\"\"\n        json_path = self.path_mixtapes / f\"{slug}.json\"\n\n        if cover_value := mixtape_data.pop(\"cover\", None):\n            if cover_value.startswith(\"data:image\"):\n                try:\n                    if path_cover := self._process_cover(\n                        cover_data=cover_value, slug=slug\n                    ):\n                        mixtape_data[\"cover\"] = path_cover\n                except Exception as e:\n                    self._logger.error(f\"Failed to save new cover for {slug}: {e}\")\n            else:\n                mixtape_data[\"cover\"] = cover_value\n\n        with open(json_path, \"w\", encoding=\"utf-8\") as f:\n            json.dump(mixtape_data, f, indent=2, ensure_ascii=False)\n\n        self._logger.info(f\"Saved mixtape '{title}' as '{slug}'\")\n        return slug\n\n    def _process_cover(self, cover_data: str, slug: str) -&gt; str | None:\n        \"\"\"\n        Processes and saves a cover image from base64-encoded data.\n\n        Decodes the image data, resizes the image, and saves it as a JPEG file in the covers directory.\n        Returns the relative path to the saved cover image, or None if processing fails.\n\n        Args:\n            cover_data: The base64-encoded image data string.\n            slug: The unique identifier for the mixtape.\n\n        Returns:\n            str | None: The relative path to the saved cover image, or None if processing fails.\n        \"\"\"\n        if not cover_data or not cover_data.startswith(\"data:image\"):\n            return None\n        try:\n            _, b64data = cover_data.split(\",\", 1)\n            image = Image.open(BytesIO(b64decode(b64data)))\n\n            # Convert to RGB mode for JPEG compatibility\n            # Handle transparency by adding white background\n            if image.mode in (\"RGBA\", \"LA\", \"P\"):\n                # Create white background\n                background = Image.new(\"RGB\", image.size, (255, 255, 255))\n                if image.mode == \"P\":\n                    image = image.convert(\"RGBA\")\n                # Paste image on white background using alpha channel as mask\n                if image.mode in (\"RGBA\", \"LA\"):\n                    background.paste(image, mask=image.split()[-1])\n                else:\n                    background.paste(image)\n                image = background\n            elif image.mode != \"RGB\":\n                image = image.convert(\"RGB\")\n\n            image = self._cover_resize(image=image)\n            file_cover = self.path_cover / f\"{slug}.jpg\"\n            image.save(file_cover, \"JPEG\", quality=95, optimize=True)\n            return f\"covers/{file_cover.name}\"\n        except Exception as e:\n            self._logger.exception(f\"Cover opslaan mislukt voor {slug}: {e}\")\n            return None\n\n    def _cover_resize(self, image: Image, new_width: int = 1200) -&gt; Image:\n        \"\"\"\n        Resizes the given image to a specified width while maintaining aspect ratio.\n\n        Calculates the new height to preserve the image's proportions and resizes using high-quality Lanczos filtering.\n        If the image width is 1200 or smaller, the original image is returned unchanged.\n\n        Args:\n            image: The PIL Image object to resize.\n            new_width: The desired width of the resized image (default is 1200).\n\n        Returns:\n            Image: The resized PIL Image object, or the original if width &lt;= new_width.\n        \"\"\"\n        width, height = image.size\n        if width &lt;= new_width:\n            return image\n        new_height = int(height * (new_width / width))\n        image = image.resize((new_width, new_height), Image.LANCZOS)\n        return image\n\n    def delete(self, slug: str) -&gt; None:\n        \"\"\"\n        Deletes a mixtape and its associated cover image by slug.\n\n        Removes the mixtape JSON file and cover image from disk if they exist.\n\n        Args:\n            slug: The slug of the mixtape to delete.\n        \"\"\"\n        json_path = self.path_mixtapes / f\"{slug}.json\"\n        json_path.unlink(missing_ok=True)\n\n        cover_path = self.path_cover / f\"{slug}.jpg\"\n        cover_path.unlink(missing_ok=True)\n\n    def list_all(self) -&gt; list[dict]:\n        \"\"\"\n        Lists all mixtapes stored on disk.\n\n        Returns a list of dictionaries containing mixtape data, sorted by update or save time.\n\n        Returns:\n            list[dict]: List of all mixtape data dictionaries.\n        \"\"\"\n        mixtapes = []\n        for file in self.path_mixtapes.glob(\"*.json\"):\n            try:\n                with open(file, \"r\", encoding=\"utf-8\") as f:\n                    data = json.load(f)\n            except (json.JSONDecodeError, OSError) as e:\n                self._logger.warning(f\"Skipping corrupted mixtape file {file}: {e}\")\n                continue\n\n            slug = file.stem\n            data[\"slug\"] = slug\n            if \"liner_notes\" not in data:\n                data[\"liner_notes\"] = \"\"\n            if \"client_id\" not in data:\n                data[\"client_id\"] = None\n\n            # Normalize timestamps (will also handle legacy saved_at)\n            data = self._normalize_timestamps(data)\n\n            mixtapes.append(data)\n\n        # Sort by most recently updated first\n        mixtapes.sort(\n            key=lambda x: x.get(\"updated_at\") or x.get(\"created_at\") or \"\", reverse=True\n        )\n        return mixtapes\n\n    def get(self, slug: str) -&gt; dict | None:\n        \"\"\"Retrieves a single mixtape by its slug if it exists and is valid.\n\n        Loads the mixtape from disk, validates its tracks against the collection, and normalizes optional fields.\n\n        Args:\n            slug: The slug identifier of the mixtape to retrieve.\n\n        Returns:\n            dict | None: The validated and normalized mixtape data, or None if the mixtape is missing or invalid.\n        \"\"\"\n        path = self.path_mixtapes / f\"{slug}.json\"\n        if not path.exists():\n            return None\n\n        try:\n            with open(path, \"r\", encoding=\"utf-8\") as f:\n                data = json.load(f)\n        except (json.JSONDecodeError, OSError) as e:\n            self._logger.error(f\"Failed to read mixtape {slug}: {e}\")\n            return None\n\n        try:\n            data_verified, has_changed = self._verify_against_collection(data=data)\n            if data_verified:\n                data_verified = self._verify_mixtape_metadata(data=data_verified)\n                data_verified[\"slug\"] = slug\n                return data_verified\n            else:\n                return None\n        except Exception as e:\n            # Database unavailable (corruption, indexing, etc.)\n            # Just use the mixtape data as-is from the JSON file\n            self._logger.warning(\n                f\"Could not verify mixtape {slug} against collection: {e}. \"\n                f\"Using cached data from JSON.\"\n            )\n\n        # Always normalize metadata\n        data = self._verify_mixtape_metadata(data=data)\n        data[\"slug\"] = slug\n        return data\n\n    def _verify_against_collection(self, data: dict) -&gt; tuple[dict, bool | None]:\n        \"\"\"Verifies and refreshes mixtape track metadata against the music collection.\n\n        Compares each track entry with current collection data and updates fields when differences are detected.\n\n        Args:\n            data: The mixtape data dictionary containing a \"tracks\" list to validate.\n\n        Returns:\n            tuple[dict, bool | None]: A tuple of the updated tracks list and a flag indicating whether any changes\n            were made, or (False, None) if there are no tracks to verify.\n        \"\"\"\n        if not (tracks := data[\"tracks\"]):\n            return False, None\n        has_changes = False\n        for track in tracks:\n            track_collection = self.collection.get_track(path=Path(track[\"path\"]))\n            keys = [\n                \"filename\",\n                \"artist\",\n                \"album\",\n                \"track\",\n                \"duration\",\n                \"cover\",\n            ]\n            for key in keys:\n                if key not in track or track[key] != track_collection.get(key):\n                    has_changes = True\n                    track[key] = track_collection.get(key)\n        return data, has_changes\n\n    def _verify_mixtape_metadata(self, data: dict) -&gt; dict:\n        \"\"\"Normalizes and completes metadata fields for a mixtape.\n\n        Converts legacy structures, ensures optional fields are present, and standardizes timestamps for consistency.\n\n        Args:\n            data: The raw mixtape data dictionary to validate and normalize.\n\n        Returns:\n            dict: The updated mixtape data dictionary with normalized metadata and timestamps.\n        \"\"\"\n        data = self._convert_old_mixtape(data)\n        if \"liner_notes\" not in data:\n            data[\"liner_notes\"] = \"\"\n        if \"client_id\" not in data:\n            data[\"client_id\"] = None\n\n        data = self._normalize_timestamps(data)\n\n        return data\n\n    def _normalize_timestamps(self, data: dict) -&gt; dict:\n        \"\"\"Normalizes timestamp fields on mixtape data for consistency.\n\n        Fills in missing created_at and updated_at values and migrates legacy fields to the current schema.\n\n        Args:\n            data: The mixtape data dictionary whose timestamps should be normalized.\n\n        Returns:\n            dict: The updated mixtape data dictionary with normalized timestamps.\n        \"\"\"\n        now = datetime.now().isoformat()\n\n        # Migrate legacy saved_at \u2192 updated_at\n        if \"saved_at\" in data and \"updated_at\" not in data:\n            data[\"updated_at\"] = data.pop(\"saved_at\")\n        if \"created_at\" not in data:\n            data[\"created_at\"] = None\n        if \"updated_at\" not in data:\n            data[\"updated_at\"] = data[\"created_at\"] or now\n\n        return data\n\n    def _convert_old_mixtape(self, data: dict) -&gt; dict:\n        \"\"\"Converts legacy mixtape data into the current schema.\n\n        Renames outdated track fields so older mixtapes can be handled consistently with newer ones.\n\n        Args:\n            data: The raw mixtape data dictionary that may use legacy field names.\n\n        Returns:\n            dict: The updated mixtape data dictionary using the current field names.\n        \"\"\"\n        for track in data.get(\"tracks\", []):\n            if \"title\" in track:\n                track[\"track\"] = track.pop(\"title\")\n        return data\n</code></pre>"},{"location":"development/mixtape_manager.html#src.mixtape_manager.mixtape_manager.MixtapeManager.__init__","title":"<code>__init__(path_mixtapes, collection, logger=None)</code>","text":"<p>Initializes the MixtapeManager with paths for mixtapes and covers.</p> <p>Sets up the directory structure for storing mixtape JSON files and cover images.</p> <p>Parameters:</p> Name Type Description Default <code>path_mixtapes</code> <code>Path</code> <p>Path to the directory where mixtapes are stored.</p> required <code>collection</code> <code>MusicCollection</code> <p>Access to the collection's metadata</p> required <code>logger</code> <code>Logger | None</code> <p>Optional logger instance for logging actions.</p> <code>None</code> Source code in <code>src/mixtape_manager/mixtape_manager.py</code> <pre><code>def __init__(\n    self,\n    path_mixtapes: Path,\n    collection: MusicCollection,\n    logger: Logger | None = None,\n) -&gt; None:\n    \"\"\"Initializes the MixtapeManager with paths for mixtapes and covers.\n\n    Sets up the directory structure for storing mixtape JSON files and cover images.\n\n    Args:\n        path_mixtapes: Path to the directory where mixtapes are stored.\n        collection: Access to the collection's metadata\n        logger: Optional logger instance for logging actions.\n    \"\"\"\n    self._logger: Logger = logger or NullLogger()\n    self.path_mixtapes: Path = path_mixtapes\n    self.path_cover: Path = path_mixtapes / \"covers\"\n    self.path_mixtapes.mkdir(exist_ok=True)\n    self.path_cover.mkdir(exist_ok=True)\n    self.collection = collection\n</code></pre>"},{"location":"development/mixtape_manager.html#src.mixtape_manager.mixtape_manager.MixtapeManager.delete","title":"<code>delete(slug)</code>","text":"<p>Deletes a mixtape and its associated cover image by slug.</p> <p>Removes the mixtape JSON file and cover image from disk if they exist.</p> <p>Parameters:</p> Name Type Description Default <code>slug</code> <code>str</code> <p>The slug of the mixtape to delete.</p> required Source code in <code>src/mixtape_manager/mixtape_manager.py</code> <pre><code>def delete(self, slug: str) -&gt; None:\n    \"\"\"\n    Deletes a mixtape and its associated cover image by slug.\n\n    Removes the mixtape JSON file and cover image from disk if they exist.\n\n    Args:\n        slug: The slug of the mixtape to delete.\n    \"\"\"\n    json_path = self.path_mixtapes / f\"{slug}.json\"\n    json_path.unlink(missing_ok=True)\n\n    cover_path = self.path_cover / f\"{slug}.jpg\"\n    cover_path.unlink(missing_ok=True)\n</code></pre>"},{"location":"development/mixtape_manager.html#src.mixtape_manager.mixtape_manager.MixtapeManager.get","title":"<code>get(slug)</code>","text":"<p>Retrieves a single mixtape by its slug if it exists and is valid.</p> <p>Loads the mixtape from disk, validates its tracks against the collection, and normalizes optional fields.</p> <p>Parameters:</p> Name Type Description Default <code>slug</code> <code>str</code> <p>The slug identifier of the mixtape to retrieve.</p> required <p>Returns:</p> Type Description <code>dict | None</code> <p>dict | None: The validated and normalized mixtape data, or None if the mixtape is missing or invalid.</p> Source code in <code>src/mixtape_manager/mixtape_manager.py</code> <pre><code>def get(self, slug: str) -&gt; dict | None:\n    \"\"\"Retrieves a single mixtape by its slug if it exists and is valid.\n\n    Loads the mixtape from disk, validates its tracks against the collection, and normalizes optional fields.\n\n    Args:\n        slug: The slug identifier of the mixtape to retrieve.\n\n    Returns:\n        dict | None: The validated and normalized mixtape data, or None if the mixtape is missing or invalid.\n    \"\"\"\n    path = self.path_mixtapes / f\"{slug}.json\"\n    if not path.exists():\n        return None\n\n    try:\n        with open(path, \"r\", encoding=\"utf-8\") as f:\n            data = json.load(f)\n    except (json.JSONDecodeError, OSError) as e:\n        self._logger.error(f\"Failed to read mixtape {slug}: {e}\")\n        return None\n\n    try:\n        data_verified, has_changed = self._verify_against_collection(data=data)\n        if data_verified:\n            data_verified = self._verify_mixtape_metadata(data=data_verified)\n            data_verified[\"slug\"] = slug\n            return data_verified\n        else:\n            return None\n    except Exception as e:\n        # Database unavailable (corruption, indexing, etc.)\n        # Just use the mixtape data as-is from the JSON file\n        self._logger.warning(\n            f\"Could not verify mixtape {slug} against collection: {e}. \"\n            f\"Using cached data from JSON.\"\n        )\n\n    # Always normalize metadata\n    data = self._verify_mixtape_metadata(data=data)\n    data[\"slug\"] = slug\n    return data\n</code></pre>"},{"location":"development/mixtape_manager.html#src.mixtape_manager.mixtape_manager.MixtapeManager.list_all","title":"<code>list_all()</code>","text":"<p>Lists all mixtapes stored on disk.</p> <p>Returns a list of dictionaries containing mixtape data, sorted by update or save time.</p> <p>Returns:</p> Type Description <code>list[dict]</code> <p>list[dict]: List of all mixtape data dictionaries.</p> Source code in <code>src/mixtape_manager/mixtape_manager.py</code> <pre><code>def list_all(self) -&gt; list[dict]:\n    \"\"\"\n    Lists all mixtapes stored on disk.\n\n    Returns a list of dictionaries containing mixtape data, sorted by update or save time.\n\n    Returns:\n        list[dict]: List of all mixtape data dictionaries.\n    \"\"\"\n    mixtapes = []\n    for file in self.path_mixtapes.glob(\"*.json\"):\n        try:\n            with open(file, \"r\", encoding=\"utf-8\") as f:\n                data = json.load(f)\n        except (json.JSONDecodeError, OSError) as e:\n            self._logger.warning(f\"Skipping corrupted mixtape file {file}: {e}\")\n            continue\n\n        slug = file.stem\n        data[\"slug\"] = slug\n        if \"liner_notes\" not in data:\n            data[\"liner_notes\"] = \"\"\n        if \"client_id\" not in data:\n            data[\"client_id\"] = None\n\n        # Normalize timestamps (will also handle legacy saved_at)\n        data = self._normalize_timestamps(data)\n\n        mixtapes.append(data)\n\n    # Sort by most recently updated first\n    mixtapes.sort(\n        key=lambda x: x.get(\"updated_at\") or x.get(\"created_at\") or \"\", reverse=True\n    )\n    return mixtapes\n</code></pre>"},{"location":"development/mixtape_manager.html#src.mixtape_manager.mixtape_manager.MixtapeManager.save","title":"<code>save(mixtape_data)</code>","text":"<p>Creates a new mixtape or updates an existing one based on client identity.</p> <p>Reuses an existing mixtape when a matching client_id is found, otherwise generates a fresh mixtape entry.</p> <p>Parameters:</p> Name Type Description Default <code>mixtape_data</code> <code>dict</code> <p>Dictionary containing mixtape information, including optional client_id, title, and tracks.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The slug of the created or updated mixtape.</p> Source code in <code>src/mixtape_manager/mixtape_manager.py</code> <pre><code>def save(self, mixtape_data: dict) -&gt; str:\n    \"\"\"Creates a new mixtape or updates an existing one based on client identity.\n\n    Reuses an existing mixtape when a matching client_id is found, otherwise generates a fresh mixtape entry.\n\n    Args:\n        mixtape_data: Dictionary containing mixtape information, including optional client_id, title, and tracks.\n\n    Returns:\n        str: The slug of the created or updated mixtape.\n    \"\"\"\n    client_id = mixtape_data.get(\"client_id\")\n    now = datetime.now().isoformat()\n\n    if existing := self._find_by_client_id(client_id):\n        slug = existing[\"slug\"]\n        self._logger.info(\n            f\"Found existing mixtape for client_id {client_id}, updating slug {slug}\"\n        )\n        return self.update(slug, mixtape_data)\n\n    # New creation\n    title = mixtape_data.get(\"title\", \"Untitled Mixtape\")\n    base_slug = self._sanitize_title(title)\n    slug = self._generate_unique_slug(base_slug)\n\n    # Preserve the client_id in the saved data\n    if client_id:\n        mixtape_data[\"client_id\"] = client_id\n\n    # Set both timestamps on first save\n    mixtape_data[\"created_at\"] = now\n    mixtape_data[\"updated_at\"] = now\n\n    return self._save_with_slug(mixtape_data=mixtape_data, title=title, slug=slug)\n</code></pre>"},{"location":"development/mixtape_manager.html#src.mixtape_manager.mixtape_manager.MixtapeManager.update","title":"<code>update(slug, updated_data)</code>","text":"<p>Updates an existing mixtape with new data while preserving its identity.</p> <p>Maintains the original slug and important metadata so clients can safely refresh mixtape content.</p> <p>Parameters:</p> Name Type Description Default <code>slug</code> <code>str</code> <p>The slug of the mixtape to update.</p> required <code>updated_data</code> <code>dict</code> <p>Dictionary containing fields to update on the mixtape.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The slug of the updated mixtape.</p> Source code in <code>src/mixtape_manager/mixtape_manager.py</code> <pre><code>def update(self, slug: str, updated_data: dict) -&gt; str:\n    \"\"\"Updates an existing mixtape with new data while preserving its identity.\n\n    Maintains the original slug and important metadata so clients can safely refresh mixtape content.\n\n    Args:\n        slug: The slug of the mixtape to update.\n        updated_data: Dictionary containing fields to update on the mixtape.\n\n    Returns:\n        str: The slug of the updated mixtape.\n    \"\"\"\n    old_json_path = self.path_mixtapes / f\"{slug}.json\"\n    if not old_json_path.exists():\n        raise FileNotFoundError(f\"Mixtape with slug '{slug}' not found.\")\n\n    with open(old_json_path, \"r\", encoding=\"utf-8\") as f:\n        existing_data = json.load(f)\n\n    # Preserve client_id\n    if \"client_id\" not in updated_data and \"client_id\" in existing_data:\n        updated_data[\"client_id\"] = existing_data[\"client_id\"]\n\n    # Merge updates\n    existing_data.update(updated_data)\n\n    # Ensure required fields\n    existing_data[\"title\"] = updated_data.get(\n        \"title\", existing_data.get(\"title\", \"Untitled Mixtape\")\n    )\n    if \"liner_notes\" not in existing_data:\n        existing_data[\"liner_notes\"] = \"\"\n\n    # Only update updated_at\n    existing_data[\"updated_at\"] = datetime.now().isoformat()\n\n    return self._save_with_slug(\n        mixtape_data=existing_data, title=existing_data[\"title\"], slug=slug\n    )\n</code></pre>"},{"location":"development/qr_generation.html","title":"QR Code creation","text":""},{"location":"development/qr_generation.html#qr-code-generation","title":"QR Code Generation","text":""},{"location":"development/qr_generation.html#what-the-package-does","title":"\ud83c\udfaf What the Package Does","text":"<p>The <code>qr_generator</code> package creates PNG QR\u2011code images that encode a mixtape\u2019s public URL. Two flavours are offered:</p> Function Output Extra visual elements <code>generate_mixtape_qr</code> Plain QR code (square) Optional centred logo (SVG/PNG) <code>generate_mixtape_qr_with_cover</code> Composite image (cover \u2192 title banner \u2192 QR) Optional cover art, title banner, centred logo <p>Both functions return raw <code>bytes</code> (PNG data) ready to be sent as a Flask <code>Response</code> or saved to disk.</p>"},{"location":"development/qr_generation.html#installation-dependencies","title":"\ud83d\udd27 Installation &amp; Dependencies","text":"Setting Where it lives Default Remarks <code>qrcode</code> (Python library) <code>project.toml</code> / <code>uv.lock</code> <code>&gt;=7.4</code> Required for both endpoints. <code>Pillow</code> (image handling) <code>project.toml</code> <code>&gt;=10.0</code> Needed for compositing the logo/cover. Static logo files <code>static/logo.svg</code> or <code>static/logo.png</code> \u2014 The blueprint prefers SVG; falls back to PNG. Cover directory <code>app.config[\"COVER_DIR\"]</code> (set in config.py) <code>collection-data/mixtapes/covers</code> Used only by the download endpoint. Cache-Control Hard-coded in the view (<code>public, max-age=3600</code>). \u2014 Adjust in the source if you need a different TTL."},{"location":"development/qr_generation.html#error-handling","title":"\u26a0\ufe0f Error Handling","text":"Situation Raised Exception Message (visible to caller) <code>qrcode</code> library missing <code>ImportError</code> (raised at the top of each public function) <code>\"qrcode library not installed. Install with: uv add qrcode pillow\"</code> Logo or cover path does not exist Silently ignored \u2014 the function falls back to a plain QR or a QR without cover. Errors are printed to stdout (<code>print</code>) but not propagated. \u2014 <code>Pillow</code> fails to open an image (corrupt file) Caught inside the helper; continues processing Prints <code>\"Failed to load cover: \u2026\"</code> or <code>\"Failed to add logo to QR code: \u2026\"</code> and continues with the rest of the image. Invalid <code>size</code> / <code>qr_size</code> (negative, zero) Not explicitly validated \u2014 <code>Pillow</code> raises a <code>ValueError</code> during resize Exception propagates to the caller (treated as a 500 error in the Flask view). <p>Best practice \u2013 Validate user\u2011supplied sizes before calling the functions, or wrap the call in a <code>try/except</code> block and return a friendly error page.</p>"},{"location":"development/qr_generation.html#api","title":"\ud83d\udd0c API","text":""},{"location":"development/qr_generation.html#src.qr_generator.qr_generator","title":"<code>src.qr_generator.qr_generator</code>","text":"<p>Enhanced QR Code generation with mixtape cover art and title.</p> <p>Functions:</p> Name Description <code>generate_mixtape_qr</code> <p>Generate a simple QR code (backward compatibility).</p> <code>generate_mixtape_qr_with_cover</code> <p>Generate a branded QR code with mixtape cover art and title.</p>"},{"location":"development/qr_generation.html#src.qr_generator.qr_generator.generate_mixtape_qr","title":"<code>generate_mixtape_qr(url, title, logo_path=None, size=400)</code>","text":"<p>Generate a simple QR code (backward compatibility).</p> <p>This is the original function for basic QR generation.</p> Source code in <code>src/qr_generator/qr_generator.py</code> <pre><code>def generate_mixtape_qr(\n    url: str,\n    title: str,\n    logo_path: Path | None = None,\n    size: int = 400\n) -&gt; bytes:\n    \"\"\"\n    Generate a simple QR code (backward compatibility).\n\n    This is the original function for basic QR generation.\n    \"\"\"\n    if not QR_AVAILABLE:\n        raise ImportError(\n            \"qrcode library not installed. \"\n            \"Install with: uv add qrcode pillow\"\n        )\n\n    qr = qrcode.QRCode(\n        version=1,\n        error_correction=qrcode.constants.ERROR_CORRECT_H,\n        box_size=10,\n        border=2,\n    )\n\n    qr.add_data(url)\n    qr.make(fit=True)\n\n    img = qr.make_image(\n        image_factory=StyledPilImage,\n        module_drawer=RoundedModuleDrawer(),\n        color_mask=SolidFillColorMask(\n            back_color=(255, 255, 255),\n            front_color=(30, 58, 95)\n        )\n    )\n\n    img = img.resize((size, size), Image.Resampling.LANCZOS)\n\n    if logo_path and logo_path.exists():\n        img = _add_logo_to_qr(img, logo_path)\n\n    buffer = io.BytesIO()\n    img.save(buffer, format='PNG', optimize=True)\n    buffer.seek(0)\n\n    return buffer.getvalue()\n</code></pre>"},{"location":"development/qr_generation.html#src.qr_generator.qr_generator.generate_mixtape_qr_with_cover","title":"<code>generate_mixtape_qr_with_cover(url, title, cover_path=None, logo_path=None, qr_size=400, include_title=True)</code>","text":"<p>Generate a branded QR code with mixtape cover art and title.</p> <p>Creates a composite image with: - Mixtape cover art at the top - Title banner below cover - QR code at the bottom - Optional logo in QR center</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>The full URL to encode in the QR code</p> required <code>title</code> <code>str</code> <p>The mixtape title to display</p> required <code>cover_path</code> <code>Path | None</code> <p>Path to mixtape cover image</p> <code>None</code> <code>logo_path</code> <code>Path | None</code> <p>Optional path to logo for QR center</p> <code>None</code> <code>qr_size</code> <code>int</code> <p>Size of the QR code portion (default 400)</p> <code>400</code> <code>include_title</code> <code>bool</code> <p>Whether to include title banner (default True)</p> <code>True</code> <p>Returns:</p> Name Type Description <code>bytes</code> <code>bytes</code> <p>PNG image data of the complete branded QR code</p> <p>Raises:</p> Type Description <code>ImportError</code> <p>If qrcode library is not installed</p> Source code in <code>src/qr_generator/qr_generator.py</code> <pre><code>def generate_mixtape_qr_with_cover(\n    url: str,\n    title: str,\n    cover_path: Path | None = None,\n    logo_path: Path | None = None,\n    qr_size: int = 400,\n    include_title: bool = True\n) -&gt; bytes:\n    \"\"\"\n    Generate a branded QR code with mixtape cover art and title.\n\n    Creates a composite image with:\n    - Mixtape cover art at the top\n    - Title banner below cover\n    - QR code at the bottom\n    - Optional logo in QR center\n\n    Args:\n        url: The full URL to encode in the QR code\n        title: The mixtape title to display\n        cover_path: Path to mixtape cover image\n        logo_path: Optional path to logo for QR center\n        qr_size: Size of the QR code portion (default 400)\n        include_title: Whether to include title banner (default True)\n\n    Returns:\n        bytes: PNG image data of the complete branded QR code\n\n    Raises:\n        ImportError: If qrcode library is not installed\n    \"\"\"\n    if not QR_AVAILABLE:\n        raise ImportError(\n            \"qrcode library not installed. \"\n            \"Install with: uv add qrcode pillow\"\n        )\n\n    # Generate base QR code\n    qr = qrcode.QRCode(\n        version=1,\n        error_correction=qrcode.constants.ERROR_CORRECT_H,\n        box_size=10,\n        border=2,\n    )\n\n    qr.add_data(url)\n    qr.make(fit=True)\n\n    # Create styled QR code\n    qr_img = qr.make_image(\n        image_factory=StyledPilImage,\n        module_drawer=RoundedModuleDrawer(),\n        color_mask=SolidFillColorMask(\n            back_color=(255, 255, 255),\n            front_color=(30, 58, 95)  # Navy blue\n        )\n    )\n\n    qr_img = qr_img.resize((qr_size, qr_size), Image.Resampling.LANCZOS)\n\n    # Add logo to QR center if provided\n    if logo_path and logo_path.exists():\n        qr_img = _add_logo_to_qr(qr_img, logo_path)\n\n    # Create composite image with cover and title\n    composite = _create_composite_image(\n        qr_img=qr_img,\n        title=title,\n        cover_path=cover_path,\n        include_title=include_title\n    )\n\n    # Convert to bytes\n    buffer = io.BytesIO()\n    composite.save(buffer, format='PNG', optimize=True)\n    buffer.seek(0)\n\n    return buffer.getvalue()\n</code></pre>"},{"location":"development/utilities.html","title":"Utilities","text":""},{"location":"development/utilities.html#utilities","title":"Utilities","text":""},{"location":"development/utilities.html#git-version-utility","title":"\ud83d\udd16 Git version utility","text":"<p>The <code>version_info.py</code> file provides <code>get_version()</code> to access Git version information of the codebase. imported wherever version information is needed within the application. Its design ensures robust and consistent version reporting across different environments and deployment scenarios.</p> <p>It attempts to retrieve the latest Git tag, formats the version string to include commit distance and hash when not on a tag, and gracefully falls back to a default value (\"dev\") if Git is unavailable or an error occurs. This ensures that the application always has a meaningful version identifier, useful for debugging, deployment, and user information.</p>"},{"location":"development/utilities.html#api-get_version","title":"API <code>get_version</code>","text":""},{"location":"development/utilities.html#src.utils.version_info","title":"<code>src.utils.version_info</code>","text":"<p>Functions:</p> Name Description <code>get_version</code> <p>Get the application version string for the current environment.</p>"},{"location":"development/utilities.html#src.utils.version_info.get_version","title":"<code>get_version()</code>","text":"<p>Get the application version string for the current environment.</p> <p>This function prefers a pre-baked version from the environment and falls back to querying Git metadata when available.</p> <p>Parameters:</p> Name Type Description Default <code>APP_VERSION</code> <p>Optional environment variable containing a pre-baked version string.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The resolved version string, or \"dev\" when no version information can</p> <code>str</code> <p>be determined.</p> Source code in <code>src/utils/version_info.py</code> <pre><code>def get_version() -&gt; str:\n    \"\"\"Get the application version string for the current environment.\n\n    This function prefers a pre-baked version from the environment and falls\n    back to querying Git metadata when available.\n\n    Args:\n        APP_VERSION: Optional environment variable containing a pre-baked\n            version string.\n\n    Returns:\n        The resolved version string, or \"dev\" when no version information can\n        be determined.\n    \"\"\"\n    if baked_version := os.getenv(\"APP_VERSION\"):\n        return baked_version\n\n    # Local development fallback\n    try:\n        if result := subprocess.check_output(\n            [\"git\", \"describe\", \"--tags\", \"--abbrev=8\", \"--always\"],\n            stderr=subprocess.DEVNULL,\n            text=True,\n            timeout=5,\n        ).strip():\n            version = _parse_and_format_version(result)\n            os.environ[\"APP_VERSION\"] = version\n            return version\n    except Exception as e:\n        logger.warning(f\"Git version detection failed: {e}\")\n\n    return \"dev\"\n</code></pre>"},{"location":"development/utilities.html#cover-image-compositor","title":"\ud83d\uddbc\ufe0f Cover image compositor","text":"<p>This file <code>cover_compositor.py</code> defines a <code>CoverCompositor</code> utility that generates a composite \u201ccollage\u201d image from a set of album cover images. It:</p> <ul> <li>Takes a directory of cover images and a list of cover filenames (with possible duplicates).</li> <li>Builds a square grid (N\u00d7N) of cover tiles sized 400\u00d7400 pixels each.</li> <li>Uses cover frequency (duplicates) to influence which covers are repeated when filling the grid.</li> <li>Performs basic image processing (center square crop, slight contrast boost).</li> <li>Outputs the final composite as a base64-encoded JPEG data URL (<code>data:image/jpeg;base64,...</code>), suitable for embedding directly in HTML or APIs without separate image hosting.</li> </ul>"},{"location":"development/utilities.html#api-covercompositor","title":"API <code>CoverCompositor</code>","text":""},{"location":"development/utilities.html#src.utils.cover_compositor.CoverCompositor","title":"<code>src.utils.cover_compositor.CoverCompositor</code>","text":"<p>Creates composite cover artwork from individual cover images.</p> <p>Provides utilities to arrange multiple covers into a grid and export the result as an embeddable image.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Initializes the compositor with a covers directory and optional logger.</p> <code>generate_grid_composite</code> <p>Generates a composite cover image from multiple individual cover files.</p> Source code in <code>src/utils/cover_compositor.py</code> <pre><code>class CoverCompositor:\n    \"\"\"Creates composite cover artwork from individual cover images.\n\n    Provides utilities to arrange multiple covers into a grid and export the result as an embeddable image.\n    \"\"\"\n    def __init__(self, covers_dir: Path, logger: Logger=None):\n        \"\"\"Initializes the compositor with a covers directory and optional logger.\n\n        Stores paths and logging configuration so composite images can be built and diagnostics recorded.\n\n        Args:\n            covers_dir: Filesystem path where individual cover images are stored.\n            logger: Optional logger instance used for warnings and diagnostic messages.\n        \"\"\"\n        self.covers_dir = covers_dir\n        self._logger = logger\n\n    def _build_fill_covers(self, cover_paths: list[str]) -&gt; tuple[list[str], int]:\n        \"\"\"Determines the grid size and expanded list of covers to fill all grid cells.\n\n        Computes a square grid dimension based on unique covers and repeats higher-frequency covers to fill remaining cells.\n\n        Args:\n            cover_paths: List of cover filenames, possibly with duplicates for weighting.\n\n        Returns:\n            tuple[list[str], int]: A tuple containing the ordered list of covers to use and the computed grid size.\n        \"\"\"\n        cover_counts = Counter(cover_paths)\n        unique_covers = list(dict.fromkeys(cover_paths))  # preserve order\n\n        n_unique = len(unique_covers)\n        grid_size = math.ceil(math.sqrt(n_unique))\n        total_cells = grid_size**2\n\n        cells_needed = total_cells - n_unique\n        if cells_needed &gt; 0:\n            sorted_by_freq = sorted(\n                unique_covers, key=lambda c: cover_counts[c], reverse=True\n            )\n            repeats: list[str] = []\n            for cover in sorted_by_freq:\n                if cells_needed &lt;= 0:\n                    break\n                repeats.append(cover)\n                cells_needed -= 1\n            fill_covers = unique_covers + repeats\n        else:\n            fill_covers = unique_covers\n\n        return fill_covers, grid_size\n\n    def _load_tiles(self, fill_covers: list[str], tile_size: int = 400) -&gt; list[Image.Image]:\n        \"\"\"Loads cover images and converts them into uniformly sized square tiles.\n\n        Opens each cover file, applies basic enhancement, and returns a list of cropped and resized tile images.\n\n        Args:\n            fill_covers: Ordered list of cover filenames to be used as tiles.\n            tile_size: Target width and height in pixels for each square tile.\n\n        Returns:\n            list[Image.Image]: A list of processed PIL Image objects ready for compositing.\n        \"\"\"\n        tiles: list[Image.Image] = []\n        for path in fill_covers:\n            full_path = self.covers_dir / Path(path).name\n            if not full_path.exists():\n                continue\n            try:\n                img = Image.open(full_path).convert(\"RGB\")\n                enhancer = ImageEnhance.Contrast(img)\n                img = enhancer.enhance(1.05)\n\n                w, h = img.size\n                crop = min(w, h)\n                left = (w - crop) // 2\n                top = (h - crop) // 2\n                square = img.crop((left, top, left + crop, top + crop))\n\n                tiles.append(square.resize((tile_size, tile_size), Image.LANCZOS))\n            except Exception as e:\n                self._logger.warning(f\"Failed to load cover {full_path}: {e}\")\n        return tiles\n\n    def _compose_grid(\n        self, tiles: list[Image.Image], grid_size: int, tile_size: int = 400\n    ) -&gt; Image.Image:\n        \"\"\"Builds a single composite image by arranging tiles into a square grid.\n\n        Places each tile into its grid cell on a new background image and returns the combined result.\n\n        Args:\n            tiles: List of tile images to arrange in the grid.\n            grid_size: Number of tiles per row and column in the square grid.\n            tile_size: Width and height in pixels of each tile.\n\n        Returns:\n            Image.Image: The composed grid image.\n        \"\"\"\n        composite = Image.new(\n            \"RGB\", (tile_size * grid_size, tile_size * grid_size), (30, 30, 30)\n        )\n        for i, tile in enumerate(tiles):\n            x = (i % grid_size) * tile_size\n            y = (i // grid_size) * tile_size\n            composite.paste(tile, (x, y))\n        return composite\n\n    def _encode_image_to_data_url(self, image: Image.Image) -&gt; str:\n        \"\"\"Encodes a PIL image as a JPEG base64 data URL.\n\n        Converts the provided image into an in-memory JPEG and returns a data URL string suitable for embedding.\n\n        Args:\n            image: The PIL Image instance to encode.\n\n        Returns:\n            str: A data URL string containing the base64-encoded JPEG image.\n        \"\"\"\n        buffered = BytesIO()\n        image.save(buffered, format=\"JPEG\", quality=95, optimize=True)\n        return f\"data:image/jpeg;base64,{base64.b64encode(buffered.getvalue()).decode()}\"\n\n    def generate_grid_composite(self, cover_paths: list[str]) -&gt; str:\n        \"\"\"Generates a composite cover image from multiple individual cover files.\n\n        Builds a square grid of cover tiles and returns the result as a base64-encoded JPEG data URL.\n\n        Args:\n            cover_paths: List of cover filenames (may contain duplicates for weighting).\n\n        Returns:\n            str: A data URL string containing the base64-encoded JPEG composite image.\n\n        Raises:\n            ValueError: If no cover paths are provided or no images can be loaded.\n        \"\"\"\n        if not cover_paths:\n            raise ValueError(\"No covers provided\")\n\n        fill_covers, grid_size = self._build_fill_covers(cover_paths)\n        tiles = self._load_tiles(fill_covers)\n\n        if not tiles:\n            raise ValueError(\"No images could be loaded\")\n\n        composite = self._compose_grid(tiles, grid_size)\n        return self._encode_image_to_data_url(composite)\n</code></pre>"},{"location":"development/utilities.html#src.utils.cover_compositor.CoverCompositor.__init__","title":"<code>__init__(covers_dir, logger=None)</code>","text":"<p>Initializes the compositor with a covers directory and optional logger.</p> <p>Stores paths and logging configuration so composite images can be built and diagnostics recorded.</p> <p>Parameters:</p> Name Type Description Default <code>covers_dir</code> <code>Path</code> <p>Filesystem path where individual cover images are stored.</p> required <code>logger</code> <code>Logger</code> <p>Optional logger instance used for warnings and diagnostic messages.</p> <code>None</code> Source code in <code>src/utils/cover_compositor.py</code> <pre><code>def __init__(self, covers_dir: Path, logger: Logger=None):\n    \"\"\"Initializes the compositor with a covers directory and optional logger.\n\n    Stores paths and logging configuration so composite images can be built and diagnostics recorded.\n\n    Args:\n        covers_dir: Filesystem path where individual cover images are stored.\n        logger: Optional logger instance used for warnings and diagnostic messages.\n    \"\"\"\n    self.covers_dir = covers_dir\n    self._logger = logger\n</code></pre>"},{"location":"development/utilities.html#src.utils.cover_compositor.CoverCompositor.generate_grid_composite","title":"<code>generate_grid_composite(cover_paths)</code>","text":"<p>Generates a composite cover image from multiple individual cover files.</p> <p>Builds a square grid of cover tiles and returns the result as a base64-encoded JPEG data URL.</p> <p>Parameters:</p> Name Type Description Default <code>cover_paths</code> <code>list[str]</code> <p>List of cover filenames (may contain duplicates for weighting).</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A data URL string containing the base64-encoded JPEG composite image.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no cover paths are provided or no images can be loaded.</p> Source code in <code>src/utils/cover_compositor.py</code> <pre><code>def generate_grid_composite(self, cover_paths: list[str]) -&gt; str:\n    \"\"\"Generates a composite cover image from multiple individual cover files.\n\n    Builds a square grid of cover tiles and returns the result as a base64-encoded JPEG data URL.\n\n    Args:\n        cover_paths: List of cover filenames (may contain duplicates for weighting).\n\n    Returns:\n        str: A data URL string containing the base64-encoded JPEG composite image.\n\n    Raises:\n        ValueError: If no cover paths are provided or no images can be loaded.\n    \"\"\"\n    if not cover_paths:\n        raise ValueError(\"No covers provided\")\n\n    fill_covers, grid_size = self._build_fill_covers(cover_paths)\n    tiles = self._load_tiles(fill_covers)\n\n    if not tiles:\n        raise ValueError(\"No images could be loaded\")\n\n    composite = self._compose_grid(tiles, grid_size)\n    return self._encode_image_to_data_url(composite)\n</code></pre>"},{"location":"development/musiclib/intro.html","title":"Introduction","text":""},{"location":"development/musiclib/intro.html#musiclib-music-collection-handling","title":"Musiclib - Music Collection handling","text":""},{"location":"development/musiclib/intro.html#overview-and-introduction","title":"\ud83d\udcd8 Overview and introduction","text":"<p>The musiclib package is the heart of the mixtape music\u2011collection service. It turns a plain directory tree of audio files into a searchable, fully\u2011indexed library that can be queried instantly from the UI.</p> <p>Below is a concise, high\u2011level walkthrough of the module\u2019s responsibilities, its main components, and how they interact to deliver a robust \u201cscan\u2011once\u2011search\u2011forever\u201d experience.</p>"},{"location":"development/musiclib/intro.html#class-diagram","title":"Class diagram","text":"<pre><code>classDiagram\n    %% ==== Core data types ====\n    class IndexEvent {\n        &lt;&lt;dataclass&gt;&gt;\n        +EventType type\n        +Path? path\n    }\n\n    class EventType {\n        &lt;&lt;enumeration&gt;&gt;\n        INDEX_FILE\n        DELETE_FILE\n        CLEAR_DB\n        REBUILD_DONE\n        RESYNC_DONE\n    }\n\n    %% ==== CollectionExtractor (main engine) ====\n    class CollectionExtractor {\n        -Path music_root\n        -Path db_path\n        -Path data_root\n        -Logger _logger\n        -Queue[IndexEvent] _write_queue\n        -Event _writer_stop\n        -Observer? _observer\n        -Thread _writer_thread\n        -set SUPPORTED_EXTS\n        +CollectionExtractor(music_root, db_path, logger=None)\n        +rebuild()\n        +resync()\n        +start_monitoring()\n        +stop()\n        +get_conn(readonly=False) Connection\n        -_init_db()\n        -_populate_fts_if_needed()\n        -_db_writer_loop()\n        -_index_file(conn, path)\n        -_write_queue.put(event)\n    }\n\n    %% ==== EnhancedWatcher (filesystem event handler) ====\n    class EnhancedWatcher {\n        -CollectionExtractor extractor\n        +EnhancedWatcher(extractor)\n        +on_any_event(event)\n        +shutdown()\n    }\n\n    %% ==== Indexing status helpers (module-level functions) ====\n    class indexing_status {\n        &lt;&lt;module&gt;&gt;\n        +set_indexing_status(data_root, status, total, current)\n        +clear_indexing_status(data_root)\n        +get_indexing_status(data_root, logger=None)\n        -_atomic_write_json(status_file, data)\n        -_calculate_progress(total, current) float\n        -_get_started_at(status_file) str|None\n        -_build_status_data(status, started_at, total, current, progress) dict\n    }\n\n    %% ==== Relationships ====\n    CollectionExtractor --&gt; IndexEvent : produces / consumes\n    CollectionExtractor --&gt; EventType : uses literals\n    CollectionExtractor --&gt; EnhancedWatcher : creates &amp; registers\n    CollectionExtractor --&gt; indexing_status : writes progress JSON\n    EnhancedWatcher --&gt; IndexEvent : enqueues events\n    EnhancedWatcher --&gt; CollectionExtractor : holds reference\n    indexing_status ..&gt; Path : works with filesystem paths</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom"},{"location":"development/musiclib/intro.html#what-the-module-does","title":"\ud83e\udde9 What the module does","text":"Goal How it\u2019s achieved Detect every supported audio file A watchdog observer (implemented as <code>EnhancedWatcher</code>) monitors the <code>music_root</code> directory in real time. It includes a 2\u202fsecond debounce to coalesce rapid edits and avoid duplicate indexing. Extract reliable metadata <code>tinytag.TinyTag</code> reads ID3/metadata tags (artist, album, title, year, duration, etc.). Persist metadata efficiently A SQLite database stores the canonical rows (<code>tracks</code> table) and an FTS5 virtual table (<code>tracks_fts</code>) that mirrors the same columns for lightning\u2011fast full\u2011text search. Keep the DB in sync A single writer thread serialises all write operations (adds, deletes, clears) via a thread\u2011safe <code>Queue[IndexEvent]</code>. Expose progress to the UI A tiny JSON file (<code>indexing_status.json</code>) is updated atomically during long\u2011running operations (rebuild, resync) so the front\u2011end can render progress bars. Provide a clean API for the UI <code>MusicCollection</code> (in <code>reader.py</code>) builds the search expression, runs the query, groups results by release directory, and returns a ready\u2011to\u2011render structure (artists, albums, tracks) together with the list of terms that need highlighting."},{"location":"development/musiclib/intro.html#core-building-blocks","title":"\ud83e\uddf1 Core building blocks","text":"Module / Class Primary responsibility <code>_extractor.py</code> \u2022 Low\u2011level DB schema creation (<code>_init_db</code>). \u2022 Full\u2011text table bootstrap (<code>_populate_fts_if_needed</code>). \u2022 <code>CollectionExtractor</code> \u2013 orchestrates indexing, resync, rebuild, and live monitoring. \u2022 <code>IndexEvent</code> / <code>EventType</code> \u2013 typed messages that drive the writer thread. \u2022 <code>_Watcher</code> \u2013 translates filesystem events into <code>IndexEvent</code>s. <code>indexing_status.py</code> Helper functions that write/read the <code>indexing_status.json</code> file in an atomic, crash\u2011safe way (e.g., <code>set_indexing_status</code>, <code>clear_indexing_status</code>, <code>get_indexing_status</code>). <code>reader.py</code> High\u2011level fa\u00e7ade (<code>MusicCollection</code>) used by the UI. It parses user queries, builds the FTS/LIKE expression, runs the query, groups rows, and formats the result payload (artists, albums, tracks, and highlight terms). <code>ui.py</code> Extends <code>MusicCollection</code> with UI\u2011specific helpers: \u2022 <code>_highlight_text</code> (term highlighting) \u2022 <code>_safe_filename</code> (sanitising filenames) \u2022 <code>_escape_for_query</code> (building click\u2011query strings) \u2022 result shaping for the front\u2011end."},{"location":"development/musiclib/intro.html#data-flow-from-file-system-to-ui","title":"\ud83d\udd00 Data flow \u2013 from file system to UI","text":"<ol> <li>Startup \u2013 <code>MusicCollection</code> creates a <code>CollectionExtractor</code>. The extractor initializes the SQLite schema and launches the writer thread.</li> <li>Initial population \u2013 If the DB is empty, <code>MusicCollection</code> schedules a rebuild. The rebuild walks the entire <code>music_root</code>, enqueues an <code>INDEX_FILE</code> event for every supported file, and updates <code>indexing_status.json</code> so the UI can show progress.</li> <li>Live updates \u2013 The <code>watchdog</code> observer fires on every create/modify/delete. <code>_Watcher</code> converts those into <code>IndexEvent</code>s, which the writer thread processes in order, keeping the DB and the FTS mirror perfectly aligned.</li> <li>Search \u2013 When the UI calls <code>search_highlighting</code>, <code>MusicCollection</code> parses the query, builds an FTS\u2011compatible expression (or a fallback <code>LIKE</code> query), runs it against the DB, groups rows by release directory, and returns a dictionary of artists, albums, and tracks plus the list of parsed terms. Internally, searches use a multi-pass candidate scoring model with optional reuse of previous search sessions for fast refinements.</li> <li>Presentation \u2013 <code>MusicCollectionUI</code> highlights the terms, builds click\u2011queries (<code>artist:\u2026</code>, <code>release_dir:\u2026</code>), and hands the ready\u2011to\u2011render JSON back to the front\u2011end. Lazy\u2011loading of an artist\u2019s full discography or an album\u2019s track list is done by re\u2011issuing <code>search_grouped</code> with the stored click\u2011query.</li> </ol>"},{"location":"development/musiclib/intro.html#why-the-design-choices-matter","title":"\ud83d\udca1 Why the design choices matter","text":"Design decision Benefit Single writer thread + queue Guarantees deterministic ordering of DB writes, avoids SQLite lock contention, and lets the UI stay responsive while heavy indexing runs in the background. FTS5 virtual table with triggers Provides sub\u2011millisecond full\u2011text look\u2011ups without having to maintain a separate index manually. Atomic JSON status file Prevents corrupted progress information even if the process crashes mid\u2011write; the UI never sees a half\u2011written file. Watchdog\u2011driven live sync Users see newly added songs appear instantly; deletions are reflected without a full rescan. Separation of concerns (<code>_extractor</code> vs. <code>reader</code> vs. <code>ui</code>) Keeps low\u2011level DB handling isolated from query parsing and UI formatting, making the code easier to test and extend. Typed <code>IndexEvent</code> dataclass Improves readability, reduces bugs caused by mismatched queue payloads, and makes future event types straightforward to add. Debouncing in the watcher (<code>EnhancedWatcher</code>) Prevents a flood of <code>INDEX_FILE</code> events when a user edits a file repeatedly (e.g., retagging). Guarantees only the final state is indexed, reducing DB churn and corruption risk."},{"location":"development/musiclib/intro.html#quick-mental-model","title":"\ud83e\udde0 Quick mental model","text":"<pre><code>flowchart LR\n    %% Nodes\n    FS[Filesystem audio files]\n    WD[EnhancedWatcher Observer]\n    Q[IndexEvent Queue]\n    WT[Writer Thread processes events]\n    DB[SQLite DB *tracks + tracks_fts*]\n    UI[MusicCollection UI layer]\n\n    %% Main data flow\n    FS --&gt; WD\n    WD --&gt; Q\n    Q --&gt; WT\n    WT --&gt; DB\n\n    %% Queries from UI\n    UI --&gt; DB\n    DB --&gt; UI</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom  <p>The arrow direction indicates the primary flow of data.</p> <p>The UI never talks directly to the filesystem; it always goes through MusicCollection, which in turn reads from the already\u2011indexed SQLite store.</p>"},{"location":"development/musiclib/intro.html#getting-started-for-developers","title":"\ud83d\ude80 Getting started (for developers)","text":"<ol> <li> <p>Instantiate the high\u2011level class:</p> <pre><code>from musiclib import MusicCollectionUI\nmc = MusicCollectionUI(\n    music_root=\"/path/to/music\",\n    db_path=\"/path/to/db.sqlite\"\n)   # Starts the watchdog observer and, if the DB is empty, schedules an initial rebuild.\n</code></pre> </li> <li> <p>Run a query (the UI does this internally):</p> <pre><code>results, terms = mc.search_highlighting(qry=\"artist:'Radiohead' love\")\n</code></pre> </li> <li> <p>Monitor progress (useful for CLI tools):</p> <pre><code>from musiclib.indexing_status import get_indexing_status\nstatus = get_indexing_status(\"/path/to/db_folder\")\nprint(status)   # \u2192 {'status': 'rebuilding', 'progress': 0.42, \u2026}\n</code></pre> </li> <li> <p>Shut down cleanly when the program exits:</p> <pre><code>mc.close()   # stops the writer thread and the watchdog observer\n</code></pre> </li> </ol>"},{"location":"development/musiclib/intro.html#where-to-look-next","title":"\ud83e\udded Where to look next","text":"<ul> <li>DB Loading for:</li> <li>low\u2011level DB schema, triggers, the writer\u2011loop logic and</li> <li>the atomic JSON status handling used by the UI progress bar.</li> <li>DB reading for:</li> <li>the query parser,</li> <li>grouping algorithm that decides which artists/albums/tracks to return,</li> <li>presentation helpers (highlighting, safe filenames, click\u2011query generation) and</li> <li>Real\u2011time monitoring of changes in your music collection</li> </ul> <p>That\u2019s the complete picture of the <code>musiclib</code> module: a tightly coupled pipeline that turns a folder of audio files into a fast, searchable, and continuously synchronized music library.</p>"},{"location":"development/musiclib/loading.html","title":"Loader","text":""},{"location":"development/musiclib/loading.html#creatingmaintaining-the-music-collection-database","title":"Creating/maintaining the music collection database","text":""},{"location":"development/musiclib/loading.html#highlevel-picture","title":"\ud83d\udcd8 High\u2011level picture","text":"<ul> <li>Watchdog watches the music directory for creations, modifications, and deletions.</li> <li>Detected changes are turned into IndexEvent objects and placed on a thread\u2011safe <code>Queue</code>.</li> <li>A dedicated writer thread (<code>_db_writer_loop</code>) consumes those events and performs the actual SQLite writes.</li> <li>The database consists of a normal <code>tracks</code> table (metadata) and an FTS5 virtual table <code>tracks_fts</code> that mirrors the metadata for fast full\u2011text search.</li> <li>Helper functions in <code>indexing_status.py</code> keep a tiny JSON status file (<code>indexing_status.json</code>) that the UI can poll to show progress during a rebuild or resync operation.</li> </ul>"},{"location":"development/musiclib/loading.html#loadingresyncing-process","title":"Loading/resyncing process","text":"<pre><code>flowchart LR\n    A[\"User initiates process (rebuild() / resync())\"] --&gt; B[\"Log start and set indexing status\"]\n    B --&gt; C{\"Operation type?\"}\n    C --&gt;|\"'rebuild()'\"| D[\"Clear database and prepare for file scan\"]\n    C --&gt;|\"'resync()'\"| E[\"Scan filesystem and compare with DB\"]\n\n    D --&gt; F[\"Scan music root for files to index\"]\n    E --&gt; G[\"Identify changes: files to add or remove\"]\n    F --&gt; H[\"Queue file operations to process\"]\n    G --&gt; H\n\n    H --&gt; I[\"Start job, update counters &amp; status\"]\n    I --&gt; J[\"Process queued file operations (Delete/Index)\"]\n\n    J --&gt; K{\"Job complete?\"}\n    K --&gt;|Yes| L[\"Finalize job, log completion, clear status\"]\n    K --&gt;|No| J\n\n    L --&gt; M[\"Indexing complete, external indexing status cleared\"]</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom"},{"location":"development/musiclib/loading.html#monitoring-process","title":"Monitoring process","text":"<pre><code>flowchart LR\n    A[\"'start_monitoring()' called\"] --&gt; B{\"Is Observer running?\"}\n    B --&gt;|Yes| C[\"No-op (return immediately)\"]\n    B --&gt;|No| D[\"Create and start Observer instance\"]\n\n    D --&gt; E[\"Schedule file watcher for 'music_root'\"]\n    E --&gt; F[\"Start Observer thread\"]\n\n    F --&gt; G[\"FS event in 'music_root' (create/modify/delete)\"]\n    G --&gt; H[\"Process event\"]\n\n    H --&gt; I{\"Is directory?\"}\n    I --&gt;|Yes| J[\"Ignore event\"]\n    I --&gt;|No| K[\"Check if extension is supported\"]\n\n    K --&gt;|No| J\n    K --&gt;|Yes| L[\"Enqueue event (Index or Delete file)\"]\n\n    L --&gt; M[\"Writer thread processes events\"]\n\n    %% Shutdown flow\n    N[\"'stop()' called\"] --&gt; O[\"Stop writer thread and join\"]\n    O --&gt; P{\"Is Observer running?\"}\n    P --&gt;|Yes| Q[\"Stop and join Observer thread\"]\n    P --&gt;|No| R[\"No monitoring to stop\"]</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom"},{"location":"development/musiclib/loading.html#core-data-structures","title":"\ud83e\uddf1 Core data structures","text":"Name Type Purpose <code>IndexEvent</code> <code>@dataclass</code> with fields <code>type: EventType</code> and <code>path: Optional[Path]</code> Represents a single action for the writer thread (index a file, delete a file, clear DB, signal rebuild/resync completion). <code>EventType</code> <code>Literal[\"INDEX_FILE\", \"DELETE_FILE\", \"CLEAR_DB\", \"REBUILD_DONE\", \"RESYNC_DONE\"]</code> Enumerates the possible actions. <code>_write_queue</code> <code>queue.Queue[IndexEvent]</code> Thread\u2011safe hand\u2011off from the watcher / public methods to the writer thread. <code>_writer_stop</code> <code>threading.Event</code> Signals the writer thread to shut down cleanly. <code>tracks</code> table SQLite table with columns <code>path, filename, artist, album, title, albumartist, genre, year, duration, mtime</code> Stores the canonical metadata for each audio file. <code>tracks_fts</code> SQLite FTS5 virtual table mirroring most columns of <code>tracks</code> Enables fast full\u2011text search across artist, album, title, etc."},{"location":"development/musiclib/loading.html#database-initialization","title":"\ud83d\uddc4\ufe0f Database initialization","text":"<p>Started in <code>_init_db</code>:</p> <ol> <li>Opens a connection (<code>sqlite3.connect(self.db_path)</code>).</li> <li>Sets WAL journal mode and normal sync for better concurrency.</li> <li>Creates the tracks <code>table</code> if it does not exist.</li> <li>Creates three case\u2011insensitive indexes on <code>artist</code>, <code>album</code>, and <code>title</code>.</li> <li>Creates the FTS5 virtual table <code>tracks_fts</code> with a Unicode tokenizer that removes diacritics.</li> <li>Installs three triggers (<code>tracks_ai</code>, <code>tracks_ad</code>, <code>tracks_au</code>) that keep <code>tracks_fts</code> in sync with inserts, deletes, and updates on <code>tracks</code>.</li> </ol> <p>Result: the DB is ready for both ordinary queries and full\u2011text search without any manual maintenance.</p>"},{"location":"development/musiclib/loading.html#fulltext-table-bootstrap","title":"\ud83d\udee0\ufe0f Full\u2011text table boot\u2011strap","text":"<p>Starts in <code>_populate_fts_if_needed</code></p> <ul> <li>Opens a read\u2011only connection.</li> <li>Checks <code>SELECT count(*) FROM tracks_fts</code>.</li> <li>If the count is zero, executes a single <code>INSERT \u2026 SELECT</code> that copies every row from <code>tracks</code> into <code>tracks_fts</code>.</li> <li>Commits the transaction.</li> </ul> <p>This routine is called once after a fresh DB creation or after a manual purge of the FTS table.</p>"},{"location":"development/musiclib/loading.html#public-connection-helper","title":"\ud83d\udd17 Public connection helper","text":"<p>Function <code>get_conn</code>:</p> <ul> <li>Read\u2011only mode (<code>readonly=True</code>) uses the URI <code>file:&lt;path&gt;?mode=ro</code>.</li> <li>Write mode opens a normal connection.</li> <li>Both connections set <code>row_factory = sqlite3.Row</code> so callers can treat rows like dictionaries.</li> </ul> <p>All higher\u2011level code (search, UI, etc.) obtains connections via this method.</p>"},{"location":"development/musiclib/loading.html#writer-thread","title":"\u270d\ufe0f Writer thread","text":"<p>Function <code>_db_writer_loop</code></p> <ul> <li>Runs forever until _writer_stop is set.</li> <li>Pulls an IndexEvent from _write_queue with a 1.0\u202fs timeout (so it can notice the stop flag).</li> <li> <p>Handles each event type:</p> Event type Action performed <code>CLEAR_DB</code> <code>DELETE FROM tracks</code> (removes all rows). <code>INDEX_FILE</code> Calls <code>_index_file(conn, path)</code> \u2013 extracts metadata and <code>INSERT OR REPLACE</code> into <code>tracks</code>. <code>DELETE_FILE</code> <code>DELETE FROM tracks WHERE path = ?</code>. <code>REBUILD_DONE</code> / <code>RESYNC_DONE</code> <code>conn.commit()</code> \u2013 flushes any pending changes. </li> <li> <p>After every 50 processed events it forces a commit to keep the transaction size reasonable.</p> </li> <li>Errors are caught and logged via the injected Logger.</li> <li>When the loop exits, it commits any remaining work and closes the connection.</li> </ul>"},{"location":"development/musiclib/loading.html#metadata-extraction","title":"\ud83d\udd0d Metadata extraction","text":"<p>Function <code>_index_file</code>:</p> <ol> <li>Calls TinyTag.get(path, tags=True, duration=True).</li> <li> <p>Safely extracts the following fields (fallbacks shown in parentheses):</p> Field Source Fallback <code>artist</code> <code>tag.artist</code> \u2192 <code>tag.albumartist</code> <code>\"Unknown\"</code> <code>album</code> <code>tag.album</code> <code>\"Unknown\"</code> <code>title</code> <code>tag.title</code> \u2192 <code>path.stem</code> <code>\"Unknown\"</code> <code>year</code> <code>int(str(tag.year)[:4])</code> (if parsable) <code>None</code> <code>duration</code> <code>tag.duration</code> <code>None</code> <code>mtime</code> <code>path.stat().st_mtime</code> \u2013 </li> <li> <p>Executes a single INSERT OR REPLACE INTO tracks (\u2026) VALUES (\u2026) with the gathered values.</p> </li> <li>Because of the triggers defined in _init_db, the same row is automatically mirrored into tracks_fts.</li> </ol>"},{"location":"development/musiclib/loading.html#full-rebuild","title":"\ud83c\udfd7\ufe0f Full rebuild","text":"<p>Function: <code>rebuild</code></p> <ul> <li>Purpose \u2013 create a fresh DB from the current file system state.</li> <li> <p>Steps:</p> <ol> <li>Write status rebuilding with <code>total = -1</code> (unknown) and <code>current = 0</code>.</li> <li>Enqueue a <code>CLEAR_DB</code> event (empties the DB).</li> <li>Recursively walk <code>music_root</code> (<code>rglob(\"*\")</code>) and collect every file whose suffix is in <code>SUPPORTED_EXTS</code>.</li> <li>Update the status file with the exact <code>total</code> count.</li> <li>For each discovered file, enqueue <code>INDEX_FILE</code> events. Every 100 files the status file is refreshed (<code>set_indexing_status</code>).</li> <li>After the loop, enqueue <code>REBUILD_DONE</code> and call <code>join()</code> on the queue (wait until the writer thread finishes processing).</li> <li>Remove the status file (<code>clear_indexing_status</code>).</li> </ol> </li> <li> <p>The UI can poll <code>indexing_status.json</code> to display a progress bar that reflects the <code>total</code>/<code>current</code>/<code>progress</code> fields.</p> </li> </ul>"},{"location":"development/musiclib/loading.html#incremental-resynchronisation","title":"\ud83d\udd04 Incremental resynchronisation","text":"<p>Function: <code>resync</code></p> <ul> <li>Purpose \u2013 bring the DB up\u2011to\u2011date after files have been added, removed, or renamed since the last run.</li> <li> <p>Steps:</p> <ol> <li>Set status <code>resyncing</code> with unknown totals (<code>total = -1</code>).</li> <li>Build a set of absolute paths for all supported files currently on disk (<code>fs_paths</code>).</li> <li>Query the DB for all stored paths (<code>db_paths</code>).</li> <li>Compute <code>to_add = fs_paths - db_paths</code> and <code>to_remove = db_paths - fs_paths</code>.</li> <li><code>total = len(to_add) + len(to_remove)</code> and update the status file.</li> <li>Enqueue <code>DELETE_FILE</code> events for each path in <code>to_remove</code>; every 100 deletions the status file is refreshed.</li> <li>Enqueue <code>INDEX_FILE</code> events for each path in <code>to_add</code>; every 100 additions the status file is refreshed.</li> <li>Enqueue <code>RESYNC_DONE</code>, clear the status file, and log a summary.</li> </ol> </li> <li> <p>As with <code>rebuild</code>, the writer thread processes the queued events sequentially, guaranteeing that the DB ends up exactly matching the file system.</p> </li> </ul>"},{"location":"development/musiclib/loading.html#realtime-monitoring","title":"\ud83d\udc40 Real\u2011time monitoring","text":"<ul> <li> <p><code>start_monitoring</code> creates a <code>watchdog.observers.Observer</code> (if none exists), registers a <code>EnhancedWatcher</code> instance for the <code>music_root</code>, and starts the observer thread.</p> </li> <li> <p><code>EnhancedWatcher</code> inherits from <code>FileSystemEventHandler</code>. Its <code>on_any_event</code> method:</p> <ol> <li>Ignores directory events.</li> <li>Filters out files whose extensions are not in <code>SUPPORTED_EXTS</code>.</li> <li>For <code>created</code> or <code>modified</code> events \u2192 enqueues <code>INDEX_FILE</code>.</li> <li>For <code>deleted</code> events \u2192 enqueues <code>DELETE_FILE</code>.</li> </ol> </li> <li> <p>This mechanism guarantees that any change made while the application is running is eventually reflected in the DB (subject to the writer thread\u2019s batching policy).</p> </li> </ul>"},{"location":"development/musiclib/loading.html#graceful-shutdown","title":"\ud83d\uded1 Graceful shutdown","text":"<p>Function <code>stop</code></p> <ul> <li>Sets the <code>_writer_stop</code> flag, joins the writer thread (max 5\u202fseconds).</li> <li>Stops and joins the watchdog observer if it was started.</li> <li>After this call the extractor is fully stopped and the SQLite connection is closed.</li> </ul>"},{"location":"development/musiclib/loading.html#indexingstatus-helper","title":"\ud83d\udcdd Indexing\u2011status helper","text":"<p>File <code>indexing_status.py</code>:</p> Function Role <code>set_indexing_status(data_root, status, total, current)</code> Computes progress (<code>current/total</code>), preserves the original <code>started_at</code> timestamp (or creates a new one), builds a dictionary with <code>status, started_at, updated_at, total, current, progress</code>, and writes it atomically to <code>indexing_status.json</code>. <code>_atomic_write_json(status_file, data)</code> Writes JSON to a temporary file in the same directory, flushes, <code>fsync</code>s, then atomically renames the temp file onto the target. Guarantees that a partially\u2011written file never appears. <code>_calculate_progress(total, current)</code> Returns a float in <code>[0.0, 1.0]</code>; guards against division by zero or negative totals. <code>_get_started_at(status_file)</code> Reads the existing JSON (if any) and returns the original <code>started_at</code> value, allowing a rebuild/resync to keep the same start\u2011time across restarts. <code>_build_status_data(...)</code> Packages all fields into a plain dict ready for JSON serialization. <code>clear_indexing_status(data_root)</code> Deletes the JSON file if it exists. <code>get_indexing_status(data_root, logger=None)</code> Reads and parses the JSON file, returning the dict or <code>None</code> on missing/corrupt files. Logs JSON decode errors via the supplied logger (defaults to <code>NullLogger</code>). <p>These utilities are deliberately lightweight: they operate purely on the filesystem and do not depend on the SQLite connection, making them safe to call from any thread (including the writer thread).</p>"},{"location":"development/musiclib/loading.html#endtoend-flow-for-a-typical-user-session","title":"\ud83e\uddd1\u200d\ud83d\udcbb End\u2011to\u2011end flow for a typical user session","text":"<pre><code>sequenceDiagram\n    actor User\n    participant CollectionExtractor\n    participant DBWriterThread\n    participant SQLite as SQLite_DB\n    participant FS\n\n    User-&gt;&gt;CollectionExtractor: resync()\n    CollectionExtractor-&gt;&gt;CollectionExtractor: set_indexing_status(resyncing, total=-1, current=0)\n\n    CollectionExtractor-&gt;&gt;CollectionExtractor: _scan_filesystem_paths()\n    FS--&gt;&gt;CollectionExtractor: fs_paths (relative)\n    CollectionExtractor-&gt;&gt;CollectionExtractor: _get_database_paths()\n    SQLite--&gt;&gt;CollectionExtractor: db_paths (relative)\n\n    CollectionExtractor-&gt;&gt;CollectionExtractor: compute to_add_rel, to_remove_rel\n    CollectionExtractor-&gt;&gt;CollectionExtractor: to_add_abs = _to_abspath(to_add_rel)\n    CollectionExtractor-&gt;&gt;CollectionExtractor: set_indexing_status(resyncing, total, current=0)\n\n    CollectionExtractor-&gt;&gt;CollectionExtractor: _start_job(\"resyncing\", total)\n    loop for each path in to_remove_rel\n        CollectionExtractor-&gt;&gt;DBWriterThread: enqueue IndexEvent(DELETE_FILE, rel_path)\n    end\n    loop for each path in to_add_abs\n        CollectionExtractor-&gt;&gt;DBWriterThread: enqueue IndexEvent(INDEX_FILE, abs_path)\n    end\n    CollectionExtractor-&gt;&gt;DBWriterThread: enqueue IndexEvent(RESYNC_DONE)\n\n    loop DB writer loop\n        alt DELETE_FILE\n            DBWriterThread-&gt;&gt;SQLite: DELETE FROM tracks WHERE path = rel_path\n        else INDEX_FILE\n            DBWriterThread-&gt;&gt;SQLite: _index_file(abs_path) INSERT OR REPLACE\n        end\n        DBWriterThread-&gt;&gt;CollectionExtractor: _processed_count++\n        alt batch_size reached\n            DBWriterThread-&gt;&gt;SQLite: COMMIT\n            DBWriterThread-&gt;&gt;CollectionExtractor: _update_progress_status()\n            CollectionExtractor-&gt;&gt;CollectionExtractor: set_indexing_status(resyncing, total, current)\n        end\n    end\n\n    DBWriterThread-&gt;&gt;SQLite: COMMIT\n    DBWriterThread-&gt;&gt;SQLite: wal_checkpoint(PASSIVE)\n    DBWriterThread-&gt;&gt;CollectionExtractor: _handle_job_completion()\n    CollectionExtractor-&gt;&gt;CollectionExtractor: set_indexing_status(resyncing, total, current=total)\n    CollectionExtractor-&gt;&gt;CollectionExtractor: clear_indexing_status()</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom"},{"location":"development/musiclib/loading.html#api","title":"\ud83d\udd0c API","text":""},{"location":"development/musiclib/loading.html#src.musiclib._extractor.EventType","title":"<code>src.musiclib._extractor.EventType = Literal['INDEX_FILE', 'DELETE_FILE', 'CLEAR_DB', 'REBUILD_DONE', 'RESYNC_DONE']</code>  <code>module-attribute</code>","text":""},{"location":"development/musiclib/loading.html#src.musiclib._extractor.IndexEvent","title":"<code>src.musiclib._extractor.IndexEvent</code>  <code>dataclass</code>","text":"<p>Represents an event for indexing or modifying music files in the collection.</p> Source code in <code>src/musiclib/_extractor.py</code> <pre><code>@dataclass(slots=True)\nclass IndexEvent:\n    \"\"\"Represents an event for indexing or modifying music files in the collection.\"\"\"\n\n    type: EventType\n    path: Path | None = None\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib._extractor.CollectionExtractor","title":"<code>src.musiclib._extractor.CollectionExtractor</code>","text":"<p>Manages extraction, indexing, and synchronization of music files in a collection.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Creates a new collection extractor for managing a music library index.</p> <code>disable_bulk_edit_mode</code> <p>Disables bulk edit mode and resyncs the database.</p> <code>enable_bulk_edit_mode</code> <p>Enables bulk edit mode by pausing file system monitoring.</p> <code>get_conn</code> <p>Opens and returns a configured SQLite connection for this collection.</p> <code>is_initial_indexing_done</code> <p>Checks whether an initial full indexing pass has been completed.</p> <code>rebuild</code> <p>Performs a full rebuild of the music collection index from the current filesystem state.</p> <code>resync</code> <p>Synchronizes the database with the current set of music files on disk.</p> <code>set_initial_indexing_done</code> <p>Marks that the initial full indexing pass has completed successfully.</p> <code>start_monitoring</code> <p>Starts monitoring the music directory for file system changes.</p> <code>stop</code> <p>Stops the database writer thread and file system observer.</p> <code>wait_for_indexing_start</code> <p>Waits until indexing has started or the specified timeout elapses.</p> Source code in <code>src/musiclib/_extractor.py</code> <pre><code>class CollectionExtractor:\n    \"\"\"Manages extraction, indexing, and synchronization of music files in a collection.\"\"\"\n\n    SUPPORTED_EXTS = SUPPORTED_EXTS\n\n    def __init__(\n        self, music_root: Path, db_path: Path, logger: Logger | None = None\n    ) -&gt; None:\n        \"\"\"Creates a new collection extractor for managing a music library index.\n\n        This initializer wires together filesystem paths, logging, database schema,\n        and background worker infrastructure so that music files can be scanned,\n        indexed, and kept in sync with the underlying SQLite database.\n\n        Args:\n            music_root (Path): Root directory containing the music files to be indexed.\n            db_path (Path): Path to the SQLite database file storing track metadata and search index.\n            logger (Logger | None): Optional logger for recording indexing progress and errors;\n                if omitted, a NullLogger is used.\n        \"\"\"\n        self.music_root = music_root.resolve()\n        self.db_path = db_path\n        self.data_root = db_path.parent\n        self._logger = logger or NullLogger()\n\n        self.data_root.mkdir(parents=True, exist_ok=True)\n\n        # Progress tracking\n        self._initial_status_event = ThreadEvent()\n        self._processed_count = 0\n        self._total_for_current_job = None\n        self._current_job_status = None\n\n        # Threading\n        self._write_queue: Queue[IndexEvent] = Queue()\n        self._writer_stop = ThreadEvent()\n        self._observer: Observer | None = None\n        self._db_lock = Lock()\n\n        try:\n            self._init_db()\n        except sqlite3.DatabaseError as e:\n            if (\n                'malformed' not in str(e).lower()\n                and 'corrupt' not in str(e).lower()\n            ):\n                raise\n            self._logger.error(f\"Corruption detected: {e}\")\n            self._delete_database_files()\n            self._init_db()  # Retry\n        self._start_writer_thread()\n\n    def _start_writer_thread(self) -&gt; None:\n        \"\"\"Starts the background database writer thread.\"\"\"\n        self._writer_thread = Thread(\n            target=self._db_writer_loop,\n            name=\"sqlite-writer\",\n            daemon=True,\n        )\n        self._writer_thread.start()\n\n    # === Database setup ===\n\n    def _init_db(self) -&gt; None:\n        \"\"\"Initializes the SQLite database schema for music tracks and full-text search.\"\"\"\n        with self._get_write_conn() as conn:\n            self._create_tracks_table(conn)\n            self._create_indexes(conn)\n            self._create_fts_table(conn)\n            self._create_triggers(conn)\n            self._create_meta_table(conn)\n            conn.commit()\n\n    def _delete_database_files(self) -&gt; None:\n        \"\"\"Deletes corrupted database files.\"\"\"\n        for suffix in ['', '-wal', '-shm', '-journal']:\n            file_path = Path(str(self.db_path) + suffix)\n            if file_path.exists():\n                file_path.unlink()\n\n    def _create_tracks_table(self, conn: sqlite3.Connection) -&gt; None:\n        \"\"\"Ensures the core tracks table exists for storing music metadata.\n\n        This method executes the predefined schema statement to create the tracks\n        table if it is missing, providing a durable home for per-file metadata and\n        avoiding recreation when the table is already present.\n\n        Args:\n            conn (sqlite3.Connection): The SQLite connection on which to create the table.\n        \"\"\"\n        conn.execute(SCHEMA_TRACKS_TABLE)\n\n    def _create_indexes(self, conn: sqlite3.Connection) -&gt; None:\n        \"\"\"Creates secondary indexes on the tracks table to speed up common queries.\n\n        This method validates index names for safety, then creates indexes for\n        artist, album, title, and album/track ordering columns so lookups and\n        sorting over track metadata perform efficiently.\n\n        Args:\n            conn (sqlite3.Connection): The SQLite connection on which to create the indexes.\n        \"\"\"\n        # Validate index names contain only safe characters\n        for idx_name, idx_cols in INDEX_DEFINITIONS:\n            # Validate index name is alphanumeric and underscores only\n            if not idx_name.replace(\"_\", \"\").isalnum():\n                raise ValueError(f\"Invalid index name: {idx_name}\")\n\n            # Create index - idx_name is validated, idx_cols is from trusted constant\n            sql = f\"CREATE INDEX IF NOT EXISTS {idx_name} ON tracks({idx_cols})\"\n            conn.execute(sql)\n\n    def _create_fts_table(self, conn: sqlite3.Connection) -&gt; None:\n        \"\"\"Ensures the full-text search (FTS) virtual table exists for track metadata.\n\n        This method executes the predefined FTS schema so that searchable views\n        of artist, album, title, and related fields are available and remain\n        linked to the primary tracks table.\n\n        Args:\n            conn (sqlite3.Connection): The SQLite connection on which to create the FTS table.\n        \"\"\"\n        conn.execute(SCHEMA_FTS_TABLE)\n\n    def _create_triggers(self, conn: sqlite3.Connection) -&gt; None:\n        \"\"\"Ensures triggers exist to keep the FTS table synchronized with track changes.\n\n        This method installs insert, delete, and update triggers on the tracks table\n        so that corresponding rows in the FTS virtual table are automatically created,\n        removed, or updated whenever track metadata changes.\n\n        Args:\n            conn (sqlite3.Connection): The SQLite connection on which to create the triggers.\n        \"\"\"\n        conn.execute(TRIGGER_INSERT)\n        conn.execute(TRIGGER_DELETE)\n        conn.execute(TRIGGER_UPDATE)\n\n    def _create_meta_table(self, conn: sqlite3.Connection) -&gt; None:\n        \"\"\"Ensures the meta table exists and seeds it with collection-wide flags.\n\n        This method creates the meta table if needed and inserts the\n        ``initial_indexing_done`` flag with a default value so that callers can\n        track whether a full initial index of the collection has been completed.\n\n        Args:\n            conn (sqlite3.Connection): The SQLite connection on which to create and seed the meta table.\n        \"\"\"\n        conn.execute(SCHEMA_META_TABLE)\n        conn.execute(\n            \"INSERT OR IGNORE INTO meta(key, value) VALUES (?, ?)\",\n            (\"initial_indexing_done\", \"0\"),\n        )\n\n    # === Connection management ===\n\n    @contextmanager\n    def _get_write_conn(self):\n        \"\"\"Provides a configured write-capable SQLite connection as a context manager.\n\n        This method opens a connection with collection-specific pragmas applied,\n        yields it for write operations, and guarantees that the connection is\n        closed when the context exits, even if an error occurs.\n\n        Yields:\n            sqlite3.Connection: A SQLite connection configured for writing to the collection database.\n        \"\"\"\n        conn = sqlite3.connect(self.db_path)\n        configure_connection(conn)\n        conn.row_factory = sqlite3.Row\n        try:\n            yield conn\n        finally:\n            conn.close()\n\n    def get_conn(self, readonly: bool = False) -&gt; sqlite3.Connection:\n        \"\"\"Opens and returns a configured SQLite connection for this collection.\n\n        This method creates either a read-only or read-write connection depending\n        on the ``readonly`` flag, applies a row factory for dict-like row access,\n        and sets a busy timeout to make concurrent access more robust.\n\n        Args:\n            readonly (bool): Whether to open the connection in read-only mode. Defaults to False.\n\n        Returns:\n            sqlite3.Connection: A SQLite connection configured with an appropriate busy timeout.\n        \"\"\"\n        if readonly:\n            uri = f\"file:{self.db_path}?mode=ro\"\n            conn = sqlite3.connect(uri, uri=True)\n        else:\n            conn = sqlite3.connect(self.db_path)\n\n        conn.row_factory = sqlite3.Row\n        # PRAGMA doesn't support parameters, but BUSY_TIMEOUT_MS is validated as int at module load\n        conn.execute(f\"PRAGMA busy_timeout={BUSY_TIMEOUT_MS}\")\n        return conn\n\n    # === Metadata management ===\n\n    def is_initial_indexing_done(self) -&gt; bool:\n        \"\"\"Checks whether an initial full indexing pass has been completed.\n\n        This method looks up the ``initial_indexing_done`` flag in the meta table\n        and interprets a stored value of \"1\" as meaning the first full index has\n        successfully run.\n\n        Returns:\n            bool: True if initial indexing has been marked as done, otherwise False.\n        \"\"\"\n        with self.get_conn(readonly=True) as conn:\n            cur = conn.execute(\n                \"SELECT value FROM meta WHERE key = ?\", (\"initial_indexing_done\",)\n            )\n            row = cur.fetchone()\n            return row is not None and row[\"value\"] == \"1\"\n\n    def set_initial_indexing_done(self) -&gt; None:\n        \"\"\"Marks that the initial full indexing pass has completed successfully.\n\n        This method updates the meta table flag so future runs and external\n        callers can detect that a complete initial index of the collection is\n        already in place.\n\n        Returns:\n            None\n        \"\"\"\n        with self.get_conn() as conn:\n            conn.execute(\n                \"UPDATE meta SET value = ? WHERE key = ?\",\n                (\"1\", \"initial_indexing_done\"),\n            )\n            conn.commit()\n\n    def _populate_fts_if_needed(self) -&gt; None:\n        \"\"\"Populates the full-text search (FTS) table if it is currently empty.\"\"\"\n        with self._get_write_conn() as conn:\n            cnt = conn.execute(\"SELECT count(*) FROM tracks_fts\").fetchone()[0]\n            if cnt &gt; 0:\n                return\n\n            conn.execute(FTS_POPULATE_QUERY)\n            conn.commit()\n\n    # === Writer loop ===\n\n    def _db_writer_loop(self) -&gt; None:\n        \"\"\"Populates the full-text search (FTS) table when it has no entries.\n\n        This method checks whether the FTS table is empty and, if so, bulk-copies\n        all existing track rows into it so full-text search becomes available\n        without requiring a separate rebuild step.\n\n        Returns:\n            None\n        \"\"\"\n        with self._get_write_conn() as conn:\n            processed_in_batch = 0\n\n            while not self._writer_stop.is_set():\n                try:\n                    event = self._write_queue.get(timeout=1.0)\n                except Empty:\n                    continue\n\n                with self._db_lock:\n                    try:\n                        self._process_event(conn, event)\n\n                        # Periodic commit and progress update\n                        processed_in_batch += self._should_increment_batch(event)\n                        if processed_in_batch &gt;= BATCH_SIZE:\n                            conn.commit()\n                            self._update_progress_status()\n                            processed_in_batch = 0\n\n                    except sqlite3.Error as e:\n                        self._handle_database_error(conn, event, e)\n\n                self._write_queue.task_done()\n\n            self._shutdown_writer(conn)\n\n    def _process_event(self, conn: sqlite3.Connection, event: IndexEvent) -&gt; None:\n        \"\"\"Routes a single indexing event to the appropriate handler based on its type.\n\n        This method inspects the event type and delegates to clear, delete,\n        index, or job-completion handlers so that all write operations and job\n        lifecycle changes are applied consistently through a single dispatch\n        point.\n\n        Args:\n            conn (sqlite3.Connection): The active database connection used to apply the event.\n            event (IndexEvent): The indexing event describing the operation to perform.\n        \"\"\"\n        if event.type == \"CLEAR_DB\":\n            self._clear_database(conn)\n\n        elif event.type == \"DELETE_FILE\":\n            self._delete_file(conn, event)\n\n        elif event.type == \"INDEX_FILE\":\n            self._handle_index_file(conn, event)\n\n        elif event.type in (\"REBUILD_DONE\", \"RESYNC_DONE\"):\n            self._handle_job_completion(conn)\n\n    def _clear_database(self, conn: sqlite3.Connection) -&gt; None:\n        \"\"\"Clears all track data from the database and resets the full-text index.\n\n        This method deletes every row from the main tracks table and its FTS\n        mirror, commits the changes, and runs a truncating WAL checkpoint so\n        the collection can be rebuilt from a clean, compact state.\n\n        Args:\n            conn (sqlite3.Connection): The SQLite connection on which to remove all track data.\n        \"\"\"\n        conn.execute(\"DELETE FROM tracks\")\n        conn.execute(\"DELETE FROM tracks_fts\")\n        conn.commit()\n        checkpoint_wal(conn, \"TRUNCATE\")\n        self._logger.debug(\"Database cleared and checkpointed\")\n\n    def _delete_file(self, conn: sqlite3.Connection, event: IndexEvent) -&gt; None:\n        \"\"\"Removes a single track record from the database for the given event path.\n\n        This method normalizes the event path to the stored relative form when needed,\n        executes a delete against the tracks table for that path, and increments the\n        processed operation counter used for job progress tracking.\n\n        Args:\n            conn (sqlite3.Connection): The active SQLite connection used to apply the deletion.\n            event (IndexEvent): The delete event containing the path of the track to remove.\n        \"\"\"\n        if event.path:\n            rel_path = (\n                self._to_relpath(event.path)\n                if event.path.is_absolute()\n                else str(event.path)\n            )\n            conn.execute(\"DELETE FROM tracks WHERE path = ?\", (rel_path,))\n        self._processed_count += 1\n\n    def _handle_index_file(self, conn: sqlite3.Connection, event: IndexEvent) -&gt; None:\n        \"\"\"Processes an index-file event and advances job progress.\n\n        This method delegates the actual metadata extraction and database update\n        to the low-level file indexer when a path is present on the event, then\n        increments the processed-operation counter so that job progress tracking\n        remains accurate regardless of whether the file was indexable.\n\n        Args:\n            conn (sqlite3.Connection): The active database connection used during indexing.\n            event (IndexEvent): The index event describing which file should be indexed.\n        \"\"\"\n        if event.path:\n            self._index_file(conn, event.path)\n        self._processed_count += 1\n\n    def _handle_job_completion(self, conn: sqlite3.Connection) -&gt; None:\n        \"\"\"Finalizes an indexing job and records its completion.\n\n        This method commits any remaining database changes, checkpoints the WAL in\n        PASSIVE mode, forces external progress reporting to 100% if a job was\n        being tracked, and then clears job state while logging that the completion\n        signal was processed.\n\n        Args:\n            conn (sqlite3.Connection): The database connection used throughout the indexing job.\n        \"\"\"\n        conn.commit()\n        checkpoint_wal(conn, \"PASSIVE\")\n\n        # Force final 100% progress\n        if self._total_for_current_job is not None:\n            set_indexing_status(\n                self.data_root,\n                self._current_job_status,\n                total=self._total_for_current_job,\n                current=self._total_for_current_job,\n            )\n\n        self._reset_job_state()\n        self._logger.debug(\"Job completed signal processed and checkpointed\")\n\n    def _should_increment_batch(self, event: IndexEvent) -&gt; int:\n        \"\"\"Determines whether an event should contribute to the current batch size.\n\n        This method returns 1 for file-level operations that modify the tracks\n        table, such as delete and index events, and 0 for control events that\n        should not trigger batch commits.\n\n        Args:\n            event (IndexEvent): The indexing event being evaluated.\n\n        Returns:\n            int: 1 if the event should increment the processed-in-batch counter, otherwise 0.\n        \"\"\"\n        return 1 if event.type in (\"DELETE_FILE\", \"INDEX_FILE\") else 0\n\n    def _update_progress_status(self) -&gt; None:\n        \"\"\"Updates external indexing progress based on the current job counters.\n\n        This method sends the latest processed and total operation counts for\n        the active job to the indexing status helper so that UIs can display\n        up-to-date progress information while rebuild or resync tasks run.\n\n        Returns:\n            None\n        \"\"\"\n        if self._total_for_current_job is not None:\n            set_indexing_status(\n                self.data_root,\n                self._current_job_status,\n                total=self._total_for_current_job,\n                current=self._processed_count,\n            )\n\n    def _reset_job_state(self) -&gt; None:\n        \"\"\"Clears internal tracking state for the current indexing job.\n\n        This method resets the total operation count, current job status label,\n        and processed-operation counter so that subsequent jobs start with a\n        clean progress state.\n        \"\"\"\n        self._total_for_current_job = None\n        self._current_job_status = None\n        self._processed_count = 0\n\n    def _handle_database_error(\n        self, conn: sqlite3.Connection, event: IndexEvent, error: sqlite3.Error\n    ) -&gt; None:\n        \"\"\"Logs and recovers from a database error encountered during event processing.\n\n        This method records the original SQLite error with context, rolls back the\n        current transaction to maintain consistency, and then attempts an integrity\n        check on the database, logging a secondary error if the check itself fails.\n\n        Args:\n            conn (sqlite3.Connection): The database connection on which the error occurred.\n            event (IndexEvent): The indexing event being processed when the error was raised.\n            error (sqlite3.Error): The SQLite error that triggered this handler.\n        \"\"\"\n        self._logger.error(\n            f\"Database error during {event.type}: {error}\", exc_info=True\n        )\n        conn.rollback()\n        try:\n            conn.execute(\"PRAGMA integrity_check\")\n        except Exception as check_error:\n            self._logger.error(f\"Database integrity check failed: {check_error}\")\n\n    def _shutdown_writer(self, conn: sqlite3.Connection) -&gt; None:\n        \"\"\"Cleanly shuts down the writer loop by flushing pending work to disk.\n\n        This method attempts a final commit and WAL checkpoint before the writer\n        thread exits, logging either a successful shutdown message or an error\n        if flushing changes fails.\n\n        Args:\n            conn (sqlite3.Connection): The SQLite connection used by the writer loop.\n        \"\"\"\n        try:\n            conn.commit()\n            checkpoint_wal(conn, \"TRUNCATE\")\n            self._logger.debug(\"Writer loop shutdown: committed and checkpointed\")\n        except Exception as e:\n            self._logger.error(f\"Error during writer loop shutdown: {e}\")\n\n    # === Metadata extraction ===\n\n    def _index_file(self, conn: sqlite3.Connection, path: Path) -&gt; None:\n        \"\"\"Indexes a single music file into the tracks table.\n\n        This method reads tags and file metadata for the given path and upserts\n        a normalized record into the tracks table so the database reflects the\n        current state of the file on disk. If the file disappears during\n        processing, it logs a warning and skips indexing; if a database error\n        occurs, it logs the failure and re-raises the exception.\n\n        Args:\n            conn (sqlite3.Connection): The SQLite connection used to write track metadata.\n            path (Path): The absolute path of the music file to index.\n\n        Raises:\n            sqlite3.Error: If the INSERT OR REPLACE operation fails at the database layer.\n        \"\"\"\n        if not path.exists():\n            self._logger.warning(f\"File no longer exists during indexing: {path}\")\n            return\n\n        tag = self._read_tags(path)\n        metadata = self._extract_metadata(tag, path)\n\n        try:\n            conn.execute(\n                \"\"\"\n                INSERT OR REPLACE INTO tracks\n                (path, filename, artist, album, title, albumartist, genre,\n                year, duration, mtime, track_number, disc_number)\n                VALUES (?,?,?,?,?,?,?,?,?,?,?,?)\n                \"\"\",\n                metadata,\n            )\n        except sqlite3.Error as e:\n            self._logger.error(f\"Failed to index {path}: {e}\")\n            raise\n\n    def _read_tags(self, path: Path) -&gt; TinyTag | None:\n        \"\"\"Reads tag metadata from an audio file, returning None if parsing fails.\n\n        This method attempts to load tag and duration information for the given\n        path and logs a warning when the file cannot be read or parsed so that\n        callers can continue indexing without raising errors.\n\n        Args:\n            path (Path): The absolute filesystem path to the audio file whose tags should be read.\n\n        Returns:\n            TinyTag | None: A TinyTag instance containing parsed metadata, or None if reading fails.\n        \"\"\"\n        try:\n            return TinyTag.get(path, tags=True, duration=True)\n        except Exception as e:\n            self._logger.warning(f\"Failed to read tags from {path}: {e}\")\n            return None\n\n    def _extract_metadata(self, tag: TinyTag | None, path: Path) -&gt; tuple:\n        \"\"\"Builds a normalized metadata tuple for a music file from tags and file attributes.\n\n        This method combines TinyTag metadata with filesystem information and\n        sensible fallbacks to produce a stable representation of a track that\n        can be stored in the database even when some tags are missing or\n        malformed.\n\n        Args:\n            tag (TinyTag | None): Parsed tag metadata for the file, or None if tags could not be read.\n            path (Path): The absolute filesystem path of the music file.\n\n        Returns:\n            tuple: A tuple of track fields in the order expected by the ``tracks`` table schema.\n        \"\"\"\n        rel_path = self._to_relpath(path)\n        artist = getattr(tag, \"artist\", None) or getattr(tag, \"albumartist\", None)\n        album = getattr(tag, \"album\", None)\n        title = getattr(tag, \"title\", None) or path.stem\n        track_number = self._parse_number(getattr(tag, \"track\", None))\n        disc_number = self._parse_number(getattr(tag, \"disc\", None))\n        year = self._parse_year(tag)\n\n        return (\n            rel_path,\n            path.name,\n            artist or \"Unknown\",\n            album or \"Unknown\",\n            title or \"Unknown\",\n            getattr(tag, \"albumartist\", None),\n            getattr(tag, \"genre\", None),\n            year,\n            getattr(tag, \"duration\", None),\n            path.stat().st_mtime if path.exists() else None,\n            track_number,\n            disc_number,\n        )\n\n    def _parse_year(self, tag: TinyTag | None) -&gt; int | None:\n        \"\"\"Parses a year value from tag metadata and returns it as a four-digit integer.\n\n        This method inspects the ``year`` attribute on the tag, normalizes it to a\n        string, and extracts the first four characters as an integer, returning\n        None if the attribute is missing or cannot be parsed.\n\n        Args:\n            tag (TinyTag | None): Tag metadata object that may contain a year attribute, or None.\n\n        Returns:\n            int | None: The four-digit year if successfully parsed, otherwise None.\n        \"\"\"\n        with suppress(AttributeError, ValueError):\n            if getattr(tag, \"year\", None):\n                return int(str(tag.year)[:4])\n        return None\n\n    def _parse_number(self, value: str | None) -&gt; int | None:\n        \"\"\"Parses an integer from a numeric tag field string.\n\n        This method extracts the leading integer component from values that may\n        be in forms like ``\"3\"`` or ``\"3/12\"``, returning None when the input\n        is empty or cannot be converted to an integer.\n\n        Args:\n            value (str | None): Raw tag value that should represent a number, optionally with a total (e.g., ``\"3/12\"``), or None.\n\n        Returns:\n            int | None: The parsed leading integer, or None if the input is missing or invalid.\n        \"\"\"\n        if not value:\n            return None\n        try:\n            return int(str(value).split(\"/\")[0])\n        except ValueError:\n            return None\n\n    # === File scanning ===\n\n    def _scan_music_files(self, job_type: str) -&gt; list[Path]:\n        \"\"\"Discovers all supported music files under the music root for an indexing job.\n\n        This method walks the directory tree rooted at the music folder, collects\n        absolute paths to files with supported audio extensions, and periodically\n        updates indexing status so callers can observe discovery progress for the\n        specified job type.\n\n        Args:\n            job_type (str): Label for the current indexing job, such as \"rebuilding\" or \"resyncing\".\n\n        Returns:\n            list[Path]: A list of absolute paths to all discovered music files.\n        \"\"\"\n        files = []\n        found = 0\n\n        for root, _, filenames in os.walk(str(self.music_root)):\n            for fn in filenames:\n                if os.path.splitext(fn)[1].lower() in self.SUPPORTED_EXTS:\n                    files.append(Path(root) / fn)\n                    found += 1\n                    if found % PROGRESS_UPDATE_INTERVAL == 0:\n                        set_indexing_status(\n                            self.data_root, job_type, total=-1, current=found\n                        )\n\n        return files\n\n    # === Main operations ===\n\n    def rebuild(self) -&gt; None:\n        \"\"\"Performs a full rebuild of the music collection index from the current filesystem state.\n\n        This method clears existing track data, scans the music root for all supported files,\n        and queues indexing operations so that the database is fully regenerated to match\n        the contents of the collection.\n\n        Returns:\n            None\n        \"\"\"\n        self._logger.info(\"Starting full rebuild\")\n        self._initial_status_event.set()\n\n        # Pause file monitoring to prevent race conditions with watcher\n        with self._pause_observer():\n            set_indexing_status(self.data_root, \"rebuilding\", total=-1, current=0)\n            self._write_queue.put(IndexEvent(\"CLEAR_DB\"))\n            self._write_queue.join()\n\n            files = self._scan_music_files(\"rebuilding\")\n            set_indexing_status(self.data_root, \"rebuilding\", total=len(files), current=0)\n\n            self._queue_file_operations(\n                to_delete=[],\n                to_index=files,\n                job_type=\"rebuilding\",\n            )\n\n        self.set_initial_indexing_done()\n\n    def resync(self) -&gt; None:\n        \"\"\"Synchronizes the database with the current set of music files on disk.\n\n        This method compares the filesystem under the music root with the paths\n        stored in the database, then enqueues add and delete operations so that\n        the index reflects files that have been created, removed, or renamed\n        since the last run.\n\n        Returns:\n            None\n        \"\"\"\n        self._logger.info(\"Starting resync\")\n\n        # Pause file monitoring to prevent race conditions with watcher\n        with self._pause_observer():\n            set_indexing_status(self.data_root, \"resyncing\", total=-1, current=0)\n\n            with self._db_lock:\n                fs_paths = self._scan_filesystem_paths()\n                db_paths = self._get_database_paths()\n\n            to_add_rel = fs_paths - db_paths\n            to_remove_rel = db_paths - fs_paths\n            to_add_abs = [self._to_abspath(p) for p in to_add_rel]\n\n            self._logger.info(\n                f\"Resync: {len(to_add_rel)} to add, {len(to_remove_rel)} to remove\"\n            )\n            set_indexing_status(\n                self.data_root,\n                \"resyncing\",\n                total=len(to_add_rel) + len(to_remove_rel),\n                current=0,\n            )\n\n            self._queue_file_operations(\n                to_delete=to_remove_rel,\n                to_index=to_add_abs,\n                job_type=\"resyncing\",\n            )\n\n    def _scan_filesystem_paths(self) -&gt; set[str]:\n        \"\"\"Scans the music root on disk and returns the set of all supported file paths.\n\n        This method walks the filesystem tree under the music root, collects\n        POSIX-style relative paths for files with supported extensions, and\n        periodically updates resync progress as files are discovered.\n\n        Returns:\n            set[str]: A set of relative paths for all supported music files currently on disk.\n        \"\"\"\n        fs_paths = set()\n        found = 0\n\n        for root, _, filenames in os.walk(str(self.music_root)):\n            for fn in filenames:\n                if os.path.splitext(fn)[1].lower() in self.SUPPORTED_EXTS:\n                    rel_path = self._to_relpath(Path(root) / fn)\n                    fs_paths.add(rel_path)\n                    found += 1\n                    if found % PROGRESS_UPDATE_INTERVAL == 0:\n                        set_indexing_status(\n                            self.data_root, \"resyncing\", total=-1, current=found\n                        )\n\n        return fs_paths\n\n    def _get_database_paths(self) -&gt; set[str]:\n        \"\"\"Returns the set of all track paths currently stored in the database.\n\n        This method queries the tracks table for the path column and collects all\n        stored relative paths into a set for efficient comparison with filesystem state.\n\n        Returns:\n            set[str]: A set of relative track paths as stored in the database.\n        \"\"\"\n        with self.get_conn(readonly=True) as conn:\n            return {r[\"path\"] for r in conn.execute(\"SELECT path FROM tracks\")}\n\n    def _queue_file_operations(\n        self,\n        to_delete: Iterable[str],\n        to_index: Iterable[Path],\n        job_type: Literal[\"rebuilding\", \"resyncing\"],\n    ) -&gt; None:\n        \"\"\"Coordinates and dispatches all file operations for a rebuild or resync job.\n\n        This method materializes the delete and index iterables, decides whether\n        there is any work to perform, and when there is, starts job tracking,\n        enqueues all operations, and blocks until the writer thread has finished\n        processing them.\n\n        Args:\n            to_delete (Iterable[str]): Relative paths of tracks that should be removed from the database.\n            to_index (Iterable[Path]): Absolute paths of files that should be indexed or reindexed.\n            job_type (Literal[\"rebuilding\", \"resyncing\"]): The type of indexing job these operations belong to.\n        \"\"\"\n        delete_list = list(to_delete)\n        index_list = list(to_index)\n        total_operations = len(delete_list) + len(index_list)\n\n        if total_operations == 0:\n            clear_indexing_status(self.data_root)\n            self._logger.debug(f\"No {job_type} operations needed\")\n            return\n\n        self._start_job(job_type, total_operations)\n        self._queue_operations(delete_list, index_list, job_type)\n        self._wait_for_job_completion(job_type, total_operations)\n\n    def _start_job(self, job_type: str, total_operations: int) -&gt; None:\n        \"\"\"Initializes tracking state for a rebuild or resync indexing job.\n\n        This method resets internal progress counters, records the job type and\n        total number of operations to perform, and writes an initial status entry\n        so external callers can monitor the job from the moment it starts.\n\n        Args:\n            job_type (str): Label for the indexing job being started, such as \"rebuilding\" or \"resyncing\".\n            total_operations (int): Total number of delete and index operations expected for this job.\n        \"\"\"\n        self._processed_count = 0\n        self._total_for_current_job = total_operations\n        self._current_job_status = job_type\n        set_indexing_status(self.data_root, job_type, total=total_operations, current=0)\n\n    def _queue_operations(\n        self, delete_list: list[str], index_list: list[Path], job_type: str\n    ) -&gt; None:\n        \"\"\"Enqueues delete and index events for all operations in the current job.\n\n        This method converts relative delete paths and absolute index paths into\n        queue events for the writer thread, then appends a final completion event\n        indicating whether the batch belongs to a rebuild or resync job.\n\n        Args:\n            delete_list (list[str]): Relative paths of tracks that should be removed from the database.\n            index_list (list[Path]): Absolute paths of tracks that should be indexed or reindexed.\n            job_type (str): The type of job being processed, such as \"rebuilding\" or \"resyncing\".\n        \"\"\"\n        for rel_path in delete_list:\n            self._write_queue.put(IndexEvent(\"DELETE_FILE\", Path(rel_path)))\n\n        for abs_path in index_list:\n            self._write_queue.put(IndexEvent(\"INDEX_FILE\", abs_path))\n\n        done_event = \"REBUILD_DONE\" if job_type == \"rebuilding\" else \"RESYNC_DONE\"\n        self._write_queue.put(IndexEvent(done_event))\n\n    def _wait_for_job_completion(self, job_type: str, total_operations: int) -&gt; None:\n        \"\"\"Blocks until all queued operations for the current job have been processed.\n\n        This method waits for the writer queue to drain, then clears the external\n        indexing status and logs a summary message indicating that the job type\n        has completed along with the total number of operations performed.\n\n        Args:\n            job_type (str): The type of indexing job being waited on, such as \"rebuilding\" or \"resyncing\".\n            total_operations (int): The total number of operations that were scheduled for the job.\n        \"\"\"\n        self._write_queue.join()\n        clear_indexing_status(self.data_root)\n        self._logger.debug(\n            f\"{job_type.capitalize()} completed ({total_operations} operations)\"\n        )\n\n    # === Path utilities ===\n\n    def _to_relpath(self, path: Path) -&gt; str:\n        \"\"\"Converts an absolute path to a POSIX-style path relative to the music root.\n\n        This method normalizes the given path, strips the music root prefix,\n        and returns a string suitable for stable storage and comparison in the database.\n\n        Args:\n            path (Path): The absolute filesystem path to convert.\n\n        Returns:\n            str: The relative POSIX-style path under the music root.\n        \"\"\"\n        return path.resolve().relative_to(self.music_root).as_posix()\n\n    def _to_abspath(self, relpath: str) -&gt; Path:\n        \"\"\"Converts a stored relative music path into an absolute filesystem path.\n\n        This method joins the configured music root with the given relative path\n        and returns a Path object that can be used for file system operations.\n\n        Args:\n            relpath (str): The POSIX-style relative path under the music root.\n\n        Returns:\n            Path: The absolute path pointing to the file within the music root.\n        \"\"\"\n        return self.music_root / Path(relpath)\n\n    # === Monitoring ===\n\n    def start_monitoring(self) -&gt; None:\n        \"\"\"Starts monitoring the music directory for file system changes.\"\"\"\n        if self._observer:\n            return\n        self._observer = Observer()\n        self._observer.schedule(_Watcher(self), str(self.music_root), recursive=True)\n        self._observer.start()\n\n    def stop(self, timeout: float = 30.0) -&gt; None:\n        \"\"\"Stops the database writer thread and file system observer.\n\n        Args:\n            timeout: Maximum time to wait for shutdown (seconds).\n        \"\"\"\n        # Shutdown watcher first to flush pending events\n        if self._observer:\n            for handler_list in self._observer._handlers.values():\n                for handler in handler_list:\n                    if hasattr(handler, 'shutdown'):\n                        handler.shutdown()\n            self._observer.stop()\n            self._observer.join(timeout=5)\n\n        # Stop writer thread\n        self._writer_stop.set()\n        self._writer_thread.join(timeout=5)\n\n    def _pause_observer(self):\n        \"\"\"Context manager to temporarily pause file monitoring.\n\n        This prevents race conditions during rebuild/resync operations by ensuring\n        the file watcher doesn't queue duplicate events while these operations\n        are scanning the filesystem and queueing their own events.\n\n        Returns:\n            Context manager that pauses monitoring on entry and resumes on exit,\n            reusing the standard observer setup logic in ``start_monitoring``.\n        \"\"\"\n\n        @contextmanager\n        def pause():\n            # Check if observer is running\n            was_monitoring = self._observer is not None and self._observer.is_alive()\n\n            if was_monitoring:\n                # Stop the observer completely\n                self._observer.stop()\n                self._observer.join(timeout=2)\n                self._logger.debug(\"File monitoring paused for rebuild/resync\")\n\n            try:\n                yield\n            finally:\n                # Restart monitoring if it was active\n                if was_monitoring:\n                    # Re-create and start observer from scratch\n                    from watchdog.observers import Observer\n                    from ._watcher import EnhancedWatcher as _Watcher\n\n                    self._observer = Observer()\n                    handler = _Watcher(self)\n                    self._observer.schedule(handler, str(self.music_root), recursive=True)\n                    self._observer.start()\n\n                    self._logger.debug(\"File monitoring resumed\")\n\n        return pause()\n\n    def enable_bulk_edit_mode(self) -&gt; None:\n        \"\"\"Enables bulk edit mode by pausing file system monitoring.\n\n        Call this before performing bulk file operations (e.g., tagging 100+ files)\n        to prevent event flooding. File changes will not be indexed in real-time\n        while bulk edit mode is active.\n\n        Example:\n            extractor.enable_bulk_edit_mode()\n            try:\n                # Perform bulk file operations here\n                for file in files:\n                    update_tags(file)\n            finally:\n                extractor.disable_bulk_edit_mode()\n        \"\"\"\n        if self._observer:\n            self._observer.unschedule_all()\n            self._logger.debug(\"File monitoring paused for bulk edit mode\")\n\n    def disable_bulk_edit_mode(self) -&gt; None:\n        \"\"\"Disables bulk edit mode and resyncs the database.\n\n        Call this after completing bulk file operations. This will resume file\n        system monitoring and trigger a resync to catch all changes made during\n        bulk edit mode.\n        \"\"\"\n        if self._observer:\n            # Resume monitoring with a new watcher instance\n            watcher = _Watcher(self)\n            self._observer.schedule(watcher, str(self.music_root), recursive=True)\n            self._logger.debug(\"File monitoring resumed, triggering resync\")\n\n        # Trigger resync to catch all changes\n        self.resync()\n\n    def wait_for_indexing_start(self, timeout: float = 5.0) -&gt; bool:\n        \"\"\"Waits until indexing has started or the specified timeout elapses.\n\n        This method blocks until the internal event indicating the beginning of\n        a rebuild or resync job is set, allowing callers to synchronize with the\n        start of indexing work.\n\n        Args:\n            timeout (float): The maximum number of seconds to wait for indexing to start.\n\n        Returns:\n            bool: True if indexing started before the timeout, or False if the timeout expired first.\n        \"\"\"\n        return self._initial_status_event.wait(timeout=timeout)\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib._extractor.CollectionExtractor.__init__","title":"<code>__init__(music_root, db_path, logger=None)</code>","text":"<p>Creates a new collection extractor for managing a music library index.</p> <p>This initializer wires together filesystem paths, logging, database schema, and background worker infrastructure so that music files can be scanned, indexed, and kept in sync with the underlying SQLite database.</p> <p>Parameters:</p> Name Type Description Default <code>music_root</code> <code>Path</code> <p>Root directory containing the music files to be indexed.</p> required <code>db_path</code> <code>Path</code> <p>Path to the SQLite database file storing track metadata and search index.</p> required <code>logger</code> <code>Logger | None</code> <p>Optional logger for recording indexing progress and errors; if omitted, a NullLogger is used.</p> <code>None</code> Source code in <code>src/musiclib/_extractor.py</code> <pre><code>def __init__(\n    self, music_root: Path, db_path: Path, logger: Logger | None = None\n) -&gt; None:\n    \"\"\"Creates a new collection extractor for managing a music library index.\n\n    This initializer wires together filesystem paths, logging, database schema,\n    and background worker infrastructure so that music files can be scanned,\n    indexed, and kept in sync with the underlying SQLite database.\n\n    Args:\n        music_root (Path): Root directory containing the music files to be indexed.\n        db_path (Path): Path to the SQLite database file storing track metadata and search index.\n        logger (Logger | None): Optional logger for recording indexing progress and errors;\n            if omitted, a NullLogger is used.\n    \"\"\"\n    self.music_root = music_root.resolve()\n    self.db_path = db_path\n    self.data_root = db_path.parent\n    self._logger = logger or NullLogger()\n\n    self.data_root.mkdir(parents=True, exist_ok=True)\n\n    # Progress tracking\n    self._initial_status_event = ThreadEvent()\n    self._processed_count = 0\n    self._total_for_current_job = None\n    self._current_job_status = None\n\n    # Threading\n    self._write_queue: Queue[IndexEvent] = Queue()\n    self._writer_stop = ThreadEvent()\n    self._observer: Observer | None = None\n    self._db_lock = Lock()\n\n    try:\n        self._init_db()\n    except sqlite3.DatabaseError as e:\n        if (\n            'malformed' not in str(e).lower()\n            and 'corrupt' not in str(e).lower()\n        ):\n            raise\n        self._logger.error(f\"Corruption detected: {e}\")\n        self._delete_database_files()\n        self._init_db()  # Retry\n    self._start_writer_thread()\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib._extractor.CollectionExtractor.disable_bulk_edit_mode","title":"<code>disable_bulk_edit_mode()</code>","text":"<p>Disables bulk edit mode and resyncs the database.</p> <p>Call this after completing bulk file operations. This will resume file system monitoring and trigger a resync to catch all changes made during bulk edit mode.</p> Source code in <code>src/musiclib/_extractor.py</code> <pre><code>def disable_bulk_edit_mode(self) -&gt; None:\n    \"\"\"Disables bulk edit mode and resyncs the database.\n\n    Call this after completing bulk file operations. This will resume file\n    system monitoring and trigger a resync to catch all changes made during\n    bulk edit mode.\n    \"\"\"\n    if self._observer:\n        # Resume monitoring with a new watcher instance\n        watcher = _Watcher(self)\n        self._observer.schedule(watcher, str(self.music_root), recursive=True)\n        self._logger.debug(\"File monitoring resumed, triggering resync\")\n\n    # Trigger resync to catch all changes\n    self.resync()\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib._extractor.CollectionExtractor.enable_bulk_edit_mode","title":"<code>enable_bulk_edit_mode()</code>","text":"<p>Enables bulk edit mode by pausing file system monitoring.</p> <p>Call this before performing bulk file operations (e.g., tagging 100+ files) to prevent event flooding. File changes will not be indexed in real-time while bulk edit mode is active.</p> Example <p>extractor.enable_bulk_edit_mode() try:     # Perform bulk file operations here     for file in files:         update_tags(file) finally:     extractor.disable_bulk_edit_mode()</p> Source code in <code>src/musiclib/_extractor.py</code> <pre><code>def enable_bulk_edit_mode(self) -&gt; None:\n    \"\"\"Enables bulk edit mode by pausing file system monitoring.\n\n    Call this before performing bulk file operations (e.g., tagging 100+ files)\n    to prevent event flooding. File changes will not be indexed in real-time\n    while bulk edit mode is active.\n\n    Example:\n        extractor.enable_bulk_edit_mode()\n        try:\n            # Perform bulk file operations here\n            for file in files:\n                update_tags(file)\n        finally:\n            extractor.disable_bulk_edit_mode()\n    \"\"\"\n    if self._observer:\n        self._observer.unschedule_all()\n        self._logger.debug(\"File monitoring paused for bulk edit mode\")\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib._extractor.CollectionExtractor.get_conn","title":"<code>get_conn(readonly=False)</code>","text":"<p>Opens and returns a configured SQLite connection for this collection.</p> <p>This method creates either a read-only or read-write connection depending on the <code>readonly</code> flag, applies a row factory for dict-like row access, and sets a busy timeout to make concurrent access more robust.</p> <p>Parameters:</p> Name Type Description Default <code>readonly</code> <code>bool</code> <p>Whether to open the connection in read-only mode. Defaults to False.</p> <code>False</code> <p>Returns:</p> Type Description <code>Connection</code> <p>sqlite3.Connection: A SQLite connection configured with an appropriate busy timeout.</p> Source code in <code>src/musiclib/_extractor.py</code> <pre><code>def get_conn(self, readonly: bool = False) -&gt; sqlite3.Connection:\n    \"\"\"Opens and returns a configured SQLite connection for this collection.\n\n    This method creates either a read-only or read-write connection depending\n    on the ``readonly`` flag, applies a row factory for dict-like row access,\n    and sets a busy timeout to make concurrent access more robust.\n\n    Args:\n        readonly (bool): Whether to open the connection in read-only mode. Defaults to False.\n\n    Returns:\n        sqlite3.Connection: A SQLite connection configured with an appropriate busy timeout.\n    \"\"\"\n    if readonly:\n        uri = f\"file:{self.db_path}?mode=ro\"\n        conn = sqlite3.connect(uri, uri=True)\n    else:\n        conn = sqlite3.connect(self.db_path)\n\n    conn.row_factory = sqlite3.Row\n    # PRAGMA doesn't support parameters, but BUSY_TIMEOUT_MS is validated as int at module load\n    conn.execute(f\"PRAGMA busy_timeout={BUSY_TIMEOUT_MS}\")\n    return conn\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib._extractor.CollectionExtractor.is_initial_indexing_done","title":"<code>is_initial_indexing_done()</code>","text":"<p>Checks whether an initial full indexing pass has been completed.</p> <p>This method looks up the <code>initial_indexing_done</code> flag in the meta table and interprets a stored value of \"1\" as meaning the first full index has successfully run.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if initial indexing has been marked as done, otherwise False.</p> Source code in <code>src/musiclib/_extractor.py</code> <pre><code>def is_initial_indexing_done(self) -&gt; bool:\n    \"\"\"Checks whether an initial full indexing pass has been completed.\n\n    This method looks up the ``initial_indexing_done`` flag in the meta table\n    and interprets a stored value of \"1\" as meaning the first full index has\n    successfully run.\n\n    Returns:\n        bool: True if initial indexing has been marked as done, otherwise False.\n    \"\"\"\n    with self.get_conn(readonly=True) as conn:\n        cur = conn.execute(\n            \"SELECT value FROM meta WHERE key = ?\", (\"initial_indexing_done\",)\n        )\n        row = cur.fetchone()\n        return row is not None and row[\"value\"] == \"1\"\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib._extractor.CollectionExtractor.rebuild","title":"<code>rebuild()</code>","text":"<p>Performs a full rebuild of the music collection index from the current filesystem state.</p> <p>This method clears existing track data, scans the music root for all supported files, and queues indexing operations so that the database is fully regenerated to match the contents of the collection.</p> <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>src/musiclib/_extractor.py</code> <pre><code>def rebuild(self) -&gt; None:\n    \"\"\"Performs a full rebuild of the music collection index from the current filesystem state.\n\n    This method clears existing track data, scans the music root for all supported files,\n    and queues indexing operations so that the database is fully regenerated to match\n    the contents of the collection.\n\n    Returns:\n        None\n    \"\"\"\n    self._logger.info(\"Starting full rebuild\")\n    self._initial_status_event.set()\n\n    # Pause file monitoring to prevent race conditions with watcher\n    with self._pause_observer():\n        set_indexing_status(self.data_root, \"rebuilding\", total=-1, current=0)\n        self._write_queue.put(IndexEvent(\"CLEAR_DB\"))\n        self._write_queue.join()\n\n        files = self._scan_music_files(\"rebuilding\")\n        set_indexing_status(self.data_root, \"rebuilding\", total=len(files), current=0)\n\n        self._queue_file_operations(\n            to_delete=[],\n            to_index=files,\n            job_type=\"rebuilding\",\n        )\n\n    self.set_initial_indexing_done()\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib._extractor.CollectionExtractor.resync","title":"<code>resync()</code>","text":"<p>Synchronizes the database with the current set of music files on disk.</p> <p>This method compares the filesystem under the music root with the paths stored in the database, then enqueues add and delete operations so that the index reflects files that have been created, removed, or renamed since the last run.</p> <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>src/musiclib/_extractor.py</code> <pre><code>def resync(self) -&gt; None:\n    \"\"\"Synchronizes the database with the current set of music files on disk.\n\n    This method compares the filesystem under the music root with the paths\n    stored in the database, then enqueues add and delete operations so that\n    the index reflects files that have been created, removed, or renamed\n    since the last run.\n\n    Returns:\n        None\n    \"\"\"\n    self._logger.info(\"Starting resync\")\n\n    # Pause file monitoring to prevent race conditions with watcher\n    with self._pause_observer():\n        set_indexing_status(self.data_root, \"resyncing\", total=-1, current=0)\n\n        with self._db_lock:\n            fs_paths = self._scan_filesystem_paths()\n            db_paths = self._get_database_paths()\n\n        to_add_rel = fs_paths - db_paths\n        to_remove_rel = db_paths - fs_paths\n        to_add_abs = [self._to_abspath(p) for p in to_add_rel]\n\n        self._logger.info(\n            f\"Resync: {len(to_add_rel)} to add, {len(to_remove_rel)} to remove\"\n        )\n        set_indexing_status(\n            self.data_root,\n            \"resyncing\",\n            total=len(to_add_rel) + len(to_remove_rel),\n            current=0,\n        )\n\n        self._queue_file_operations(\n            to_delete=to_remove_rel,\n            to_index=to_add_abs,\n            job_type=\"resyncing\",\n        )\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib._extractor.CollectionExtractor.set_initial_indexing_done","title":"<code>set_initial_indexing_done()</code>","text":"<p>Marks that the initial full indexing pass has completed successfully.</p> <p>This method updates the meta table flag so future runs and external callers can detect that a complete initial index of the collection is already in place.</p> <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>src/musiclib/_extractor.py</code> <pre><code>def set_initial_indexing_done(self) -&gt; None:\n    \"\"\"Marks that the initial full indexing pass has completed successfully.\n\n    This method updates the meta table flag so future runs and external\n    callers can detect that a complete initial index of the collection is\n    already in place.\n\n    Returns:\n        None\n    \"\"\"\n    with self.get_conn() as conn:\n        conn.execute(\n            \"UPDATE meta SET value = ? WHERE key = ?\",\n            (\"1\", \"initial_indexing_done\"),\n        )\n        conn.commit()\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib._extractor.CollectionExtractor.start_monitoring","title":"<code>start_monitoring()</code>","text":"<p>Starts monitoring the music directory for file system changes.</p> Source code in <code>src/musiclib/_extractor.py</code> <pre><code>def start_monitoring(self) -&gt; None:\n    \"\"\"Starts monitoring the music directory for file system changes.\"\"\"\n    if self._observer:\n        return\n    self._observer = Observer()\n    self._observer.schedule(_Watcher(self), str(self.music_root), recursive=True)\n    self._observer.start()\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib._extractor.CollectionExtractor.stop","title":"<code>stop(timeout=30.0)</code>","text":"<p>Stops the database writer thread and file system observer.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>Maximum time to wait for shutdown (seconds).</p> <code>30.0</code> Source code in <code>src/musiclib/_extractor.py</code> <pre><code>def stop(self, timeout: float = 30.0) -&gt; None:\n    \"\"\"Stops the database writer thread and file system observer.\n\n    Args:\n        timeout: Maximum time to wait for shutdown (seconds).\n    \"\"\"\n    # Shutdown watcher first to flush pending events\n    if self._observer:\n        for handler_list in self._observer._handlers.values():\n            for handler in handler_list:\n                if hasattr(handler, 'shutdown'):\n                    handler.shutdown()\n        self._observer.stop()\n        self._observer.join(timeout=5)\n\n    # Stop writer thread\n    self._writer_stop.set()\n    self._writer_thread.join(timeout=5)\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib._extractor.CollectionExtractor.wait_for_indexing_start","title":"<code>wait_for_indexing_start(timeout=5.0)</code>","text":"<p>Waits until indexing has started or the specified timeout elapses.</p> <p>This method blocks until the internal event indicating the beginning of a rebuild or resync job is set, allowing callers to synchronize with the start of indexing work.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>The maximum number of seconds to wait for indexing to start.</p> <code>5.0</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if indexing started before the timeout, or False if the timeout expired first.</p> Source code in <code>src/musiclib/_extractor.py</code> <pre><code>def wait_for_indexing_start(self, timeout: float = 5.0) -&gt; bool:\n    \"\"\"Waits until indexing has started or the specified timeout elapses.\n\n    This method blocks until the internal event indicating the beginning of\n    a rebuild or resync job is set, allowing callers to synchronize with the\n    start of indexing work.\n\n    Args:\n        timeout (float): The maximum number of seconds to wait for indexing to start.\n\n    Returns:\n        bool: True if indexing started before the timeout, or False if the timeout expired first.\n    \"\"\"\n    return self._initial_status_event.wait(timeout=timeout)\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib.indexing_status","title":"<code>src.musiclib.indexing_status</code>","text":"<p>Functions:</p> Name Description <code>clear_indexing_status</code> <p>Removes the indexing status file for the given data root.</p> <code>get_indexing_status</code> <p>Retrieves the current indexing status from the status file for the given data root.</p> <code>set_indexing_status</code> <p>Writes the current indexing status to a JSON file.</p>"},{"location":"development/musiclib/loading.html#src.musiclib.indexing_status.clear_indexing_status","title":"<code>clear_indexing_status(data_root)</code>","text":"<p>Removes the indexing status file for the given data root.</p> <p>Deletes the indexing status JSON file if it exists, effectively clearing any current indexing progress or state.</p> <p>Parameters:</p> Name Type Description Default <code>data_root</code> <code>Path | str</code> <p>The root directory containing the indexing status file.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>src/musiclib/indexing_status.py</code> <pre><code>def clear_indexing_status(data_root: Path | str) -&gt; None:\n    \"\"\"Removes the indexing status file for the given data root.\n\n    Deletes the indexing status JSON file if it exists, effectively clearing any current indexing progress or state.\n\n    Args:\n        data_root (Path | str): The root directory containing the indexing status file.\n\n    Returns:\n        None\n    \"\"\"\n    data_root = Path(data_root)\n    status_file = data_root / \"indexing_status.json\"\n    status_file.unlink(missing_ok=True)\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib.indexing_status.get_indexing_status","title":"<code>get_indexing_status(data_root, logger=None)</code>","text":"<p>Retrieves the current indexing status from the status file for the given data root.</p> <p>Attempts to read and parse the indexing status JSON file, returning its contents as a dictionary. Handles missing files and JSON decode errors gracefully, logging errors if a logger is provided.</p> <p>Parameters:</p> Name Type Description Default <code>data_root</code> <code>Path | str</code> <p>The root directory containing the indexing status file.</p> required <code>logger</code> <code>Logger</code> <p>Logger for error reporting. Uses NullLogger if not provided.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict | None</code> <p>dict | None: The indexing status data as a dictionary, or None if the file does not exist or cannot be read.</p> Source code in <code>src/musiclib/indexing_status.py</code> <pre><code>def get_indexing_status(\n    data_root: Path | str, logger: Logger | None = None\n) -&gt; dict | None:\n    \"\"\"\n    Retrieves the current indexing status from the status file for the given data root.\n\n    Attempts to read and parse the indexing status JSON file, returning its contents as a dictionary.\n    Handles missing files and JSON decode errors gracefully, logging errors if a logger is provided.\n\n    Args:\n        data_root (Path | str): The root directory containing the indexing status file.\n        logger (Logger, optional): Logger for error reporting. Uses NullLogger if not provided.\n\n    Returns:\n        dict | None: The indexing status data as a dictionary, or None if the file does not exist or cannot be read.\n    \"\"\"\n    logger = logger or NullLogger()\n    data_root = Path(data_root)\n    status_file = data_root / \"indexing_status.json\"\n\n    try:\n        with status_file.open(\"r\", encoding=\"utf-8\") as f:\n            return json.load(f)\n    except FileNotFoundError:\n        return None\n    except json.JSONDecodeError as e:\n        logger.error(f\"JSON decode error in {status_file}: {e}\")\n        return None\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib.indexing_status.set_indexing_status","title":"<code>set_indexing_status(data_root, status, total, current)</code>","text":"<p>Writes the current indexing status to a JSON file.</p> <p>Calculates progress, determines the start time, builds the status data, and writes it atomically to the status file for the given data root.</p> <p>Parameters:</p> Name Type Description Default <code>data_root</code> <code>Path | str</code> <p>The root directory containing the indexing status file.</p> required <code>status</code> <code>str</code> <p>The current status string (e.g., 'rebuilding', 'resyncing').</p> required <code>total</code> <code>int</code> <p>The total number of items to process.</p> required <code>current</code> <code>int</code> <p>The number of items processed so far.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>src/musiclib/indexing_status.py</code> <pre><code>def set_indexing_status(\n    data_root: Path | str, status: str, total: int | None, current: int\n) -&gt; None:\n    \"\"\"Writes the current indexing status to a JSON file.\n\n    Calculates progress, determines the start time, builds the status data, and writes it atomically to the status file for the given data root.\n\n    Args:\n        data_root (Path | str): The root directory containing the indexing status file.\n        status (str): The current status string (e.g., 'rebuilding', 'resyncing').\n        total (int): The total number of items to process.\n        current (int): The number of items processed so far.\n\n    Returns:\n        None\n    \"\"\"\n    data_root = Path(data_root)\n    status_file = data_root / \"indexing_status.json\"\n    status_file.parent.mkdir(parents=True, exist_ok=True)\n    if total is None or total &lt; 0:\n        progress = 0.0\n    else:\n        progress = _calculate_progress(total, current)\n    started_at = _get_started_at(status_file) or datetime.now(timezone.utc).isoformat()\n    data = _build_status_data(status, started_at, total, current, progress)\n    _atomic_write_json(status_file, data)\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib._watcher.EnhancedWatcher","title":"<code>src.musiclib._watcher.EnhancedWatcher</code>","text":"<p>               Bases: <code>FileSystemEventHandler</code></p> <p>Enhanced file system watcher with debouncing to prevent corruption.</p> <p>This watcher prevents database corruption during bulk file editing by: 1. Waiting DEBOUNCE_DELAY seconds after the last file change before indexing 2. Coalescing multiple modifications to the same file into a single operation 3. Properly flushing all pending events on shutdown</p> Example <p>Without debouncing:     Edit file.mp3 5 times rapidly \u2192 5 index operations \u2192 corruption risk</p> <p>With debouncing:     Edit file.mp3 5 times rapidly \u2192 wait 2 seconds \u2192 1 index operation \u2192 safe</p> <p>Attributes:</p> Name Type Description <code>extractor</code> <p>The CollectionExtractor instance that processes index/delete events.</p> <code>debounce_delay</code> <p>Number of seconds to wait after last file change (default: 2.0).</p> <code>pending_events</code> <code>Dict[str, Tuple[str, float]]</code> <p>Dict mapping file paths to their pending event type and timestamp.</p> <code>pending_lock</code> <p>Thread lock for synchronizing access to pending events.</p> <code>timers</code> <code>Dict[str, Timer]</code> <p>Dict mapping file paths to their active debounce timers.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Initializes the enhanced watcher with debouncing.</p> <code>on_any_event</code> <p>Handles file system events with debouncing.</p> <code>shutdown</code> <p>Cancels all pending timers and processes remaining events immediately.</p> Source code in <code>src/musiclib/_watcher.py</code> <pre><code>class EnhancedWatcher(FileSystemEventHandler):\n    \"\"\"Enhanced file system watcher with debouncing to prevent corruption.\n\n    This watcher prevents database corruption during bulk file editing by:\n    1. Waiting DEBOUNCE_DELAY seconds after the last file change before indexing\n    2. Coalescing multiple modifications to the same file into a single operation\n    3. Properly flushing all pending events on shutdown\n\n    Example:\n        Without debouncing:\n            Edit file.mp3 5 times rapidly \u2192 5 index operations \u2192 corruption risk\n\n        With debouncing:\n            Edit file.mp3 5 times rapidly \u2192 wait 2 seconds \u2192 1 index operation \u2192 safe\n\n    Attributes:\n        extractor: The CollectionExtractor instance that processes index/delete events.\n        debounce_delay: Number of seconds to wait after last file change (default: 2.0).\n        pending_events: Dict mapping file paths to their pending event type and timestamp.\n        pending_lock: Thread lock for synchronizing access to pending events.\n        timers: Dict mapping file paths to their active debounce timers.\n    \"\"\"\n\n    def __init__(self, extractor) -&gt; None:\n        \"\"\"Initializes the enhanced watcher with debouncing.\n\n        Args:\n            extractor: The CollectionExtractor instance that will handle the events.\n        \"\"\"\n        self.extractor = extractor\n        self.debounce_delay = DEBOUNCE_DELAY\n\n        # Track pending events: path -&gt; (event_type, timestamp)\n        self.pending_events: Dict[str, Tuple[str, float]] = {}\n        self.pending_lock = Lock()\n\n        # Track active timers: path -&gt; Timer\n        self.timers: Dict[str, Timer] = {}\n\n    def on_any_event(self, event: object) -&gt; None:\n        \"\"\"Handles file system events with debouncing.\n\n        This method ignores directory changes and unsupported file types, then\n        applies debouncing to file modification and deletion events. Multiple\n        rapid changes to the same file are coalesced into a single operation.\n\n        Args:\n            event: A watchdog file system event with is_directory, src_path,\n                   and event_type attributes.\n        \"\"\"\n        # Ignore directory changes\n        if event.is_directory:\n            return\n\n        path = Path(event.src_path)\n\n        # Only process supported audio file extensions\n        if path.suffix.lower() not in self.extractor.SUPPORTED_EXTS:\n            return\n\n        path_str = str(path)\n        event_type = event.event_type\n\n        with self.pending_lock:\n            # Cancel any existing timer for this file\n            if path_str in self.timers:\n                self.timers[path_str].cancel()\n\n            # Update pending event (this coalesces multiple events)\n            if event_type in (\"created\", \"modified\"):\n                # Both created and modified should result in reindexing\n                self.pending_events[path_str] = (\"modified\", time.time())\n            elif event_type == \"deleted\":\n                # Delete takes precedence over everything\n                self.pending_events[path_str] = (\"deleted\", time.time())\n\n            # Set new debounce timer\n            timer = Timer(\n                self.debounce_delay,\n                self._process_debounced_event,\n                args=(path_str,)\n            )\n            self.timers[path_str] = timer\n            timer.start()\n\n    def _process_debounced_event(self, path_str: str) -&gt; None:\n        \"\"\"Processes a debounced event after the delay period.\n\n        This is called by the timer after debounce_delay seconds of inactivity\n        on a particular file. It sends the coalesced event to the write queue.\n\n        Args:\n            path_str: String representation of the file path.\n        \"\"\"\n        with self.pending_lock:\n            # Get and remove the pending event\n            if path_str not in self.pending_events:\n                return\n\n            event_type, _ = self.pending_events.pop(path_str)\n\n            # Clean up timer reference\n            if path_str in self.timers:\n                del self.timers[path_str]\n\n        # Import IndexEvent here to avoid circular imports\n        from ._extractor import IndexEvent\n\n        # Send to processing queue\n        path = Path(path_str)\n\n        if event_type == \"modified\":\n            self.extractor._write_queue.put(IndexEvent(\"INDEX_FILE\", path))\n        elif event_type == \"deleted\":\n            self.extractor._write_queue.put(IndexEvent(\"DELETE_FILE\", path))\n\n    def shutdown(self) -&gt; None:\n        \"\"\"Cancels all pending timers and processes remaining events immediately.\n\n        This ensures no events are lost when stopping the watcher. All pending\n        events are flushed to the processing queue before shutdown completes.\n\n        This method should be called before stopping the file system observer.\n        \"\"\"\n        # Import IndexEvent here to avoid circular imports\n        from ._extractor import IndexEvent\n\n        with self.pending_lock:\n            # Cancel all pending timers\n            for timer in self.timers.values():\n                timer.cancel()\n\n            # Process all remaining pending events immediately\n            for path_str, (event_type, _) in self.pending_events.items():\n                path = Path(path_str)\n                if event_type == \"modified\":\n                    self.extractor._write_queue.put(IndexEvent(\"INDEX_FILE\", path))\n                elif event_type == \"deleted\":\n                    self.extractor._write_queue.put(IndexEvent(\"DELETE_FILE\", path))\n\n            # Clear all state\n            self.pending_events.clear()\n            self.timers.clear()\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib._watcher.EnhancedWatcher.__init__","title":"<code>__init__(extractor)</code>","text":"<p>Initializes the enhanced watcher with debouncing.</p> <p>Parameters:</p> Name Type Description Default <code>extractor</code> <p>The CollectionExtractor instance that will handle the events.</p> required Source code in <code>src/musiclib/_watcher.py</code> <pre><code>def __init__(self, extractor) -&gt; None:\n    \"\"\"Initializes the enhanced watcher with debouncing.\n\n    Args:\n        extractor: The CollectionExtractor instance that will handle the events.\n    \"\"\"\n    self.extractor = extractor\n    self.debounce_delay = DEBOUNCE_DELAY\n\n    # Track pending events: path -&gt; (event_type, timestamp)\n    self.pending_events: Dict[str, Tuple[str, float]] = {}\n    self.pending_lock = Lock()\n\n    # Track active timers: path -&gt; Timer\n    self.timers: Dict[str, Timer] = {}\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib._watcher.EnhancedWatcher.on_any_event","title":"<code>on_any_event(event)</code>","text":"<p>Handles file system events with debouncing.</p> <p>This method ignores directory changes and unsupported file types, then applies debouncing to file modification and deletion events. Multiple rapid changes to the same file are coalesced into a single operation.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>object</code> <p>A watchdog file system event with is_directory, src_path,    and event_type attributes.</p> required Source code in <code>src/musiclib/_watcher.py</code> <pre><code>def on_any_event(self, event: object) -&gt; None:\n    \"\"\"Handles file system events with debouncing.\n\n    This method ignores directory changes and unsupported file types, then\n    applies debouncing to file modification and deletion events. Multiple\n    rapid changes to the same file are coalesced into a single operation.\n\n    Args:\n        event: A watchdog file system event with is_directory, src_path,\n               and event_type attributes.\n    \"\"\"\n    # Ignore directory changes\n    if event.is_directory:\n        return\n\n    path = Path(event.src_path)\n\n    # Only process supported audio file extensions\n    if path.suffix.lower() not in self.extractor.SUPPORTED_EXTS:\n        return\n\n    path_str = str(path)\n    event_type = event.event_type\n\n    with self.pending_lock:\n        # Cancel any existing timer for this file\n        if path_str in self.timers:\n            self.timers[path_str].cancel()\n\n        # Update pending event (this coalesces multiple events)\n        if event_type in (\"created\", \"modified\"):\n            # Both created and modified should result in reindexing\n            self.pending_events[path_str] = (\"modified\", time.time())\n        elif event_type == \"deleted\":\n            # Delete takes precedence over everything\n            self.pending_events[path_str] = (\"deleted\", time.time())\n\n        # Set new debounce timer\n        timer = Timer(\n            self.debounce_delay,\n            self._process_debounced_event,\n            args=(path_str,)\n        )\n        self.timers[path_str] = timer\n        timer.start()\n</code></pre>"},{"location":"development/musiclib/loading.html#src.musiclib._watcher.EnhancedWatcher.shutdown","title":"<code>shutdown()</code>","text":"<p>Cancels all pending timers and processes remaining events immediately.</p> <p>This ensures no events are lost when stopping the watcher. All pending events are flushed to the processing queue before shutdown completes.</p> <p>This method should be called before stopping the file system observer.</p> Source code in <code>src/musiclib/_watcher.py</code> <pre><code>def shutdown(self) -&gt; None:\n    \"\"\"Cancels all pending timers and processes remaining events immediately.\n\n    This ensures no events are lost when stopping the watcher. All pending\n    events are flushed to the processing queue before shutdown completes.\n\n    This method should be called before stopping the file system observer.\n    \"\"\"\n    # Import IndexEvent here to avoid circular imports\n    from ._extractor import IndexEvent\n\n    with self.pending_lock:\n        # Cancel all pending timers\n        for timer in self.timers.values():\n            timer.cancel()\n\n        # Process all remaining pending events immediately\n        for path_str, (event_type, _) in self.pending_events.items():\n            path = Path(path_str)\n            if event_type == \"modified\":\n                self.extractor._write_queue.put(IndexEvent(\"INDEX_FILE\", path))\n            elif event_type == \"deleted\":\n                self.extractor._write_queue.put(IndexEvent(\"DELETE_FILE\", path))\n\n        # Clear all state\n        self.pending_events.clear()\n        self.timers.clear()\n</code></pre>"},{"location":"development/musiclib/reading.html","title":"Reader","text":""},{"location":"development/musiclib/reading.html#searching-the-music-collection","title":"Searching the music collection","text":"<p>The music collection supports a flexible, tag-based search language that can return artists, albums, and tracks in a single request. Search results are grouped, scored, highlighted, and designed for lazy navigation via follow-up queries.</p> <p>Searching is implemented in two layers:</p> <ul> <li>Core search engine \u2013 <code>MusicCollection.search_grouped(...)</code> (in <code>reader.py</code>)</li> <li>UI-facing API \u2013 <code>MusicCollectionUI.search_highlighting(...)</code> (in <code>ui.py</code>)</li> </ul> <p>Most applications should call the UI-facing method.</p>"},{"location":"development/musiclib/reading.html#entry-points","title":"\ud83d\ude80 Entry points","text":""},{"location":"development/musiclib/reading.html#ui-facing-search-recommended","title":"UI-facing search (recommended)","text":"<pre><code>results = mc_ui.search_highlighting(query, limit=30)\n</code></pre> <p>Returns a single list of result objects, ready for rendering in a UI. Each result object has a <code>type</code> field (<code>artist</code>, <code>album</code>, or <code>track</code>) and includes highlighted text and navigation metadata.</p>"},{"location":"development/musiclib/reading.html#core-grouped-search-lower-level","title":"Core grouped search (lower-level)","text":"<pre><code>grouped, terms = mc.search_grouped(query, limit=20)\n</code></pre> <p>Returns:</p> <ol> <li>A dictionary with three lists: <code>artists</code>, <code>albums</code>, <code>tracks</code></li> <li>The parsed search terms, grouped by category</li> </ol> <p>This method is primarily used internally by the UI layer.</p>"},{"location":"development/musiclib/reading.html#query-language","title":"\ud83d\udd0e Query language","text":"<p>The query parser recognizes tagged terms and general free-text terms.</p>"},{"location":"development/musiclib/reading.html#supported-tags","title":"Supported tags","text":"Tag Example Meaning <code>artist:</code> <code>artist:Prince</code> Restrict results to a specific artist <code>album:</code> <code>album:\"Purple Rain\"</code> Restrict results to a specific album <code>track:</code> / <code>song:</code> <code>track:\"When Doves Cry\"</code> Restrict results to track titles"},{"location":"development/musiclib/reading.html#free-text-terms","title":"Free-text terms","text":"<p>Any term not prefixed by a tag is treated as free-text and matched against:</p> <ul> <li>artist</li> <li>album</li> <li>track title</li> </ul> <p>Example:</p> <pre><code>love\n</code></pre>"},{"location":"development/musiclib/reading.html#quoting-and-escaping","title":"Quoting and escaping","text":"<ul> <li>Single or double quotes allow multi-word values</li> </ul> <pre><code>artist:\"The Beatles\"\nalbum:'Purple Rain'\n</code></pre> * Backslashes can escape special characters inside quoted values"},{"location":"development/musiclib/reading.html#parsed-term-structure","title":"\u2699\ufe0f Parsed term structure","text":"<p>The query is normalized into a dictionary:</p> <pre><code>{\n    \"artist\":  [...],\n    \"album\":   [...],\n    \"track\":   [...],\n    \"general\": [...]\n}\n</code></pre> <p>This structure is returned alongside the search results and reused for:</p> <ul> <li>scoring</li> <li>highlighting</li> <li>UI explanation (\u201cwhy did this match?\u201d)</li> </ul>"},{"location":"development/musiclib/reading.html#search-execution-model","title":"\u26a1 Search execution model","text":""},{"location":"development/musiclib/reading.html#pass-one-candidate-collection","title":"Pass-one candidate collection","text":"<p>The search engine performs a first pass to collect candidates:</p> <ul> <li>Artists are scored by how well they match artist terms</li> <li>Albums are scored by album name and artist context</li> <li>Tracks are scored by title and tag matches</li> </ul> <p>The engine may reuse candidates from the previous search session if the new query is a refinement (e.g. clicking an artist).</p>"},{"location":"development/musiclib/reading.html#scoring-simplified","title":"Scoring (simplified)","text":"<p>Matches are weighted using:</p> <ul> <li>Exact matches</li> <li>Prefix matches</li> <li>Substring matches</li> <li>Tag bonuses (explicit <code>artist:</code>, <code>album:</code>, <code>track:</code>)</li> </ul> <p>This produces ranked candidate sets for artists, albums, and tracks.</p>"},{"location":"development/musiclib/reading.html#result-grouping-and-hierarchy","title":"\ud83c\udfd8\ufe0f Result grouping and hierarchy","text":"<p>After scoring, results are assembled into a hierarchical structure:</p> <ul> <li>Artists</li> <li>Albums</li> <li>Tracks</li> </ul> <p>The engine decides which groups to include based on the query:</p> Query type Included sections Free-text only Artists, albums, and tracks <code>artist:</code> present Artists + related albums/tracks <code>album:</code> present Albums + tracks <code>track:</code> present Tracks only"},{"location":"development/musiclib/reading.html#ui-result-model","title":"\ud83c\udfaf UI result model","text":"<p>The UI layer converts grouped results into a single flat list of result objects via the function <code>search_highlighting</code>.</p> <p>Each object has a <code>type</code> field and a shape appropriate for rendering.</p>"},{"location":"development/musiclib/reading.html#artist-results","title":"Artist results","text":"<pre><code>{\n  \"type\": \"artist\",\n  \"artist\": \"&lt;mark&gt;Prince&lt;/mark&gt;\",\n  \"raw_artist\": \"Prince\",\n  \"reasons\": [\n    { \"type\": \"album\", \"text\": \"3 album(s)\" },\n    { \"type\": \"track\", \"text\": \"12 nummer(s)\" }\n  ],\n  \"load_on_demand\": true,\n  \"clickable\": true,\n  \"click_query\": \"artist:'Prince'\"\n}\n</code></pre> <p>Characteristics</p> <ul> <li>Summary only (no albums or tracks included)</li> <li>Always lazy-loaded</li> <li>Clicking triggers a new search using <code>click_query</code></li> </ul>"},{"location":"development/musiclib/reading.html#album-results","title":"Album results","text":"<pre><code>{\n  \"type\": \"album\",\n  \"artist\": \"Prince\",\n  \"album\": \"&lt;mark&gt;Purple Rain&lt;/mark&gt;\",\n  \"is_compilation\": false,\n  \"reasons\": [\n    { \"type\": \"track\", \"text\": \"5 nummer(s)\" }\n  ],\n  \"load_on_demand\": true,\n  \"clickable\": true,\n  \"click_query\": \"release_dir:'/Prince/Purple Rain'\"\n}\n</code></pre> <p>Characteristics</p> <ul> <li>Summary only</li> <li>Tracks are loaded on demand</li> <li>Albums with tracks by more than three artists are shown as \u201cVarious Artists\u201d</li> </ul>"},{"location":"development/musiclib/reading.html#track-results","title":"Track results","text":"<pre><code>{\n  \"type\": \"track\",\n  \"artist\": \"Prince\",\n  \"album\": \"Purple Rain\",\n  \"track\": \"&lt;mark&gt;When Doves Cry&lt;/mark&gt;\",\n  \"duration\": \"5:54\",\n  \"path\": \"Prince/Purple Rain/01 - When Doves Cry.flac\",\n  \"artist_click_query\": \"artist:'Prince'\",\n  \"album_click_query\": \"album:'Purple Rain'\"\n}\n</code></pre> <p>Characteristics</p> <ul> <li>Fully populated (no lazy loading)</li> <li>Includes navigation queries for artist and album</li> </ul>"},{"location":"development/musiclib/reading.html#lazy-loading","title":"\ud83d\udca4 Lazy loading","text":"<p>Artist and album results include a <code>click_query</code> field.</p> <p>When the user clicks such a result:</p> <ol> <li>The UI issues a new search</li> <li>Using the stored <code>click_query</code></li> <li>Which returns a more specific result set</li> </ol> <p>This keeps the API stateless and avoids nested payloads.</p>"},{"location":"development/musiclib/reading.html#highlighting","title":"\u2728 Highlighting","text":"<p>All matched terms are automatically highlighted:</p> <ul> <li>Implemented in <code>_highlight_text(...)</code></li> <li>Case-insensitive</li> <li>Wrapped in <code>&lt;mark&gt;...&lt;/mark&gt;</code></li> </ul> <p>Highlighting applies to:</p> <ul> <li>Artist names</li> <li>Album titles</li> <li>Track titles</li> </ul> <p>This behavior is UI-specific and not part of the core search engine.</p>"},{"location":"development/musiclib/reading.html#match-explanations","title":"\ud83d\udca1 Match explanations","text":"<p>Each result may include a <code>reasons</code> list explaining why it matched:</p> <ul> <li>Matching artist name</li> <li>Number of matching albums</li> <li>Number of matching tracks</li> </ul> <p>These are intended for UI hints, badges, or tooltips.</p>"},{"location":"development/musiclib/reading.html#realtime-monitoring","title":"\ud83d\udc40 Real\u2011time monitoring","text":"<p><code>MusicCollection.start_monitoring()</code> creates a <code>watchdog.observers.Observer</code> that uses the <code>EnhancedWatcher</code> class (defined in <code>src/musiclib/_watcher.py</code>). The enhanced watcher adds two important behaviours that differ from a na\u00efve <code>FileSystemEventHandler</code>:</p> Feature What it does Why it matters Debounce delay (<code>DEBOUNCE_DELAY = 2.0\u202fs</code>) After the last change to a given file, the watcher waits 2\u202fseconds before queuing an <code>INDEX_FILE</code> or <code>DELETE_FILE</code> event. Prevents a burst of rapid edits (e.g., a tag\u2011editing batch) from generating many separate index operations, which could corrupt the DB. Coalescing Multiple <code>created</code>/<code>modified</code> events for the same path are merged into a single <code>INDEX_FILE</code> event; a later <code>deleted</code> event overrides any pending <code>modified</code> events. Guarantees that the final state of the file is what gets indexed. Graceful shutdown (<code>shutdown()</code> method) Cancels all pending timers and flushes any remaining events to the write queue before the observer is stopped. Ensures no file\u2011system changes are lost when the application exits. <p>The rest of the monitoring flow (observer start/stop, queue \u2192 writer thread) remains exactly as described in the original diagram.</p>"},{"location":"development/musiclib/reading.html#summary","title":"\ud83d\udcc4 Summary","text":"<p>In short, searching works as follows:</p> <ol> <li>Parse the query into tagged and free-text terms</li> <li>Collect and score artist, album, and track candidates</li> <li>Build hierarchical grouped results</li> <li>Flatten results into UI-friendly objects</li> <li>Highlight matches and attach navigation queries</li> <li>Support lazy exploration through follow-up searches</li> </ol> <p>This design allows the UI to deliver a fast, expressive, and navigable search experience without embedding deep hierarchies in a single response.</p>"},{"location":"development/musiclib/reading.html#api","title":"\ud83d\udd0c API","text":"<p>Only the following methods are considered stable public APIs: <code>MusicCollection.search_grouped</code>, <code>MusicCollectionUI.search_highlighting</code>, <code>MusicCollection.rebuild</code>, <code>MusicCollection.resync</code>, <code>MusicCollection.close</code>, <code>MusicCollection.get_collection_stats</code>.</p>"},{"location":"development/musiclib/reading.html#src.musiclib.reader.MusicCollection","title":"<code>src.musiclib.reader.MusicCollection</code>","text":"<p>Manages a music collection database and provides search and detail retrieval functionality. Handles lazy loading, background indexing, and query parsing for artists, albums, and tracks.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Initializes a MusicCollection backed by a SQLite database and music root directory.</p> <code>close</code> <p>Stops monitoring and closes resources associated with the music collection.</p> <code>count</code> <p>Returns the total number of tracks in the music collection database.</p> <code>get_album_details</code> <p>Retrieves detailed information about an album given its release directory.</p> <code>get_artist_details</code> <p>Retrieves detailed information about an artist, including their albums and tracks.</p> <code>get_collection_stats</code> <p>Returns high-level statistics about the music collection.</p> <code>get_cover</code> <p>Retrieves or generates the cover image path for a given album release directory.</p> <code>get_track</code> <p>Retrieves metadata for a single track identified by its path.</p> <code>rebuild</code> <p>Triggers a full rebuild of the music collection database.</p> <code>resync</code> <p>Performs a resync of the music collection database.</p> <code>search_grouped</code> <p>Searches the music collection and returns grouped results by artists, albums, and tracks.</p> Source code in <code>src/musiclib/reader.py</code> <pre><code>class MusicCollection:\n    \"\"\"Manages a music collection database and provides search and detail retrieval functionality.\n    Handles lazy loading, background indexing, and query parsing for artists, albums, and tracks.\n    \"\"\"\n\n    EXACT_MATCH_SCORE = 100\n    PREFIX_MATCH_SCORE = 70\n    SUBSTRING_MATCH_SCORE = 40\n    TAG_BONUS = 30\n\n    def __init__(\n        self, music_root: Path | str, db_path: Path | str, logger: Logger = None\n    ) -&gt; None:\n        \"\"\"Initializes a MusicCollection backed by a SQLite database and music root directory.\n        Sets up logging, collection extraction, and schedules any required initial indexing or resync operations.\n\n        Args:\n            music_root: The root directory containing the music files to be indexed.\n            db_path: The path to the SQLite database file used to store collection metadata.\n            logger: Optional logger instance for recording informational and error messages.\n        \"\"\"\n        self.music_root = Path(music_root).resolve()\n        self.db_path = Path(db_path)\n        self._logger = logger or NullLogger()\n        self._extractor = CollectionExtractor(self.music_root, self.db_path, logger=logger)\n\n        track_count = self.count()\n        self._startup_mode = \"rebuild\" if track_count == 0 else \"resync\"\n        self._logger.info(\n            \"No tracks in DB \u2014 scheduling initial rebuild\"\n            if track_count == 0\n            else \"Start resync of DB\"\n        )\n\n        self._background_task_running = False\n        self._extractor.start_monitoring()\n        self._start_background_startup_job()\n        if track_count == 0:\n            self._extractor.wait_for_indexing_start()\n\n        self._last_search_session: SearchSession | None = None\n\n        self.covers_dir = self.db_path.parent / \"cache\" / \"covers\"\n        self.covers_dir.mkdir(parents=True, exist_ok=True)\n        self._setup_fallback_cover()\n\n    def is_indexing(self) -&gt; bool:\n        status = get_indexing_status(self.db_path.parent)\n\n        # If status file exists and indicates active job \u2192 yes\n        if status and status.get(\"status\") in (\"rebuilding\", \"resyncing\"):\n            return True\n\n        return status is None and self.count() == 0\n\n    def _start_background_startup_job(self) -&gt; None:\n        \"\"\"Starts a background thread to perform initial indexing or resync of the music collection.\n        Ensures that only one background startup job runs at a time.\n\n        Returns:\n            None\n        \"\"\"\n        if self._background_task_running:\n            return\n\n        # Only start initial rebuild if not already marked as done\n        if self._extractor.is_initial_indexing_done():\n            return\n\n        self._background_task_running = True\n\n        def task():\n            try:\n                self._extractor.rebuild()  # This will set status, etc.\n                self._extractor.set_initial_indexing_done()\n                self._logger.debug(\"Initial indexing completed and marked as done\")\n            except Exception as e:\n                self._logger.error(f\"Initial indexing failed: {e}\", exc_info=True)\n            finally:\n                self._background_task_running = False\n\n        Thread(target=task, daemon=True).start()\n\n    def rebuild(self) -&gt; None:\n        \"\"\"Triggers a full rebuild of the music collection database.\n        Rebuilds the database from scratch using the current music files.\n\n        Returns:\n            None\n        \"\"\"\n        self._extractor.rebuild()\n\n    def resync(self) -&gt; None:\n        \"\"\"Performs a resync of the music collection database.\n        Updates the database to reflect changes in the music files without a full rebuild.\n\n        Returns:\n            None\n        \"\"\"\n        self._extractor.resync()\n\n    def close(self) -&gt; None:\n        \"\"\"Stops monitoring and closes resources associated with the music collection.\n        Cleans up background tasks and releases any held resources.\n\n        Returns:\n            None\n        \"\"\"\n        self._extractor.stop()\n\n    @contextmanager\n    def _get_conn(self):\n        \"\"\"Returns a read-only SQLite connection with corruption detection.\n\n        Yields:\n            sqlite3.Connection: A read-only SQLite database connection.\n\n        Raises:\n            DatabaseCorruptionError: If database corruption is detected.\n        \"\"\"\n        with self._extractor.get_conn(readonly=True) as conn:\n            try:\n                yield conn\n            except sqlite3.DatabaseError as e:\n                error_msg = str(e).lower()\n                if any(k in error_msg for k in [\"malformed\", \"corrupt\", \"damaged\", \"disk image\"]):\n                    raise DatabaseCorruptionError(\n                        \"The music library database is corrupted and needs to be rebuilt.\"\n                    ) from e\n                raise\n\n    def count(self) -&gt; int:\n        \"\"\"Returns the total number of tracks in the music collection database.\n        Executes a query to count all tracks currently indexed.\n\n        Returns:\n            int: The number of tracks in the database.\n        \"\"\"\n        with self._get_conn() as conn:\n            return conn.execute(\"SELECT COUNT(*) FROM tracks\").fetchone()[0]\n\n    def _use_fts(self, conn: sqlite3.Connection) -&gt; bool:\n        \"\"\"Checks if the full-text search (FTS) table exists in the database.\n        Determines whether FTS-based queries can be used for searching tracks.\n\n        Args:\n            conn: SQLite database connection.\n\n        Returns:\n            bool: True if the FTS table exists, False otherwise.\n        \"\"\"\n        return (\n            conn.execute(\n                \"SELECT name FROM sqlite_master WHERE type='table' AND name='tracks_fts'\"\n            ).fetchone()\n            is not None\n        )\n\n    def _fts_escape(self, txt: str) -&gt; str:\n        \"\"\"Escapes special characters in a string for use in full-text search queries.\n\n        SQLite FTS5 treats certain characters as token separators (including periods).\n        For search terms like \"R.E.M.\", we need to quote them to search as a phrase.\n\n        Args:\n            txt: The input string to escape.\n\n        Returns:\n            str: The escaped string safe for FTS queries.\n        \"\"\"\n        # If the text contains FTS special characters (like periods), quote it\n        # to search it as a phrase\n        has_special = any(c in txt for c in '.,-()[]{}!@#$%^&amp;*+=|\\\\/&lt;&gt;?;:')\n\n        # Escape any double quotes in the text\n        escaped = txt.replace('\"', '\"\"')\n\n        # If it has special characters, wrap in quotes to search as phrase\n        if has_special:\n            return f'\"{escaped}\"'\n\n        return escaped\n\n    def _search_album_tracks(\n        self, conn: sqlite3.Connection, artist: str, album: str\n    ) -&gt; list[dict[str, str]]:\n        \"\"\"Fetches all tracks for a given artist and album from the database.\n        Returns a list of track details including title, path, filename, and duration.\n\n        Args:\n            conn: SQLite database connection.\n            artist: Name of the artist.\n            album: Name of the album.\n\n        Returns:\n            list[dict[str, str]]: List of dictionaries containing track details.\n        \"\"\"\n        cur = conn.execute(\n            \"SELECT title, path, filename, duration FROM tracks WHERE artist = ? AND album = ? ORDER BY title COLLATE NOCASE\",\n            (artist, album),\n        )\n        return [\n            {\n                \"track\": r[\"title\"],\n                \"path\": self._relative_path(r[\"path\"]),\n                \"filename\": r[\"filename\"],\n                \"duration\": self._format_duration(r[\"duration\"]),\n            }\n            for r in cur\n        ]\n\n    @staticmethod\n    def _format_duration(duration: float | int | str | None) -&gt; str:\n        \"\"\"Converts a duration value into a human-readable minutes and seconds string.\n        Handles numeric seconds, pre-formatted strings, and missing or invalid values gracefully.\n\n        Args:\n            duration: The duration value as seconds, a pre-formatted string, or None.\n\n        Returns:\n            str: A string in the format 'M:SS' or a placeholder such as '?:??' when unknown.\n        \"\"\"\n        if duration is None:\n            return \"?:??\"\n\n        # Already formatted string (e.g. '8:27')\n        if isinstance(duration, str):\n            duration = duration.strip()\n            # If it's a time-like string, preserve it, but handle degenerate values\n            if \":\" in duration:\n                # Remove a single leading '0' from the minutes part (e.g. '08:27' -&gt; '8:27')\n                normalized = duration.lstrip(\"0\")\n                # If everything was zeros (e.g. '00:00', '0:00'), fall back to a clear zero duration\n                return normalized or \"0:00\"\n            # Non time-like strings are treated as unknown duration\n            return duration.lstrip(\"0\") or \"0:00\" if \":\" in duration else \"?:??\"\n        # Numeric seconds\n        try:\n            total_seconds = int(float(duration))\n        except (TypeError, ValueError):\n            return \"?:??\"\n\n        minutes, seconds = divmod(total_seconds, 60)\n        return f\"{minutes}:{seconds:02d}\"\n\n    def _build_track_dict(\n        self, row: dict, artist: str | None = None, album: str | None = None\n    ) -&gt; dict:\n        \"\"\"Builds a track dictionary from a database row.\n\n        Args:\n            row: The database row.\n            artist: Optional artist override.\n            album: Optional album override.\n\n        Returns:\n            dict: The track dictionary with cover information.\n        \"\"\"\n        # Get the release directory to fetch the cover\n        release_dir = self._get_release_dir(row[\"path\"])\n\n        return {\n            \"artist\": artist or row[\"artist\"],\n            \"track\": row[\"title\"],\n            \"path\": self._relative_path(row[\"path\"]),\n            \"filename\": row[\"filename\"],\n            \"duration\": self._format_duration(row[\"duration\"]),\n            \"album\": album or row[\"album\"],\n            \"cover\": self.get_cover(release_dir),\n        }\n\n    def _relative_path(self, path: str) -&gt; str:\n        \"\"\"Converts an absolute track path to a path relative to the music root directory.\n        Used to display or store paths in a consistent, relative format.\n\n        Args:\n            path: The absolute path to the track file.\n\n        Returns:\n            str: The path relative to the music root directory.\n        \"\"\"\n        p = Path(path)\n        if p.is_absolute():\n            try:\n                return str(p.relative_to(self.music_root))\n            except ValueError:\n                # Fallback if not a subpath (shouldn't happen normally)\n                self._logger.warning(\n                    f\"Path {path} not under music_root {self.music_root}\"\n                )\n                return str(p)\n        else:\n            # Already relative \u2013 return unchanged\n            return str(p)\n\n    def _parse_query(self, query: str) -&gt; dict[str, list[str]]:\n        \"\"\"Parses a search query string into tagged and general search terms.\n        Extracts artist, album, track tags and general terms for advanced search functionality.\n\n        Args:\n            query: The search query string to parse.\n\n        Returns:\n            dict[str, list[str]]: Dictionary containing lists of terms for 'artist', 'album', 'track', and 'general'.\n        \"\"\"\n        query = query.strip()\n        if not query:\n            return {\"artist\": [], \"album\": [], \"track\": [], \"general\": []}\n\n        tagged_terms, remaining = self._parse_tagged_terms(query)\n        general_terms = self._parse_general_terms(remaining)\n        tagged_terms[\"general\"].extend([p for p in general_terms if p])\n\n        for key in tagged_terms:\n            tagged_terms[key] = self._clean_term_list(tagged_terms[key])\n\n        return tagged_terms\n\n    def _parse_tagged_terms(self, query: str) -&gt; tuple[dict[str, list[str]], str]:\n        \"\"\"Extracts tagged terms from the query using regex and removes them, returning the terms and remaining query.\n\n        Args:\n            query: The search query string to parse.\n\n        Returns:\n            tuple[dict[str, list[str]], str]: Tagged terms grouped by category and the remaining query string.\n        \"\"\"\n        terms = {\n            \"artist\": [],\n            \"album\": [],\n            \"track\": [],  # also used for 'song:'\n            \"general\": [],\n        }\n\n        # Regex to match: tag:\"quoted phrase\" or tag:word\n        tag_pattern = re.compile(\n            r'(artist|album|track|song):\"([^\"]+)\"|(artist|album|track|song):([^\\s]+)',\n            re.IGNORECASE,\n        )\n\n        # Find all tagged terms first\n        tagged_matches = list(tag_pattern.finditer(query))\n\n        # Process tagged terms (from right to left to avoid index shifting)\n        for match in reversed(tagged_matches):\n            tag_type = (match.group(1) or match.group(3)).lower()\n            value = match.group(2) or match.group(4)\n\n            if tag_type == \"song\":\n                tag_type = \"track\"\n\n            if value.strip():\n                terms[tag_type].append(value.strip())\n\n            # Remove this tagged part from the query\n            start, end = match.span()\n            query = query[:start] + query[end:]\n\n        return terms, query.strip()\n\n    def _parse_general_terms(self, remaining: str) -&gt; list[str]:\n        \"\"\"Parses the remaining query into general terms, preserving quoted phrases and splitting words.\n\n        Args:\n            remaining: The remaining query string after tagged terms are removed.\n\n        Returns:\n            list[str]: List of general terms.\n        \"\"\"\n        parts = []\n        i = 0\n        while i &lt; len(remaining):\n            if remaining[i] == '\"':\n                # Find closing quote\n                j = remaining.find('\"', i + 1)\n                if j == -1:\n                    j = len(remaining)\n                phrase = remaining[i + 1 : j]\n                if phrase.strip():\n                    parts.append(phrase.strip())\n                i = j + 1\n            elif remaining[i].isspace():\n                i += 1\n            else:\n                # Find end of word\n                j = i\n                while j &lt; len(remaining) and not remaining[j].isspace():\n                    j += 1\n                word = remaining[i:j]\n                if word.strip():\n                    parts.append(word.strip())\n                i = j\n        return parts\n\n    def _clean_term_list(self, term_list: list[str]) -&gt; list[str]:\n        \"\"\"Cleans a list of terms by stripping, removing invalid wildcards, and normalizing.\n\n        Args:\n            term_list: List of terms to clean.\n\n        Returns:\n            list[str]: Cleaned and normalized list of terms.\n        \"\"\"\n        cleaned = []\n        for term in term_list:\n            term = term.strip()\n            # Remove standalone wildcards\n            if term in (\"*\", \"%\"):\n                continue\n            if term := term.strip(\"*%\"):\n                cleaned.append(term)\n        return cleaned\n\n    def search_grouped(self, query: str, limit: int = 30) -&gt; tuple[dict, dict]:\n        \"\"\"Searches the music collection and returns grouped results by artists, albums, and tracks.\n        Also returns the parsed terms for highlighting.\n\n        Args:\n            query: The search query string.\n            limit: Maximum number of results to return per group.\n\n        Returns:\n            tuple[dict, dict]: A tuple of (grouped results, parsed terms).\n        \"\"\"\n        if not query.strip():\n            return {\"artists\": [], \"albums\": [], \"tracks\": []}, {}\n\n        terms = self._parse_query(query)\n        has_artist = bool(terms[\"artist\"])\n        has_album = bool(terms[\"album\"])\n        is_free_text = not has_artist and not has_album and not terms[\"track\"]\n\n        reuse_session = self._last_search_session\n        can_reuse = self._can_reuse_session(reuse_session, query, terms)\n\n        if can_reuse:\n            artist_candidates, album_candidates, track_candidates = (\n                self._collect_pass_two_candidates(\n                    reuse_session=reuse_session,\n                    terms=terms,\n                    has_artist=has_artist,\n                    has_album=has_album,\n                    is_free_text=is_free_text,\n                )\n            )\n        else:\n            artist_candidates, album_candidates, track_candidates = (\n                self._collect_pass_one_candidates(\n                    terms=terms,\n                    limit=limit,\n                    has_artist=has_artist,\n                    has_album=has_album,\n                    is_free_text=is_free_text,\n                )\n            )\n\n        grouped = self._build_hierarchical_results(\n            artist_candidates=artist_candidates,\n            album_candidates=album_candidates,\n            track_candidates=track_candidates,\n            limit=limit,\n        )\n\n        self._last_search_session = SearchSession(\n            query=query,\n            terms=terms,\n            artist_candidates=artist_candidates,\n            album_candidates=album_candidates,\n            track_candidates=track_candidates,\n            timestamp=time(),\n        )\n\n        return grouped, terms\n\n    def _collect_pass_two_candidates(\n        self,\n        reuse_session: SearchSession,\n        terms: dict,\n        has_artist: bool,\n        has_album: bool,\n        is_free_text: bool,\n    ) -&gt; tuple[dict[str, int], dict[str, dict], list[dict]]:\n        \"\"\"Refines existing search session candidates by rescoring them with new terms.\n        Avoids re-querying the database when the query is a refinement of a previous search.\n\n        Args:\n            reuse_session: The previous search session to reuse.\n            terms: The new parsed search terms.\n            has_artist: Whether the query includes explicit artist terms.\n            has_album: Whether the query includes explicit album terms.\n            is_free_text: Whether the query is purely free-text without explicit tags.\n\n        Returns:\n            tuple[dict[str, int], dict[str, dict], list[dict]]: Updated mappings of artist, album, and track candidates.\n        \"\"\"\n        artist_candidates: dict[str, int] = {}\n        album_candidates: dict[str, dict] = {}\n        track_candidates: list[dict] = []\n\n        for artist, _ in reuse_session.artist_candidates.items():\n            score = self._score_artist(artist, terms, has_artist, is_free_text)\n            if score &gt; 0:\n                artist_candidates[artist] = score\n\n        for release_dir, album in reuse_session.album_candidates.items():\n            score = self._score_album(album[\"album\"], terms, has_album, is_free_text)\n            if score &gt; 0:\n                album_candidates[release_dir] = {\n                    **album,\n                    \"score\": score,\n                }\n\n        for track in reuse_session.track_candidates:\n            score = self._score_track(track[\"track\"], terms)\n            if score &gt; 0:\n                t = dict(track)\n                t[\"score\"] = score\n                track_candidates.append(t)\n\n        return artist_candidates, album_candidates, track_candidates\n\n    def _collect_pass_one_candidates(\n        self,\n        terms: dict,\n        limit: int,\n        has_artist: bool,\n        has_album: bool,\n        is_free_text: bool,\n    ) -&gt; tuple[dict[str, int], dict[str, dict], list[dict]]:\n        \"\"\"Collects and scores artist, album, and track candidates for the first search pass.\n        Queries the database using FTS when available and converts matching rows into scored result candidates.\n\n        Args:\n            terms: Parsed search terms grouped by category.\n            limit: Base limit used to bound the number of rows fetched from the database.\n            has_artist: Whether the query includes explicit artist terms.\n            has_album: Whether the query includes explicit album terms.\n            is_free_text: Whether the query is purely free-text without explicit tags.\n\n        Returns:\n            tuple[dict[str, int], dict[str, dict], list[dict]]: Mappings of artist and album candidates,\n                and a list of track candidates, each with associated relevance scores.\n        \"\"\"\n        artist_candidates: dict[str, int] = {}\n        album_candidates: dict[str, dict] = {}\n        track_candidates: list[dict] = []\n\n        rows = self._fetch_candidate_rows(terms, limit)\n\n        for row in rows:\n            self._score_row_candidates(\n                row=row,\n                terms=terms,\n                has_artist=has_artist,\n                has_album=has_album,\n                is_free_text=is_free_text,\n                artist_candidates=artist_candidates,\n                album_candidates=album_candidates,\n                track_candidates=track_candidates,\n            )\n\n        return artist_candidates, album_candidates, track_candidates\n\n    def _fetch_candidate_rows(\n        self, terms: dict, limit: int\n    ) -&gt; list[dict[str, str | float]]:\n        \"\"\"Fetches raw candidate rows from the database for the first search pass.\n        Uses full-text search when available, falling back to ordered scans otherwise.\n\n        Args:\n            terms: Parsed search terms grouped by category.\n            limit: Base limit used to bound the number of rows fetched from the database.\n\n        Returns:\n            list[dict[str, str | float]]: A sequence of database rows containing artist, album, title, path, filename, duration, and release_dir.\n        \"\"\"\n        with self._get_conn() as conn:\n            if use_fts := self._use_fts(conn):  # noqa: F841\n                all_terms = (\n                    terms[\"artist\"] + terms[\"album\"] + terms[\"track\"] + terms[\"general\"]\n                )\n                # Build FTS query parts\n                fts_parts = []\n                for t in all_terms:\n                    escaped = self._fts_escape(t)\n                    # Only add wildcard if not a quoted phrase\n                    if escaped.startswith('\"') and escaped.endswith('\"'):\n                        fts_parts.append(escaped)\n                    else:\n                        fts_parts.append(f\"{escaped}*\")\n\n                fts_query = \" OR \".join(fts_parts) if fts_parts else \"1=0\"\n\n                sql = \"\"\"\n                    SELECT artist, album, title, path, filename, duration,\n                        SUBSTR(path, 1, LENGTH(path) - LENGTH(filename)) AS release_dir\n                    FROM tracks_fts\n                    WHERE tracks_fts MATCH ?\n                    ORDER BY rank\n                    LIMIT ?\n                \"\"\"\n                rows = conn.execute(sql, (fts_query, limit * 6)).fetchall()\n            else:\n                rows = conn.execute(\n                    \"\"\"\n                    SELECT artist, album, title, path, filename, duration,\n                        SUBSTR(path, 1, LENGTH(path) - LENGTH(filename)) AS release_dir\n                    FROM tracks\n                    ORDER BY artist COLLATE NOCASE,\n                            album COLLATE NOCASE,\n                            title COLLATE NOCASE\n                    LIMIT ?\n                    \"\"\",\n                    (limit * 6,),\n                ).fetchall()\n\n        return rows\n\n    def _score_row_candidates(\n        self,\n        row,\n        terms: dict,\n        has_artist: bool,\n        has_album: bool,\n        is_free_text: bool,\n        artist_candidates: dict[str, int],\n        album_candidates: dict[str, dict],\n        track_candidates: list[dict],\n    ) -&gt; None:\n        \"\"\"Scores a single database row and updates artist, album, and track candidate collections.\n        Computes relevance scores for each level and appends or updates candidates when they match.\n\n        Args:\n            row: A database row containing track metadata and release directory.\n            terms: Parsed search terms grouped by category.\n            has_artist: Whether the query includes explicit artist terms.\n            has_album: Whether the query includes explicit album terms.\n            is_free_text: Whether the query is purely free-text without explicit tags.\n            artist_candidates: Mapping of artist names to their current best scores.\n            album_candidates: Mapping of release directories to album metadata and scores.\n            track_candidates: List of track candidate dictionaries to be appended to.\n        \"\"\"\n        artist = row[\"artist\"] or \"Unknown Artist\"\n        album = row[\"album\"] or \"Unknown Album\"\n        title = row[\"title\"] or \"\"\n        release_dir = row[\"release_dir\"]\n\n        artist_score = self._score_artist(artist, terms, has_artist, is_free_text)\n        if artist_score &gt; 0:\n            artist_candidates[artist] = max(\n                artist_candidates.get(artist, 0), artist_score\n            )\n\n        album_score = self._score_album(album, terms, has_album, is_free_text)\n        if album_score &gt; 0:\n            album_candidates[release_dir] = {\n                \"artist\": artist,\n                \"album\": album,\n                \"release_dir\": release_dir,\n                \"score\": max(\n                    album_candidates.get(release_dir, {}).get(\"score\", 0),\n                    album_score,\n                ),\n            }\n\n        track_score = self._score_track(title, terms)\n        if track_score &gt; 0:\n            track_candidates.append(\n                {\n                    \"artist\": artist,\n                    \"album\": album,\n                    \"track\": title,\n                    \"path\": self._relative_path(row[\"path\"]),\n                    \"filename\": row[\"filename\"],\n                    \"duration\": self._format_duration(row[\"duration\"]),\n                    \"release_dir\": release_dir,\n                    \"score\": track_score,\n                }\n            )\n\n    def _is_compilation_album(self, release_dir: str) -&gt; bool:\n        \"\"\"Determines if an album is a compilation by checking the number of unique artists.\n\n        Args:\n            release_dir: The release directory to check.\n\n        Returns:\n            bool: True if the album has more than 3 unique artists, False otherwise.\n        \"\"\"\n        expected_dir = release_dir if release_dir.endswith(\"/\") else f\"{release_dir}/\"\n\n        with self._get_conn() as conn:\n            cur = conn.execute(\n                f\"\"\"\n                SELECT COUNT(DISTINCT artist) as artist_count\n                FROM tracks\n                WHERE {self._sql_release_dir_expr()} = ?\n                \"\"\",\n                (expected_dir,),\n            )\n            row = cur.fetchone()\n            artist_count = row[\"artist_count\"] if row else 0\n            return artist_count &gt; 3\n\n    def _build_hierarchical_results(\n        self,\n        artist_candidates: dict[str, int],\n        album_candidates: dict[str, dict],\n        track_candidates: list[dict],\n        limit: int,\n    ) -&gt; dict:\n        \"\"\"Builds grouped artist, album, and track results from flat candidate scores.\n        Applies hierarchical suppression so that higher-level matches hide lower-level ones from the same scope.\n\n        Args:\n            artist_candidates: Mapping of artist names to their relevance scores.\n            album_candidates: Mapping of release directories to album metadata and relevance scores.\n            track_candidates: List of track candidate dictionaries including scores and metadata.\n            limit: Maximum number of items to include per result group.\n\n        Returns:\n            dict: A dictionary with grouped 'artists', 'albums', and 'tracks' search results.\n        \"\"\"\n        artists, covered_artists = self._build_artist_results(\n            artist_candidates=artist_candidates,\n            limit=limit,\n        )\n\n        albums, covered_albums = self._build_album_results(\n            album_candidates=album_candidates,\n            covered_artists=covered_artists,\n            limit=limit,\n        )\n\n        tracks = self._build_track_results(\n            track_candidates=track_candidates,\n            covered_artists=covered_artists,\n            covered_albums=covered_albums,\n            limit=limit,\n        )\n        with self._get_conn() as conn:\n            # Add album counts for each artist result\n            for artist in artists:\n                count_query = (\n                    \"SELECT COUNT(DISTINCT album) FROM tracks WHERE artist = ?\"\n                )\n                artist[\"num_albums\"] = (\n                    conn.execute(count_query, (artist[\"artist\"],)).fetchone()[0] or 0\n                )\n\n            # Add track counts for each album result\n            for album in albums:\n                expr = (\n                    self._sql_release_dir_expr()\n                )  # Reuses your existing method for release_dir expression\n                count_query = f\"SELECT COUNT(*) FROM tracks WHERE {expr} = ?\"\n                album[\"num_tracks\"] = (\n                    conn.execute(count_query, (album[\"release_dir\"],)).fetchone()[0]\n                    or 0\n                )\n\n        return {\n            \"artists\": artists,\n            \"albums\": albums,\n            \"tracks\": tracks,\n        }\n\n    def _build_artist_results(\n        self,\n        artist_candidates: dict[str, int],\n        limit: int,\n    ) -&gt; tuple[list[dict], set[str]]:\n        \"\"\"Builds the ordered list of artist results from artist candidates.\n        Tracks which artists are covered so albums and tracks can be suppressed accordingly.\n\n        Args:\n            artist_candidates: Mapping of artist names to their relevance scores.\n            limit: Maximum number of artist items to include.\n\n        Returns:\n            tuple[list[dict], set[str]]: The artist result list and the set of covered artist names.\n        \"\"\"\n        artists: list[dict] = []\n        covered_artists: set[str] = set()\n\n        for artist, score in sorted(\n            artist_candidates.items(), key=lambda x: x[1], reverse=True\n        ):\n            if len(artists) &gt;= limit:\n                break\n            artists.append({\"artist\": artist})\n            covered_artists.add(artist)\n\n        return artists, covered_artists\n\n    def _build_album_results(\n        self,\n        album_candidates: dict[str, dict],\n        covered_artists: set[str],\n        limit: int,\n    ) -&gt; tuple[list[dict], set[str]]:\n        \"\"\"Builds the ordered list of album results from album candidates.\n        Skips albums whose artist is already covered by an artist result and tracks covered release directories.\n\n        Args:\n            album_candidates: Mapping of release directories to album metadata and relevance scores.\n            covered_artists: Set of artist names already represented in artist results.\n            limit: Maximum number of album items to include.\n\n        Returns:\n            tuple[list[dict], set[str]]: The album result list and the set of covered release directory identifiers.\n        \"\"\"\n        albums: list[dict] = []\n        covered_albums: set[str] = set()\n\n        for album in sorted(\n            album_candidates.values(), key=lambda x: x[\"score\"], reverse=True\n        ):\n            if len(albums) &gt;= limit:\n                break\n            if album[\"artist\"] in covered_artists:\n                continue\n\n            # Check if this is a compilation album\n            is_compilation = self._is_compilation_album(album[\"release_dir\"])\n            display_artist = \"Various Artists\" if is_compilation else album[\"artist\"]\n\n            albums.append(\n                {\n                    \"artist\": album[\"artist\"],\n                    \"display_artist\": display_artist,\n                    \"album\": album[\"album\"],\n                    \"release_dir\": album[\"release_dir\"],\n                    \"is_compilation\": is_compilation,\n                    \"cover\": self.get_cover(album[\"release_dir\"])\n                }\n            )\n            covered_albums.add(album[\"release_dir\"])\n\n        return albums, covered_albums\n\n    def _build_track_results(\n        self,\n        track_candidates: list[dict],\n        covered_artists: set[str],\n        covered_albums: set[str],\n        limit: int,\n    ) -&gt; list[dict]:\n        \"\"\"Builds the ordered list of track results from track candidates.\n        Skips tracks whose artist or release directory is already covered by higher-level results.\n\n        Args:\n            track_candidates: List of track candidate dictionaries including scores and metadata.\n            covered_artists: Set of artist names already represented in artist results.\n            covered_albums: Set of release directory identifiers already represented in album results.\n            limit: Maximum number of track items to include.\n\n        Returns:\n            list[dict]: The final list of track result dictionaries.\n        \"\"\"\n        tracks: list[dict] = []\n\n        for track in sorted(track_candidates, key=lambda x: x[\"score\"], reverse=True):\n            if len(tracks) &gt;= limit:\n                break\n            if track[\"artist\"] in covered_artists:\n                continue\n            if track[\"release_dir\"] in covered_albums:\n                continue\n\n            tracks.append(track)\n\n        return tracks\n\n    def _terms_compatible(self, old: dict, new: dict) -&gt; bool:\n        \"\"\"Checks whether a new set of parsed search terms is compatible with a previous set.\n        Ensures the new terms only refine the old terms without introducing independent new prefixes.\n\n        Args:\n            old: The previous set of parsed terms grouped by category.\n            new: The new set of parsed terms to compare against the previous set.\n\n        Returns:\n            bool: True if the new terms are compatible refinements of the old terms, False otherwise.\n        \"\"\"\n        for key in (\"artist\", \"album\", \"track\", \"general\"):\n            old_terms = old.get(key, [])\n            new_terms = new.get(key, [])\n\n            # cannot add new independent terms\n            if len(new_terms) &lt; len(old_terms):\n                return False\n\n            for i, old_term in enumerate(old_terms):\n                if not new_terms[i].startswith(old_term):\n                    return False\n\n        return True\n\n    def _can_reuse_session(\n        self, session: SearchSession, query: str, terms: dict\n    ) -&gt; bool:\n        \"\"\"Determines whether a previous search session can be reused for the current query.\n        Checks that the new query extends the previous one and that the term structure remains compatible.\n\n        Args:\n            session: The previous search session to compare against.\n            query: The new search query string.\n            terms: The parsed terms for the new query.\n\n        Returns:\n            bool: True if the previous session can be reused, False otherwise.\n        \"\"\"\n        if not session:\n            return False\n\n        if not query.startswith(session.query):\n            return False\n\n        # Same tag structure\n        for key in (\"artist\", \"album\", \"track\"):\n            if bool(session.terms[key]) != bool(terms[key]):\n                return False\n\n        return self._terms_compatible(session.terms, terms)\n\n    def _score_text(self, text: str, terms: list[str]) -&gt; int:\n        # sourcery skip: assign-if-exp, reintroduce-else\n        \"\"\"Calculates a relevance score for text based on a list of search terms.\n        Prioritizes exact, prefix, and substring matches to influence search ranking.\n\n        Args:\n            text: The text to evaluate against the search terms.\n            terms: A list of search terms to compare with the text.\n\n        Returns:\n            int: The highest relevance score assigned based on the best matching term.\n        \"\"\"\n        if not text or not terms:\n            return 0\n\n        text_l = text.lower()\n        best = 0\n        matched_terms = 0\n\n        for term in terms:\n            term_l = term.lower()\n            term_matched = False\n\n            if text_l == term_l:\n                best = max(best, self.EXACT_MATCH_SCORE)\n                term_matched = True\n            elif text_l.startswith(term_l):\n                best = max(best, self.PREFIX_MATCH_SCORE)\n                term_matched = True\n            elif term_l in text_l:\n                best = max(best, self.SUBSTRING_MATCH_SCORE)\n                term_matched = True\n\n            if term_matched:\n                matched_terms += 1\n\n        # For multi-word queries, ALL terms must match\n        if len(terms) &gt; 1 and matched_terms &lt; len(terms):\n            return 0\n\n        return best\n\n    def _score_artist(\n        self, artist: str, terms: dict, has_artist: bool, is_free_text: bool\n    ) -&gt; int:\n        \"\"\"Computes the score for an artist based on terms.\n\n        Args:\n            artist: The artist name to score.\n            terms: Parsed search terms.\n            has_artist: Whether explicit artist terms are present.\n            is_free_text: Whether the query is free-text.\n\n        Returns:\n            int: The computed score.\n        \"\"\"\n        score = 0\n        if has_artist:\n            score = self._score_text(artist, terms[\"artist\"]) + self._tag_bonus(True)\n        elif is_free_text:\n            score = self._score_text(artist, terms[\"general\"])\n        return score\n\n    def _score_album(\n        self, album_name: str, terms: dict, has_album: bool, is_free_text: bool\n    ) -&gt; int:\n        \"\"\"Computes the score for an album based on terms.\n\n        Args:\n            album_name: The album name to score.\n            terms: Parsed search terms.\n            has_album: Whether explicit album terms are present.\n            is_free_text: Whether the query is free-text.\n\n        Returns:\n            int: The computed score.\n        \"\"\"\n        score = 0\n        if has_album:\n            score = self._score_text(album_name, terms[\"album\"]) + self._tag_bonus(True)\n        elif is_free_text:\n            score = self._score_text(album_name, terms[\"general\"])\n        return score\n\n    def _score_track(self, track_name: str, terms: dict) -&gt; int:\n        \"\"\"Computes the score for a track based on terms.\n\n        Args:\n            track_name: The track name to score.\n            terms: Parsed search terms.\n\n        Returns:\n            int: The computed score.\n        \"\"\"\n        return self._score_text(track_name, terms[\"track\"] + terms[\"general\"])\n\n    def _tag_bonus(self, has_tag: bool) -&gt; int:\n        \"\"\"Calculates a score bonus when a search term is explicitly tagged.\n        Helps prioritize results that match user-specified tags over general matches.\n\n        Args:\n            has_tag: Whether the term was provided with an explicit tag (e.g., artist:, album:).\n\n        Returns:\n            int: The bonus score to be added when a tag is present.\n        \"\"\"\n        return self.TAG_BONUS if has_tag else 0\n\n    def get_artist_details(self, artist: str) -&gt; dict:\n        \"\"\"Retrieves detailed information about an artist, including their albums and tracks.\n        Returns a dictionary with the artist name and a list of albums containing track details.\n\n        Args:\n            artist: The name of the artist to retrieve details for.\n\n        Returns:\n            dict: Dictionary containing the artist name and a list of albums with track information.\n        \"\"\"\n        with self._get_conn() as conn:\n            cur = conn.execute(\n                \"\"\"\n                SELECT album, title, path, filename, duration, disc_number, track_number\n                FROM tracks\n                WHERE artist = ?\n                ORDER BY album COLLATE NOCASE, disc_number, track_number, title COLLATE NOCASE\n                \"\"\",\n                (artist,),\n            )\n            releases_map = defaultdict(list)\n            for row in cur:\n                release_dir = self._get_release_dir(row[\"path\"])\n                releases_map[release_dir].append(\n                    self._build_track_dict(row, artist=artist, album=row[\"album\"])\n                )\n\n            albums = []\n            for release_dir, tracks in sorted(\n                releases_map.items(),\n                key=lambda x: x[1][0].get(\"album\", \"\") if x[1] else \"\",\n            ):\n                album_name = (\n                    tracks[0].get(\"album\", \"Unknown Album\")\n                    if tracks\n                    else \"Unknown Album\"\n                )\n                album_cover = self.get_cover(release_dir)\n\n                # Check if this is a compilation album\n                is_compilation = self._is_compilation_album(release_dir)\n\n                albums.append(\n                    {\n                        \"album\": album_name,\n                        \"cover\": album_cover,\n                        \"tracks\": tracks,\n                        \"release_dir\": release_dir,\n                        \"is_compilation\": is_compilation,\n                    }\n                )\n\n            return {\"artist\": artist, \"albums\": albums}\n\n    def get_album_details(self, release_dir: str) -&gt; dict:\n        \"\"\"Retrieves detailed information about an album given its release directory.\n        Returns a dictionary with album details, including artist, tracks, compilation status, and release directory.\n\n        Args:\n            release_dir: The release directory relative to the music root.\n\n        Returns:\n            dict: Dictionary containing album details, track list, and compilation status.\n        \"\"\"\n        # Construct the expected directory pattern with trailing slash\n        expected_dir = release_dir if release_dir.endswith(\"/\") else f\"{release_dir}/\"\n\n        with self._get_conn() as conn:\n            cur = conn.execute(\n                f\"\"\"\n                SELECT artist, title, path, filename, duration, album\n                FROM tracks\n                WHERE {self._sql_release_dir_expr()} = ?\n                ORDER BY disc_number, track_number, title COLLATE NOCASE\n                \"\"\",\n                (expected_dir,),\n            )\n            rows = cur.fetchall()\n\n            if not rows:\n                return {\n                    \"artist\": \"\",\n                    \"album\": \"\",\n                    \"tracks\": [],\n                    \"is_compilation\": False,\n                    \"release_dir\": release_dir,\n                }\n\n            # Album name from first row\n            album_name = rows[0][\"album\"] or \"Unknown Album\"\n\n            # Build track list\n            track_list = [self._build_track_dict(row) for row in rows]\n\n            # Detect compilation\n            artists = {t[\"artist\"] for t in track_list if t[\"artist\"]}\n            is_compilation = len(artists) &gt; 3\n            display_artist = (\n                \"Various Artists\" if is_compilation else next(iter(artists))\n            )\n\n            return {\n                \"artist\": display_artist,\n                \"album\": album_name,\n                \"tracks\": track_list,\n                \"is_compilation\": is_compilation,\n                \"release_dir\": release_dir,\n            }\n\n    def _get_release_dir(self, path: str) -&gt; str:\n        \"\"\"Computes the release directory for a given track path relative to the music root.\n        Returns the parent directory path, typically representing 'artist/album'.\n\n        Args:\n            path: The path to the track file.\n\n        Returns:\n            str: The release directory relative to the music root.\n        \"\"\"\n        full_path = Path(path)\n        relative_path = (\n            full_path.relative_to(self.music_root)\n            if full_path.is_absolute()\n            else full_path\n        )\n        return f\"{str(relative_path.parent)}/\"\n\n    def _sql_release_dir_expr(self) -&gt; str:\n        \"\"\"Returns the SQL expression to extract the release directory from a track's path.\n        Used to match and group tracks by their release directory in database queries.\n\n        Returns:\n            str: SQL expression for extracting the release directory from the path.\n        \"\"\"\n        return \"SUBSTR(path, 1, LENGTH(path) - LENGTH(filename))\"  # e.g., 'artist/album/' (with trailing /)\n\n    def get_track(self, path: Path) -&gt; dict | None:\n        \"\"\"Retrieves metadata for a single track identified by its path.\n\n        Queries the music library database for the track and returns a normalized metadata dictionary if found.\n\n        Args:\n            path: The full or relative path of the track to look up.\n\n        Returns:\n            dict | None: A dictionary of track metadata if the track exists, otherwise None.\n        \"\"\"\n        with self._get_conn() as conn:\n            cur = conn.execute(\n                \"\"\"\n                SELECT path, filename, artist, album, title, albumartist, track_number, disc_number, duration, mtime\n                FROM tracks\n                WHERE path = ?\n                \"\"\",\n                (str(path),),\n            )\n            if rows := cur.fetchall():\n                track = rows[0]\n                # Get the release directory to fetch the cover\n                release_dir = self._get_release_dir(track[\"path\"])\n\n                return {\n                    \"path\": Path(path),\n                    \"filename\": track[\"filename\"],\n                    \"artist\": track[\"artist\"],\n                    \"album\": track[\"album\"],\n                    \"track\": track[\"title\"],\n                    \"albumartist\": track[\"albumartist\"],\n                    \"track_number\": track[\"track_number\"],\n                    \"disc_number\": track[\"disc_number\"],\n                    \"duration\": self._format_duration(track[\"duration\"]),\n                    \"time_added\": track[\"mtime\"],\n                    \"cover\": self.get_cover(release_dir),\n                }\n            else:\n                return None\n\n\n    def get_cover(self, release_dir: str) -&gt; str | None:\n        \"\"\"Retrieves or generates the cover image path for a given album release directory.\n        Returns a relative path to a cached or newly extracted cover image if available,\n        or a fallback image path if no cover is found.\n\n        Args:\n            release_dir: The release directory identifier used to locate or derive the cover image.\n\n        Returns:\n            str | None: Relative path to the cover image within the covers directory, or fallback image path if not found.\n        \"\"\"\n        if not release_dir:\n            return \"covers/_fallback.jpg\"\n\n        # Sanitize to a safe filename slug\n        slug = self._sanitize_release_dir(release_dir)\n        cover_path = self.covers_dir / f\"{slug}.jpg\"\n\n        if cover_path.exists():\n            return f\"covers/{slug}.jpg\"\n\n        # Extract on demand\n        if self._extract_cover(release_dir, cover_path):\n            return f\"covers/{slug}.jpg\"\n\n        return \"covers/_fallback.jpg\"\n\n    def _setup_fallback_cover(self) -&gt; None:\n        \"\"\"Ensures the fallback cover image exists in the covers directory.\n        Copies the fallback image from static directory if it doesn't already exist.\n\n        Returns:\n            None\n        \"\"\"\n        fallback_path = self.covers_dir / \"_fallback.jpg\"\n\n        # If fallback already exists, we're done\n        if fallback_path.exists():\n            return\n\n        if (\n            source_path := Path(__file__).parent.parent\n            / \"static\"\n            / \"cover-art-text.jpg\"\n        ):\n            try:\n                # Copy the fallback image to covers directory\n                import shutil\n                shutil.copy2(source_path, fallback_path)\n                self._logger.debug(f\"Copied fallback cover from {source_path} to {fallback_path}\")\n            except Exception as e:\n                self._logger.warning(f\"Failed to copy fallback cover: {e}\")\n        else:\n            self._logger.warning(\n                \"Fallback cover image 'cover-art-text.jpg' not found in static directory. \"\n                \"Tracks without covers will not display properly.\"\n            )\n\n    def _sanitize_release_dir(self, release_dir: str) -&gt; str:\n        \"\"\"Normalizes a release directory string into a safe filename slug.\n        Produces a stable, filesystem-friendly identifier suitable for use in cover cache filenames.\n\n        Args:\n            release_dir: The release directory identifier to sanitize.\n\n        Returns:\n            str: A normalized slug representation of the release directory.\n        \"\"\"\n        clean = (\n            \"\".join(c if c.isalnum() or c in \"-_ /\" else \"_\" for c in release_dir)\n            .strip(\"/\")\n            .replace(\"/\", \"_\")\n        )\n        if len(clean) &gt; 200:  # Truncate long paths\n            clean = f\"{clean[:100]}_{hashlib.md5(release_dir.encode()).hexdigest()[:8]}\"\n        return clean\n\n    def _extract_cover(self, release_dir: str, target_path: Path) -&gt; bool:\n        \"\"\"Attempts to locate or extract a cover image for a given release directory.\n        Searches for existing image files or embedded artwork, resizes them to a reasonable size,\n        and writes an optimized copy to the target path.\n\n        Args:\n            release_dir: The release directory relative to the music root where audio and image files are stored.\n            target_path: The filesystem path where the discovered or extracted cover image should be written.\n\n        Returns:\n            bool: True if a cover image was successfully found and written, otherwise False.\n        \"\"\"\n        abs_dir = self.music_root / release_dir.rstrip(\"/\")\n        if not abs_dir.is_dir():\n            self._logger.warning(f\"Release directory not found: {abs_dir}\")\n            return False\n\n        # Configuration for cover optimization\n        MAX_SIZE = 800  # Maximum width/height in pixels\n        JPEG_QUALITY = 85  # JPEG quality (1-100)\n        MAX_FILE_SIZE = 500 * 1024  # 500KB max file size\n\n        # Step 1: Check for common image files in the folder\n        common_cover_names = [\n            \"cover.jpg\",\n            \"folder.jpg\",\n            \"album.jpg\",\n            \"front.jpg\",\n            \"cover.png\",\n            \"folder.png\",\n        ]\n\n        for name in common_cover_names:\n            img_path = abs_dir / name\n            if img_path.exists():\n                try:\n                    if self._resize_and_save_cover(img_path, target_path, MAX_SIZE, JPEG_QUALITY, MAX_FILE_SIZE):\n                        self._logger.debug(f\"Processed cover from {img_path} for {release_dir}\")\n                        return True\n                except Exception as e:\n                    self._logger.error(f\"Failed to process cover {img_path}: {e}\")\n                    continue\n\n        # Step 2: Extract embedded art from audio files\n        for file in abs_dir.iterdir():\n            if file.suffix.lower() in CollectionExtractor.SUPPORTED_EXTS:\n                try:\n                    tag = TinyTag.get(str(file), image=True)\n                    if img_data := tag.get_image():\n                        if self._resize_and_save_cover_from_bytes(img_data, target_path, MAX_SIZE, JPEG_QUALITY, MAX_FILE_SIZE):\n                            self._logger.debug(\n                                f\"Extracted and processed embedded cover from {file} for {release_dir}\"\n                            )\n                            return True\n                except Exception as e:\n                    self._logger.warning(f\"Failed to extract from {file}: {e}\")\n                    continue\n\n        self._logger.info(f\"No cover found for {release_dir}\")\n        return False\n\n    def _resize_and_save_cover(\n        self,\n        source_path: Path,\n        target_path: Path,\n        max_size: int,\n        quality: int,\n        max_file_size: int\n    ) -&gt; bool:\n        \"\"\"Resize and optimize a cover image from a file path.\n\n        Args:\n            source_path: Path to the source image file.\n            target_path: Path where the optimized image should be saved.\n            max_size: Maximum dimension (width or height) in pixels.\n            quality: JPEG quality (1-100).\n            max_file_size: Maximum file size in bytes.\n\n        Returns:\n            bool: True if successful, False otherwise.\n        \"\"\"\n        try:\n            with Image.open(source_path) as img:\n                return self._process_and_save_image(img, target_path, max_size, quality, max_file_size)\n        except Exception as e:\n            self._logger.error(f\"Failed to resize cover from {source_path}: {e}\")\n            return False\n\n    def _resize_and_save_cover_from_bytes(\n        self,\n        img_data: bytes,\n        target_path: Path,\n        max_size: int,\n        quality: int,\n        max_file_size: int\n    ) -&gt; bool:\n        \"\"\"Resize and optimize a cover image from raw bytes.\n\n        Args:\n            img_data: Raw image data bytes.\n            target_path: Path where the optimized image should be saved.\n            max_size: Maximum dimension (width or height) in pixels.\n            quality: JPEG quality (1-100).\n            max_file_size: Maximum file size in bytes.\n\n        Returns:\n            bool: True if successful, False otherwise.\n        \"\"\"\n        try:\n            with Image.open(io.BytesIO(img_data)) as img:\n                return self._process_and_save_image(img, target_path, max_size, quality, max_file_size)\n        except Exception as e:\n            self._logger.error(f\"Failed to resize cover from bytes: {e}\")\n            return False\n\n    def _process_and_save_image(\n        self,\n        img: Image.Image,\n        target_path: Path,\n        max_size: int,\n        quality: int,\n        max_file_size: int\n    ) -&gt; bool:\n        \"\"\"Process and save an image with resizing and optimization.\n\n        Args:\n            img: PIL Image object to process.\n            target_path: Path where the optimized image should be saved.\n            max_size: Maximum dimension (width or height) in pixels.\n            quality: JPEG quality (1-100).\n            max_file_size: Maximum file size in bytes.\n\n        Returns:\n            bool: True if successful, False otherwise.\n        \"\"\"\n        try:\n            # Convert to RGB if necessary (handles RGBA, P mode, etc.)\n            if img.mode not in ('RGB', 'L'):\n                # Handle transparency by compositing on white background\n                if img.mode == 'RGBA':\n                    background = Image.new('RGB', img.size, (255, 255, 255))\n                    background.paste(img, mask=img.split()[3])  # Use alpha channel as mask\n                    img = background\n                else:\n                    img = img.convert('RGB')\n\n            # Resize if needed\n            original_size = img.size\n            if max(img.size) &gt; max_size:\n                img.thumbnail((max_size, max_size), Image.Resampling.LANCZOS)\n                self._logger.debug(f\"Resized cover from {original_size} to {img.size}\")\n\n            # Save with optimization\n            # Try with the specified quality first\n            img.save(target_path, 'JPEG', quality=quality, optimize=True)\n\n            # If still too large, reduce quality iteratively\n            current_quality = quality\n            while target_path.stat().st_size &gt; max_file_size and current_quality &gt; 50:\n                current_quality -= 10\n                img.save(target_path, 'JPEG', quality=current_quality, optimize=True)\n                self._logger.debug(f\"Reduced quality to {current_quality} to meet size limit\")\n\n            final_size = target_path.stat().st_size\n            self._logger.debug(\n                f\"Saved cover: {img.size[0]}x{img.size[1]}, \"\n                f\"{final_size / 1024:.1f}KB, quality={current_quality}\"\n            )\n\n            return True\n        except Exception as e:\n            self._logger.error(f\"Failed to process and save image: {e}\")\n            return False\n\n    def get_collection_stats(self) -&gt; dict:\n        \"\"\"Returns high-level statistics about the music collection.\n\n        This method queries the tracks table to compute aggregate counts of\n        distinct artists, distinct artist/album combinations, total tracks, and\n        the timestamp of the most recently added track.\n\n        Returns:\n            dict: A dictionary containing ``qty_tracks``, ``qty_artists``,\n            ``qty_albums``, and ``last_added`` (or None when no tracks exist).\n        \"\"\"\n        with self._get_conn() as conn:\n            cur = conn.execute(\n                \"\"\"\n                SELECT COUNT(DISTINCT artist) as qty_artists,\n                    COUNT(DISTINCT artist || album) as qty_albums,\n                    COUNT(*) AS qty_tracks,\n                    SUM(duration) AS total_duration,\n                    MAX(mtime) AS time_lastadded\n                FROM tracks\n                \"\"\"\n            )\n            if rows := cur.fetchall():\n                return {\n                        \"num_tracks\": rows[0][\"qty_tracks\"],\n                        \"num_artists\": rows[0][\"qty_artists\"],\n                        \"num_albums\": rows[0][\"qty_albums\"],\n                        \"total_duration\": rows[0][\"total_duration\"],\n                        \"last_added\": rows[0][\"time_lastadded\"],\n                    }\n            else:\n                return {\n                    \"num_tracks\": 0,\n                    \"num_artists\": 0,\n                    \"num_albums\": 0,\n                    \"total_duration\": 0,\n                    \"last_added\": None,\n                }\n</code></pre>"},{"location":"development/musiclib/reading.html#src.musiclib.reader.MusicCollection.__init__","title":"<code>__init__(music_root, db_path, logger=None)</code>","text":"<p>Initializes a MusicCollection backed by a SQLite database and music root directory. Sets up logging, collection extraction, and schedules any required initial indexing or resync operations.</p> <p>Parameters:</p> Name Type Description Default <code>music_root</code> <code>Path | str</code> <p>The root directory containing the music files to be indexed.</p> required <code>db_path</code> <code>Path | str</code> <p>The path to the SQLite database file used to store collection metadata.</p> required <code>logger</code> <code>Logger</code> <p>Optional logger instance for recording informational and error messages.</p> <code>None</code> Source code in <code>src/musiclib/reader.py</code> <pre><code>def __init__(\n    self, music_root: Path | str, db_path: Path | str, logger: Logger = None\n) -&gt; None:\n    \"\"\"Initializes a MusicCollection backed by a SQLite database and music root directory.\n    Sets up logging, collection extraction, and schedules any required initial indexing or resync operations.\n\n    Args:\n        music_root: The root directory containing the music files to be indexed.\n        db_path: The path to the SQLite database file used to store collection metadata.\n        logger: Optional logger instance for recording informational and error messages.\n    \"\"\"\n    self.music_root = Path(music_root).resolve()\n    self.db_path = Path(db_path)\n    self._logger = logger or NullLogger()\n    self._extractor = CollectionExtractor(self.music_root, self.db_path, logger=logger)\n\n    track_count = self.count()\n    self._startup_mode = \"rebuild\" if track_count == 0 else \"resync\"\n    self._logger.info(\n        \"No tracks in DB \u2014 scheduling initial rebuild\"\n        if track_count == 0\n        else \"Start resync of DB\"\n    )\n\n    self._background_task_running = False\n    self._extractor.start_monitoring()\n    self._start_background_startup_job()\n    if track_count == 0:\n        self._extractor.wait_for_indexing_start()\n\n    self._last_search_session: SearchSession | None = None\n\n    self.covers_dir = self.db_path.parent / \"cache\" / \"covers\"\n    self.covers_dir.mkdir(parents=True, exist_ok=True)\n    self._setup_fallback_cover()\n</code></pre>"},{"location":"development/musiclib/reading.html#src.musiclib.reader.MusicCollection.close","title":"<code>close()</code>","text":"<p>Stops monitoring and closes resources associated with the music collection. Cleans up background tasks and releases any held resources.</p> <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>src/musiclib/reader.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Stops monitoring and closes resources associated with the music collection.\n    Cleans up background tasks and releases any held resources.\n\n    Returns:\n        None\n    \"\"\"\n    self._extractor.stop()\n</code></pre>"},{"location":"development/musiclib/reading.html#src.musiclib.reader.MusicCollection.count","title":"<code>count()</code>","text":"<p>Returns the total number of tracks in the music collection database. Executes a query to count all tracks currently indexed.</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>The number of tracks in the database.</p> Source code in <code>src/musiclib/reader.py</code> <pre><code>def count(self) -&gt; int:\n    \"\"\"Returns the total number of tracks in the music collection database.\n    Executes a query to count all tracks currently indexed.\n\n    Returns:\n        int: The number of tracks in the database.\n    \"\"\"\n    with self._get_conn() as conn:\n        return conn.execute(\"SELECT COUNT(*) FROM tracks\").fetchone()[0]\n</code></pre>"},{"location":"development/musiclib/reading.html#src.musiclib.reader.MusicCollection.get_album_details","title":"<code>get_album_details(release_dir)</code>","text":"<p>Retrieves detailed information about an album given its release directory. Returns a dictionary with album details, including artist, tracks, compilation status, and release directory.</p> <p>Parameters:</p> Name Type Description Default <code>release_dir</code> <code>str</code> <p>The release directory relative to the music root.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>Dictionary containing album details, track list, and compilation status.</p> Source code in <code>src/musiclib/reader.py</code> <pre><code>def get_album_details(self, release_dir: str) -&gt; dict:\n    \"\"\"Retrieves detailed information about an album given its release directory.\n    Returns a dictionary with album details, including artist, tracks, compilation status, and release directory.\n\n    Args:\n        release_dir: The release directory relative to the music root.\n\n    Returns:\n        dict: Dictionary containing album details, track list, and compilation status.\n    \"\"\"\n    # Construct the expected directory pattern with trailing slash\n    expected_dir = release_dir if release_dir.endswith(\"/\") else f\"{release_dir}/\"\n\n    with self._get_conn() as conn:\n        cur = conn.execute(\n            f\"\"\"\n            SELECT artist, title, path, filename, duration, album\n            FROM tracks\n            WHERE {self._sql_release_dir_expr()} = ?\n            ORDER BY disc_number, track_number, title COLLATE NOCASE\n            \"\"\",\n            (expected_dir,),\n        )\n        rows = cur.fetchall()\n\n        if not rows:\n            return {\n                \"artist\": \"\",\n                \"album\": \"\",\n                \"tracks\": [],\n                \"is_compilation\": False,\n                \"release_dir\": release_dir,\n            }\n\n        # Album name from first row\n        album_name = rows[0][\"album\"] or \"Unknown Album\"\n\n        # Build track list\n        track_list = [self._build_track_dict(row) for row in rows]\n\n        # Detect compilation\n        artists = {t[\"artist\"] for t in track_list if t[\"artist\"]}\n        is_compilation = len(artists) &gt; 3\n        display_artist = (\n            \"Various Artists\" if is_compilation else next(iter(artists))\n        )\n\n        return {\n            \"artist\": display_artist,\n            \"album\": album_name,\n            \"tracks\": track_list,\n            \"is_compilation\": is_compilation,\n            \"release_dir\": release_dir,\n        }\n</code></pre>"},{"location":"development/musiclib/reading.html#src.musiclib.reader.MusicCollection.get_artist_details","title":"<code>get_artist_details(artist)</code>","text":"<p>Retrieves detailed information about an artist, including their albums and tracks. Returns a dictionary with the artist name and a list of albums containing track details.</p> <p>Parameters:</p> Name Type Description Default <code>artist</code> <code>str</code> <p>The name of the artist to retrieve details for.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>Dictionary containing the artist name and a list of albums with track information.</p> Source code in <code>src/musiclib/reader.py</code> <pre><code>def get_artist_details(self, artist: str) -&gt; dict:\n    \"\"\"Retrieves detailed information about an artist, including their albums and tracks.\n    Returns a dictionary with the artist name and a list of albums containing track details.\n\n    Args:\n        artist: The name of the artist to retrieve details for.\n\n    Returns:\n        dict: Dictionary containing the artist name and a list of albums with track information.\n    \"\"\"\n    with self._get_conn() as conn:\n        cur = conn.execute(\n            \"\"\"\n            SELECT album, title, path, filename, duration, disc_number, track_number\n            FROM tracks\n            WHERE artist = ?\n            ORDER BY album COLLATE NOCASE, disc_number, track_number, title COLLATE NOCASE\n            \"\"\",\n            (artist,),\n        )\n        releases_map = defaultdict(list)\n        for row in cur:\n            release_dir = self._get_release_dir(row[\"path\"])\n            releases_map[release_dir].append(\n                self._build_track_dict(row, artist=artist, album=row[\"album\"])\n            )\n\n        albums = []\n        for release_dir, tracks in sorted(\n            releases_map.items(),\n            key=lambda x: x[1][0].get(\"album\", \"\") if x[1] else \"\",\n        ):\n            album_name = (\n                tracks[0].get(\"album\", \"Unknown Album\")\n                if tracks\n                else \"Unknown Album\"\n            )\n            album_cover = self.get_cover(release_dir)\n\n            # Check if this is a compilation album\n            is_compilation = self._is_compilation_album(release_dir)\n\n            albums.append(\n                {\n                    \"album\": album_name,\n                    \"cover\": album_cover,\n                    \"tracks\": tracks,\n                    \"release_dir\": release_dir,\n                    \"is_compilation\": is_compilation,\n                }\n            )\n\n        return {\"artist\": artist, \"albums\": albums}\n</code></pre>"},{"location":"development/musiclib/reading.html#src.musiclib.reader.MusicCollection.get_collection_stats","title":"<code>get_collection_stats()</code>","text":"<p>Returns high-level statistics about the music collection.</p> <p>This method queries the tracks table to compute aggregate counts of distinct artists, distinct artist/album combinations, total tracks, and the timestamp of the most recently added track.</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>A dictionary containing <code>qty_tracks</code>, <code>qty_artists</code>,</p> <code>dict</code> <p><code>qty_albums</code>, and <code>last_added</code> (or None when no tracks exist).</p> Source code in <code>src/musiclib/reader.py</code> <pre><code>def get_collection_stats(self) -&gt; dict:\n    \"\"\"Returns high-level statistics about the music collection.\n\n    This method queries the tracks table to compute aggregate counts of\n    distinct artists, distinct artist/album combinations, total tracks, and\n    the timestamp of the most recently added track.\n\n    Returns:\n        dict: A dictionary containing ``qty_tracks``, ``qty_artists``,\n        ``qty_albums``, and ``last_added`` (or None when no tracks exist).\n    \"\"\"\n    with self._get_conn() as conn:\n        cur = conn.execute(\n            \"\"\"\n            SELECT COUNT(DISTINCT artist) as qty_artists,\n                COUNT(DISTINCT artist || album) as qty_albums,\n                COUNT(*) AS qty_tracks,\n                SUM(duration) AS total_duration,\n                MAX(mtime) AS time_lastadded\n            FROM tracks\n            \"\"\"\n        )\n        if rows := cur.fetchall():\n            return {\n                    \"num_tracks\": rows[0][\"qty_tracks\"],\n                    \"num_artists\": rows[0][\"qty_artists\"],\n                    \"num_albums\": rows[0][\"qty_albums\"],\n                    \"total_duration\": rows[0][\"total_duration\"],\n                    \"last_added\": rows[0][\"time_lastadded\"],\n                }\n        else:\n            return {\n                \"num_tracks\": 0,\n                \"num_artists\": 0,\n                \"num_albums\": 0,\n                \"total_duration\": 0,\n                \"last_added\": None,\n            }\n</code></pre>"},{"location":"development/musiclib/reading.html#src.musiclib.reader.MusicCollection.get_cover","title":"<code>get_cover(release_dir)</code>","text":"<p>Retrieves or generates the cover image path for a given album release directory. Returns a relative path to a cached or newly extracted cover image if available, or a fallback image path if no cover is found.</p> <p>Parameters:</p> Name Type Description Default <code>release_dir</code> <code>str</code> <p>The release directory identifier used to locate or derive the cover image.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>str | None: Relative path to the cover image within the covers directory, or fallback image path if not found.</p> Source code in <code>src/musiclib/reader.py</code> <pre><code>def get_cover(self, release_dir: str) -&gt; str | None:\n    \"\"\"Retrieves or generates the cover image path for a given album release directory.\n    Returns a relative path to a cached or newly extracted cover image if available,\n    or a fallback image path if no cover is found.\n\n    Args:\n        release_dir: The release directory identifier used to locate or derive the cover image.\n\n    Returns:\n        str | None: Relative path to the cover image within the covers directory, or fallback image path if not found.\n    \"\"\"\n    if not release_dir:\n        return \"covers/_fallback.jpg\"\n\n    # Sanitize to a safe filename slug\n    slug = self._sanitize_release_dir(release_dir)\n    cover_path = self.covers_dir / f\"{slug}.jpg\"\n\n    if cover_path.exists():\n        return f\"covers/{slug}.jpg\"\n\n    # Extract on demand\n    if self._extract_cover(release_dir, cover_path):\n        return f\"covers/{slug}.jpg\"\n\n    return \"covers/_fallback.jpg\"\n</code></pre>"},{"location":"development/musiclib/reading.html#src.musiclib.reader.MusicCollection.get_track","title":"<code>get_track(path)</code>","text":"<p>Retrieves metadata for a single track identified by its path.</p> <p>Queries the music library database for the track and returns a normalized metadata dictionary if found.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>Path</code> <p>The full or relative path of the track to look up.</p> required <p>Returns:</p> Type Description <code>dict | None</code> <p>dict | None: A dictionary of track metadata if the track exists, otherwise None.</p> Source code in <code>src/musiclib/reader.py</code> <pre><code>def get_track(self, path: Path) -&gt; dict | None:\n    \"\"\"Retrieves metadata for a single track identified by its path.\n\n    Queries the music library database for the track and returns a normalized metadata dictionary if found.\n\n    Args:\n        path: The full or relative path of the track to look up.\n\n    Returns:\n        dict | None: A dictionary of track metadata if the track exists, otherwise None.\n    \"\"\"\n    with self._get_conn() as conn:\n        cur = conn.execute(\n            \"\"\"\n            SELECT path, filename, artist, album, title, albumartist, track_number, disc_number, duration, mtime\n            FROM tracks\n            WHERE path = ?\n            \"\"\",\n            (str(path),),\n        )\n        if rows := cur.fetchall():\n            track = rows[0]\n            # Get the release directory to fetch the cover\n            release_dir = self._get_release_dir(track[\"path\"])\n\n            return {\n                \"path\": Path(path),\n                \"filename\": track[\"filename\"],\n                \"artist\": track[\"artist\"],\n                \"album\": track[\"album\"],\n                \"track\": track[\"title\"],\n                \"albumartist\": track[\"albumartist\"],\n                \"track_number\": track[\"track_number\"],\n                \"disc_number\": track[\"disc_number\"],\n                \"duration\": self._format_duration(track[\"duration\"]),\n                \"time_added\": track[\"mtime\"],\n                \"cover\": self.get_cover(release_dir),\n            }\n        else:\n            return None\n</code></pre>"},{"location":"development/musiclib/reading.html#src.musiclib.reader.MusicCollection.rebuild","title":"<code>rebuild()</code>","text":"<p>Triggers a full rebuild of the music collection database. Rebuilds the database from scratch using the current music files.</p> <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>src/musiclib/reader.py</code> <pre><code>def rebuild(self) -&gt; None:\n    \"\"\"Triggers a full rebuild of the music collection database.\n    Rebuilds the database from scratch using the current music files.\n\n    Returns:\n        None\n    \"\"\"\n    self._extractor.rebuild()\n</code></pre>"},{"location":"development/musiclib/reading.html#src.musiclib.reader.MusicCollection.resync","title":"<code>resync()</code>","text":"<p>Performs a resync of the music collection database. Updates the database to reflect changes in the music files without a full rebuild.</p> <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>src/musiclib/reader.py</code> <pre><code>def resync(self) -&gt; None:\n    \"\"\"Performs a resync of the music collection database.\n    Updates the database to reflect changes in the music files without a full rebuild.\n\n    Returns:\n        None\n    \"\"\"\n    self._extractor.resync()\n</code></pre>"},{"location":"development/musiclib/reading.html#src.musiclib.reader.MusicCollection.search_grouped","title":"<code>search_grouped(query, limit=30)</code>","text":"<p>Searches the music collection and returns grouped results by artists, albums, and tracks. Also returns the parsed terms for highlighting.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>str</code> <p>The search query string.</p> required <code>limit</code> <code>int</code> <p>Maximum number of results to return per group.</p> <code>30</code> <p>Returns:</p> Type Description <code>tuple[dict, dict]</code> <p>tuple[dict, dict]: A tuple of (grouped results, parsed terms).</p> Source code in <code>src/musiclib/reader.py</code> <pre><code>def search_grouped(self, query: str, limit: int = 30) -&gt; tuple[dict, dict]:\n    \"\"\"Searches the music collection and returns grouped results by artists, albums, and tracks.\n    Also returns the parsed terms for highlighting.\n\n    Args:\n        query: The search query string.\n        limit: Maximum number of results to return per group.\n\n    Returns:\n        tuple[dict, dict]: A tuple of (grouped results, parsed terms).\n    \"\"\"\n    if not query.strip():\n        return {\"artists\": [], \"albums\": [], \"tracks\": []}, {}\n\n    terms = self._parse_query(query)\n    has_artist = bool(terms[\"artist\"])\n    has_album = bool(terms[\"album\"])\n    is_free_text = not has_artist and not has_album and not terms[\"track\"]\n\n    reuse_session = self._last_search_session\n    can_reuse = self._can_reuse_session(reuse_session, query, terms)\n\n    if can_reuse:\n        artist_candidates, album_candidates, track_candidates = (\n            self._collect_pass_two_candidates(\n                reuse_session=reuse_session,\n                terms=terms,\n                has_artist=has_artist,\n                has_album=has_album,\n                is_free_text=is_free_text,\n            )\n        )\n    else:\n        artist_candidates, album_candidates, track_candidates = (\n            self._collect_pass_one_candidates(\n                terms=terms,\n                limit=limit,\n                has_artist=has_artist,\n                has_album=has_album,\n                is_free_text=is_free_text,\n            )\n        )\n\n    grouped = self._build_hierarchical_results(\n        artist_candidates=artist_candidates,\n        album_candidates=album_candidates,\n        track_candidates=track_candidates,\n        limit=limit,\n    )\n\n    self._last_search_session = SearchSession(\n        query=query,\n        terms=terms,\n        artist_candidates=artist_candidates,\n        album_candidates=album_candidates,\n        track_candidates=track_candidates,\n        timestamp=time(),\n    )\n\n    return grouped, terms\n</code></pre>"},{"location":"development/musiclib/reading.html#src.musiclib.ui.MusicCollectionUI","title":"<code>src.musiclib.ui.MusicCollectionUI</code>","text":"<p>               Bases: <code>MusicCollection</code></p> <p>Extends MusicCollection to provide UI-specific search and highlighting features. Adds methods for formatting, escaping, and highlighting search results for user interfaces.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Initializes the MusicCollectionUI with the given music root, database path, and optional logger.</p> <code>search_highlighting</code> <p>Performs a search and returns results with highlighted matching terms for UI display.</p> Source code in <code>src/musiclib/ui.py</code> <pre><code>class MusicCollectionUI(MusicCollection):\n    \"\"\"Extends MusicCollection to provide UI-specific search and highlighting features.\n    Adds methods for formatting, escaping, and highlighting search results for user interfaces.\n    \"\"\"\n\n    def __init__(self, music_root: Path, db_path: Path, logger: Logger = None) -&gt; None:\n        \"\"\"Initializes the MusicCollectionUI with the given music root, database path, and optional logger.\n        Sets up the UI-specific extension of the music collection functionality.\n\n        Args:\n            music_root: Path to the root directory containing music files.\n            db_path: Path to the SQLite database file.\n            logger: Optional logger instance.\n\n        Returns:\n            None\n        \"\"\"\n        super().__init__(music_root, db_path, logger)\n\n    @staticmethod\n    def _highlight_text(text: str, terms: list[str]) -&gt; str:\n        \"\"\"Highlights all occurrences of search terms in the given text for UI display.\n        Wraps matching terms in &lt;mark&gt; tags to visually emphasize them in the interface.\n\n        Args:\n            text: The text to highlight.\n            terms: List of search terms to highlight in the text.\n\n        Returns:\n            str: The text with matching terms wrapped in &lt;mark&gt; tags.\n        \"\"\"\n        if not terms:\n            return text\n        sorted_terms = sorted(terms, key=len, reverse=True)\n        pattern = \"|\".join(re.escape(t) for t in sorted_terms if t)\n        return re.sub(f\"({pattern})\", r\"&lt;mark&gt;\\1&lt;/mark&gt;\", text, flags=re.I)\n\n    def _track_display_dict(self, track: dict) -&gt; dict:\n        \"\"\"Formats a track dictionary for UI display.\n        Returns a dictionary with title, duration, path, cover, and a safe filename for the track.\n\n        Args:\n            track: Dictionary containing track information.\n\n        Returns:\n            dict: Dictionary with formatted track details for UI display.\n        \"\"\"\n        return {\n            \"artist\": track.get(\"artist\", \"\"),\n            \"album\": track.get(\"album\", \"\"),\n            \"track\": track[\"track\"],\n            \"duration\": track.get(\"duration\") or \"?:??\",\n            \"path\": track[\"path\"],\n            \"filename\": self._safe_filename(track[\"track\"], track[\"path\"]),\n            \"cover\": track.get(\"cover\"),\n        }\n\n    @staticmethod\n    def _safe_filename(track: str, path: str) -&gt; str:\n        \"\"\"Generates a safe filename for a track by sanitizing the title and preserving the file extension.\n        Removes unsafe characters from the title and appends the original file extension.\n\n        Args:\n            track: The track title to sanitize.\n            path: The original file path to extract the extension.\n\n        Returns:\n            str: A safe filename suitable for saving or displaying.\n        \"\"\"\n        ext = Path(path).suffix or \"\"\n        safe = \"\".join(c for c in track if c.isalnum() or c in \" _-\").strip()\n        return f\"{safe}{ext}\"\n\n    @staticmethod\n    def _escape_for_query(name: str) -&gt; str:\n        \"\"\"\n        Escapes a string for inclusion in a search query.\n\n        * If the string contains no single\u2011quote characters, it is wrapped in single quotes:\n        `'Prince'`.\n        * If a single quote is present, the string is wrapped in double quotes:\n        `\"O'Connor\"`.\n\n        This mirrors the quoting rules used by the query parser in ``reader.py``.\n        \"\"\"\n        # Single\u2011quote is preferred; fall back to double\u2011quote when needed.\n        return f\"'{name}'\" if \"'\" not in name else f'\"{name}\"'\n\n    def _should_filter_strict(self, terms: dict) -&gt; tuple[bool, bool]:\n        \"\"\"Determines if strict filtering should be applied based on search terms.\n\n        Args:\n            terms: Dictionary of parsed search terms.\n\n        Returns:\n            tuple[bool, bool]: (filter_artists, filter_albums) flags.\n        \"\"\"\n        has_track_terms = bool(terms.get(\"track\") or terms.get(\"song\"))\n        return has_track_terms, has_track_terms\n\n    def _matches_artist_terms(\n        self, artist_name: str, artist_terms: list[str], query_lower: str\n    ) -&gt; bool:\n        \"\"\"Checks if an artist name matches the specified artist search terms.\n\n        Args:\n            artist_name: The artist name to check.\n            artist_terms: List of artist-specific search terms.\n            query_lower: Lowercase version of the full query.\n\n        Returns:\n            bool: True if the artist matches the terms, False otherwise.\n        \"\"\"\n        artist_lower = artist_name.lower()\n        for term in artist_terms:\n            term_lower = term.lower()\n            # Prefer exact quoted match\n            if (\n                f'\"{term_lower}\"' in query_lower\n                and artist_lower == term_lower\n                or f'\"{term_lower}\"' not in query_lower\n                and term_lower in artist_lower\n            ):\n                return True\n        return False\n\n    def _matches_album_terms(\n        self, album_name: str, album_terms: list[str], query_lower: str\n    ) -&gt; bool:\n        \"\"\"Checks if an album name matches the specified album search terms.\n\n        Args:\n            album_name: The album name to check.\n            album_terms: List of album-specific search terms.\n            query_lower: Lowercase version of the full query.\n\n        Returns:\n            bool: True if the album matches the terms, False otherwise.\n        \"\"\"\n        album_lower = album_name.lower()\n        for term in album_terms:\n            term_lower = term.lower()\n            if (\n                f'\"{term_lower}\"' in query_lower\n                and album_lower == term_lower\n                or f'\"{term_lower}\"' not in query_lower\n                and term_lower in album_lower\n            ):\n                return True\n        return False\n\n    def _get_artist_match_counts(\n        self, artist_name: str, query_lower: str\n    ) -&gt; tuple[int, int]:\n        \"\"\"Gets the count of matched albums and tracks for an artist.\n\n        Args:\n            artist_name: The artist name to query.\n            query_lower: Lowercase version of the search query.\n\n        Returns:\n            tuple[int, int]: (matched_albums, matched_tracks) counts.\n        \"\"\"\n        with self._get_conn() as conn:\n            cur = conn.execute(\n                \"SELECT COUNT(DISTINCT album) FROM tracks WHERE artist = ? AND (album LIKE ? OR title LIKE ?)\",\n                (artist_name, f\"%{query_lower}%\", f\"%{query_lower}%\"),\n            )\n            matched_albums = cur.fetchone()[0] or 0\n\n            cur = conn.execute(\n                \"SELECT COUNT(*) FROM tracks WHERE artist = ? AND title LIKE ?\",\n                (artist_name, f\"%{query_lower}%\"),\n            )\n            matched_tracks = cur.fetchone()[0] or 0\n\n        return matched_albums, matched_tracks\n\n    def _build_artist_reasons(\n        self,\n        artist_name: str,\n        query_lower: str,\n        matched_albums: int,\n        matched_tracks: int,\n    ) -&gt; list[dict]:\n        \"\"\"Builds the list of reasons why an artist was matched.\n\n        Args:\n            artist_name: The artist name.\n            query_lower: Lowercase version of the search query.\n            matched_albums: Number of matched albums.\n            matched_tracks: Number of matched tracks.\n\n        Returns:\n            list[dict]: List of reason dictionaries with type and text.\n        \"\"\"\n        reasons = []\n        if query_lower in artist_name.lower():\n            reasons.append({\"type\": \"artist\", \"text\": artist_name})\n        if matched_albums:\n            reasons.append({\"type\": \"album\", \"text\": f\"{matched_albums} album(s)\"})\n        if matched_tracks:\n            reasons.append({\"type\": \"track\", \"text\": f\"{matched_tracks} track(s)\"})\n        return reasons\n\n    def _process_artist_result(\n        self, artist: dict, terms: dict, all_terms: list[str], query_lower: str\n    ) -&gt; dict | None:\n        \"\"\"Processes a single artist result for search highlighting.\n\n        Args:\n            artist: Dictionary containing artist information.\n            terms: Dictionary of parsed search terms.\n            all_terms: Combined list of all search terms.\n            query_lower: Lowercase version of the search query.\n\n        Returns:\n            dict | None: Processed artist result dictionary, or None if filtered out.\n        \"\"\"\n        artist_name = artist[\"artist\"]\n\n        # Filter if artist-specific terms don't match\n        if terms.get(\"artist\") and not self._matches_artist_terms(\n            artist_name, terms[\"artist\"], query_lower\n        ):\n            return None\n\n        highlighted_artist = self._highlight_text(artist_name, all_terms)\n        matched_albums, matched_tracks = self._get_artist_match_counts(\n            artist_name, query_lower\n        )\n        reasons = self._build_artist_reasons(\n            artist_name, query_lower, matched_albums, matched_tracks\n        )\n\n        return {\n            \"type\": \"artist\",\n            \"raw_artist\": artist_name,\n            \"artist\": highlighted_artist,\n            \"reasons\": reasons,\n            \"num_albums\": artist[\"num_albums\"],\n            \"albums\": [],\n            \"load_on_demand\": True,\n            \"clickable\": True,\n            \"click_query\": f\"artist:{self._escape_for_query(artist['artist'])}\",\n        }\n\n    def _get_album_matched_track_count(self, release_dir: str, query_lower: str) -&gt; int:\n        \"\"\"Gets the count of matched tracks for an album.\n\n        Args:\n            release_dir: The release directory path.\n            query_lower: Lowercase version of the search query.\n\n        Returns:\n            int: Number of matched tracks.\n        \"\"\"\n        with self._get_conn() as conn:\n            cur = conn.execute(\n                \"\"\"\n                SELECT COUNT(*) FROM tracks\n                WHERE SUBSTR(path, 1, LENGTH(path) - LENGTH(filename)) = ?\n                AND title LIKE ?\n                \"\"\",\n                (f\"{release_dir}/\", f\"%{query_lower}%\"),\n            )\n            return cur.fetchone()[0] or 0\n\n    def _build_album_reasons(\n        self,\n        display_artist: str,\n        album_name: str,\n        query_lower: str,\n        matched_tracks: int,\n        is_compilation: bool,\n    ) -&gt; list[dict]:\n        \"\"\"Builds the list of reasons why an album was matched.\n\n        Args:\n            display_artist: The artist name to display.\n            album_name: The album name.\n            query_lower: Lowercase version of the search query.\n            matched_tracks: Number of matched tracks.\n            is_compilation: Whether this is a compilation album.\n\n        Returns:\n            list[dict]: List of reason dictionaries with type and text.\n        \"\"\"\n        reasons = []\n        if query_lower in display_artist.lower() and not is_compilation:\n            reasons.append({\"type\": \"artist\", \"text\": display_artist})\n        if query_lower in album_name.lower():\n            reasons.append({\"type\": \"album\", \"text\": album_name})\n        if matched_tracks:\n            reasons.append({\"type\": \"track\", \"text\": f\"{matched_tracks} nummer(s)\"})\n        return reasons\n\n    def _process_album_result(\n        self, album: dict, terms: dict, all_terms: list[str], query_lower: str\n    ) -&gt; dict | None:\n        \"\"\"Processes a single album result for search highlighting.\n\n        Args:\n            album: Dictionary containing album information.\n            terms: Dictionary of parsed search terms.\n            all_terms: Combined list of all search terms.\n            query_lower: Lowercase version of the search query.\n\n        Returns:\n            dict | None: Processed album result dictionary, or None if filtered out.\n        \"\"\"\n        album_name = album[\"album\"]\n\n        # Filter if album-specific terms don't match\n        if terms.get(\"album\") and not self._matches_album_terms(\n            album_name, terms[\"album\"], query_lower\n        ):\n            return None\n\n        display_artist = album[\"display_artist\"]\n        is_compilation = album[\"is_compilation\"]\n        release_dir = album[\"release_dir\"]\n\n        highlighted_artist = (\n            \"Various Artists\"\n            if is_compilation\n            else self._highlight_text(display_artist, all_terms)\n        )\n        highlighted_album = self._highlight_text(album_name, all_terms)\n\n        matched_tracks = self._get_album_matched_track_count(release_dir, query_lower)\n        reasons = self._build_album_reasons(\n            display_artist, album_name, query_lower, matched_tracks, is_compilation\n        )\n\n        return {\n            \"type\": \"album\",\n            \"raw_artist\": display_artist,\n            \"artist\": highlighted_artist,\n            \"raw_album\": album_name,\n            \"album\": highlighted_album,\n            \"reasons\": reasons,\n            \"num_tracks\": album[\"num_tracks\"],\n            \"tracks\": [],\n            \"highlighted_tracks\": None,\n            \"load_on_demand\": True,\n            \"is_compilation\": is_compilation,\n            \"clickable\": True,\n            \"click_query\": f\"release_dir:{self._escape_for_query(release_dir)}\",\n            \"artist_click_query\": None\n            if is_compilation\n            else f\"artist:{self._escape_for_query(display_artist)}\",\n            \"release_dir\": release_dir,\n            \"cover\": self.get_cover(release_dir),\n        }\n\n    def _process_track_result(self, track: dict, all_terms: list[str]) -&gt; dict:\n        \"\"\"Processes a single track result for search highlighting.\n\n        Args:\n            track: Dictionary containing track information.\n            all_terms: Combined list of all search terms.\n\n        Returns:\n            dict: Processed track result dictionary.\n        \"\"\"\n        track_name = track[\"track\"]\n        highlighted_tracks = self._highlight_text(track_name, all_terms)\n        highlighted_artist = self._highlight_text(track[\"artist\"], all_terms)\n        highlighted_album = self._highlight_text(track[\"album\"], all_terms)\n        release_dir = self._get_release_dir(track[\"path\"])\n        track[\"cover\"] = self.get_cover(release_dir)\n\n        return {\n            \"type\": \"track\",\n            \"raw_artist\": track[\"artist\"],\n            \"artist\": highlighted_artist,\n            \"raw_album\": track[\"album\"],\n            \"album\": highlighted_album,\n            \"reasons\": [{\"type\": \"track\", \"text\": track_name}],\n            \"tracks\": [self._track_display_dict(track)],\n            \"highlighted_tracks\": [\n                {\n                    \"original\": {\n                        \"track\": track_name,\n                        \"duration\": self._format_duration(track.get(\"duration\")),\n                    },\n                    \"highlighted\": highlighted_tracks,\n                    \"match_type\": \"track\",\n                }\n            ],\n            \"artist_click_query\": f\"artist:{self._escape_for_query(track['artist'])}\",\n            \"album_click_query\": f\"album:{self._escape_for_query(track['album'])}\",\n        }\n\n    def search_highlighting(self, query: str, limit: int = 30) -&gt; list[dict]:\n        \"\"\"Performs a search and returns results with highlighted matching terms for UI display.\n        Groups and highlights artists, albums, and tracks based on the search query and terms.\n\n        Args:\n            query: The search query string.\n            limit: Maximum number of results to return.\n\n        Returns:\n            list[dict]: List of dictionaries containing highlighted search results for artists, albums, and tracks.\n        \"\"\"\n        if not query.strip():\n            return []\n\n        grouped, terms = self.search_grouped(query, limit=limit)\n\n        # Combine all search terms for highlighting\n        all_terms = (\n            terms.get(\"artist\", [])\n            + terms.get(\"album\", [])\n            + terms.get(\"track\", [])\n            + terms.get(\"general\", [])\n        )\n        query_lower = query.lower()\n\n        # Apply strict filtering for tagged searches\n        filter_artists, filter_albums = self._should_filter_strict(terms)\n        if filter_artists:\n            grouped[\"artists\"] = []\n        if filter_albums:\n            grouped[\"albums\"] = []\n\n        results = []\n\n        # Process artists\n        for artist in grouped[\"artists\"]:\n            if artist_result := self._process_artist_result(\n                artist, terms, all_terms, query_lower\n            ):\n                results.append(artist_result)\n\n        # Process albums\n        for album in grouped[\"albums\"]:\n            if album_result := self._process_album_result(\n                album, terms, all_terms, query_lower\n            ):\n                results.append(album_result)\n\n        # Process tracks\n        for track in grouped[\"tracks\"]:\n            track_result = self._process_track_result(track, all_terms)\n            results.append(track_result)\n\n        return results\n</code></pre>"},{"location":"development/musiclib/reading.html#src.musiclib.ui.MusicCollectionUI.__init__","title":"<code>__init__(music_root, db_path, logger=None)</code>","text":"<p>Initializes the MusicCollectionUI with the given music root, database path, and optional logger. Sets up the UI-specific extension of the music collection functionality.</p> <p>Parameters:</p> Name Type Description Default <code>music_root</code> <code>Path</code> <p>Path to the root directory containing music files.</p> required <code>db_path</code> <code>Path</code> <p>Path to the SQLite database file.</p> required <code>logger</code> <code>Logger</code> <p>Optional logger instance.</p> <code>None</code> <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>src/musiclib/ui.py</code> <pre><code>def __init__(self, music_root: Path, db_path: Path, logger: Logger = None) -&gt; None:\n    \"\"\"Initializes the MusicCollectionUI with the given music root, database path, and optional logger.\n    Sets up the UI-specific extension of the music collection functionality.\n\n    Args:\n        music_root: Path to the root directory containing music files.\n        db_path: Path to the SQLite database file.\n        logger: Optional logger instance.\n\n    Returns:\n        None\n    \"\"\"\n    super().__init__(music_root, db_path, logger)\n</code></pre>"},{"location":"development/musiclib/reading.html#src.musiclib.ui.MusicCollectionUI.search_highlighting","title":"<code>search_highlighting(query, limit=30)</code>","text":"<p>Performs a search and returns results with highlighted matching terms for UI display. Groups and highlights artists, albums, and tracks based on the search query and terms.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>str</code> <p>The search query string.</p> required <code>limit</code> <code>int</code> <p>Maximum number of results to return.</p> <code>30</code> <p>Returns:</p> Type Description <code>list[dict]</code> <p>list[dict]: List of dictionaries containing highlighted search results for artists, albums, and tracks.</p> Source code in <code>src/musiclib/ui.py</code> <pre><code>def search_highlighting(self, query: str, limit: int = 30) -&gt; list[dict]:\n    \"\"\"Performs a search and returns results with highlighted matching terms for UI display.\n    Groups and highlights artists, albums, and tracks based on the search query and terms.\n\n    Args:\n        query: The search query string.\n        limit: Maximum number of results to return.\n\n    Returns:\n        list[dict]: List of dictionaries containing highlighted search results for artists, albums, and tracks.\n    \"\"\"\n    if not query.strip():\n        return []\n\n    grouped, terms = self.search_grouped(query, limit=limit)\n\n    # Combine all search terms for highlighting\n    all_terms = (\n        terms.get(\"artist\", [])\n        + terms.get(\"album\", [])\n        + terms.get(\"track\", [])\n        + terms.get(\"general\", [])\n    )\n    query_lower = query.lower()\n\n    # Apply strict filtering for tagged searches\n    filter_artists, filter_albums = self._should_filter_strict(terms)\n    if filter_artists:\n        grouped[\"artists\"] = []\n    if filter_albums:\n        grouped[\"albums\"] = []\n\n    results = []\n\n    # Process artists\n    for artist in grouped[\"artists\"]:\n        if artist_result := self._process_artist_result(\n            artist, terms, all_terms, query_lower\n        ):\n            results.append(artist_result)\n\n    # Process albums\n    for album in grouped[\"albums\"]:\n        if album_result := self._process_album_result(\n            album, terms, all_terms, query_lower\n        ):\n            results.append(album_result)\n\n    # Process tracks\n    for track in grouped[\"tracks\"]:\n        track_result = self._process_track_result(track, all_terms)\n        results.append(track_result)\n\n    return results\n</code></pre>"},{"location":"development/routes/authentication.html","title":"Authentication","text":""},{"location":"development/routes/authentication.html#authentication","title":"Authentication","text":"<p>The <code>routes/authentication.py</code> file defines the authentication subsystem for the Flask app as a reusable blueprint. it's purpose is to centralize login/logout behavior, enforce rate limiting on login attempts, and provide a blueprint that can be registered on the main Flask app to enable password-based access control using a single shared secret.</p> <p>Functionally, it:</p> <ul> <li>Creates an authentication blueprint<ul> <li><code>create_authentication_blueprint(limiter, logger=None)</code> constructs and returns a Blueprint named \"authenticator\".</li> <li>It accepts:<ul> <li>A <code>Limiter</code> instance (from <code>flask_limiter</code>) used to rate-limit login attempts.</li> <li>An optional <code>Logger</code> for recording authentication-related events (with a <code>NullLogger</code> fallback, though the current code doesn\u2019t explicitly log yet).</li> </ul> </li> </ul> </li> <li>Implements login logic (<code>POST /login</code>)<ul> <li>Exposed as <code>/login</code> on the authentication blueprint.</li> <li>Protected by <code>@limiter.limit(\"5 per minute\")</code>, meaning each client can only attempt login five times per minute.</li> <li>Reads the <code>password</code> field from <code>request.form</code>.</li> <li>Compares the submitted password to <code>current_app.secret_key</code>:<ul> <li>If they match: sets <code>session[\"authenticated\"] = True</code>.</li> <li>If they do not match: uses <code>flash(\"Invalid password\", \"danger\")</code> to send an error message to the UI.</li> </ul> </li> <li>Always redirects to <code>/</code> afterward (both success and failure).</li> </ul> </li> <li>Implements logout logic (<code>GET /logout</code>)<ul> <li>Exposed as <code>/logout</code> on the <code>authentication</code> blueprint.</li> <li>Removes the authenticated flag from the session (<code>session.pop(\"authenticated\", None)</code>).</li> <li>Redirects the user back to <code>/</code> (the landing or main page).</li> </ul> </li> </ul> <p>The file <code>src/auth.py</code> defines simple authentication utilities for a Flask application. Its primary purpose is to:</p> <ul> <li>Provide a helper to check whether the current user is authenticated based on the Flask session.</li> <li>Provide a decorator to protect view functions, ensuring only authenticated users can access them and redirecting unauthenticated users to a landing page.</li> </ul> <p>In the larger system, this module centralizes basic auth checks so route handlers can easily enforce authentication without duplicating logic.</p>"},{"location":"development/routes/authentication.html#api","title":"\ud83d\udd0c API","text":""},{"location":"development/routes/authentication.html#src.routes.authentication","title":"<code>src.routes.authentication</code>","text":"<p>Functions:</p> Name Description <code>create_authentication_blueprint</code> <p>Creates and configures the Flask blueprint for user authentication.</p>"},{"location":"development/routes/authentication.html#src.routes.authentication.create_authentication_blueprint","title":"<code>create_authentication_blueprint(limiter, logger=None)</code>","text":"<p>Creates and configures the Flask blueprint for user authentication.</p> <p>Sets up routes for user login and logout, applies rate limiting to login attempts, and provides logging support.</p> <p>Parameters:</p> Name Type Description Default <code>limiter</code> <code>Limiter</code> <p>The Flask-Limiter instance for rate limiting login attempts.</p> required <code>logger</code> <code>Logger | None</code> <p>Optional logger for logging authentication events. Uses NullLogger if not provided.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>Blueprint</code> <code>Blueprint</code> <p>The configured Flask blueprint for authentication.</p> Source code in <code>src/routes/authentication.py</code> <pre><code>def create_authentication_blueprint(limiter: Limiter, logger: Logger | None = None) -&gt; Blueprint:\n    \"\"\"\n    Creates and configures the Flask blueprint for user authentication.\n\n    Sets up routes for user login and logout, applies rate limiting to login attempts, and provides logging support.\n\n    Args:\n        limiter (Limiter): The Flask-Limiter instance for rate limiting login attempts.\n        logger (Logger | None): Optional logger for logging authentication events. Uses NullLogger if not provided.\n\n    Returns:\n        Blueprint: The configured Flask blueprint for authentication.\n    \"\"\"\n\n    authenticator = Blueprint(\"authenticator\", __name__)\n\n    logger: Logger = logger or NullLogger()\n\n    # === Authentication Routes ===\n    @authenticator.route(\"/login\", methods=[\"POST\"])\n    @limiter.limit(\"5 per minute\")\n    def login() -&gt; Response:\n        \"\"\"\n        Authenticates a user based on the submitted password.\n\n        Checks the provided password against the configured password and sets the session as authenticated if correct.\n        Redirects to the mixtapes page on success or flashes an error and redirects to the landing page on failure.\n\n        Returns:\n            Response: The Flask response object for the appropriate redirect.\n        \"\"\"\n        password = request.form.get(\"password\")\n        if password == current_app.secret_key:\n            session[\"authenticated\"] = True\n        else:\n            flash(\"Invalid password\", \"danger\")\n        return redirect(\"/\")\n\n    @authenticator.route(\"/logout\")\n    def logout() -&gt; Response:\n        \"\"\"\n        Logs out the current user by removing authentication from the session.\n\n        Clears the user's session and redirects to the landing page.\n\n        Returns:\n            Response: The Flask response object for the redirect to the landing page.\n        \"\"\"\n        session.pop(\"authenticated\", None)\n        return redirect(\"/\")\n\n    return authenticator\n</code></pre>"},{"location":"development/routes/authentication.html#src.auth","title":"<code>src.auth</code>","text":"<p>Functions:</p> Name Description <code>check_auth</code> <p>Checks if the current user is authenticated.</p> <code>require_auth</code> <p>Decorator that enforces authentication for a view function.</p>"},{"location":"development/routes/authentication.html#src.auth.check_auth","title":"<code>check_auth()</code>","text":"<p>Checks if the current user is authenticated.</p> <p>Returns True if the user is authenticated in the session, otherwise False.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Authentication status of the current user. Returns True only if the</p> <code>bool</code> <p>session \"authenticated\" flag is explicitly set to True.</p> Source code in <code>src/auth.py</code> <pre><code>def check_auth() -&gt; bool:\n    \"\"\"\n    Checks if the current user is authenticated.\n\n    Returns True if the user is authenticated in the session, otherwise False.\n\n    Returns:\n        bool: Authentication status of the current user. Returns True only if the\n        session \"authenticated\" flag is explicitly set to True.\n    \"\"\"\n    return session.get(\"authenticated\") is True\n</code></pre>"},{"location":"development/routes/authentication.html#src.auth.require_auth","title":"<code>require_auth(view)</code>","text":"<p>Decorator that enforces authentication for a view function.</p> <p>Redirects unauthenticated users to the landing page before allowing access to the decorated view.</p> <p>Parameters:</p> Name Type Description Default <code>view</code> <code>Callable</code> <p>The view function to be protected.</p> required <p>Returns:</p> Name Type Description <code>function</code> <code>Callable</code> <p>The wrapped view function with authentication enforcement.</p> Source code in <code>src/auth.py</code> <pre><code>def require_auth(view: Callable) -&gt; Callable:\n    \"\"\"\n    Decorator that enforces authentication for a view function.\n\n    Redirects unauthenticated users to the landing page before allowing access to the decorated view.\n\n    Args:\n        view: The view function to be protected.\n\n    Returns:\n        function: The wrapped view function with authentication enforcement.\n    \"\"\"\n    from functools import wraps\n    @wraps(view)\n    def wrapper(*args, **kwargs):\n        if not check_auth():\n            return redirect(url_for(\"landing\"))\n        return view(*args, **kwargs)\n    return wrapper\n</code></pre>"},{"location":"development/routes/browse_mixtapes.html","title":"Browser","text":""},{"location":"development/routes/browse_mixtapes.html#browse-mixtapes","title":"Browse Mixtapes","text":"<p>The <code>browse_mixtapes</code> Flask blueprint (<code>routes/browse_mixtapes.py</code>) powers the Mixtapes listing page with advanced search and sorting capabilities, file serving, and redirection to the public player. It explains the routes, authentication flow, interaction with MixtapeManager, and the front-end assets (browse_mixtapes.html, CSS, and JavaScript).</p>"},{"location":"development/routes/browse_mixtapes.html#high-level-overview","title":"\ud83c\udf10 High-Level Overview","text":"Component Responsibility <code>browse_mixtapes</code> Blueprint (<code>routes/browse_mixtapes.py</code>) Registers all UI-facing routes under the <code>/mixtapes</code> prefix, enforces authentication, handles search/sort parameters, and delegates data access to <code>MixtapeManager</code>. <code>MixtapeManager</code> (<code>mixtape_manager.py</code>) Reads/writes mixtape JSON files, manages cover images, and provides <code>list_all()</code> for the browse view. Templates (<code>templates/browse_mixtapes.html</code>) Renders the list of mixtapes with a compact search/sort bar and mixtape cards, each with cover, meta info, and action buttons (edit, play, share, delete). Static assets (<code>static/css/browse_mixtapes.css</code>, <code>static/js/browser/*.js</code>) Provide responsive styling, hybrid search functionality (instant title + deep track search), sorting logic, and delete-confirmation modal. Authentication (<code>auth.py</code>) <code>@require_auth</code> decorator and <code>check_auth()</code> helper ensure only logged-in users can reach any route in this blueprint."},{"location":"development/routes/browse_mixtapes.html#flask-blueprint-routes","title":"\ud83d\uddfa\ufe0f Flask Blueprint &amp; Routes","text":"HTTP Method URL Pattern View Function Query Parameters Description <code>GET</code> <code>/mixtapes/</code> <code>browse()</code> <code>sort_by</code>, <code>sort_order</code>, <code>search</code>, <code>deep</code> Retrieves all mixtapes (<code>MixtapeManager.list_all()</code>), applies search/sort filters, and renders <code>browse_mixtapes.html</code>. <code>GET</code> <code>/mixtapes/play/&lt;slug&gt;</code> <code>play(slug)</code> - Redirect to the public player (<code>play.public_play</code>) for the given mixtape slug. <code>GET</code> <code>/mixtapes/files/&lt;path:filename&gt;</code> <code>files(filename)</code> - Serves static files (JSON, cover images, etc.) from the configured <code>MIXTAPE_DIR</code>. <code>POST</code> <code>/mixtapes/delete/&lt;slug&gt;</code> <code>delete_mixtape(slug)</code> - Deletes the mixtape JSON and its cover image; returns JSON <code>{ success: true }</code> or an error. <code>before_request</code> \u2014 <code>blueprint_require_auth()</code> - Runs before every request in this blueprint; redirects unauthenticated users to the landing page (<code>url_for(\"landing\")</code>). <p>All routes are wrapped with <code>@require_auth</code> (except the <code>before_request</code> hook, which performs the same check).</p>"},{"location":"development/routes/browse_mixtapes.html#query-parameters","title":"Query Parameters","text":"<p>Sort Parameters: - <code>sort_by</code>: Field to sort by (<code>updated_at</code>, <code>created_at</code>, <code>title</code>, <code>track_count</code>) - <code>sort_order</code>: Sort direction (<code>asc</code>, <code>desc</code>) - Default: <code>sort_by=updated_at&amp;sort_order=desc</code> (most recently modified first)</p> <p>Search Parameters: - <code>search</code>: Search query string - <code>deep</code>: Boolean (<code>true</code>/<code>false</code>) indicating deep search mode   - <code>false</code> or absent: Client-side title filtering only   - <code>true</code>: Server-side search through tracks, artists, albums, and liner notes</p> <p>Examples: </p><pre><code>/mixtapes/?sort_by=title&amp;sort_order=asc\n/mixtapes/?search=rock&amp;deep=true&amp;sort_by=track_count&amp;sort_order=desc\n</code></pre><p></p>"},{"location":"development/routes/browse_mixtapes.html#authentication-access-control","title":"\ud83d\udd12 Authentication &amp; Access Control","text":"<ul> <li>Decorator \u2014 <code>@require_auth</code> (imported from <code>auth.py</code>) checks the session for a valid user. If the check fails, the decorator returns a redirect to the login page.</li> <li>Blueprint-wide guard \u2014 <code>@browser.before_request</code> executes <code>check_auth()</code> for every request hitting this blueprint. This is a defensive second line; even if a route is accidentally left undecorated, the guard will still enforce authentication.</li> </ul> <p>Result: Only logged-in users can view the mixtape list, play a mixtape, download files, or delete a mixtape.</p>"},{"location":"development/routes/browse_mixtapes.html#search-sort-features","title":"\ud83d\udd0d Search &amp; Sort Features","text":""},{"location":"development/routes/browse_mixtapes.html#hybrid-search-system","title":"Hybrid Search System","text":"<p>The browse page implements a two-tier search system:</p> <p>1. Instant Title Search (Client-Side) - Filters mixtapes by title as you type - No server round-trip required - Instant visual feedback - Implemented in <code>static/js/browser/search.js</code></p> <p>2. Deep Search (Server-Side) - Click \"Tracks\" button to search within:   - Track names (song titles)   - Artist names   - Album names   - Mixtape titles   - Liner notes - Implemented in <code>_deep_search_mixtapes()</code> helper function - Returns filtered list to template</p>"},{"location":"development/routes/browse_mixtapes.html#sorting-system","title":"Sorting System","text":"<p>Users can sort by 8 different combinations via a single dropdown:</p> <p>Date Options: - \ud83d\udcc5 Recent First (updated_at desc) - \ud83d\udcc5 Oldest First (updated_at asc) - \ud83d\udcc5 Newest Created (created_at desc) - \ud83d\udcc5 Oldest Created (created_at asc)</p> <p>Name &amp; Size Options: - \ud83d\udd24 A \u2192 Z (title asc) - \ud83d\udd24 Z \u2192 A (title desc) - \ud83c\udfb5 Most Tracks (track_count desc) - \ud83c\udfb5 Fewest Tracks (track_count asc)</p> <p>Sorting is applied after search filtering, so search results respect the selected sort order.</p>"},{"location":"development/routes/browse_mixtapes.html#data-flow-server-side-logic","title":"\ud83d\udcc4 Data Flow &amp; Server-Side Logic","text":""},{"location":"development/routes/browse_mixtapes.html#listing-mixtapes-get-mixtapes","title":"Listing Mixtapes (<code>GET</code> <code>/mixtapes/</code>)","text":"<ol> <li>Request \u2192 Flask routes the request to <code>browse()</code> (protected).</li> <li>Parse parameters \u2014 Extract <code>sort_by</code>, <code>sort_order</code>, <code>search</code>, and <code>deep</code> from query string.</li> <li>Mixtape retrieval \u2014 <code>mixtape_manager.list_all()</code> reads every <code>*.json</code> file in <code>app.config[\"MIXTAPE_DIR\"]</code> and returns a list of dicts.</li> <li>Apply search \u2014 If <code>search</code> and <code>deep=true</code>, filter mixtapes using <code>_deep_search_mixtapes()</code>:     <pre><code>def _deep_search_mixtapes(mixtapes: list[dict], query: str) -&gt; list[dict]:\n    \"\"\"Searches across mixtape metadata and all tracks\"\"\"\n    query_lower = query.lower()\n    results = []\n\n    for mixtape in mixtapes:\n        # Check title, liner notes\n        if query_lower in mixtape.get('title', '').lower():\n            results.append(mixtape)\n            continue\n\n        # Check all tracks (name, artist, album)\n        for track in mixtape.get('tracks', []):\n            if (query_lower in track.get('track', '').lower() or\n                query_lower in track.get('artist', '').lower() or\n                query_lower in track.get('album', '').lower()):\n                results.append(mixtape)\n                break\n\n    return results\n</code></pre></li> <li>Apply sorting \u2014 Sort the filtered list based on <code>sort_by</code> and <code>sort_order</code>:     <pre><code>if sort_by == 'title':\n    mixtapes.sort(key=lambda x: x.get('title', '').lower(), \n                 reverse=(sort_order == 'desc'))\nelif sort_by == 'track_count':\n    mixtapes.sort(key=lambda x: len(x.get('tracks', [])), \n                 reverse=(sort_order == 'desc'))\n# ... etc\n</code></pre></li> <li>Template rendering \u2014 Pass <code>mixtapes</code>, <code>sort_by</code>, <code>sort_order</code>, <code>search_query</code>, and <code>search_deep</code> to template.</li> <li>HTML output \u2014 Renders search/sort controls and filtered mixtape cards.</li> </ol>"},{"location":"development/routes/browse_mixtapes.html#playing-a-mixtape-get-mixtapesplayslug","title":"Playing a Mixtape (<code>GET</code> <code>/mixtapes/play/&lt;slug&gt;</code>)","text":"<ul> <li>The view simply redirects to the public player route defined elsewhere (e.g. play.public_play).     <pre><code>return redirect(url_for(\"public_play\", slug=slug))\n</code></pre></li> <li>The client ends up on <code>/play/&lt;slug&gt;#play</code>, where the full mixtape UI is rendered.</li> </ul>"},{"location":"development/routes/browse_mixtapes.html#serving-files-get-mixtapesfilesfilename","title":"Serving Files (<code>GET</code> <code>/mixtapes/files/&lt;filename&gt;</code>)","text":"<ul> <li>Uses Flask's <code>send_from_directory</code> to serve any file under <code>app.config[\"MIXTAPE_DIR\"]</code>.</li> <li>This includes the JSON file (<code>&lt;slug&gt;.json</code>) and cover images (<code>covers/&lt;slug&gt;.jpg</code>).</li> </ul>"},{"location":"development/routes/browse_mixtapes.html#deleting-a-mixtape-post-mixtapesdeleteslug","title":"Deleting a Mixtape (<code>POST</code> <code>/mixtapes/delete/&lt;slug&gt;</code>)","text":"<ol> <li>Existence check \u2014 Verifies that <code>&lt;slug&gt;.json</code> exists; if not, returns <code>404</code> with JSON error.</li> <li>Calls <code>mixtape_manager.delete(slug)</code>, which removes the JSON file and any associated cover image (<code>covers/&lt;slug&gt;.jpg</code>).</li> <li>Returns JSON <code>{ \"success\": true }</code> on success, or <code>{ \"success\": false, \"error\": \"...\" }</code> with the appropriate HTTP status on failure.</li> </ol>"},{"location":"development/routes/browse_mixtapes.html#error-handling","title":"Error Handling","text":"<ul> <li>All routes catch generic <code>Exception</code> and log the traceback via the injected <code>logger</code>.</li> <li>Errors are reported to the client as JSON with a descriptive <code>error</code> field and an appropriate HTTP status code (<code>400</code>, <code>404</code>, <code>500</code>).</li> </ul>"},{"location":"development/routes/browse_mixtapes.html#ui-layout-jinja-template-browse_mixtapeshtml","title":"\ud83d\udda5\ufe0f UI Layout (Jinja Template \u2014 <code>browse_mixtapes.html</code>)","text":"Section Details Header Compact header with \"My Mixtapes\" title and a \"New\" button linking to <code>/editor</code>. Responsive sizing (smaller on mobile). Search &amp; Sort Bar Compact card containing: \u2022 Search input with clear button \u2022 \"Tracks\" button (with tooltip) for deep search \u2022 Combined sort dropdown (field + order in one) \u2022 Minimal vertical space (~50px desktop, ~70px mobile) \u2022 Bootstrap theme-aware styling Search Result Banner When search is active, shows info banner with query, result count, and \"Clear Search\" button. Distinguishes between title search and deep search results. Mixtape Cards Loop over filtered/sorted mixtapes. Each card (<code>.mixtape-item</code>) contains: \u2022 Cover (<code>.mixtape-cover</code>) \u2022 Title (<code>.mixtape-title</code>) \u2022 Meta info (<code>.mixtape-meta</code>) \u2022 Action buttons (<code>.action-btn</code>): edit, play, QR share, delete Empty State Context-aware: \u2022 No mixtapes: \"No mixtapes yet...\" \u2022 No search results: \"No mixtapes found\" with clear search button Delete Confirmation Modal Modal (<code>#deleteConfirmModal</code>) that asks the user to confirm deletion; populated with the mixtape title via JS. Delete Success Toast Toast (<code>#deleteSuccessToast</code>) shown after a successful deletion. JS Entry Point <code>&lt;script type=\"module\" src=\"{{ url_for('static', filename='js/browser/index.js') }}\"&gt;&lt;/script&gt;</code> \u2014 wires up search, sort, delete, QR share, and tooltip functionality. <p>All UI elements use Bootstrap 5 utilities and custom CSS variables (<code>--bs-body-bg</code>, <code>--bs-border-color</code>, <code>--bs-body-color</code>) to stay theme-aware (light/dark modes).</p>"},{"location":"development/routes/browse_mixtapes.html#compact-searchsort-bar-features","title":"Compact Search/Sort Bar Features","text":"<p>Space Efficiency: - 60%+ vertical space reduction compared to separate cards - Single card with all controls - Mobile-optimized with reduced padding</p> <p>Responsive Layout: - Desktop (\u2265992px): Search input expands to fill space, Tracks + Sort align right - Tablet (768-991px): Two-row layout with natural wrapping - Mobile (&lt;768px): Stacked controls, full-width inputs - Tiny (&lt;576px): Extra compact sizing</p> <p>Theming: - Search icon respects Bootstrap color variables - No hardcoded colors (works in light/dark mode) - Proper contrast in all themes</p> <p>Tooltips: - Tracks button has tooltip: \"Search within song names, artists, and albums\" - Works on both enabled and disabled states - Helps users understand deep search feature</p>"},{"location":"development/routes/browse_mixtapes.html#static-assets-css-js","title":"\ud83e\uddf1 Static Assets (CSS &amp; JS)","text":""},{"location":"development/routes/browse_mixtapes.html#browse_mixtapescss","title":"<code>browse_mixtapes.css</code>","text":"<p>Search &amp; Sort Styling: - Compact card with minimal padding (<code>p-2</code> on mobile, <code>p-md-3</code> on desktop) - Theme-aware colors using CSS variables - Input group styling with seamless borders - Combined sort dropdown with emoji icons - Responsive adjustments for all screen sizes</p> <p>Mixtape Cards: - Responsive card layout \u2014 Flexbox with wrapping, subtle shadows, and a hover lift effect - Action buttons \u2014 Circular, color-coded (edit=primary, play=success, share=info, delete=danger). Hover scales the button - Mobile adjustments \u2014 Smaller cover size, reduced button dimensions, and a stacked layout for very narrow viewports (<code>max-width: 480px</code>)</p> <p>Animations: - Slide-down animation for search result banner - Smooth hover transitions on cards and buttons - Subtle lift effect on card hover</p>"},{"location":"development/routes/browse_mixtapes.html#javascript-modules","title":"JavaScript Modules","text":"File Exported function(s) Purpose <code>search.js</code> <code>initSearch()</code> Handles hybrid search: instant title filtering (client-side), deep search button click, clear search, and Enter key support. Disables client-side filtering when viewing deep search results. <code>sorting.js</code> <code>initSorting()</code> Handles combined sort dropdown changes, updates URL with sort parameters, preserves search state during sort changes. <code>deleteMixtape.js</code> <code>initDeleteMixtape()</code> Handles the delete workflow: opens the confirmation modal, sends a POST <code>/mixtapes/delete/&lt;slug&gt;</code> request, shows success toast, and reloads the page. <code>index.js</code> \u2014 Imports and initializes all modules on <code>DOMContentLoaded</code>: search, sorting, delete, QR share, and Bootstrap tooltips. <p>Common Module (Shared): - <code>../common/qrShare.js</code> - Provides QR code generation, modal display, copy-to-clipboard, and download functionality. Used across browser, editor, and player pages.</p> <p>All scripts are ES6 modules (<code>type=\"module\"</code>), ensuring they are loaded after the DOM is ready and that they don't pollute the global namespace.</p>"},{"location":"development/routes/browse_mixtapes.html#search-implementation-details","title":"Search Implementation Details","text":"<p>Client-Side Title Search: </p><pre><code>function filterByTitle(query) {\n    // Skip if on deep search results page\n    if (isDeepSearchActive) return;\n\n    mixtapeItems.forEach(item =&gt; {\n        const title = item.querySelector('.mixtape-title').textContent;\n        if (title.toLowerCase().includes(query.toLowerCase())) {\n            item.style.display = originalDisplays.get(item);\n        } else {\n            item.style.display = 'none';\n        }\n    });\n}\n</code></pre><p></p> <p>Deep Search Navigation: </p><pre><code>deepSearchBtn.addEventListener('click', () =&gt; {\n    const url = new URL(window.location.href);\n    url.searchParams.set('search', query);\n    url.searchParams.set('deep', 'true');\n    // Preserve sort parameters\n    window.location.href = url.toString();\n});\n</code></pre><p></p> <p>Key Features: - Instant filtering respects search state (doesn't interfere with server-side results) - Clear button removes both client-side filters and navigates away from deep search - Enter key triggers deep search - URL parameters maintain complete state (search + sort)</p>"},{"location":"development/routes/browse_mixtapes.html#class-sequence-diagrams","title":"\ud83d\udcca Class &amp; Sequence Diagrams","text":""},{"location":"development/routes/browse_mixtapes.html#class-diagram","title":"Class Diagram","text":"<pre><code>classDiagram\n    class browse_mixtapes_Blueprint {\n        +browse()\n        +play(slug)\n        +files(filename)\n        +delete_mixtape(slug)\n        +blueprint_require_auth()\n        -_deep_search_mixtapes()\n    }\n    class MixtapeManager {\n        +list_all()\n        +delete(slug)\n    }\n    class AuthSystem {\n        +require_auth()\n        +check_auth()\n    }\n    class SearchSystem {\n        +initSearch()\n        +filterByTitle()\n        +deepSearch()\n    }\n    class SortSystem {\n        +initSorting()\n        +updateURL()\n    }\n\n    browse_mixtapes_Blueprint --&gt; MixtapeManager : uses\n    browse_mixtapes_Blueprint --&gt; AuthSystem : enforces authentication\n    browse_mixtapes_Blueprint --&gt; SearchSystem : client-side filtering\n    browse_mixtapes_Blueprint --&gt; SortSystem : URL parameter management</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom"},{"location":"development/routes/browse_mixtapes.html#sequence-diagram-searching-and-sorting-mixtapes","title":"Sequence Diagram - Searching and Sorting Mixtapes","text":"<pre><code>sequenceDiagram\n    participant User\n    participant BrowserJS\n    participant FlaskApp\n    participant Blueprint as browse_mixtapes\n    participant MixtapeManager\n\n    User-&gt;&gt;BrowserJS: Types in search box\n    BrowserJS-&gt;&gt;BrowserJS: Filter by title (instant)\n\n    User-&gt;&gt;BrowserJS: Clicks \"Tracks\" button\n    BrowserJS-&gt;&gt;FlaskApp: GET /mixtapes/?search=rock&amp;deep=true&amp;sort_by=title&amp;sort_order=asc\n    FlaskApp-&gt;&gt;Blueprint: browse(search='rock', deep='true', ...)\n    Blueprint-&gt;&gt;MixtapeManager: list_all()\n    MixtapeManager--&gt;&gt;Blueprint: all mixtapes\n    Blueprint-&gt;&gt;Blueprint: _deep_search_mixtapes(mixtapes, 'rock')\n    Blueprint-&gt;&gt;Blueprint: sort by title ascending\n    Blueprint--&gt;&gt;FlaskApp: render template with filtered results\n    FlaskApp--&gt;&gt;User: HTML page with search results</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom"},{"location":"development/routes/browse_mixtapes.html#sequence-diagram-deleting-a-mixtape","title":"Sequence Diagram - Deleting a Mixtape","text":"<pre><code>sequenceDiagram\n    participant User\n    participant BrowserJS\n    participant FlaskApp\n    participant browse_mixtapes as Blueprint\n    participant MixtapeManager\n\n    User-&gt;&gt;BrowserJS: Click Delete \u2192 opens modal\n    BrowserJS-&gt;&gt;FlaskApp: POST /mixtapes/delete/&lt;slug&gt;\n    FlaskApp-&gt;&gt;Blueprint: delete_mixtape(slug)\n    Blueprint-&gt;&gt;MixtapeManager: delete(slug)\n    MixtapeManager--&gt;&gt;Blueprint: success\n    Blueprint--&gt;&gt;FlaskApp: JSON {success:true}\n    FlaskApp--&gt;&gt;BrowserJS: JSON response\n    BrowserJS-&gt;&gt;BrowserJS: Show success toast, reload page</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom"},{"location":"development/routes/browse_mixtapes.html#user-experience-features","title":"\ud83c\udfaf User Experience Features","text":""},{"location":"development/routes/browse_mixtapes.html#search-flow","title":"Search Flow","text":"<ol> <li>Quick Title Search:</li> <li>User types in search box</li> <li>Results filter instantly (no page reload)</li> <li>Clear button appears</li> <li> <p>Tracks button becomes enabled</p> </li> <li> <p>Deep Search:</p> </li> <li>User clicks \"Tracks\" button (or presses Enter)</li> <li>Page reloads with <code>?search=query&amp;deep=true</code></li> <li>Server searches all metadata</li> <li>Results include mixtapes with matching songs/artists/albums</li> <li> <p>Banner shows \"Deep search results for: {query}\"</p> </li> <li> <p>Clear Search:</p> </li> <li>Click clear button (X)</li> <li>Returns to full mixtape list</li> <li>Preserves sort settings</li> </ol>"},{"location":"development/routes/browse_mixtapes.html#sort-flow","title":"Sort Flow","text":"<ol> <li>Select Sort Option:</li> <li>User selects from dropdown (e.g., \"\ud83c\udfb5 Most Tracks\")</li> <li>Page reloads with new sort parameters</li> <li> <p>Search state is preserved if active</p> </li> <li> <p>Combined with Search:</p> </li> <li>Search results can be re-sorted</li> <li>Sort order maintained during searches</li> <li>URL reflects complete state</li> </ol>"},{"location":"development/routes/browse_mixtapes.html#mobile-optimizations","title":"Mobile Optimizations","text":"<ul> <li>Compact Controls: 60% less vertical space than desktop</li> <li>Touch-Friendly: All buttons meet WCAG size standards</li> <li>Smart Layout: Inputs stack appropriately</li> <li>Visual Clarity: Emoji icons aid recognition</li> <li>Performance: Client-side filtering remains instant</li> </ul>"},{"location":"development/routes/browse_mixtapes.html#api","title":"\ud83d\udccc API","text":""},{"location":"development/routes/browse_mixtapes.html#src.routes.browse_mixtapes","title":"<code>src.routes.browse_mixtapes</code>","text":"<p>Functions:</p> Name Description <code>create_browser_blueprint</code> <p>Creates and configures the Flask blueprint for browsing, playing, and managing mixtapes.</p>"},{"location":"development/routes/browse_mixtapes.html#src.routes.browse_mixtapes.create_browser_blueprint","title":"<code>create_browser_blueprint(mixtape_manager, func_processing_status, logger=None)</code>","text":"<p>Creates and configures the Flask blueprint for browsing, playing, and managing mixtapes.</p> <p>Sets up routes for listing mixtapes, serving cover images and files, deleting mixtapes, and handling authentication for all routes in the blueprint.</p> <p>Parameters:</p> Name Type Description Default <code>mixtape_manager</code> <code>MixtapeManager</code> <p>The manager instance for retrieving and managing mixtapes.</p> required <code>func_processing_status</code> <code>any</code> <p>A function to check the current indexing or processing status.</p> required <code>logger</code> <code>Logger</code> <p>The logger instance for error reporting.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>Blueprint</code> <code>Blueprint</code> <p>The configured Flask blueprint for browsing and managing mixtapes.</p> Source code in <code>src/routes/browse_mixtapes.py</code> <pre><code>def create_browser_blueprint(\n    mixtape_manager: MixtapeManager,\n    func_processing_status: any,\n    logger: Logger | None = None,\n) -&gt; Blueprint:\n    \"\"\"\n    Creates and configures the Flask blueprint for browsing, playing, and managing mixtapes.\n\n    Sets up routes for listing mixtapes, serving cover images and files, deleting mixtapes, and handling authentication for all routes in the blueprint.\n\n    Args:\n        mixtape_manager (MixtapeManager): The manager instance for retrieving and managing mixtapes.\n        func_processing_status (any): A function to check the current indexing or processing status.\n        logger (Logger): The logger instance for error reporting.\n\n    Returns:\n        Blueprint: The configured Flask blueprint for browsing and managing mixtapes.\n    \"\"\"\n    browser = Blueprint(\"browse_mixtapes\", __name__, template_folder=\"../templates\")\n\n    logger: Logger = logger or NullLogger()\n\n    def _deep_search_mixtapes(mixtapes: list[dict], query: str) -&gt; list[dict]:\n        \"\"\"\n        Performs a deep search across mixtape tracks, artists, albums, and liner notes.\n\n        Args:\n            mixtapes: List of mixtape dictionaries to search\n            query: Search query string (case-insensitive)\n\n        Returns:\n            Filtered list of mixtapes matching the search query\n        \"\"\"\n        if not query:\n            return mixtapes\n\n        query_lower = query.lower()\n        results = []\n\n        for mixtape in mixtapes:\n            # Search in mixtape title\n            if query_lower in mixtape.get('title', '').lower():\n                results.append(mixtape)\n                continue\n\n            # Search in liner notes\n            if query_lower in mixtape.get('liner_notes', '').lower():\n                results.append(mixtape)\n                continue\n\n            # Search within tracks\n            tracks = mixtape.get('tracks', [])\n            found = False\n            for track in tracks:\n                # Search track name\n                if query_lower in track.get('track', '').lower():\n                    found = True\n                    break\n                # Search artist\n                if query_lower in track.get('artist', '').lower():\n                    found = True\n                    break\n                # Search album\n                if query_lower in track.get('album', '').lower():\n                    found = True\n                    break\n\n            if found:\n                results.append(mixtape)\n\n        return results\n\n    @browser.route(\"/\")\n    @require_auth\n    def browse() -&gt; Response:\n        \"\"\"\n        Renders the browse mixtapes page or indexing progress if active.\n\n        Checks for ongoing indexing and shows progress if active. Otherwise, lists all mixtapes.\n        Supports sorting by title, date (created/updated), and track count with ascending/descending order.\n        Supports search by title (client-side) and deep search by tracks/artists/albums (server-side).\n\n        Returns:\n            Response: The rendered template for mixtapes or indexing progress.\n        \"\"\"\n        from flask import request\n\n        # Get sorting parameters from query string\n        sort_by = request.args.get('sort_by', 'updated_at')  # default: most recent\n        sort_order = request.args.get('sort_order', 'desc')  # default: descending\n        search_query = request.args.get('search', '').strip()\n        search_deep = request.args.get('deep', '').lower() == 'true'\n\n        mixtapes = mixtape_manager.list_all()\n\n        # Apply deep search if requested (searches within tracks, artists, albums)\n        if search_query and search_deep:\n            mixtapes = _deep_search_mixtapes(mixtapes, search_query)\n\n        # Apply sorting\n        if sort_by == 'title':\n            mixtapes.sort(key=lambda x: x.get('title', '').lower(), \n                         reverse=(sort_order == 'desc'))\n        elif sort_by == 'created_at':\n            mixtapes.sort(key=lambda x: x.get('created_at') or '', \n                         reverse=(sort_order == 'desc'))\n        elif sort_by == 'updated_at':\n            mixtapes.sort(key=lambda x: x.get('updated_at') or x.get('created_at') or '', \n                         reverse=(sort_order == 'desc'))\n        elif sort_by == 'track_count':\n            mixtapes.sort(key=lambda x: len(x.get('tracks', [])), \n                         reverse=(sort_order == 'desc'))\n\n        return render_template(\"browse_mixtapes.html\", \n                             mixtapes=mixtapes,\n                             sort_by=sort_by,\n                             sort_order=sort_order,\n                             search_query=search_query,\n                             search_deep=search_deep)\n\n    @browser.route(\"/play/&lt;slug&gt;\")\n    @require_auth\n    def play(slug: str) -&gt; Response:\n        \"\"\"\n        Redirects to the public play page for a given mixtape slug.\n\n        Takes the mixtape slug and redirects the user to the corresponding public play route.\n\n        Args:\n            slug: The unique identifier for the mixtape.\n\n        Returns:\n            Response: A redirect response to the public play page for the mixtape.\n        \"\"\"\n        return redirect(url_for(\"public_play\", slug=slug))\n\n    @browser.route(\"/files/&lt;path:filename&gt;\")\n    @require_auth\n    def files(filename: str) -&gt; Response:\n        \"\"\"\n        Serves a mixtape file from the mixtape directory.\n\n        Returns the requested file as a Flask response for download or display.\n\n        Args:\n            filename: The path to the mixtape file within the mixtape directory.\n\n        Returns:\n            Response: A Flask response serving the requested file.\n        \"\"\"\n        return send_from_directory(current_app.config[\"MIXTAPE_DIR\"], filename)\n\n    @browser.route(\"/delete/&lt;slug&gt;\", methods=[\"POST\"])\n    @require_auth\n    def delete_mixtape(slug: str) -&gt; Response:\n        \"\"\"\n        Deletes a mixtape and its associated cover image.\n\n        Removes the mixtape JSON file and cover image from disk if they exist. Returns a 200 response on success or 404 if the mixtape is not found.\n\n        Args:\n            slug: The unique identifier for the mixtape to delete.\n\n        Returns:\n            Response: An empty response with status 200 if successful, or 404 if the mixtape does not exist.\n        \"\"\"\n        try:\n            # First check if it exists\n            json_path = mixtape_manager.path_mixtapes / f\"{slug}.json\"\n            if not json_path.exists():\n                return jsonify({\"success\": False, \"error\": \"Mixtape not found\"}), 404\n\n            mixtape_manager.delete(slug)\n            return jsonify({\"success\": True}), 200\n\n        except Exception as e:\n            logger.exception(\"Error deleting mixtape\")  # if you have logger\n            return jsonify({\"success\": False, \"error\": str(e)}), 500\n\n    @browser.before_request\n    def blueprint_require_auth() -&gt; Response | None:\n        \"\"\"\n        Ensures authentication for all routes in the browse_mixtapes blueprint.\n\n        Checks if the user is authenticated before allowing access to any route in this blueprint. Redirects unauthenticated users to the landing page.\n\n        Returns:\n            Response or None: A redirect response to the landing page if not authenticated, otherwise None.\n        \"\"\"\n        # Protect everything in this blueprint\n        if not check_auth():\n            return redirect(url_for(\"landing\"))\n\n    return browser\n</code></pre>"},{"location":"development/routes/browse_mixtapes.html#configuration","title":"\ud83d\udd27 Configuration","text":"<p>The browse functionality relies on the following configuration values:</p> <ul> <li><code>MIXTAPE_DIR</code>: Directory where mixtape JSON files and covers are stored</li> <li><code>DATA_ROOT</code>: Root directory for application data</li> <li>Session configuration for authentication</li> </ul>"},{"location":"development/routes/browse_mixtapes.html#customization","title":"\ud83c\udfa8 Customization","text":""},{"location":"development/routes/browse_mixtapes.html#adding-new-sort-options","title":"Adding New Sort Options","text":"<ol> <li>Add option to HTML template select dropdown</li> <li>Update <code>browse()</code> function to handle new sort field</li> <li>Ensure proper sorting logic (ascending/descending)</li> </ol>"},{"location":"development/routes/browse_mixtapes.html#modifying-search-behavior","title":"Modifying Search Behavior","text":"<p>Client-side (Title Search): - Edit <code>static/js/browser/search.js</code> - Modify <code>filterByTitle()</code> function</p> <p>Server-side (Deep Search): - Edit <code>_deep_search_mixtapes()</code> in <code>routes/browse_mixtapes.py</code> - Add or modify search fields</p>"},{"location":"development/routes/browse_mixtapes.html#theming","title":"Theming","text":"<p>All colors use CSS custom properties: </p><pre><code>.input-group-text {\n    background-color: var(--bs-body-bg);\n    color: var(--bs-body-color);\n}\n</code></pre><p></p> <p>Override these in your theme CSS to customize appearance.</p>"},{"location":"development/routes/browse_mixtapes.html#responsive-breakpoints","title":"\ud83d\udcf1 Responsive Breakpoints","text":"Breakpoint Width Layout Changes Desktop \u2265992px Search expands, controls align right, single row Tablet 768-991px Two-row layout, natural wrapping Mobile 576-767px Stacked pairs, larger touch targets Tiny &lt;576px Full stack, extra compact sizing"},{"location":"development/routes/editor.html","title":"Editor","text":""},{"location":"development/routes/editor.html#mixtape-editor","title":"Mixtape editor","text":"<p>The file <code>routes/editor.py</code> acts as the backend interface for the mixtape editor UI, managing the lifecycle of mixtape creation and editing, integrating with the music library, and handling file storage for mixtape data and cover images. It ensures that only authenticated users can access editor functionality. Its primary responsibility is to provide endpoints for creating, editing, searching, and saving mixtapes, including handling cover images and metadata. The file uses authentication, interacts with a music collection, and manages mixtape data as JSON files on disk.</p>"},{"location":"development/routes/editor.html#highlevel-overview","title":"\ud83c\udf0d High\u2011Level Overview","text":"<p>The Mixtape\u202fEditor is a full\u2011featured UI that lets a logged\u2011in user:</p> Feature Description Search &amp; add tracks Unchanged \u2013 powered by <code>MusicCollectionUI.search_highlighting</code>. Playlist management Unchanged \u2013 drag-and-drop, reorder, delete, preview. Cover handling Upload a custom image or generate a composite cover from track art. Save / update Creates or updates a mixtape JSON file on disk. Background audio-caching Triggered after a successful save (optional). QR-code sharing After a mixtape is saved, a Share button appears. Clicking it opens a modal that shows a simple QR (preview) and lets the user download an enhanced QR that includes the mixtape\u2019s cover and title. Progress modal Shows background-caching progress via SSE. <p>All of this lives under the Flask blueprint editor (<code>/editor/*</code>) and is protected by the <code>@require_auth</code> decorator.</p>"},{"location":"development/routes/editor.html#flask-blueprint-routes","title":"\ud83d\uddfa\ufe0f Flask Blueprint &amp; Routes","text":"HTTP URL Pattern Handler Key Behaviour <code>GET</code> <code>/editor/</code> <code>new_mixtape()</code> Renders a blank editor (<code>preload_mixtape</code> contains empty fields). <code>GET</code> <code>/editor/&lt;slug&gt;</code> <code>edit_mixtape(slug)</code> Loads mixtape JSON via <code>MixtapeManager.get(slug)</code> and renders the editor pre-filled. <code>GET</code> <code>/editor/search?q=</code> <code>search()</code> Returns up to 50 highlighted results (artist/album/track) as JSON. Minimum query length = 3. <code>GET</code> <code>/editor/artist_details?artist=</code> <code>artist_details()</code> Returns JSON <code>{artist, albums:[{album, cover, tracks,\u2026}], \u2026}</code>. 400 if missing. <code>GET</code> <code>/editor/album_details?release_dir=</code> <code>album_details()</code> Returns JSON <code>{artist, album, tracks:[\u2026], cover, is_compilation}</code>. 400 if missing. <code>POST</code> <code>/editor/save</code> <code>save_mixtape()</code> Accepts a JSON payload (<code>title, cover, liner_notes, tracks, optional slug</code>). Handles create (new UUID) and update (existing slug). Returns <code>{success:true, slug, url,\u2026}</code>. <code>GET</code> <code>/editor/progress/&lt;slug&gt;</code> <code>progress_stream(slug)</code> Server-Sent Events (SSE) that emit <code>{type:\"connected\"}</code> then <code>{step, status, message, current, total}</code> objects. Used by Progress Modal. <code>POST</code> <code>/editor/generate_composite</code> <code>generate_composite()</code> Takes <code>{covers: [url,\u2026]}</code> \u2192 returns <code>{data_url: \"data:image/png;base64,\u2026\"}</code> (composite cover). 400/500 on error. <code>GET</code> <code>/qr/&lt;slug&gt;.png</code> <code>qr.generate_qr(slug)</code> Simple QR code (PNG) that encodes the public mixtape URL. Optional query params: <code>size</code> (max 800) and <code>logo</code> (true/false). <code>GET</code> <code>/qr/&lt;slug&gt;/download</code> <code>qr.download_qr(slug)</code> Enhanced QR code (PNG) that can include the mixtape\u2019s cover art, title banner, and optional logo. Optional query params: <code>size</code> (max 1200), <code>include_cover</code>, <code>include_title</code>. <p>All routes are protected by <code>@require_auth</code>, so unauthenticated users are redirected to the login flow.</p>"},{"location":"development/routes/editor.html#data-flow-serverside-logic","title":"\ud83d\udd04 Data Flow &amp; Server\u2011Side Logic","text":"<ol> <li>Creating a New Mixtape<ul> <li><code>new_mixtape()</code> builds an empty dict (<code>title</code>, <code>cover</code>, <code>liner_notes</code>, <code>tracks</code>, <code>slug</code>, <code>timestamps</code>) and renders <code>editor.html</code> with preload_mixtape set to that dict.</li> <li>The client receives a <code>client\u2011id</code> (generated lazily via <code>crypto.randomUUID()</code> and stored in <code>localStorage[\"current_mixtape_client_id\"])</code> to guarantee idempotent creates.</li> </ul> </li> <li>Editing an Existing Mixtape<ul> <li><code>edit_mixtape(slug)</code> instantiates a <code>MixtapeManager</code> (pointing at <code>app.config[\"MIXTAPE_DIR\"]</code>) and calls <code>get(slug)</code>.</li> <li>The resulting dict (including cover, liner_notes, tracks, timestamps) is passed to the template as preload_mixtape.</li> <li>The hidden  carries the slug so the client knows it is an update.</li> </ul> </li> <li>Saving (<code>save_mixtape</code>)<ul> <li>Payload is parsed from JSON.</li> <li>Track enrichment \u2013 each track\u2019s cover is refreshed via <code>collection.get_cover(release_dir)</code>.</li> <li>If a <code>slug</code> is present \u2192 update (<code>MixtapeManager.update</code>). Otherwise \u2192 create (<code>MixtapeManager.save</code>).</li> <li>After a successful write, a background audio\u2011caching job is launched (if <code>AUDIO_CACHE_PRECACHE_ON_UPLOAD</code> is true) via a daemon thread that calls <code>_trigger_audio_caching_async</code>.</li> <li>The response includes the final slug and a URL (<code>/editor/&lt;slug&gt;</code>).</li> </ul> </li> <li>Progress Streaming (<code>progress_stream</code>)<ul> <li>Uses <code>audio_cache.get_progress_tracker</code> to obtain a <code>ProgressTracker</code>.</li> <li>The generator yields SSE lines (<code>data: {...}\\n\\n</code>).</li> <li>The client consumes these events in <code>progressModal.js</code>.</li> </ul> </li> <li>Composite Cover Generation (<code>generate_composite</code>)<ul> <li>Receives an array of cover URLs, builds a grid composite (via <code>CoverCompositor.generate_grid_composite</code>) and returns a data\u2011URL (<code>image/png;base64,\u2026</code>).</li> </ul> </li> </ol>"},{"location":"development/routes/editor.html#clientside-architecture","title":"\ud83c\udfdb\ufe0f Client\u2011Side Architecture","text":"<p>All editor\u2011related scripts live under <code>static/js/editor/</code>. They are module\u2011scoped (ES6 <code>import</code>/<code>export</code>) and loaded by <code>index.js</code> after the DOM is ready.</p> Module Exported Symbol(s) Core Responsibility <code>index.js</code> \u2013 Bootstraps the whole page: preload mixtape data, initialise EasyMDE, search, playlist, UI, and set the initial \u201cLiner-Notes\u201d sub-tab. <code>search.js</code> <code>initSearch</code> Debounced search input \u2192 <code>/editor/search</code> \u2192 render grouped results (artists, albums, tracks). Handles lazy loading of album/artist details, \u201cAdd\u201d buttons, and preview play/pause. <code>playlist.js</code> <code>playlist, initPlaylist, addToPlaylist, setPlaylist, register*Callback</code> Manages the playlist array, renders the sortable list, handles track-play preview, removal, and \u201cAdd whole album\u201d actions. Emits callbacks for unsaved-changes and toast notifications. <code>editorNotes.js</code> <code>initEditorNotes</code> Instantiates EasyMDE, wires up the preview pane, and registers a custom <code>previewRender</code> that expands #1, #2-4 references using the current playlist. <code>ui.js</code> <code>initUI, activateInitialNotesTab</code> Handles cover upload/composite modal, Save button (including client-id handling), floating-button behaviour, unsaved-changes detection, navigation guard, and the bottom audio player. <code>progressModal.js</code> <code>showProgressModal</code> Constructs a Bootstrap modal that displays a progress bar, log of caching events, and a \u201cClose\u201d button that appears only after completion. Connects to the SSE endpoint. <code>utils.js</code> <code>escapeHtml, escapeRegExp, highlightText, showAlert, showConfirm, renderTrackReferences, htmlSafeJson</code> Miscellaneous helpers used across the UI (HTML escaping, markdown rendering, modal dialogs). <code>index.js</code> \u2013 Bootstraps the whole page: preload mixtape data, initialise EasyMDE, search, playlist, UI, QR sharing (via common module), and set the initial \"Liner-Notes\" sub-tab. <code>coverCompositor.js</code> (via <code>utils.CoverCompositor</code>) \u2013 Generates a composite cover image from a set of track covers (used by <code>/editor/generate_composite</code>). <code>qrShare.js</code> <code>static/js/common/qrShare.js</code> Shared QR functionality \u2013 Used by browser, editor, and player pages. Handles QR modal display, QR code loading, link copying, and QR download. Eliminates code duplication across pages. <p>All modules share a single source of truth (<code>playlist</code> array) and communicate via callback registration (unsaved\u2011changes, toast notifications). No circular imports occur.</p>"},{"location":"development/routes/editor.html#ui-layout-jinja-template-editorhtml","title":"\ud83d\udda5\ufe0f UI Layout (Jinja Template \u2013 <code>editor.html</code>)","text":"Section Description Header (<code>&lt;h1&gt;</code>) Dynamically shows \u201cCreate Mixtape\u201d, \u201cEdit Mixtape\u201d, or \u201cEdit: <code>title</code>\u201d. Search Bar Large pill-shaped input with a search icon, info-popover (advanced search tips), and a loading spinner. Results Column (<code>col-lg-7</code>) Card titled Library \u2013 initially shows a placeholder; populated by <code>search.js</code> with artists, albums, and tracks. Mixtape Column (<code>col-lg-5</code>) Card titled My Mixtape \u2013 contains cover image + upload button, title textarea, tabs (Tracks / Liner\u202fNotes), playlist <code>&lt;ol&gt;</code> (rendered by <code>playlist.js</code>), and a Clear button. Floating Buttons (mobile only) \u201cSave\u201d (hidden until unsaved) and \u201cTracks\u201d (always visible) \u2013 positioned bottom-right. Bottom Audio Player (<code>#audio-player-container</code>) Fixed-bottom panel that shows the currently playing preview track (cover, title, artist) and a native <code>&lt;audio&gt;</code> element. Modals Generic <code>appModal</code> (alerts/confirmations), Cover Options modal (Upload\u202fvs\u202fComposite). Toasts \u201cTrack added\u201d, \u201cTrack removed\u201d, and \u201cPublic link copied\u201d toasts (Bootstrap). Progress Modal (injected by <code>progressModal.js</code>) Not in the template; created dynamically when saving. <p>All elements use Bootstrap 5 utilities and custom CSS variables (<code>--color\u2011track</code>, <code>--bs-body-bg</code>, etc.) for light/dark and semantic Theming.</p>"},{"location":"development/routes/editor.html#static-javascript-modules","title":"\ud83e\uddf1 Static JavaScript Modules","text":""},{"location":"development/routes/editor.html#page-load-indexjs","title":"Page Load (<code>index.js</code>)","text":"<pre><code>document.addEventListener(\"DOMContentLoaded\", () =&gt; {\n    // 1\ufe0f\u20e3 Pre\u2011populate playlist, cover, title (if editing)\n    // 2\ufe0f\u20e3 Initialise EasyMDE (with pre\u2011loaded liner notes)\n    // 3\ufe0f\u20e3 Initialise search, playlist UI, and UI glue code\n    // 4\ufe0f\u20e3 Initialise QR share (from common module)\n    //    - autoShow: true for existing mixtapes (has slug)\n    //    - autoShow: false for new mixtapes (hidden until save)\n    // 5\ufe0f\u20e3 Activate the correct Liner\u2011Notes sub\u2011tab (Write vs Preview)\n});\n</code></pre> <ul> <li>Pre\u2011load data \u2013 <code>window.PRELOADED_MIXTAPE</code> (populated by Flask) is read.</li> <li>Playlist \u2013 <code>setPlaylist(preloadMixtape.tracks)</code> populates the UI list.</li> <li>Cover &amp; Title \u2013 If a cover URL exists, it is set on <code>#playlist-cover</code>; title is placed in the textarea (<code>#playlist-title</code>).</li> <li>Unsaved\u2011Changes Reset \u2013 When editing an existing mixtape (<code>slug</code> present) the stored <code>client_id</code> is kept; for a brand\u2011new mixtape the local\u2011storage <code>current_mixtape_client_id</code> is cleared.</li> <li>Editor (EasyMDE) \u2013 <code>initEditorNotes(preloadMixtape.liner_notes)</code> creates the markdown editor with the appropriate initial value.</li> <li>UI Init \u2013 <code>initSearch()</code>, <code>initPlaylist()</code>, <code>initUI()</code> wire up all interactive pieces (search bar, playlist drag\u2011&amp;\u2011drop, floating buttons, unsaved\u2011change detection, etc.).</li> <li>Liner\u2011Notes Tab \u2013 <code>activateInitialNotesTab(hasNotes)</code> selects Preview if there are saved notes, otherwise Write.</li> </ul>"},{"location":"development/routes/editor.html#library-search-searchjs","title":"Library Search (<code>search.js</code>)","text":"<ul> <li>Debounced input \u2013 300\u202fms after the user stops typing, a request to <code>/editor/search?q=</code> is sent.</li> <li>Results Rendering \u2013 Returned JSON is grouped by <code>type</code> (<code>artist</code>, <code>album</code>, <code>track</code>).</li> <li>Artists \u2192 collapsible accordion (<code>bg-artist</code>). Clicking expands to lazily load the artist\u2019s albums via <code>/editor/artist_details</code>.</li> <li>Albums \u2192 collapsible accordion (<code>bg-album</code>). Expanding loads tracks via <code>/editor/album_details</code>.</li> <li>Tracks \u2192 list items with a preview button (plays a short preview via the global audio player) and an add button (pushes the track into the playlist).</li> <li>Add\u2011Album \u2013 Inside an album accordion a \u201cAdd whole album\u201d button adds all tracks at once.</li> <li>Highlighting \u2013 Search terms are wrapped in <code>&lt;mark&gt;</code> by the back\u2011end (<code>MusicCollection.search_highlighting</code>). The UI adds extra colour classes for visual distinction.</li> </ul>"},{"location":"development/routes/editor.html#qr-share-integration-commonqrsharejs","title":"QR Share Integration (<code>common/qrShare.js</code>)","text":"<ul> <li>Module location \u2013 <code>static/js/common/qrShare.js</code> (shared across browser, editor, and player)</li> <li>Modal definition \u2013 <code>#qrShareModal</code> in <code>base.html</code> (global, no duplication)</li> <li>Button visibility logic \u2013</li> <li>New mixtape: Share button starts hidden (<code>autoShow: false</code>)</li> <li>Existing mixtape: Share button shown immediately (<code>autoShow: true</code>)</li> <li>After save: <code>mixtape-saved</code> event triggers button visibility update</li> <li>Slug detection \u2013 Checks <code>#editing-slug</code> input value, falls back to <code>window.PRELOADED_MIXTAPE.slug</code></li> <li>QR display \u2013 Loads <code>/qr/&lt;slug&gt;.png</code> (simple QR for preview)</li> <li>Download \u2013 Calls <code>/qr/&lt;slug&gt;/download</code> (enhanced QR with cover art and title)</li> <li>Copy link \u2013 Copies <code>/play/share/&lt;slug&gt;</code> to clipboard with toast notification</li> </ul> <p>Integration in <code>index.js</code>:</p> <pre><code>// Determine if existing mixtape (has slug)\nconst isExistingMixtape = Boolean(\n    (preloadMixtape &amp;&amp; preloadMixtape.slug) ||\n    document.getElementById('editing-slug')?.value\n);\n\ninitQRShare({\n    shareButtonSelector: '#share-playlist',\n    modalId: 'qrShareModal',\n    getSlug: () =&gt; {\n        const editingInput = document.getElementById('editing-slug');\n        if (editingInput &amp;&amp; editingInput.value) return editingInput.value;\n        if (window.PRELOADED_MIXTAPE?.slug) return window.PRELOADED_MIXTAPE.slug;\n        return null;\n    },\n    autoShow: isExistingMixtape  // Show immediately for existing, hide for new\n});\n</code></pre> <p>Save integration in <code>ui.js</code>: After successful save, trigger the share button visibility:</p> <pre><code>// In the save function after success\ndocument.getElementById('editing-slug').value = data.slug;\ndocument.dispatchEvent(new CustomEvent('mixtape-saved', {\n    detail: { slug: data.slug }\n}));\n</code></pre>"},{"location":"development/routes/editor.html#playlist-playlistjs","title":"Playlist (<code>playlist.js</code>)","text":"<ul> <li>Data model \u2013 <code>window.playlist</code> is a plain array of objects <code>{artist, album, track, duration, path, filename, cover}</code>.</li> <li>Rendering \u2013 <code>renderPlaylist()</code> builds an ordered list (<code>&lt;ol id=\"playlist\"&gt;</code>) with:</li> <li>Drag handle (<code>.drag-handle</code>) for reordering.</li> <li>Cover thumbnail (or placeholder).</li> <li>Play\u2011preview overlay button (<code>.play-overlay-btn</code>).</li> <li>Track title, artist\u202f\u00b7\u202falbum, duration, and a delete button.</li> <li>Drag\u2011&amp;\u2011Drop \u2013 Powered by Sortable.js; after a reorder the array is rebuilt from the DOM order.</li> <li>Play\u2011preview \u2013 Clicking a track\u2019s overlay button:</li> <li>Loads the file into the global audio player (<code>#global-audio-player</code>).</li> <li>Shows the bottom player container (<code>#audio-player-container</code>).</li> <li>Updates \u201cNow playing\u201d title/artist and cover image.</li> <li>Toggles the button icon between play (<code>bi-play-fill</code>) and pause (<code>bi-pause-fill</code>).</li> <li>Add / Remove callbacks \u2013 <code>registerTrackAddedCallback</code> and <code>registerTrackRemovedCallback</code> fire toast notifications (<code>#addTrackToast</code>, <code>#removeTrackToast</code>).</li> </ul>"},{"location":"development/routes/editor.html#linernotes-editornotesjs","title":"Liner\u2011Notes (<code>editorNotes.js</code>)","text":"<ul> <li>EasyMDE \u2013 Configured with a custom toolbar, spell\u2011checker disabled, and a preview renderer that expands <code>#1</code>, <code>#2\u20114</code>, etc. using the current playlist (<code>renderTrackReferences</code>).</li> <li>Live preview \u2013 When the Preview tab becomes visible, the markdown is rendered with marked \u2192 DOMPurify \u2192 inserted into <code>#markdown-preview</code>.</li> <li>Two\u2011way sync \u2013 Switching back to Write retains the editor\u2019s current value; changes in the editor automatically update the preview when the tab is active.</li> </ul>"},{"location":"development/routes/editor.html#save-workflow-uijs","title":"Save Workflow (<code>ui.js</code>)","text":"<ol> <li>Mark unsaved \u2013 Any mutation (cover upload, title edit, playlist change, liner\u2011notes edit) calls <code>markUnsaved()</code>.</li> <li>Badge &amp; floating button \u2013 An \u201cUnsaved\u201d badge appears on the top\u2011right Save button; a floating Save button (<code>#floating-save</code>) appears on mobile.</li> <li>Client\u2011ID handling \u2013</li> <li>For a new mixtape a UUID (<code>crypto.randomUUID()</code>) is stored in <code>localStorage.current_mixtape_client_id</code>.</li> <li>The same ID is reused for subsequent saves, guaranteeing idempotent creation.</li> <li>POST <code>/editor/save</code> \u2013 Payload includes:</li> <li><code>title</code>, <code>cover</code> (data\u2011URL or <code>null</code>), <code>liner_notes</code>, <code>tracks</code> (plain objects), optional <code>slug</code>, and <code>client_id</code>.</li> <li>Server response \u2013 Returns <code>{success:true, slug, url,\u2026}</code>.</li> <li>Open progress modal \u2013 <code>showProgressModal(slug)</code> displays the Progress Modal.</li> </ol>"},{"location":"development/routes/editor.html#cover-generation-upload-uijs-editorgenerate_composite","title":"Cover Generation / Upload (<code>ui.js</code> + <code>/editor/generate_composite</code>)","text":"<ul> <li>Cover Options Modal \u2013 Clicking the camera button opens <code>#coverOptionsModal</code> with two choices:</li> <li>Upload Image \u2013 Triggers the hidden file input (<code>#cover-upload</code>). Validation checks file type (<code>jpg/png/gif/webp</code>) and size (\u2264\u202f5\u202fMiB). On success the image is read as a data\u2011URL and displayed.</li> <li>Generate Composite \u2013 Sends the list of track cover URLs (<code>playlist.map(t=&gt;t.cover)</code>) to <code>/editor/generate_composite</code>. The server returns a data\u2011URL PNG which replaces the current cover.</li> <li>Both actions set <code>coverDataUrl</code> and call <code>markUnsaved()</code>.</li> </ul>"},{"location":"development/routes/editor.html#navigation-guard-browser-warning","title":"Navigation Guard &amp; Browser Warning","text":"<ul> <li>Link interception \u2013 Any internal <code>&lt;a&gt;</code> click while <code>hasUnsavedChanges</code> is <code>true</code> shows a confirmation modal (<code>showConfirm</code>). If the user confirms, navigation proceeds; otherwise it is cancelled.</li> <li><code>beforeunload</code> \u2013 Browsers display a native \u201cYou have unsaved changes\u2026\u201d dialog when the user tries to close, refresh, or navigate away.</li> </ul>"},{"location":"development/routes/editor.html#reorder-mode","title":"Reorder Mode","text":"<ul> <li>Toggle button \u2013 <code>#toggle-reorder-mode</code> adds/removes the class <code>reorder-mode</code> on <code>&lt;body&gt;</code>.</li> <li>Effects \u2013</li> <li>Hides the library column (<code>.col-lg-7</code>).</li> <li>Expands the mixtape column to full width.</li> <li>Enlarges the playlist area for easier drag\u2011&amp;\u2011drop.</li> <li>Hides the cover/title section to maximize vertical space.</li> </ul>"},{"location":"development/routes/editor.html#floating-buttons-mobile","title":"Floating Buttons (Mobile)","text":"<ul> <li>Save \u2013 <code>#floating-save</code> mirrors the top\u2011right Save button; it becomes visible when there are unsaved changes.</li> <li>Tracks \u2013 <code>#floating-tracks</code> jumps to the Tracks tab and scrolls the mixtape card into view.</li> </ul> <p>Summary \u2013 The editor page is a tightly\u2011coupled SPA\u2011style UI built on vanilla JavaScript, Bootstrap, and a handful of third\u2011party libraries (Sortable.js, EasyMDE, marked, DOMPurify). All user actions funnel through the central <code>playlist</code> model, which synchronises the visual list, the global audio player, and the back\u2011end save endpoint. Unsaved\u2011change detection, background audio\u2011caching progress, and cover\u2011generation utilities provide a polished, production\u2011ready experience.</p>"},{"location":"development/routes/editor.html#interaction-flow-typical-user-journey","title":"\ud83d\udee4\ufe0f Interaction Flow (Typical User Journey)","text":"<ol> <li>Open the editor (<code>/editor/</code> or <code>/editor/&lt;slug&gt;</code>).</li> <li>Page loads \u2192 <code>index.js</code> pre\u2011populates playlist, cover, title, and liner notes (if editing).</li> <li>Search \u2192 type \u2265\u202f2 characters \u2192 <code>search.js</code> fetches results \u2192 user clicks Add \u2192 track appears in the playlist (toast shown).</li> <li>Reorder (optional) \u2192 click the expand button \u2192 drag handles appear \u2192 rearrange tracks.</li> <li>Edit title / liner notes \u2192 any change marks the mixtape as unsaved (badge appears, floating save button fades in).</li> <li>Cover \u2192 click camera button \u2192 choose Upload or Generate Composite \u2192 cover preview updates \u2192 unsaved flag set.</li> <li>Preview a track \u2192 click the play overlay \u2192 bottom player appears, showing cover, title, artist; button toggles play/pause.</li> <li>Save \u2192 click Save (or floating save).<ul> <li>Client\u2011id generated (if new).</li> <li>POST <code>/editor/save</code>.</li> <li>Server writes JSON, possibly triggers background audio\u2011caching.</li> <li>UI shows Progress Modal (<code>showProgressModal(slug)</code>).</li> </ul> </li> <li>Progress Modal receives SSE events \u2192 updates progress bar &amp; log.<ul> <li>When <code>completed</code> \u2192 \u201cClose\u201d button enabled \u2192 user</li> </ul> </li> </ol>"},{"location":"development/routes/editor.html#qrcode-sharing","title":"\ud83d\udce4\u202fQR\u2011Code Sharing","text":""},{"location":"development/routes/editor.html#share-button-visibility","title":"Share Button &amp; Visibility","text":"<ul> <li>The Share button (<code>#share-playlist</code>) is hidden (<code>display:none</code>) until the editor has a valid <code>slug</code>.</li> <li>The client\u2011side module <code>static/js/editor/qrShare.js</code> listens for the custom <code>mixtape-saved</code> event (dispatched by <code>ui.js</code> after a successful <code>POST</code> <code>/editor/save</code>). When the event fires, the module calls <code>updateShareButtonVisibility()</code> which sets <code>shareBtn.style.display = ''</code>.</li> <li>If the user opens an existing mixtape (<code>/editor/&lt;slug&gt;</code>), the hidden input <code>#editing-slug</code> already contains the <code>slug</code>, so the button is shown immediately on page load.</li> </ul>"},{"location":"development/routes/editor.html#qr-share-modal","title":"QR\u202fShare\u202fModal","text":"<p>The modal contains three UI states:</p> State Element Behaviour Loading <code>#qr-loading</code> (spinner) Shown while the QR image is being fetched. Success <code>#qr-code-img</code> Displayed once the PNG loads (<code>onload</code> event). Error <code>#qr-error</code> Shown if the image request fails (e.g., 404 or server error). <p>The Copy Link button copies the public mixtape URL (<code>/share/&lt;slug&gt;</code>) to the clipboard and shows a toast (<code>#shareToast</code>). The Download button triggers the enhanced QR endpoint (<code>/qr/&lt;slug&gt;/download?...</code>) and forces a file download with a friendly filename (<code>&lt;Mixtape\u2011Title&gt;-qr-code.png</code>).</p>"},{"location":"development/routes/editor.html#backend-qr-blueprint-qr","title":"Backend QR Blueprint (<code>/qr/...</code>)","text":"<p>Implemented in <code>routes/qr_blueprint.py</code> and exposed via the <code>qr</code> Flask blueprint:</p> Route Method Description <code>/qr/&lt;slug&gt;.png</code> <code>GET</code> Returns a simple QR PNG (<code>size</code> = size px, optional logo). <code>/qr/&lt;slug&gt;/download</code> <code>GET</code> Returns an enhanced QR PNG that can include the mixtape\u2019s cover art, title banner, and optional logo. <p>Both routes:</p> <ul> <li>Validate that the mixtape exists (<code>MixtapeManager.get(slug)</code>).</li> <li>Resolve the optional logo (<code>static/logo.svg</code> \u2192 fallback <code>static/logo.png</code>).</li> <li>For the download endpoint, also resolve the mixtape cover (<code>app.config[\"COVER_DIR\"]</code>).</li> <li>Use the <code>qr_generator</code> package (<code>generate_mixtape_qr</code> / <code>generate_mixtape_qr_with_cover</code>).</li> <li>Return <code>Cache\u2011Control: public, max-age=3600</code> and an appropriate <code>Content\u2011Disposition</code> header (<code>inline</code> for the preview, <code>attachment</code> for the download).</li> </ul> <p>Important</p> <p>The QR blueprint is registered separately from the editor blueprint:</p> <p><code>python   # app.py (excerpt)   from routes.qr_blueprint import create_qr_blueprint   app.register_blueprint(       create_qr_blueprint(mixtape_manager, logger=get_logger(__name__)),       url_prefix=\"/qr\",   )</code></p>"},{"location":"development/routes/editor.html#frontend-integration-staticjseditorqrsharejs","title":"Front\u2011End Integration (<code>static/js/editor/qrShare.js</code>)","text":"<p>Key functions (full source in <code>editorQrShare.js</code>):</p> Function What it does <code>initEditorQRShare()</code> Finds the Share button and modal, registers the mixtape-saved listener, toggles button visibility, and wires up the modal\u2019s Copy and Download actions. <code>showQRModal(modal, slug)</code> Builds the preview URL (<code>/qr/&lt;slug&gt;.png?...</code>), shows the loading spinner, loads the image, and handles success/error UI transitions. <code>downloadQRCode(slug)</code> Calls <code>/qr/&lt;slug&gt;/download?...</code> with <code>size=800&amp;include_cover=true&amp;include_title=true</code>, parses the <code>Content-Disposition</code> header for a friendly filename, and triggers a browser download. <code>copyShareLink()</code> Copies the public share URL (<code>window.location.origin + \"/share/\" + slug</code>) to the clipboard and shows a toast. <code>triggerShare()</code> Public helper that can be called from other scripts (e.g., a keyboard shortcut) to open the QR modal programmatically. <p>The module is imported at the bottom of <code>editor.html</code>:</p> <pre><code>&lt;script type=\"module\" src=\"{{ url_for('static', filename='js/editor/qrShare.js') }}\"&gt;&lt;/script&gt;\n</code></pre>"},{"location":"development/routes/editor.html#example-request-flow","title":"Example Request Flow","text":"<pre><code>sequenceDiagram\n    participant UI as Editor UI\n    participant QRBlueprint as /qr Blueprint\n    participant MM as MixtapeManager\n    participant QRGen as qr_generator\n    participant FS as FileSystem (static folder)\n\n    Note over UI,FS: Simple QR Request Flow\n    UI-&gt;&gt;QRBlueprint: GET /qr/awesome-mixtape.png?size=400&amp;logo=true\n    QRBlueprint-&gt;&gt;MM: get('awesome-mixtape')\n    MM--&gt;&gt;QRBlueprint: mixtape dict\n\n    Note over QRBlueprint,FS: Resolve logo (SVG preferred, fallback PNG, else None)\n    QRBlueprint-&gt;&gt;FS: Check static/logo.svg or static/logo.png\n    alt logo found\n        FS--&gt;&gt;QRBlueprint: logo_path\n    else none\n        FS--&gt;&gt;QRBlueprint: logo_path = None\n    end\n\n    Note over QRBlueprint,QRGen: Build share URL &amp; generate QR\n    QRBlueprint-&gt;&gt;QRBlueprint: share_url = url_for('play.public_play', slug, _external=True)\n    QRBlueprint-&gt;&gt;QRGen: generate_mixtape_qr(url, title, logo_path, size=400)\n    QRGen--&gt;&gt;QRBlueprint: PNG bytes\n\n    Note over QRBlueprint,UI: Return simple QR\n    QRBlueprint-&gt;&gt;UI: 200 OK, image/png, inline filename\n    UI-&gt;&gt;UI: Hide spinner, display img element\n\n    Note over UI,FS: Enhanced QR Request Flow (download)\n    UI-&gt;&gt;QRBlueprint: GET /qr/awesome-mixtape/download?size=800&amp;include_cover=true\n    QRBlueprint-&gt;&gt;MM: get('awesome-mixtape')\n    MM--&gt;&gt;QRBlueprint: mixtape dict\n\n    Note over QRBlueprint,FS: Resolve logo &amp; cover\n    QRBlueprint-&gt;&gt;FS: Check static/logo.svg/logo.png and COVER_DIR\n    alt logo or cover exists\n        FS--&gt;&gt;QRBlueprint: logo_path, cover_path\n    else missing\n        FS--&gt;&gt;QRBlueprint: logo_path/cover_path = None\n    end\n\n    Note over QRBlueprint,QRGen: Generate enhanced QR\n    QRBlueprint-&gt;&gt;QRBlueprint: share_url = url_for('play.public_play', slug, _external=True)\n    QRBlueprint-&gt;&gt;QRGen: generate_mixtape_qr_with_cover(params)\n    QRGen--&gt;&gt;QRBlueprint: PNG bytes\n\n    Note over QRBlueprint,UI: Return downloadable QR\n    QRBlueprint-&gt;&gt;UI: 200 OK, image/png, attachment filename\n    UI-&gt;&gt;UI: Prompt file download dialog</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom  <p>Explanation of the diagram</p> <ol> <li> <p>Simple QR preview \u2013 The editor UI requests <code>/qr/&lt;slug&gt;.png</code>.</p> <ul> <li>The QR blueprint validates the mixtape, resolves an optional logo, builds the public share URL, and calls <code>qr_generator.generate_mixtape_qr</code>.</li> <li>The generated PNG is returned with caching headers; the UI hides the spinner and shows the image inside the modal.</li> </ul> </li> <li> <p>Enhanced QR (download) \u2013 When the user clicks Download, the editor requests <code>/qr/&lt;slug&gt;/download</code> with extra query parameters.</p> <ul> <li>The blueprint additionally resolves the mixtape\u2019s cover image (if it exists) and passes it to <code>generate_mixtape_qr_with_cover</code>.</li> <li>The response includes a Content\u2011Disposition: attachment header so the browser prompts a file\u2011save dialog with a friendly filename.</li> </ul> </li> <li> <p>Error handling (not shown in the happy\u2011path diagram) \u2013</p> <ul> <li>If <code>MixtapeManager.get(slug)</code> returns <code>None</code>, the blueprint aborts with <code>404</code>.</li> <li>If the <code>qrcode</code> library is missing, an <code>ImportError</code> is caught and a <code>500</code> response with a helpful message is sent.</li> <li>Any unexpected exception is logged (<code>logger.exception</code>) and results in a <code>500</code> error.</li> </ul> </li> <li> <p>QR Share Architecture \u2013</p> <ul> <li>The QR modal (<code>#qrShareModal</code>) is defined once in <code>base.html</code> (available globally on all pages)</li> <li>The common module (<code>static/js/common/qrShare.js</code>) handles all QR functionality</li> <li>Each page (browser, editor, player) calls <code>initQRShare()</code> with page-specific config</li> <li>For the editor: Share button is hidden initially (<code>autoShow: false</code>), shown after save</li> <li>After saving, the UI dispatches <code>mixtape-saved</code> event, which triggers share button visibility</li> </ul> </li> </ol> <p>This sequence diagram captures the complete round\u2011trip for both the preview and download QR flows, illustrating how the front\u2011end (now using a common module), Flask blueprint, and QR\u2011generation library collaborate.</p>"},{"location":"development/routes/editor.html#core-helper-functions-backend","title":"\ud83d\udd27 Core Helper Functions (Back\u2011End)","text":"Function File Purpose <code>new_mixtape()</code> <code>editor.py</code> Returns a fresh empty mixtape JSON for the template. <code>edit_mixtape(slug)</code> <code>editor.py</code> Retrieves a mixtape via <code>MixtapeManager.get(slug)</code> and renders the editor with pre-loaded data. <code>search()</code> <code>editor.py</code> Calls <code>collection.search_highlighting(query, limit=50)</code> (the same high-level search used elsewhere). <code>artist_details()</code> <code>editor.py</code> Wrapper around <code>MusicCollection.get_artist_details(artist)</code>. <code>album_details()</code> <code>editor.py</code> Wrapper around <code>MusicCollection.get_album_details(release_dir)</code>. <code>save_mixtape()</code> <code>editor.py</code> Normalises incoming JSON, enriches each track with its cover (<code>collection.get_cover(release_dir)</code>), then either <code>MixtapeManager.update(slug, \u2026)</code> or <code>MixtapeManager.save(...)</code>. <code>_trigger_audio_caching_async()</code> <code>editor.py</code> Fires a background thread that calls <code>schedule_mixtape_caching</code> (from <code>audio_cache</code>) and emits progress events via <code>ProgressTracker</code>. <code>generate_composite()</code> <code>editor.py</code> Calls <code>CoverCompositor.generate_grid_composite(covers)</code> and returns a data-URL. <code>progress_stream(slug)</code> <code>editor.py</code> Returns an SSE Response that streams events from the shared <code>ProgressTracker</code>."},{"location":"development/routes/editor.html#important-behaviours","title":"Important behaviours","text":"<ul> <li>Cover handling \u2013 If the client sends a <code>data:image/...</code> URL, the server decodes, resizes (max\u202f1200\u202fpx width) and stores it as <code>covers/&lt;slug&gt;.jpg</code>.</li> <li>Timestamp handling \u2013 <code>MixtapeManager</code> adds <code>created_at</code> and <code>updated_at</code> ISO\u20118601 timestamps on creation; <code>updated_at</code> is refreshed on each edit.</li> <li>Client\u2011ID reuse \u2013 <code>save_mixtape</code> preserves the original <code>client_id</code> when updating, ensuring idempotent uploads.</li> </ul>"},{"location":"development/routes/editor.html#api-contract-json-schemas","title":"\ud83d\udcc4 API Contract (JSON Schemas)","text":""},{"location":"development/routes/editor.html#save-payload-post-editorsave","title":"Save Payload (<code>POST</code> <code>/editor/save</code>)","text":"<pre><code>{\n  \"title\": \"string (optional, defaults to \\\"Unnamed Mixtape\\\")\",\n  \"cover\": \"string | null \u2013 data:image/*;base64 or null\",\n  \"liner_notes\": \"string (markdown)\",\n  \"tracks\": [\n    {\n      \"artist\": \"string\",\n      \"album\": \"string\",\n      \"track\": \"string\",\n      \"duration\": \"string (MM:SS or seconds)\",\n      \"path\": \"string \u2013 relative to MUSIC_ROOT\",\n      \"filename\": \"string\",\n      \"cover\": \"string | null \u2013 relative URL to cover image\"\n    }\n  ],\n  \"slug\": \"string | null \u2013 present only when editing\",\n  \"client_id\": \"string \u2013 UUID, generated client\u2011side\"\n}\n</code></pre>"},{"location":"development/routes/editor.html#response-success","title":"Response (success)","text":"<pre><code>{\n  \"success\": true,\n  \"title\": \"...\",\n  \"slug\": \"...\",\n  \"client_id\": \"...\",\n  \"url\": \"/editor/&lt;slug&gt;\"\n}\n</code></pre> <p>Error responses \u2013 <code>400</code> for validation errors, <code>404</code> if an edit targets a missing mixtape, <code>500</code> for server failures.</p>"},{"location":"development/routes/editor.html#search-result-excerpt","title":"Search Result (excerpt)","text":"<pre><code>[\n  {\n    \"type\": \"artist\",\n    \"artist\": \"The Beatles\",\n    \"raw_artist\": \"The Beatles\",\n    \"reasons\": [{ \"type\": \"album\", \"text\": \"3 album(s)\" }],\n    \"num_albums\": 3,\n    \"albums\": [],               // lazy\u2011loaded later\n    \"load_on_demand\": true,\n    \"clickable\": true,\n    \"click_query\": \"artist:'The Beatles'\"\n  },\n  {\n    \"type\": \"album\",\n    \"artist\": \"Radiohead\",\n    \"album\": \"OK Computer\",\n    \"raw_album\": \"OK Computer\",\n    \"cover\": \"covers/ok_computer.jpg\",\n    \"is_compilation\": false,\n    \"reasons\": [{ \"type\": \"track\", \"text\": \"12 track(s)\" }],\n    \"num_tracks\": 12,\n    \"tracks\": [],               // lazy\u2011loaded later\n    \"load_on_demand\": true,\n    \"click_query\": \"release_dir:/Radiohead/OK%20Computer/\"\n  },\n  {\n    \"type\": \"track\",\n    \"artist\": \"Pink Floyd\",\n    \"album\": \"The Wall\",\n    \"track\": \"Another Brick in the Wall\",\n    \"duration\": \"5:12\",\n    \"path\": \"Pink Floyd/The Wall/05 Another Brick in the Wall.flac\",\n    \"filename\": \"05 Another Brick in the Wall.flac\",\n    \"cover\": \"covers/the_wall.jpg\",\n    \"highlighted_track\": \"&lt;mark&gt;Another&lt;/mark&gt; Brick in the Wall\"\n  }\n]\n</code></pre>"},{"location":"development/routes/editor.html#sse-progress-event-from-editorprogressslug","title":"SSE Progress Event (from <code>/editor/progress/&lt;slug&gt;</code>)","text":"<pre><code>{\n  \"type\": \"progress\",\n  \"step\": \"caching\",               // initializing | analyzing | caching | completed | error\n  \"status\": \"in_progress\",        // pending | in_progress | completed | failed | skipped\n  \"message\": \"Caching 3/12 files\u2026\",\n  \"current\": 3,\n  \"total\": 12\n}\n</code></pre>"},{"location":"development/routes/editor.html#api","title":"\ud83d\udd0c API","text":""},{"location":"development/routes/editor.html#src.routes.editor","title":"<code>src.routes.editor</code>","text":"<p>Functions:</p> Name Description <code>create_editor_blueprint</code> <p>Creates and configures the Flask blueprint for the mixtape editor.</p>"},{"location":"development/routes/editor.html#src.routes.editor.create_editor_blueprint","title":"<code>create_editor_blueprint(collection, logger=None)</code>","text":"<p>Creates and configures the Flask blueprint for the mixtape editor.</p> <p>Sets up routes for creating, editing, searching, and saving mixtapes, and provides helper functions for cover image and JSON handling.</p> <p>Parameters:</p> Name Type Description Default <code>collection</code> <code>MusicCollection</code> <p>The music collection instance to use for searching.</p> required <code>logger</code> <code>Logger</code> <p>The logger instance for error reporting.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>Blueprint</code> <code>Blueprint</code> <p>The configured Flask blueprint for the editor.</p> Source code in <code>src/routes/editor.py</code> <pre><code>def create_editor_blueprint(\n    collection: MusicCollectionUI, logger: Logger | None = None\n) -&gt; Blueprint:\n    \"\"\"\n    Creates and configures the Flask blueprint for the mixtape editor.\n\n    Sets up routes for creating, editing, searching, and saving mixtapes, and provides helper functions for cover image and JSON handling.\n\n    Args:\n        collection (MusicCollection): The music collection instance to use for searching.\n        logger (Logger): The logger instance for error reporting.\n\n    Returns:\n        Blueprint: The configured Flask blueprint for the editor.\n    \"\"\"\n    editor = Blueprint(\"editor\", __name__)\n\n    logger: Logger = logger or NullLogger()\n\n    @editor.route(\"/\")\n    @require_auth\n    def new_mixtape() -&gt; str:\n        \"\"\"\n        Render de pagina voor het aanmaken van een nieuwe mixtape.\n\n        Geeft het HTML-template terug voor het aanmaken van een nieuwe mixtape.\n\n        Returns:\n            str: De gerenderde HTML-pagina voor het aanmaken van een nieuwe mixtape.\n        \"\"\"\n        empty_mixtape = {\n            \"title\": \"\",\n            \"cover\": None,\n            \"liner_notes\": \"\",\n            \"tracks\": [],\n            \"slug\": None,\n            \"created_at\": None,\n            \"updated_at\": None,\n        }\n        return render_template(\"editor.html\", preload_mixtape=empty_mixtape)\n\n    @editor.route(\"/&lt;slug&gt;\")\n    @require_auth\n    def edit_mixtape(slug: str) -&gt; str:\n        \"\"\"\n        Loads and renders the page for editing an existing mixtape.\n\n        Retrieves the mixtape data from a JSON file and returns the HTML template with the mixtape preloaded.\n\n        Args:\n            slug: The unique identifier for the mixtape.\n\n        Returns:\n            str: The rendered HTML page for editing the mixtape.\n        \"\"\"\n        mixtape_manager = MixtapeManager(\n            path_mixtapes=current_app.config[\"MIXTAPE_DIR\"], collection=collection\n        )\n        mixtape = mixtape_manager.get(slug)\n        return render_template(\n            \"editor.html\", preload_mixtape=mixtape, editing_slug=slug\n        )\n\n    @editor.route(\"/search\")\n    @require_auth\n    def search() -&gt; object:\n        \"\"\"\n        Searches the music collection and returns the results.\n\n        Receives a search query, searches the collection, and returns the results as JSON.\n        Includes comprehensive error handling to ensure JSON is always returned.\n\n        Returns:\n            Response: A JSON response containing the search results or error information.\n        \"\"\"\n        try:\n            query = request.args.get(\"q\", \"\").strip()\n            if len(query) &lt; 2:\n                return jsonify([])\n\n            # Log the search query for debugging\n            logger.debug(f\"Search query: {query}\")\n\n            results = collection.search_highlighting(query, limit=50)\n            return jsonify(results)\n\n        except Exception as e:\n            # Log the full error with traceback\n            logger.error(f\"Search error for query '{query}': {e}\", exc_info=True)\n\n            # Return a proper JSON error response\n            return jsonify({\n                \"error\": \"Search failed\",\n                \"message\": str(e),\n                \"query\": query\n            }), 500\n\n    @editor.route(\"/artist_details\")\n    @require_auth\n    def artist_details() -&gt; Response:\n        \"\"\"\n        Retrieves and returns detailed information about an artist in JSON format.\n\n        Fetches artist details from the music collection using the artist name from the request arguments and returns them as a JSON response.\n        Handles missing artist errors and returns an error response if necessary.\n\n        Returns:\n            Response: A JSON response containing the artist's details or an error message.\n        \"\"\"\n        artist = request.args.get(\"artist\", \"\").strip()\n        if not artist:\n            return jsonify({\"error\": \"Missing artist\"}), 400\n        details = collection.get_artist_details(artist)\n        return jsonify(details)\n\n    @editor.route(\"/album_details\")\n    @require_auth\n    def album_details() -&gt; Response:\n        \"\"\"\n        Retrieves and returns detailed information about an album in JSON format.\n\n        Fetches album details from the music collection using the release directory from the request arguments and returns them as a JSON response.\n        Handles missing release directory errors and returns an error response if necessary.\n\n        Returns:\n            Response: A JSON response containing the album's details or an error message.\n        \"\"\"\n        release_dir = request.args.get(\"release_dir\", \"\").strip()\n        if not release_dir:\n            return jsonify({\"error\": \"Missing release_dir\"}), 400\n        details = collection.get_album_details(release_dir)\n        return jsonify(details)\n\n    @editor.route(\"/save\", methods=[\"POST\"])\n    @require_auth\n    def save_mixtape() -&gt; object:\n        \"\"\"\n        Saves a new or edited mixtape based on the provided data.\n\n        Handles both creation of new mixtapes and updates to existing ones, including cover image processing and validation.\n        Returns a JSON response indicating success or failure.\n\n        Returns:\n            Response: A JSON response with the result of the save operation.\n        \"\"\"\n        try:\n            data = request.get_json()\n            if not data or not data.get(\"tracks\"):\n                return jsonify({\"error\": \"Empty mixtape\"}), 400\n\n            title = data.get(\"title\", \"\").strip() or \"Unnamed Mixtape\"\n            liner_notes = data.get(\"liner_notes\", \"\")\n            slug = data.get(\"slug\")  # Present only when editing\n\n            # Adding track covers\n            tracks = data.get(\"tracks\", [])\n            for track in tracks:\n                release_dir = collection._get_release_dir(\n                    track.get(\"path\", \"\")\n                )  # Reuse existing helper\n                track[\"cover\"] = collection.get_cover(release_dir)\n\n            # Prepare clean data for the manager\n            mixtape_data = {\n                \"title\": title,\n                \"tracks\": tracks,\n                \"liner_notes\": liner_notes,\n                \"cover\": data.get(\"cover\"),\n            }\n\n            # Instantiate the manager\n            mixtape_manager = MixtapeManager(\n                path_mixtapes=current_app.config[\"MIXTAPE_DIR\"],\n                collection=collection,\n                logger=logger,\n            )\n\n            if slug:\n                # Editing\n                existing = mixtape_manager.get(slug)\n                if not existing:\n                    return jsonify({\"error\": \"Mixtape not found\"}), 404\n\n                mixtape_data.setdefault(\"created_at\", existing.get(\"created_at\"))\n                if mixtape_data[\"cover\"] is None:  # Preserve old cover if no new one\n                    mixtape_data[\"cover\"] = existing.get(\"cover\")\n\n                final_slug = mixtape_manager.update(slug, mixtape_data)\n            else:\n                # Creating\n                mixtape_data[\"created_at\"] = datetime.now().isoformat()\n                final_slug = mixtape_manager.save(mixtape_data)\n\n            # Trigger background audio caching in a separate thread\n            if current_app.config.get(\"AUDIO_CACHE_PRECACHE_ON_UPLOAD\", False):\n                # Copy app reference and context for the thread\n                app = current_app._get_current_object()\n\n                def run_with_context():\n                    with app.app_context():\n                        _trigger_audio_caching_async(\n                            final_slug, mixtape_manager, bool(slug)\n                        )\n\n                threading.Thread(target=run_with_context, daemon=True).start()\n\n            return jsonify(\n                {\n                    \"success\": True,\n                    \"title\": title,\n                    \"slug\": final_slug,\n                    \"client_id\": mixtape_data.get(\"client_id\"),\n                    \"url\": f\"/editor/{final_slug}\",\n                }\n            )\n        except Exception as e:\n            logger.error(f\"Mixtape save error: {e}\")\n            return jsonify({\"error\": str(e)}), 500\n\n    @editor.route(\"/progress/&lt;slug&gt;\")\n    @require_auth\n    def progress(slug: str) -&gt; Response:\n        \"\"\"\n        Server-Sent Events endpoint for real-time progress updates.\n\n        Streams progress events during mixtape caching operations.\n\n        Args:\n            slug: The mixtape slug to track progress for\n\n        Returns:\n            Response: SSE stream of progress events\n        \"\"\"\n        tracker = get_progress_tracker(logger)\n\n        def generate():\n            try:\n                yield from tracker.listen(slug, timeout=300)\n            except Exception as e:\n                logger.error(f\"Progress stream error for {slug}: {e}\")\n                yield f\"data: {{'error': '{str(e)}'}}\\n\\n\"\n\n        return Response(\n            stream_with_context(generate()),\n            mimetype=\"text/event-stream\",\n            headers={\n                \"Cache-Control\": \"no-cache\",\n                \"X-Accel-Buffering\": \"no\",  # Disable nginx buffering\n            },\n        )\n\n    def _trigger_audio_caching_async(\n        slug: str, mixtape_manager: MixtapeManager, is_update: bool = False\n    ) -&gt; None:\n        \"\"\"\n        Triggers background audio caching with progress tracking.\n\n        This runs in a separate thread to avoid blocking the save response.\n\n        Args:\n            slug: The unique identifier for the mixtape.\n            mixtape_manager: The MixtapeManager instance to retrieve mixtape data.\n            is_update: Whether this is an update to an existing mixtape.\n        \"\"\"\n        tracker = get_progress_tracker(logger)\n\n        try:\n            # Emit initial event\n            tracker.emit(\n                task_id=slug,\n                step=\"initializing\",\n                status=ProgressStatus.IN_PROGRESS,\n                message=\"Starting cache process...\",\n                current=0,\n                total=1,\n            )\n\n            # Check if audio_cache is available\n            if not hasattr(current_app, \"audio_cache\"):\n                tracker.emit(\n                    task_id=slug,\n                    step=\"error\",\n                    status=ProgressStatus.FAILED,\n                    message=\"Audio cache not initialized\",\n                    current=0,\n                    total=0,\n                )\n                logger.warning(\"Audio cache not initialized, skipping pre-caching\")\n                return\n\n            # Get the saved mixtape data\n            saved_mixtape = mixtape_manager.get(slug)\n\n            if not saved_mixtape or not saved_mixtape.get(\"tracks\"):\n                tracker.emit(\n                    task_id=slug,\n                    step=\"completed\",\n                    status=ProgressStatus.COMPLETED,\n                    message=\"No tracks to cache\",\n                    current=1,\n                    total=1,\n                )\n                logger.debug(f\"No tracks to cache for mixtape: {slug}\")\n                return\n\n            tracks = saved_mixtape[\"tracks\"]\n            total_tracks = len(tracks)\n\n            # Emit starting cache event\n            tracker.emit(\n                task_id=slug,\n                step=\"analyzing\",\n                status=ProgressStatus.IN_PROGRESS,\n                message=f\"Analyzing {total_tracks} tracks...\",\n                current=0,\n                total=total_tracks,\n            )\n\n            # Get configuration\n            qualities = current_app.config.get(\n                \"AUDIO_CACHE_PRECACHE_QUALITIES\", [\"medium\"]\n            )\n            music_root = Path(current_app.config[\"MUSIC_ROOT\"])\n\n            logger.debug(\n                f\"Starting pre-cache for mixtape '{slug}' \"\n                f\"({'update' if is_update else 'new'}) with {total_tracks} tracks\"\n            )\n\n            # Create progress callback\n            progress_callback = ProgressCallback(slug, tracker, total_tracks)\n\n            # Start caching\n            results = schedule_mixtape_caching(\n                mixtape_tracks=tracks,\n                music_root=music_root,\n                audio_cache=current_app.audio_cache,\n                logger=logger,\n                qualities=qualities,\n                async_mode=True,\n                progress_callback=progress_callback,\n            )\n\n            # Small delay to ensure all progress events are queued\n            time.sleep(0.2)\n\n            # Analyze results\n            total_files = len(results)\n\n            logger.debug(f\"Processing completed: {total_files} results returned\")\n\n            if total_files == 0:\n                # No results means something went wrong\n                tracker.emit(\n                    task_id=slug,\n                    step=\"completed\",\n                    status=ProgressStatus.COMPLETED,\n                    message=\"No files processed (empty results)\",\n                    current=total_tracks,\n                    total=total_tracks,\n                )\n                logger.warning(f\"No results returned for mixtape '{slug}'\")\n                return\n\n            # Count successful operations (cached or skipped)\n            cached = sum(\n                isinstance(r, dict)\n                and any(k not in [\"skipped\", \"reason\"] for k in r.keys())\n                for r in results.values()\n            )\n            skipped = sum(\n                bool(isinstance(r, dict) and r.get(\"skipped\")) for r in results.values()\n            )\n            failed = total_files - cached - skipped\n\n            # Build completion message\n            parts = []\n            if cached &gt; 0:\n                parts.append(f\"{cached} cached\")\n            if skipped &gt; 0:\n                parts.append(f\"{skipped} skipped\")\n            if failed &gt; 0:\n                parts.append(f\"{failed} failed\")\n\n            message = f\"Complete! {', '.join(parts) if parts else 'No files processed'}\"\n\n            # Emit completion event\n            tracker.emit(\n                task_id=slug,\n                step=\"completed\",\n                status=ProgressStatus.COMPLETED,\n                message=message,\n                current=total_files,\n                total=total_files,\n            )\n\n            # Small delay to ensure completion event is queued\n            time.sleep(0.1)\n\n            logger.debug(\n                f\"Pre-caching completed for '{slug}': \"\n                f\"cached={cached}, skipped={skipped}, failed={failed}\"\n            )\n\n        except Exception as e:\n            # Emit error event\n            tracker.emit(\n                task_id=slug,\n                step=\"error\",\n                status=ProgressStatus.FAILED,\n                message=f\"Caching failed: {str(e)}\",\n                current=0,\n                total=0,\n            )\n            logger.error(f\"Pre-caching failed for mixtape '{slug}': {e}\")\n            logger.exception(\"Detailed error:\")\n\n    @editor.route(\"/generate_composite\", methods=[\"POST\"])\n    @require_auth\n    def generate_composite() -&gt; Response:\n        \"\"\"\n        Generates a composite cover image from a list of individual cover images.\n\n        Accepts a JSON payload containing cover identifiers, composes them into a grid image, and returns the result as a data URL.\n        Validates the input list and returns JSON error responses for invalid requests or generation failures.\n\n        Returns:\n            Response: A JSON response containing the composite image data URL or an error message.\n        \"\"\"\n        data = request.get_json()\n        if not data or not isinstance(data.get(\"covers\"), list):\n            return jsonify({\"error\": \"Missing covers list\"}), 400\n\n        covers = data[\"covers\"]\n        if not covers:\n            return jsonify({\"error\": \"No covers provided\"}), 400\n\n        try:\n            compositor = CoverCompositor(collection.covers_dir)\n            data_url = compositor.generate_grid_composite(covers)\n            return jsonify({\"data_url\": data_url})\n        except ValueError as e:\n            return jsonify({\"error\": str(e)}), 400\n        except Exception as e:\n            logger.error(f\"Composite generation failed: {e}\")\n            return jsonify({\"error\": \"Failed to generate composite\"}), 500\n\n    return editor\n</code></pre>"},{"location":"development/routes/opengraph_images.html","title":"OpenGraph","text":""},{"location":"development/routes/opengraph_images.html#add-logo-to-cover-for-open-graph","title":"Add logo to cover for Open Graph","text":"<p>The <code>logo_on_cover.py</code> file provides a Flask blueprint that dynamically generates cover images with a logo overlaid in a specified corner as an image that can be used for an Open Graph . It exposes an HTTP endpoint that takes a cover image filename and optional query parameters (logo scale, corner, margin), composites the logo onto the cover, and returns the resulting PNG image. The file also implements caching to optimize repeated requests for the same parameters.</p> <p>The main responsibilities of this file are:</p> <ul> <li>Loading and converting an SVG logo to PNG.</li> <li>Overlaying the logo onto a cover image at a configurable position and scale.</li> <li>Serving the composited image via a Flask route, with input validation and caching.</li> </ul>"},{"location":"development/routes/opengraph_images.html#api","title":"\ud83d\udd0c API","text":""},{"location":"development/routes/opengraph_images.html#src.routes.logo_on_cover","title":"<code>src.routes.logo_on_cover</code>","text":"<p>Functions:</p> Name Description <code>create_og_cover_blueprint</code> <p>Creates a Flask blueprint for serving cover images with a logo overlay.</p> <code>overlay_logo_bytes</code> <p>Overlays a logo onto a cover image and returns the composited image as PNG bytes.</p>"},{"location":"development/routes/opengraph_images.html#src.routes.logo_on_cover.create_og_cover_blueprint","title":"<code>create_og_cover_blueprint(path_logo, logger=None)</code>","text":"<p>Creates a Flask blueprint for serving cover images with a logo overlay.</p> <p>This function sets up routes and logic for handling requests to overlay a logo on cover images, including caching and query parameter validation.</p> <p>Parameters:</p> Name Type Description Default <code>logger</code> <code>Logger | None</code> <p>An optional Logger instance for logging.</p> <code>None</code> <p>Returns:</p> Type Description <code>Blueprint</code> <p>A Flask Blueprint configured for cover image overlay routes.</p> Source code in <code>src/routes/logo_on_cover.py</code> <pre><code>def create_og_cover_blueprint(\n    path_logo: Path, logger: Logger | None = None\n) -&gt; Blueprint:\n    \"\"\"\n    Creates a Flask blueprint for serving cover images with a logo overlay.\n\n    This function sets up routes and logic for handling requests to overlay a logo on cover images,\n    including caching and query parameter validation.\n\n    Args:\n        logger: An optional Logger instance for logging.\n\n    Returns:\n        A Flask Blueprint configured for cover image overlay routes.\n    \"\"\"\n    og = Blueprint(\"og\", __name__)\n\n    logger: Logger = logger or NullLogger()\n\n    @og.route(\"/cover/&lt;path:filename&gt;\")\n    def cover_with_logo(filename):\n        \"\"\"\n        Serves a cover image with a logo overlaid, using query parameters for customization.\n\n        This function locates the requested cover image, validates input parameters, overlays the logo,\n        and returns the composited PNG image. Results are cached for performance.\n\n        Args:\n            filename: The name of the cover image file.\n\n        Returns:\n            A Flask response containing the PNG image with the logo overlaid.\n\n        Raises:\n            Aborts with a 404 error if the cover image is not found.\n            Aborts with a 400 error if query parameters are invalid.\n        \"\"\"\n        cover_path = _get_cover_path(filename)\n        _validate_cover_path(cover_path)\n        logo_scale, corner, margin = _get_query_params()\n        _validate_query_params(logo_scale, corner, margin)\n        # TODO: Add redis caching\n        # Use cover file modification time for cache key\n        # cover_mtime = cover_path.stat().st_mtime\n        # cache_key = f\"{filename}:{cover_mtime}:{logo_scale}:{corner}:{margin}\"\n        # if cached := current_app.cache.get(cache_key):\n        #     return send_file(BytesIO(cached), mimetype=\"image/png\")\n        with open(path_logo, \"rb\") as f:\n            file_logo = f.read()\n        result = overlay_logo_bytes(\n            cover_bytes=cover_path.read_bytes(),\n            svg_bytes=file_logo,\n            logo_scale=logo_scale,\n            corner=corner,\n            margin=margin,\n        )\n        # current_app.cache.set(cache_key, result, timeout=60 * 60 * 24)\n        return send_file(BytesIO(result), mimetype=\"image/png\")\n\n    return og\n</code></pre>"},{"location":"development/routes/opengraph_images.html#src.routes.logo_on_cover.overlay_logo_bytes","title":"<code>overlay_logo_bytes(cover_bytes, svg_bytes, *, logo_scale=0.4, corner='bottom_right', margin=10)</code>","text":"<p>Overlays a logo onto a cover image and returns the composited image as PNG bytes.</p> <p>This function places a logo, provided as SVG bytes, onto a cover image at a specified corner and scale.</p> <p>Parameters:</p> Name Type Description Default <code>cover_bytes</code> <code>bytes</code> <p>The cover image data as bytes.</p> required <code>svg_bytes</code> <code>bytes</code> <p>The SVG logo image data as bytes.</p> required <code>logo_scale</code> <code>float</code> <p>The scale of the logo relative to the shortest side of the cover image.</p> <code>0.4</code> <code>corner</code> <code>str</code> <p>The corner of the cover image to place the logo ('bottom_right', 'bottom_left', 'top_right', 'top_left').</p> <code>'bottom_right'</code> <code>margin</code> <code>int</code> <p>The margin in pixels between the logo and the edge of the cover image.</p> <code>10</code> <p>Returns:</p> Type Description <code>bytes</code> <p>PNG image data as bytes with the logo overlaid.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If an unsupported corner is specified.</p> Source code in <code>src/routes/logo_on_cover.py</code> <pre><code>def overlay_logo_bytes(\n    cover_bytes: bytes,\n    svg_bytes: bytes,\n    *,\n    logo_scale: float = 0.4,\n    corner: str = \"bottom_right\",\n    margin: int = 10,\n) -&gt; bytes:\n    \"\"\"Overlays a logo onto a cover image and returns the composited image as PNG bytes.\n\n    This function places a logo, provided as SVG bytes, onto a cover image at a specified corner and scale.\n\n    Args:\n        cover_bytes (bytes): The cover image data as bytes.\n        svg_bytes (bytes): The SVG logo image data as bytes.\n        logo_scale (float): The scale of the logo relative to the shortest side of the cover image.\n        corner (str): The corner of the cover image to place the logo ('bottom_right', 'bottom_left', 'top_right', 'top_left').\n        margin (int): The margin in pixels between the logo and the edge of the cover image.\n\n    Returns:\n        PNG image data as bytes with the logo overlaid.\n\n    Raises:\n        ValueError: If an unsupported corner is specified.\n    \"\"\"\n    cover = _prepare_cover(cover_bytes)\n    canvas = _create_blurred_canvas(cover)\n\n    orig_width, orig_height = cover.size\n    img_logo = _prepare_logo(svg_bytes, (orig_width, orig_height), logo_scale)\n    x, y = _calculate_logo_position(canvas.size, img_logo.size, corner, margin)\n\n    # Ensure logo has an alpha channel for use as mask\n    if img_logo.mode != \"RGBA\":\n        img_logo = img_logo.convert(\"RGBA\")\n\n    canvas.paste(img_logo, (x, y), img_logo)\n\n    return _save_image_to_bytes(canvas)\n</code></pre>"},{"location":"development/routes/playback.html","title":"Playback","text":"<p>Purpose \u2013 This document describes the audio\u2011streaming subsystem of the Mixtape Society web app. It explains how the Flask blueprint (<code>play</code>) validates requests, resolves file paths, chooses the correct MIME type, handles quality\u2011aware caching, supports HTTP range requests, and serves the public mixtape page. All statements below are verified against the current source files (<code>routes/play.py</code>, the static JavaScript/CSS assets, and the auxiliary <code>audio_cache</code> module).</p>"},{"location":"development/routes/playback.html#highlevel-overview","title":"\ud83c\udf0d High\u2011Level Overview","text":"Responsibility Implementation Validate &amp; resolve the requested audio file path (prevent directory-traversal) <code>_resolve_and_validate_path()</code> Determine MIME type (including custom audio extensions) <code>_guess_mime_type()</code> Select the file to serve \u2013 original or a cached/transcoded version   to the quality query param <code>_get_serving_path()</code> (uses <code>AudioCache</code>) Serve full file or partial byte ranges (seeking) <code>send_file()</code> for full files, <code>_handle_range_request()</code> for partial Return appropriate HTTP status (200, 206, 403, 404, 416, 500) <code>abort()</code> + explicit <code>Response</code> objects Render public mixtape page (<code>/share/&lt;slug&gt;</code>) <code>public_play()</code> Serve cover images (<code>/covers/&lt;filename&gt;</code>) <code>serve_cover()</code> Admin cache utilities (<code>/admin/cache/*</code>) <code>cache_stats()</code>, <code>clear_cache()</code> Generate QR code for sharing <code>qr</code> blueprint (<code>/qr/&lt;slug&gt;.png</code>, <code>/qr/&lt;slug&gt;/download</code>) <p>All routes live under the Flask Blueprint named <code>play</code>.</p>"},{"location":"development/routes/playback.html#flask-blueprint-routes","title":"\ud83d\uddfa\ufe0f Flask Blueprint &amp; Routes","text":"<pre><code>play = Blueprint(\"play\", __name__)   # registered in the main Flask app\n</code></pre> HTTP Method URL Pattern Handler Description <code>GET</code> <code>/play/&lt;path:file_path&gt;</code> <code>stream_audio(file_path)</code> Streams an audio file (original or cached) with optional quality &amp; range support. <code>GET</code> <code>/share/&lt;slug&gt;</code> <code>public_play(slug)</code> Renders <code>play_mixtape.html</code> for a public mixtape; 404 if slug not found. <code>GET</code> <code>/covers/&lt;filename&gt;</code> <code>serve_cover(filename)</code> Serves a cover image from the configured <code>COVER_DIR</code>. <code>GET</code> <code>/qr/&lt;slug&gt;.png</code> <code>qr.generate_qr(slug)</code> Simple QR \u2013 returns a PNG QR code that encodes the public mixtape URL. Optional <code>size</code> and <code>logo</code> query params. <code>GET</code> <code>/qr/&lt;slug&gt;/download</code> <code>qr.download_qr(slug)</code> Enhanced QR \u2013 returns a PNG that includes the mixtape\u2019s cover art, title banner, and optional logo. Optional <code>size</code>, <code>include_cover</code>, <code>include_title</code> query params. <code>GET</code> <code>/admin/cache/stats</code> <code>cache_stats()</code> Returns JSON with cache size (bytes) and number of cached files. <code>POST</code> <code>/admin/cache/clear</code> <code>clear_cache()</code> Clears the audio cache; optional <code>older_than_days</code> query param."},{"location":"development/routes/playback.html#request-flow-detailed-sequence","title":"\ud83d\udd04 Request Flow (Detailed Sequence)","text":"<pre><code>sequenceDiagram\n    participant Client\n    participant FlaskApp\n    participant stream_audio()\n    participant _resolve_and_validate_path()\n    participant _guess_mime_type()\n    participant _get_serving_path()\n    participant AudioCache\n    participant _handle_range_request()\n    participant send_file\n    participant QRBlueprint\n    participant QRGen\n\n    Client-&gt;&gt;FlaskApp: GET /play/&lt;file_path&gt;?quality=medium [&amp; Range]\n    FlaskApp-&gt;&gt;stream_audio(): Call with file_path + query\n    stream_audio()-&gt;&gt;_resolve_and_validate_path(): Validate &amp; resolve\n    _resolve_and_validate_path()--&gt;&gt;stream_audio(): full_path (or abort 403/404)\n    stream_audio()-&gt;&gt;_guess_mime_type(): Guess MIME\n    _guess_mime_type()--&gt;&gt;stream_audio(): mime_type\n    stream_audio()-&gt;&gt;_get_serving_path(): Choose original or cached file (quality)\n    _get_serving_path()-&gt;&gt;AudioCache: cache.is_cached / cache.get_cache_path\n    AudioCache--&gt;&gt;_get_serving_path(): serve_path\n    alt Range header present\n        stream_audio()-&gt;&gt;_handle_range_request(): Serve partial bytes\n        _handle_range_request()--&gt;&gt;stream_audio(): 206 Partial Content response\n        stream_audio--&gt;&gt;FlaskApp: Return 206\n    else No Range header\n        stream_audio-&gt;&gt;send_file: Send full file\n        send_file--&gt;&gt;stream_audio(): 200 OK response\n        stream_audio--&gt;&gt;FlaskApp: Return 200\n    end\n    FlaskApp--&gt;&gt;Client: Stream audio bytes\n\n    %% QR flow -------------------------------------------------\n    Client-&gt;&gt;FlaskApp: GET /qr/&lt;slug&gt;.png?size=400&amp;logo=true\n    FlaskApp-&gt;&gt;QRBlueprint: qr.generate_qr(slug)\n    QRBlueprint-&gt;&gt;QRGen: generate_mixtape_qr(...)\n    QRGen--&gt;&gt;QRBlueprint: PNG bytes\n    QRBlueprint--&gt;&gt;Client: 200 PNG (Cache\u2011Control: public, max\u2011age=3600)\n\n    Client-&gt;&gt;FlaskApp: GET /qr/&lt;slug&gt;/download?size=800&amp;include_cover=true&amp;include_title=true\n    FlaskApp-&gt;&gt;QRBlueprint: qr.download_qr(slug)\n    QRBlueprint-&gt;&gt;QRGen: generate_mixtape_qr_with_cover(...)\n    QRGen--&gt;&gt;QRBlueprint: PNG bytes\n    QRBlueprint--&gt;&gt;Client: 200 PNG (attachment filename=\"\u2026-qr-code.png\")</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom  <p>The diagram now includes the quality / cache decision step (<code>_get_serving_path</code>).</p>"},{"location":"development/routes/playback.html#qrcode-generation-backend","title":"\ud83d\udce6\u202fQR\u2011Code Generation (Backend)","text":"<p>Implemented in <code>src/qr_generator/qr_generator.py</code> and exposed via the <code>qr</code> blueprint (<code>routes/qr_blueprint.py</code>).</p> <p>See the dedicated API reference page for the QR routes for a full description of the two endpoints, query parameters, and error handling.</p>"},{"location":"development/routes/playback.html#frontend-integration-editor-player","title":"Front\u2011End Integration (Editor &amp; Player)","text":"Location Trigger What happens Editor page (<code>editor.html</code>) Click the Share button (<code>#share-playlist</code>) <code>static/js/editor/qrShare.js</code> fires \u2192 opens the QR Share Modal (<code>#qrShareModal</code>) \u2192 loads <code>/qr/&lt;slug&gt;.png</code> \u2192 shows a loading spinner \u2192 displays the QR image. The Download button calls <code>/qr/&lt;slug&gt;/download</code>. Public player (<code>play_mixtape.html</code>) Click the Share button (<code>#big-share-btn</code>) <code>static/js/player/qrShare.js</code> performs the same flow as the editor, but uses the download endpoint by default (so the user gets a high-resolution QR with cover). <p>Both modules also expose a <code>triggerShare()</code> helper that can be called programmatically (e.g., from a keyboard shortcut).</p>"},{"location":"development/routes/playback.html#core-helper-functions","title":"\ud83d\udd27 Core Helper Functions","text":"Function Signature What it does <code>_resolve_and_validate_path</code> <code>Path _resolve_and_validate_path(file_path: str)</code> Joins <code>MUSIC_ROOT + file_path</code>, resolves symlinks, ensures the result stays inside <code>MUSIC_ROOT</code>. Aborts with 403 (outside) or 404 (non-existent). <code>_guess_mime_type</code> <code>str _guess_mime_type(full_path: Path)</code> Uses <code>mimetypes.guess_type</code>. If <code>None</code>, falls back to a hard-coded map for <code>.flac</code>, <code>.m4a</code>, <code>.aac</code>, <code>.ogg</code>, <code>.mp3</code>. <code>_get_serving_path</code> <code>Path _get_serving_path(original_path, quality, cache, logger)</code> Quality handling: \u2022 If <code>quality == \"original\"</code> or the file does not need transcoding \u2192 return <code>original_path</code>. \u2022 If a cached version exists (<code>cache.is_cached</code>) \u2192 return that path (log a debug message). \u2022 Otherwise log a cache miss warning and fall back to the original file. (On-demand transcoding is commented out but ready for future use.) <code>_handle_range_request</code> <code>Response _handle_range_request(full_path, mime_type, range_header, file_size)</code> Parses <code>Range: bytes=start-end</code>. Validates bounds, returns 206 with <code>Content-Range</code>, <code>Accept-Ranges</code>, <code>Content-Length</code>, plus CORS (<code>Access-Control-Allow-Origin: *</code>) and caching (<code>Cache-Control: public, max-age=3600</code>). Invalid ranges \u2192 416. <code>stream_audio</code> <code>Response stream_audio(file_path)</code> Orchestrates the steps above, adds <code>Accept-Ranges</code> header for full responses, and returns the appropriate Flask <code>Response</code>. <code>public_play</code> <code>Response public_play(slug)</code> Retrieves mixtape JSON via <code>MixtapeManager.get(slug)</code>. 404 if missing. Renders <code>play_mixtape.html</code>. <code>serve_cover</code> <code>Response serve_cover(filename)</code> Sends the requested cover from <code>app.config[\"COVER_DIR\"]</code>. <code>cache_stats</code> <code>dict cache_stats()</code> Returns <code>{ \"cache_size_bytes\": \u2026, \"cache_size_mb\": \u2026, \"cached_files\": \u2026 }</code>. <code>clear_cache</code> <code>dict clear_cache()</code> Deletes cached files (optionally older than X days) and returns <code>{ \"deleted_files\": n, \"message\": \u2026 }</code>."},{"location":"development/routes/playback.html#quality-caching-logic","title":"\ud83d\udcbe Quality &amp; Caching Logic","text":"<p>Implemented in <code>routes/play.py</code> and the auxiliary <code>audio_cache.py</code> module.</p> <p>Why this matters \u2013 The client can request a lower bitrate to save bandwidth on mobile connections, while the server can pre\u2011populate the cache (via a background job) for faster subsequent deliveries.</p>"},{"location":"development/routes/playback.html#http-range-support","title":"\ud83d\udcf6 HTTP Range Support","text":"<p>Only activated when the request contains a <code>Range</code> header.</p> <ol> <li>Parse \u2013 <code>bytes=start-end</code> (both start and end are optional).</li> <li>Validate \u2013 start/end must be within <code>[0, file_size\u20111]</code> and <code>start \u2264 end</code>.</li> <li>Respond \u2013<ul> <li>Status 206 Partial Content.</li> <li><code>Content\u2011Range: bytes start\u2011end/file_size</code>.</li> <li><code>Accept\u2011Ranges: bytes</code>.</li> <li><code>Content\u2011Length: (end\u2011start+1)</code>.</li> <li>CORS &amp; cache headers identical to the full\u2011file response.</li> </ul> </li> <li>Error \u2013 Out\u2011of\u2011bounds ranges \u2192 416 Range Not Satisfiable.</li> </ol>"},{"location":"development/routes/playback.html#response-headers-logging","title":"\ud83d\udcdc Response Headers &amp; Logging","text":"Header Value (example) Reason <code>Accept-Ranges</code> <code>bytes</code> Advertise range support. <code>Access-Control-Allow-Origin</code> <code>*</code> Allow any origin to embed the audio element (needed for cross-origin playback). <code>Cache-Control</code> <code>public, max-age=3600</code> Enable browsers/CDNs to cache the file for 1 hour. <code>Content-Type</code> MIME from <code>_guess_mime_type</code> (e.g., <code>audio/flac</code>) Correct media type for the player. <code>Content-Range</code> (partial) <code>bytes 0-1023/1234567</code> Required for HTTP 206. <code>Content-Length</code> (partial) <code>1024</code> Size of the delivered chunk. <p>Logging (via the injected <code>Logger</code>):</p> <ul> <li>INFO \u2013 Successful stream start, chosen quality, cache hit/miss.</li> <li>WARNING \u2013 Cache miss, malformed range header, missing file.</li> <li>ERROR \u2013 Exceptions while reading the file, transcoding failures, unexpected server errors.</li> </ul> <p>All logs include the request path and the selected serving path, making troubleshooting straightforward.</p>"},{"location":"development/routes/playback.html#error-handling-status-codes","title":"\u26a0\ufe0f Error Handling &amp; Status Codes","text":"Situation Flask call HTTP status Log level Requested file outside <code>MUSIC_ROOT</code> <code>abort(403)</code> 403 Forbidden warning File does not exist <code>abort(404)</code> 404 Not Found warning Invalid Range header (out of bounds) <code>Response(..., 416)</code> 416 Range Not Satisfiable warning Unexpected I/O error while reading the file <code>abort(500)</code> (caught in <code>_handle_range_request</code>) 500 Internal Server Error error General uncaught exception in <code>stream_audio</code> Propagates \u2192 Flask\u2019s default error handling 500 error"},{"location":"development/routes/playback.html#static-assets-that-complement-the-endpoint","title":"\ud83e\uddf1 Static Assets that Complement the Endpoint","text":"Asset Path Role <code>index.js</code> <code>static/js/player/index.js</code> Bootstraps the player UI, adaptive theming, quality selector, cassette-mode UI, and QR sharing. <code>playerControls.js</code> <code>static/js/player/playerControls.js</code> Implements the quality-selector dropdown, play/pause/skip logic, and UI-state synchronization with the Flask audio element. <code>linerNotes.js</code> <code>static/js/player/linerNotes.js</code> Renders markdown liner notes (via marked + DOMPurify). <code>qrShare.js</code> (common) <code>static/js/common/qrShare.js</code> Shared module - Handles QR modal display, link copying, and QR download. Used across browser, editor, and player pages. <code>adaptiveTheming.js</code> <code>static/js/player/adaptiveTheming.js</code> Extracts dominant colors from the mixtape cover (Vibrant.js) and injects CSS custom properties for dynamic theming. <code>cassettePlayer.js</code> <code>static/js/player/cassettePlayer.js</code> Provides the optional \"retro cassette\" UI (view-mode toggle, spinning reels, VU meters). <p>These assets are loaded by <code>play_mixtape.html</code> (the template rendered by <code>public_play</code>). They rely on the JSON API exposed by the Flask routes (e.g., <code>/play/&lt;file_path&gt;</code> for streaming, <code>/covers/&lt;filename&gt;</code> for cover art).</p>"},{"location":"development/routes/playback.html#class-sequence-diagrams","title":"\ud83d\udcd0 Class &amp; Sequence Diagrams","text":""},{"location":"development/routes/playback.html#blueprintclass-diagram","title":"Blueprint/Class Diagram","text":"<pre><code>classDiagram\n    class play {\n    }\n    class stream_audio {\n        +Response stream_audio(file_path: str)\n    }\n    class _resolve_and_validate_path {\n        +Path _resolve_and_validate_path(file_path: str)\n    }\n    class _guess_mime_type {\n        +str _guess_mime_type(full_path: Path)\n    }\n    class _get_serving_path {\n        +Path _get_serving_path(original_path: Path, quality: str, cache: AudioCache, log: Logger)\n    }\n    class _handle_range_request {\n        +Response _handle_range_request(full_path: Path, mime_type: str, range_header: str, file_size: int)\n    }\n    class AudioCache {\n        &lt;&lt;module&gt;&gt;\n        +should_transcode(path: Path) bool\n        +is_cached(path: Path, quality: str) bool\n        +get_cache_path(path: Path, quality: str) Path\n        +clear_cache(older_than_days: int|None) int\n        +get_cache_size() int\n    }\n    play &lt;.. stream_audio : route\n    stream_audio ..&gt; _resolve_and_validate_path : uses\n    stream_audio ..&gt; _guess_mime_type : uses\n    stream_audio ..&gt; _get_serving_path : uses (quality &amp; cache)\n    stream_audio ..&gt; _handle_range_request : uses (if Range header)\n    _get_serving_path ..&gt; AudioCache : uses</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom"},{"location":"development/routes/playback.html#sequence-diagram","title":"\ud83d\udd01 Sequence Diagram","text":"<pre><code>sequenceDiagram\n    participant Client\n    participant FlaskApp\n    participant stream_audio()\n    participant _resolve_and_validate_path()\n    participant _guess_mime_type()\n    participant _get_serving_path()\n    participant AudioCache\n    participant _handle_range_request()\n    participant send_file\n\n    Client-&gt;&gt;FlaskApp: GET /play/&lt;file_path&gt;?quality=medium [&amp; Range]\n    FlaskApp-&gt;&gt;stream_audio(): Call with file_path + query\n    stream_audio-&gt;&gt;_resolve_and_validate_path(): Validate &amp; resolve\n    _resolve_and_validate_path--&gt;&gt;stream_audio(): full_path (or abort 403/404)\n    stream_audio-&gt;&gt;_guess_mime_type(): Guess MIME\n    _guess_mime_type--&gt;&gt;stream_audio(): mime_type\n    stream_audio-&gt;&gt;_get_serving_path(): Choose original or cached file (quality)\n    _get_serving_path-&gt;&gt;AudioCache: cache.is_cached / cache.get_cache_path\n    AudioCache--&gt;&gt;_get_serving_path(): serve_path\n    alt Range header present\n        stream_audio-&gt;&gt;_handle_range_request(): Serve partial bytes\n        _handle_range_request--&gt;&gt;stream_audio(): 206 Partial Content response\n        stream_audio--&gt;&gt;FlaskApp: Return 206\n    else No Range header\n        stream_audio-&gt;&gt;send_file: Send full file\n        send_file--&gt;&gt;stream_audio(): 200 OK response\n        stream_audio--&gt;&gt;FlaskApp: Return 200\n    end\n    FlaskApp--&gt;&gt;Client: Stream audio bytes</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom"},{"location":"development/routes/playback.html#api","title":"\ud83d\udd0c API","text":""},{"location":"development/routes/playback.html#src.routes.play","title":"<code>src.routes.play</code>","text":"<p>Functions:</p> Name Description <code>create_play_blueprint</code> <p>Creates and configures the Flask blueprint for mixtape playback and audio streaming.</p>"},{"location":"development/routes/playback.html#src.routes.play.create_play_blueprint","title":"<code>create_play_blueprint(mixtape_manager, path_audio_cache, logger=None)</code>","text":"<p>Creates and configures the Flask blueprint for mixtape playback and audio streaming.</p> <p>Sets up routes for streaming audio files with optional transcoding/caching, handling HTTP range requests, and rendering public mixtape playback pages.</p> <p>Parameters:</p> Name Type Description Default <code>mixtape_manager</code> <code>MixtapeManager</code> <p>The manager instance for retrieving mixtape data.</p> required <code>logger</code> <code>Logger</code> <p>The logger instance for error reporting.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>Blueprint</code> <code>Blueprint</code> <p>The configured Flask blueprint for playback and streaming.</p> Source code in <code>src/routes/play.py</code> <pre><code>def create_play_blueprint(mixtape_manager: MixtapeManager, path_audio_cache: Path, logger: Logger | None = None) -&gt; Blueprint:\n    \"\"\"\n    Creates and configures the Flask blueprint for mixtape playback and audio streaming.\n\n    Sets up routes for streaming audio files with optional transcoding/caching,\n    handling HTTP range requests, and rendering public mixtape playback pages.\n\n    Args:\n        mixtape_manager (MixtapeManager): The manager instance for retrieving mixtape data.\n        logger (Logger): The logger instance for error reporting.\n\n    Returns:\n        Blueprint: The configured Flask blueprint for playback and streaming.\n    \"\"\"\n    play = Blueprint(\"play\", __name__)\n\n    logger: Logger = logger or NullLogger()\n\n    mimetypes.add_type(\"audio/flac\", \".flac\")\n    mimetypes.add_type(\"audio/mp4\", \".m4a\")\n    mimetypes.add_type(\"audio/aac\", \".aac\")\n    mimetypes.add_type(\"audio/ogg\", \".ogg\")\n\n    # Initialize audio cache\n    cache_dir = path_audio_cache\n    audio_cache = AudioCache(cache_dir, logger)\n\n    @play.route(\"/&lt;path:file_path&gt;\")\n    def stream_audio(file_path: str) -&gt; Response:\n        \"\"\"\n        Streams an audio file from the music directory to the client.\n\n        Supports optional quality parameter for transcoded versions. Validates the file path,\n        determines the correct MIME type, and supports HTTP range requests for seeking.\n\n        Query Parameters:\n            quality: Quality level - \"high\" (256k), \"medium\" (192k), \"low\" (128k), or \"original\"\n\n        Args:\n            file_path: The relative path to the audio file within the music directory.\n\n        Returns:\n            Response: The Flask response object streaming the audio file or an error response.\n        \"\"\"\n        original_path = _resolve_and_validate_path(file_path)\n\n        # Get quality preference from query parameter\n        quality: QualityLevel = request.args.get(\"quality\", \"medium\")\n        if quality not in [\"high\", \"medium\", \"low\", \"original\"]:\n            quality = \"medium\"\n\n        # Determine which file to serve (cached or original)\n        serve_path = _get_serving_path(original_path, quality, audio_cache, logger)\n\n        mime_type = _guess_mime_type(serve_path)\n        file_size = serve_path.stat().st_size\n\n        if range_header := request.headers.get(\"Range\"):\n            return _handle_range_request(serve_path, mime_type, range_header, file_size)\n\n        # Full file request\n        response = send_file(serve_path, mimetype=mime_type, download_name=serve_path.name)\n        response.headers[\"Accept-Ranges\"] = \"bytes\"\n        response.headers[\"Access-Control-Allow-Origin\"] = \"*\"\n        response.headers[\"Cache-Control\"] = \"public, max-age=3600\"\n        return response\n\n    def _get_serving_path(\n        original_path: Path,\n        quality: QualityLevel,\n        cache: AudioCache,\n        log: Logger\n    ) -&gt; Path:\n        \"\"\"\n        Determine which file path to serve based on quality and cache availability.\n\n        Args:\n            original_path: Path to the original audio file.\n            quality: Requested quality level.\n            cache: AudioCache instance.\n            log: Logger instance.\n\n        Returns:\n            Path to the file that should be served.\n        \"\"\"\n        # If original quality or format doesn't need transcoding, serve original\n        if quality == \"original\" or not cache.should_transcode(original_path):\n            return original_path\n\n        # Check if cached version exists\n        if cache.is_cached(original_path, quality):\n            cached_path = cache.get_cache_path(original_path, quality)\n            log.debug(f\"Serving cached {quality} version: {cached_path.name}\")\n            return cached_path\n\n        # Cache doesn't exist - we have options:\n        # Option 1: Serve original (fast, but bandwidth-heavy)\n        # Option 2: Transcode on-the-fly (slower first load, but saves bandwidth)\n\n        # Default behavior: serve original and log that cache should be generated\n        log.warning(\n            f\"Cache miss for {original_path.name} at {quality} quality. \"\n            f\"Consider pre-caching this file.\"\n        )\n\n        # Optional: Uncomment to transcode on-demand (will cause delay on first play)\n        # try:\n        #     return cache.transcode_file(original_path, quality)\n        # except Exception as e:\n        #     log.error(f\"On-demand transcoding failed: {e}\")\n        #     return original_path\n\n        return original_path\n\n    def _resolve_and_validate_path(file_path: str) -&gt; Path:\n        \"\"\"\n        Resolves and validates the requested file path within the music directory.\n\n        Ensures the file is within the allowed music root and exists as a file.\n\n        Args:\n            file_path: The relative path to the audio file within the music directory.\n\n        Returns:\n            Path: The resolved and validated Path object for the requested file.\n\n        Raises:\n            403: If the file is outside the music root directory.\n            404: If the file does not exist.\n        \"\"\"\n        path_music = Path(current_app.config[\"MUSIC_ROOT\"]).resolve()\n        full_path = (path_music / file_path).resolve()\n        try:\n            full_path.relative_to(path_music)\n        except ValueError:\n            abort(403)\n        if not full_path.is_file():\n            abort(404)\n        return full_path\n\n    def _guess_mime_type(full_path: Path) -&gt; str:\n        \"\"\"\n        Determines the MIME type for a given file path.\n\n        Args:\n            full_path: The Path object representing the file.\n\n        Returns:\n            str: The MIME type string for the file.\n        \"\"\"\n        mime_type, _ = mimetypes.guess_type(str(full_path))\n        if mime_type is None:\n            suffix = full_path.suffix.lower()\n            mime_type = {\n                \".flac\": \"audio/flac\",\n                \".m4a\": \"audio/mp4\",\n                \".aac\": \"audio/aac\",\n                \".ogg\": \"audio/ogg\",\n                \".mp3\": \"audio/mpeg\",\n            }.get(suffix, \"application/octet-stream\")\n        return mime_type\n\n    def _handle_range_request(\n        full_path: Path, mime_type: str, range_header: str, file_size: int\n    ) -&gt; Response:\n        \"\"\"\n        Handle HTTP range requests for audio seeking.\n\n        Args:\n            full_path: Path to the audio file.\n            mime_type: MIME type of the file.\n            range_header: The Range header value from the request.\n            file_size: Total size of the file in bytes.\n\n        Returns:\n            Response with partial content (206) or error response.\n        \"\"\"\n        try:\n            range_match = range_header.replace(\"bytes=\", \"\")\n            byte1_str, byte2_str = (range_match.split(\"-\") + [\"\"])[:2]\n            byte1 = int(byte1_str) if byte1_str else 0\n            byte2 = int(byte2_str) if byte2_str else file_size - 1\n\n            if byte1 &gt;= file_size or byte2 &gt;= file_size or byte1 &lt; 0 or byte2 &lt; byte1:\n                return Response(\"Range Not Satisfiable\", 416)\n\n            length = byte2 - byte1 + 1\n\n            with open(full_path, \"rb\") as f:\n                f.seek(byte1)\n                data = f.read(length)\n\n            rv = Response(data, 206, mimetype=mime_type, direct_passthrough=True)\n            rv.headers.update(\n                {\n                    \"Content-Range\": f\"bytes {byte1}-{byte2}/{file_size}\",\n                    \"Accept-Ranges\": \"bytes\",\n                    \"Content-Length\": str(length),\n                    \"Access-Control-Allow-Origin\": \"*\",\n                    \"Cache-Control\": \"public, max-age=3600\",\n                }\n            )\n            return rv\n        except (ValueError, OSError):\n            abort(500)\n\n    @play.route(\"/share/&lt;slug&gt;\")\n    def public_play(slug: str) -&gt; Response:\n        \"\"\"\n        Renders the public mixtape playback page for a given slug.\n\n        Retrieves the mixtape by slug and displays it for public playback,\n        or returns a 404 error if not found.\n\n        Args:\n            slug: The unique identifier for the mixtape.\n\n        Returns:\n            Response: The Flask response object containing the rendered mixtape playback page.\n        \"\"\"\n        mixtape = mixtape_manager.get(slug)\n        if not mixtape:\n            abort(404)\n        return render_template(\"play_mixtape.html\", mixtape=mixtape, public=True)\n\n    @play.route(\"/covers/&lt;filename&gt;\")\n    def serve_cover(filename: str) -&gt; Response:\n        \"\"\"\n        Serves a cover image file from the covers directory.\n\n        Args:\n            filename: The name of the cover image file to serve.\n\n        Returns:\n            Response: The Flask response object containing the requested file.\n        \"\"\"\n        return send_from_directory(current_app.config[\"COVER_DIR\"], filename)\n\n    # Admin/utility endpoints for cache management\n\n    @play.route(\"/admin/cache/stats\")\n    def cache_stats() -&gt; dict:\n        \"\"\"\n        Get statistics about the audio cache.\n\n        Returns:\n            JSON with cache size and file count.\n        \"\"\"\n        cache_size = audio_cache.get_cache_size()\n        file_count = len(list(audio_cache.cache_dir.rglob(\"*.mp3\")))\n\n        return {\n            \"cache_size_bytes\": cache_size,\n            \"cache_size_mb\": round(cache_size / (1024 * 1024), 2),\n            \"cached_files\": file_count,\n        }\n\n    @play.route(\"/admin/cache/clear\", methods=[\"POST\"])\n    def clear_cache() -&gt; dict:\n        \"\"\"\n        Clear the audio cache.\n\n        Query Parameters:\n            older_than_days: Optional, only delete files older than this many days.\n\n        Returns:\n            JSON with number of deleted files.\n        \"\"\"\n        older_than = request.args.get(\"older_than_days\", type=int)\n        deleted = audio_cache.clear_cache(older_than_days=older_than)\n\n        return {\n            \"deleted_files\": deleted,\n            \"message\": f\"Cleared {deleted} cached files\",\n        }\n\n    return play\n</code></pre>"},{"location":"development/routes/qr_codes.html","title":"QR Codes","text":""},{"location":"development/routes/qr_codes.html#qr-codes","title":"QR Codes","text":""},{"location":"development/routes/qr_codes.html#highlevel-overview","title":"\ud83c\udf0d High\u2011Level Overview","text":"<p>The QR blueprint (qr_blueprint.py) provides two public endpoints that generate QR\u2011code images for a mixtape\u2019s share URL:</p> Feature Description Simple QR (<code>/qr/&lt;slug&gt;.png</code>) Returns a plain PNG QR code (optional logo overlay, configurable size). Enhanced QR (<code>/qr/&lt;slug&gt;/download</code>) Returns a QR code that can embed the mixtape\u2019s cover art and title banner, and is served as a downloadable attachment. Cache-Control Both endpoints set <code>Cache-Control: public, max-age=3600</code> so browsers cache the image for one hour. Graceful fallback If the logo or cover image is missing, the generator falls back to a plain QR code. Stateless The blueprint only reads data from the injected MixtapeManager and Flask\u2019s static folder; no session data is stored."},{"location":"development/routes/qr_codes.html#flask-blueprint-routes","title":"\ud83d\uddfa\ufe0f Flask Blueprint &amp; Routes","text":""},{"location":"development/routes/qr_codes.html#qrslugpng","title":"<code>/qr/&lt;slug&gt;.png</code>","text":"Method Description <code>GET</code> Returns a simple QR code PNG that encodes the public share URL for the mixtape identified by <code>&lt;slug&gt;</code>. <p>Request flow (simplified)</p> <pre><code>sequenceDiagram\n    participant UI as Browser\n    participant Flask as QR Blueprint\n    participant MM as MixtapeManager\n    participant FS as FileSystem (static folder)\n\n    UI-&gt;&gt;Flask: GET /qr/awesome-mixtape.png?size=400&amp;logo=true\n    Flask-&gt;&gt;MM: mixtape_manager.get('awesome-mixtape')\n    alt mixtape exists\n        MM--&gt;&gt;Flask: mixtape dict\n        Flask-&gt;&gt;FS: Resolve logo.svg / logo.png (if requested)\n        Flask-&gt;&gt;QRGen: generate_mixtape_qr(...)\n        QRGen--&gt;&gt;Flask: PNG bytes\n        Flask--&gt;&gt;UI: 200 PNG (Cache\u2011Control: public, max\u2011age=3600)\n    else\n        Flask--&gt;&gt;UI: 404 \u201cMixtape not found\u201d\n    end</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom"},{"location":"development/routes/qr_codes.html#qrslugdownload","title":"<code>/qr/&lt;slug&gt;/download</code>","text":"Method Description <code>GET</code> Returns an enhanced QR code PNG that can include the mixtape\u2019s cover art and a title banner. The image is served as a downloadable attachment (<code>Content-Disposition: attachment</code>). <p>Request flow (simplified)</p> <pre><code>sequenceDiagram\n    participant UI as Browser\n    participant Flask as QR Blueprint\n    participant MM as MixtapeManager\n    participant FS as FileSystem (static folder)\n\n    UI-&gt;&gt;Flask: GET /qr/awesome-mixtape/download?size=800&amp;include_cover=true&amp;include_title=true\n    Flask-&gt;&gt;MM: mixtape_manager.get('awesome-mixtape')\n    alt mixtape exists\n        MM--&gt;&gt;Flask: mixtape dict\n        Flask-&gt;&gt;FS: Resolve logo.svg / logo.png\n        Flask-&gt;&gt;FS: Resolve mixtape cover (if requested)\n        Flask-&gt;&gt;QRGen: generate_mixtape_qr_with_cover(...)\n        QRGen--&gt;&gt;Flask: PNG bytes\n        Flask--&gt;&gt;UI: 200 PNG (attachment filename=\"&lt;title&gt;-qr-code.png\")\n    else\n        Flask--&gt;&gt;UI: 404 \u201cMixtape not found\u201d\n    end</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom"},{"location":"development/routes/qr_codes.html#query-parameters","title":"\ud83d\udd75\ud83c\udffb\u200d\u2642\ufe0f Query Parameters","text":"Parameter Endpoint Type Default Allowed range / values Description <code>size</code> both integer 400 (simple) / 800 (download) 1\u2013800 (simple), 1\u20131200 (download) Pixel dimension of the generated QR code (square). <code>logo</code> simple boolean-like string <code>\"true\"</code> <code>\"true\"</code> / <code>\"false\"</code> (case-insensitive) Whether to overlay the site logo (SVG or PNG) in the centre of the QR. <code>include_cover</code> download boolean-like string <code>\"true\"</code> <code>\"true\"</code> / <code>\"false\"</code> Include the mixtape\u2019s cover art in the QR (centered). <code>include_title</code> download boolean-like string <code>\"true\"</code> <code>\"true\"</code> / <code>\"false\"</code> Render the mixtape title as a banner underneath the QR. <p>Note</p> <p>All parameters are optional. Missing or malformed values fall back to the defaults.</p>"},{"location":"development/routes/qr_codes.html#response-details","title":"\ud83d\udde8\ufe0f Response Details","text":"Status Content-Type Body Headers 200 OK <code>image/png</code> Raw PNG bytes (the QR code). <code>Cache-Control: public, max-age=3600</code><code>Content-Disposition:</code>\u2022 Simple QR \u2013 <code>inline; filename=\"&lt;slug&gt;-qr.png\"</code>\u2022 Download QR \u2013 <code>attachment; filename=\"&lt;title&gt;-qr-code.png\"</code> 404 Not Found <code>application/json</code> <code>{ \"error\": \"Mixtape not found\" }</code> \u2014 500 Internal Server Error <code>application/json</code> <code>{ \"error\": \"Failed to generate QR code\" }</code> (or a more specific message if the <code>qrcode</code> library is missing). \u2014"},{"location":"development/routes/qr_codes.html#error-handling","title":"\u26a0\ufe0f Error Handling","text":"<p>The blueprint catches two main error families:</p> <ol> <li> <p>Missing third\u2011party library \u2013 If <code>qrcode</code> (or <code>Pillow</code>) is not installed, an <code>ImportError</code> is raised. The handler logs the error and returns 500 with a helpful message:</p> <pre><code>QR code generation not available. Install qrcode library.\n</code></pre> </li> <li> <p>Unexpected runtime errors \u2013 Any other exception is logged (<code>logger.exception</code>) and a generic 500 response is sent:</p> <pre><code>Failed to generate QR code\n</code></pre> </li> </ol> <p>All 404 cases are handled by checking <code>mixtape_manager.get(slug)</code> and calling <code>abort(404, description=\"Mixtape not found\")</code>.</p>"},{"location":"development/routes/qr_codes.html#internal-workflow","title":"\ud83d\udd00 Internal Workflow","text":"<pre><code>sequenceDiagram\n    participant UI as Browser\n    participant Flask as QR Blueprint\n    participant MM as MixtapeManager\n    participant QRGen as QR Generator (qr_generator.py)\n    participant FS as FileSystem (static folder)\n\n    UI-&gt;&gt;Flask: GET /qr/&lt;slug&gt;.png?size=400&amp;logo=true\n    Flask-&gt;&gt;MM: mixtape_manager.get(slug)\n    alt mixtape exists\n        MM--&gt;&gt;Flask: mixtape dict\n        Flask-&gt;&gt;FS: Resolve logo.svg (fallback to logo.png)\n        Flask-&gt;&gt;QRGen: generate_mixtape_qr(url, title, logo_path, size)\n        QRGen--&gt;&gt;Flask: PNG bytes\n        Flask--&gt;&gt;UI: 200 PNG (Cache\u2011Control)\n    else\n        Flask--&gt;&gt;UI: 404\n    end</code></pre>  Hold \"Alt\" / \"Option\" to enable pan &amp; zoom  <p>The download endpoint follows the same flow, with two extra steps: resolving the mixtape cover image and calling <code>generate_mixtape_qr_with_cover</code>.</p>"},{"location":"development/routes/qr_codes.html#configuration-dependencies","title":"\ud83d\udd27 Configuration &amp; Dependencies","text":"Setting Where it lives Default Remarks <code>qrcode</code> (Python library) <code>project.toml</code> / <code>uv.lock</code> <code>&gt;=7.4</code> Required for both endpoints. <code>Pillow</code> (image handling) <code>project.toml</code> <code>&gt;=10.0</code> Needed for compositing the logo/cover. Static logo files <code>static/logo.svg</code> or <code>static/logo.png</code> \u2014 The blueprint prefers SVG; falls back to PNG. Cover directory <code>app.config[\"COVER_DIR\"]</code> (set in config.py) <code>collection-data/mixtapes/covers</code> Used only by the download endpoint. Cache-Control Hard-coded in the view (<code>public, max-age=3600</code>). \u2014 Adjust in the source if you need a different TTL. <p>Tip</p> <p>If you ever need to change the logo location, edit qr_blueprint.py where logo_path is resolved:</p> <pre><code>logo_path = Path(current_app.static_folder) / \"logo.svg\"\nif not logo_path.exists():\n    logo_path = Path(current_app.static_folder) / \"logo.png\"\n</code></pre>"},{"location":"development/routes/qr_codes.html#example-requests","title":"\ud83d\udccc Example Requests","text":""},{"location":"development/routes/qr_codes.html#simple-qr-browser","title":"Simple QR (browser)","text":"<pre><code>curl -L -o mixtape-qr.png \\\n  \"http://localhost:5000/qr/awesome-mixtape.png?size=400&amp;logo=true\"\n</code></pre> <p>Result: <code>mixtape-qr.png</code> \u2013 a 400\u202f\u00d7\u202f400 PNG with the site logo in the centre.</p>"},{"location":"development/routes/qr_codes.html#enhanced-qr-download","title":"Enhanced QR (download)","text":"<pre><code>curl -L -O \\\n  \"http://localhost:5000/qr/awesome-mixtape/download?size=800&amp;include_cover=true&amp;include_title=true\"\n</code></pre> <p>Result: The server sends a <code>Content\u2011Disposition: attachment</code> header; the file will be saved as something like <code>Awesome\u2011Mixtape-qr-code.png</code>.</p>"},{"location":"development/routes/qr_codes.html#using-the-qr-from-the-editor-ui","title":"Using the QR from the Editor UI","text":"<p>The editor page (<code>editor.html</code>) contains a Share button (<code>#share-playlist</code>). When the user clicks it, the JavaScript module <code>static/js/editor/qrShare.js</code>:</p> <ol> <li>Retrieves the current mixtape slug (from the hidden <code>#editing-slug</code> input or <code>window.PRELOADED_MIXTAPE</code>).</li> <li>Opens the QR Share Modal (<code>#qrShareModal</code>).</li> <li>Sets the <code>&lt;img id=\"qr-code-img\"&gt;</code> source to <code>/qr/&lt;slug&gt;.png?...</code>.</li> <li>Shows a loading spinner until the image loads, then displays the QR.</li> <li>You can see the full client\u2011side implementation in <code>editorQrShare.txt</code>.</li> </ol>"},{"location":"development/routes/qr_codes.html#using-the-qr-from-the-public-player-ui","title":"Using the QR from the Public Player UI","text":"<p>The public player page (<code>play_mixtape.html</code>) includes a Share button (<code>#big-share-btn</code>). Its logic lives in <code>static/js/player/qrShare.js</code>:</p> <ul> <li>The same modal (<code>#qrShareModal</code>) is reused, but the download endpoint is called (<code>/qr/&lt;slug&gt;/download</code>) when the user clicks the Download button.</li> </ul> <p>Both modules share the same modal markup (see the bottom of <code>editor.html</code> and <code>play_mixtape.html</code>).</p>"},{"location":"development/routes/qr_codes.html#frontend-integration","title":"\ud83d\udda5\ufe0f Front\u2011End Integration","text":""},{"location":"development/routes/qr_codes.html#architecture","title":"Architecture","text":"Component Location Role QR Modal <code>base.html</code> Global modal (<code>#qrShareModal</code>) available on all pages Common Module <code>static/js/common/qrShare.js</code> Shared logic for QR display, copying, and downloading Browser Integration <code>static/js/browser/index.js</code> Initializes QR for multiple mixtapes with <code>autoShow: true</code> Editor Integration <code>static/js/editor/index.js</code> Initializes QR for single mixtape with <code>autoShow: false</code> (shows after save) Player Integration <code>static/js/player/index.js</code> Initializes QR for public player with <code>autoShow: true</code>"},{"location":"development/routes/qr_codes.html#how-it-works","title":"How It Works","text":"<ol> <li>Modal defined once - <code>base.html</code> contains the QR modal markup, making it available globally</li> <li>Common module handles logic - <code>qrShare.js</code> manages modal display, QR loading, copy, and download</li> <li>Page-specific config - Each page calls <code>initQRShare()</code> with appropriate settings: <pre><code>// Browser page - multiple share buttons\ninitQRShare({\n    shareButtonSelector: '.qr-share-btn',\n    getSlug: (button) =&gt; button ? button.dataset.slug : null,\n    autoShow: true\n});\n\n// Editor page - single share button, hidden until saved\ninitQRShare({\n    shareButtonSelector: '#share-playlist',\n    getSlug: () =&gt; document.getElementById('editing-slug')?.value,\n    autoShow: false  // Shows after save\n});\n\n// Player page - single share button\ninitQRShare({\n    shareButtonSelector: '#big-share-btn',\n    getSlug: () =&gt; extractSlugFromURL(),\n    autoShow: true\n});\n</code></pre></li> </ol>"},{"location":"development/routes/qr_codes.html#using-the-qr-from-the-editor-ui_1","title":"Using the QR from the Editor UI","text":"<p>The editor page (<code>editor.html</code>) contains a Share button (<code>#share-playlist</code>). When the user clicks it, the common QR module (<code>static/js/common/qrShare.js</code>):</p> <ol> <li>Retrieves the current mixtape slug (from the hidden <code>#editing-slug</code> input or <code>window.PRELOADED_MIXTAPE</code>)</li> <li>Opens the QR Share Modal (<code>#qrShareModal</code>) from <code>base.html</code></li> <li>Sets the <code>&lt;img id=\"qr-code-img\"&gt;</code> source to <code>/qr/&lt;slug&gt;.png?...</code></li> <li>Shows a loading spinner until the image loads, then displays the QR</li> <li>Provides copy link and download buttons within the modal</li> </ol>"},{"location":"development/routes/qr_codes.html#using-the-qr-from-the-public-player-ui_1","title":"Using the QR from the Public Player UI","text":"<p>The public player page (<code>play_mixtape.html</code>) includes a Share button (<code>#big-share-btn</code>). The same common module handles the logic:</p> <ul> <li>The modal (<code>#qrShareModal</code>) is loaded from <code>base.html</code></li> <li>The download endpoint (<code>/qr/&lt;slug&gt;/download</code>) is called when the user clicks Download</li> <li>All functionality is identical to the editor page, ensuring consistency</li> </ul> <p>Both pages share the same modal markup (from <code>base.html</code>) and logic (from <code>common/qrShare.js</code>).</p>"},{"location":"development/routes/qr_codes.html#api","title":"\ud83d\udd0c API","text":""},{"location":"development/routes/qr_codes.html#src.routes.qr_blueprint","title":"<code>src.routes.qr_blueprint</code>","text":"<p>Functions:</p> Name Description <code>create_qr_blueprint</code> <p>Creates Flask blueprint for QR code generation.</p>"},{"location":"development/routes/qr_codes.html#src.routes.qr_blueprint.create_qr_blueprint","title":"<code>create_qr_blueprint(mixtape_manager, logger=None)</code>","text":"<p>Creates Flask blueprint for QR code generation.</p> <p>Parameters:</p> Name Type Description Default <code>mixtape_manager</code> <code>MixtapeManager</code> <p>MixtapeManager instance for retrieving mixtape data</p> required <code>logger</code> <code>Logger | None</code> <p>Logger instance for error reporting</p> <code>None</code> <p>Returns:</p> Name Type Description <code>Blueprint</code> <code>Blueprint</code> <p>Configured Flask blueprint for QR endpoints</p> Source code in <code>src/routes/qr_blueprint.py</code> <pre><code>def create_qr_blueprint(\n    mixtape_manager: MixtapeManager,\n    logger: Logger | None = None\n) -&gt; Blueprint:\n    \"\"\"\n    Creates Flask blueprint for QR code generation.\n\n    Args:\n        mixtape_manager: MixtapeManager instance for retrieving mixtape data\n        logger: Logger instance for error reporting\n\n    Returns:\n        Blueprint: Configured Flask blueprint for QR endpoints\n    \"\"\"\n    qr = Blueprint(\"qr\", __name__)\n    logger: Logger = logger or NullLogger()\n\n    @qr.route(\"/qr/&lt;slug&gt;.png\")\n    def generate_qr(slug: str) -&gt; Response:\n        \"\"\"\n        Generate a simple QR code PNG for a mixtape share URL.\n\n        Query parameters:\n            size: QR code size in pixels (default 400, max 800)\n            logo: Include logo in center (default true)\n\n        Args:\n            slug: The mixtape slug identifier (URL-encoded)\n\n        Returns:\n            Response: PNG image of the QR code\n        \"\"\"\n        # URL decode the slug (handles spaces and special characters)\n        slug = unquote(slug)\n\n        # Verify mixtape exists\n        mixtape = mixtape_manager.get(slug)\n        if not mixtape:\n            abort(404, description=\"Mixtape not found\")\n\n        # Get parameters\n        size = min(int(request.args.get('size', 400)), 800)\n        include_logo = request.args.get('logo', 'true').lower() != 'false'\n\n        # Build the full share URL\n        from flask import url_for\n        share_url = url_for('play.public_play', slug=slug, _external=True)\n\n        # Generate QR code\n        try:\n\n\n            # Get logo path if requested\n            logo_path = None\n            if include_logo:\n                logo_path = Path(current_app.static_folder) / 'logo.svg'\n                if not logo_path.exists():\n                    logo_path = Path(current_app.static_folder) / 'logo.png'\n                    if not logo_path.exists():\n                        logo_path = None\n\n            qr_bytes = generate_mixtape_qr(\n                url=share_url,\n                title=mixtape.get('title', 'Mixtape'),\n                logo_path=logo_path,\n                size=size\n            )\n\n            response = Response(qr_bytes, mimetype='image/png')\n            response.headers['Cache-Control'] = 'public, max-age=3600'\n            response.headers['Content-Disposition'] = f'inline; filename=\"{slug}-qr.png\"'\n\n            return response\n\n        except ImportError as e:\n            logger.error(f\"QR code generation failed - library not installed: {e}\")\n            abort(\n                500,\n                description=\"QR code generation not available. Install qrcode library.\"\n            )\n        except Exception as e:\n            logger.exception(\"QR code generation failed\")\n            abort(500, description=\"Failed to generate QR code\")\n\n    @qr.route(\"/qr/&lt;slug&gt;/download\")\n    def download_qr(slug: str) -&gt; Response:\n        \"\"\"\n        Download enhanced QR code with cover art and title.\n\n        Query parameters:\n            size: QR code size in pixels (default 800, max 1200)\n            include_cover: Include mixtape cover art (default true)\n            include_title: Include mixtape title banner (default true)\n\n        Args:\n            slug: The mixtape slug identifier (URL-encoded)\n\n        Returns:\n            Response: PNG image as downloadable attachment\n        \"\"\"\n        # URL decode the slug (handles spaces and special characters)\n        slug = unquote(slug)\n\n        # Verify mixtape exists\n        mixtape = mixtape_manager.get(slug)\n        if not mixtape:\n            abort(404, description=\"Mixtape not found\")\n\n        # Get parameters\n        qr_size = min(int(request.args.get('size', 800)), 1200)\n        include_cover = request.args.get('include_cover', 'true').lower() != 'false'\n        include_title = request.args.get('include_title', 'true').lower() != 'false'\n\n        # Build the full share URL\n        share_url = url_for('play.public_play', slug=slug, _external=True)\n\n        try:\n            # Get logo path\n            logo_path = Path(current_app.static_folder) / 'logo.svg'\n            if not logo_path.exists():\n                logo_path = Path(current_app.static_folder) / 'logo.png'\n                if not logo_path.exists():\n                    logo_path = None\n\n            # Get cover path if requested\n            cover_path = None\n            if include_cover and mixtape.get('cover'):\n                cover_filename = mixtape['cover'].split('/')[-1]\n                cover_path = Path(current_app.config['COVER_DIR']) / cover_filename\n                if not cover_path.exists():\n                    cover_path = None\n\n            # Generate enhanced QR code\n            qr_bytes = generate_mixtape_qr_with_cover(\n                url=share_url,\n                title=mixtape.get('title', 'Mixtape'),\n                cover_path=cover_path,\n                logo_path=logo_path,\n                qr_size=qr_size,\n                include_title=include_title\n            )\n\n            # Sanitize title for filename\n            title = mixtape.get('title', 'mixtape')\n            safe_title = \"\".join(c if c.isalnum() or c in \" -_\" else \"_\" for c in title)\n            filename = f\"{safe_title}-qr-code.png\"\n\n            response = Response(qr_bytes, mimetype='image/png')\n            response.headers['Content-Disposition'] = f'attachment; filename=\"{filename}\"'\n\n            return response\n\n        except ImportError as e:\n            logger.error(f\"QR code generation failed - library not installed: {e}\")\n            abort(\n                500,\n                description=\"QR code generation not available. Install qrcode library.\"\n            )\n        except Exception as e:\n            logger.exception(\"QR code download failed\")\n            abort(500, description=\"Failed to generate QR code\")\n\n    return qr\n</code></pre>"},{"location":"project/index.html","title":"Overview","text":""},{"location":"project/index.html#project","title":"Project","text":"<p>Mixtape Society is still early days\u2014a solo project that's finding its footing. This section covers where it's headed and how you can get involved if you're interested.</p>"},{"location":"project/index.html#whats-happening","title":"\ud83d\udce3 What's Happening","text":"<ul> <li>\ud83c\udfaf Roadmap - What I'm working on next</li> <li>\ud83d\udca1 Ideas - Things being explored (lots here!)</li> <li>\ud83d\udcdd Changelog - What's been shipped so far</li> </ul>"},{"location":"project/index.html#get-involved","title":"\ud83d\udc99 Get Involved","text":"<ul> <li>\ud83d\udcac GitHub Discussions - Share thoughts or ask questions</li> <li>\ud83d\udc1b Open an issue - Report bugs or suggest features</li> <li>\ud83e\udd1d Contributing guide - How to help out</li> </ul> <p>No pressure, but if something here resonates, feel free to jump in.</p>"},{"location":"project/index.html#core-principles","title":"\ud83e\udded Core principles","text":"<ul> <li>No central-server accounts</li> <li>No Spotify / Apple Music integration</li> <li>No social features (likes, comments, following)</li> <li>No ads, analytics, or telemetry by default</li> </ul>"},{"location":"project/index.html#principle-enforcement","title":"Principle enforcement","text":"<ul> <li>No accounts \u2013 Public routes (<code>/share/&lt;slug&gt;</code>, <code>/play/...</code>, <code>/qr/...</code>) have no <code>@login_required</code> decorators. The optional <code>authentication</code> blueprint is not registered in production.</li> <li>No streaming\u2011service integration \u2013 All audio files are served from <code>MUSIC_ROOT</code>; <code>play.py</code> never contacts Spotify, Apple\u202fMusic, or any third\u2011party API.</li> <li>No social features \u2013 Search results, mixtape pages, and the editor contain no \u201clike\u201d, \u201ccomment\u201d, or \u201cfollow\u201d UI elements.</li> <li>No ads / telemetry \u2013 No analytics scripts, no tracking pixels, and all responses omit <code>Set\u2011Cookie</code> headers (except the optional session cookie for the admin UI).</li> </ul>"},{"location":"project/index.html#project-terminology","title":"\ud83d\udcd6 Project Terminology","text":"Term Meaning (in this project) Mixtape A JSON bundle (<code>mixtape.json</code>) plus optional cover image and audio files. Slug URL-safe identifier derived from the mixtape title (used in <code>/share/&lt;slug&gt;</code>). QR card Physical artifact that embeds a QR code pointing to the public mixtape URL. NFC tag Optional near-field-communication sticker that stores the same URL. Access policy Optional JSON field that controls time-based or \u201conce-only\u201d playback. QR blueprint Flask blueprint (<code>qr_blueprint.py</code>) that serves <code>/qr/&lt;slug&gt;.png</code> and <code>/qr/&lt;slug&gt;/download</code>. Editor blueprint Flask blueprint (<code>editor.py</code>) that handles creation, editing, and saving. Play blueprint Flask blueprint (<code>play.py</code>) that streams audio and renders the public page."},{"location":"project/about.html","title":"About Mixtape Society","text":""},{"location":"project/about.html#about-mixtape-society","title":"About Mixtape Society","text":"<p>Mixtape Society is a self-hosted web application that lets you create, manage, and share personalized music mixtapes from your own music library. Inspired by the nostalgia of cassette tapes, it brings a modern twist: drag-and-drop editing, custom covers, and public share links for easy listening without accounts or apps.</p>"},{"location":"project/about.html#project-goals","title":"\ud83e\udd45 Project Goals","text":"<ul> <li>Privacy-First: Everything runs on your hardware\u2014no cloud uploads, no tracking.</li> <li>Simplicity: Easy to set up and use, even for non-techies.</li> <li>Nostalgia Meets Modern: Recreate the joy of mixtapes with features like themes and browser-based streaming.</li> <li>Open Source: Free and open for contributions, under the MIT License.</li> </ul>"},{"location":"project/about.html#who-built-this","title":"\ud83e\uddd4\ud83c\udffc\u200d\u2642\ufe0f Who Built This?","text":"<p>Hi, I'm Mark Zwart (e.g., mark-me on GitHub), a Dutch developer and music enthusiast based in The Netherlands. I created Mixtape Society because I wanted a simple way to share curated playlists from my personal collection without relying on big streaming services.</p> <p>If you'd like to connect:</p> <ul> <li>GitHub: github.com/mark-me</li> </ul>"},{"location":"project/about.html#acknowledgements","title":"\ud83d\ude4c Acknowledgements","text":"<p>Mixtape Society leverages several fantastic open-source libraries and tools. A big thank you to the developers and communities behind these projects.</p>"},{"location":"project/about.html#python-backend","title":"Python &amp; Backend","text":"<ul> <li>Flask \u2013 Web framework that powers everything</li> <li>Jinja2 \u2013 Templating engine used for dynamic HTML rendering in all pages.</li> <li>Flask-CORS \u2013 Simple cross-origin resource sharing</li> <li>Flask-Limiter \u2013 Rate limiting for the login route</li> <li>Flask-Login \u2013 Session management</li> <li>FFmpeg - Transcoding audio quality</li> <li>Gunicorn \u2013 Production WSGI server</li> <li>Pillow \u2013 Image processing for uploaded covers and OpenGraph image generation</li> <li>python-dotenv \u2013 Easy .env file support</li> <li>python-json-logger \u2013 Structured logging</li> <li>TinyTag \u2013 Fast and lightweight audio metadata reading</li> <li>Watchdog \u2013 File system events for auto-reindexing the music library</li> <li>SQLite \u2013 Embedded, zero-configuration database that stores the music library index and makes instant search possible.</li> </ul>"},{"location":"project/about.html#documentation","title":"Documentation","text":"<ul> <li>MkDocs &amp; Material for MkDocs \u2013 Static documentation you\u2019re reading right now</li> <li>mkdocstrings \u2013 Auto-generated API docs from code</li> <li>mkdocs-git-revision-date-localized-plugin \u2013 Page last-updated dates</li> <li>mkdocs-git-committers-plugin \u2013 Contributor credits</li> <li>mkdocs-panzoom-plugin \u2013 Zoomable screenshots</li> <li>mermaid2 \u2013 Diagrams</li> </ul>"},{"location":"project/about.html#frontend-design","title":"Frontend &amp; Design","text":"<ul> <li>Cassette Tape SVG Vector by flat-icon-design- Favicon is based on this</li> <li>Bootstrap 5 \u2013 The entire responsive UI, cards, buttons, modals, and grid system</li> <li>Bootstrap Icons \u2013 All the lovely icons you see everywhere</li> <li>Sortable.js \u2013 Drag-and-drop reordering in the editor</li> <li>Vibrant.js \u2013 Extract prominent colors from an image.</li> </ul>"},{"location":"project/about.html#license","title":"\u2696\ufe0f License","text":"<p>Mixtape Society is released under the . Feel free to fork, modify, and share\u2014but remember, users are responsible for their own music content (no copyrighted media is included or distributed).</p>"},{"location":"project/about.html#get-involved","title":"\ud83d\udc65\ud83d\udd27 Get Involved","text":"<ul> <li>Report issues or suggest features on GitHub Issues.</li> <li>Star the repo if you like it!</li> </ul> <p>Thanks for checking out Mixtape Society\u2014happy mixtaping! \ud83c\udfa7</p>"},{"location":"project/changelog.html","title":"Changelog","text":""},{"location":"project/changelog.html#changelog","title":"Changelog","text":"<p>All notable changes to Mixtape Society are documented here. We follow Semantic Versioning and the spirit of Keep a Changelog.</p> <p>Each release rewinds and fast-forwards your mixtape experience\u2014just like a real cassette! \ud83c\udfa7</p>"},{"location":"project/changelog.html#v057","title":"\ud83d\udce6 v0.5.7","text":"<p>Development</p> <p>\ud83d\uddd3\ufe0f 2026-01-xx</p>"},{"location":"project/changelog.html#fixed","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Allow searching for strange characters in artist/album/track names</li> <li>Type 3 characters to start searching -&gt; 2 characters</li> </ul> <p>Changes since last version</p>"},{"location":"project/changelog.html#v056","title":"\ud83d\udce6 v0.5.6","text":"<p>Release</p> <p>\ud83d\uddd3\ufe0f 2026-01-11</p>"},{"location":"project/changelog.html#added","title":"\u2728 Added","text":"<ul> <li>Multi-Architecture Docker Build Setup</li> </ul>"},{"location":"project/changelog.html#fixed_1","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Slug sanitation for mixtape titles with spaces and non-URL-safe characters</li> <li>Centered QR code in modal</li> </ul> <p>Changes since last version</p>"},{"location":"project/changelog.html#v055","title":"\ud83d\udce6 v0.5.5","text":"<p>Release</p> <p>\ud83d\uddd3\ufe0f 2026-01-09</p>"},{"location":"project/changelog.html#added_1","title":"\u2728 Added","text":"<ul> <li>Sharing through QR codes on browse, editor and playback pages</li> <li>Filtering and sorting on the mixtape browsing page</li> </ul>"},{"location":"project/changelog.html#changed","title":"\ud83d\udd27 Changed","text":"<ul> <li>Changed search bar for the mixtape editor</li> </ul> <p>Changes since last version</p>"},{"location":"project/changelog.html#v054","title":"\ud83d\udce6 v0.5.4","text":"<p>Release</p> <p>\ud83d\uddd3\ufe0f 2026-01-07</p>"},{"location":"project/changelog.html#fixed_2","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Color cassette tape strip</li> </ul> <p>Changes since last version</p>"},{"location":"project/changelog.html#v053","title":"\ud83d\udce6 v0.5.3","text":"<p>Release</p> <p>\ud83d\uddd3\ufe0f 2026-01-07</p>"},{"location":"project/changelog.html#added_2","title":"\u2728 Added","text":"<ul> <li>Immersive walkman mode on mobile</li> </ul>"},{"location":"project/changelog.html#fixed_3","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Restored mobile notification pause/play functionality</li> </ul> <p>Changes since last version</p>"},{"location":"project/changelog.html#v052","title":"\ud83d\udce6 v0.5.2","text":"<p>Release</p> <p>\ud83d\uddd3\ufe0f 2026-01-06</p>"},{"location":"project/changelog.html#added_3","title":"\u2728 Added","text":"<ul> <li>Mixtape cover resizing to reduce bandwidth needs</li> <li>Added cassette tape look option to playback</li> </ul>"},{"location":"project/changelog.html#changed_1","title":"\ud83d\udd27 Changed","text":"<ul> <li>Normalize cover images to RGB and handle transparency correctly before saving as JPEG, while slightly adjusting JPEG output settings.</li> </ul>"},{"location":"project/changelog.html#fixed_4","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Prevent unnecessary resizing of small cover images</li> </ul> <p>Changes since last version</p>"},{"location":"project/changelog.html#v051","title":"\ud83d\udce6 v0.5.1","text":"<p>Release</p> <p>All notable changes to Mixtape Society are documented here. We follow Semantic Versioning and the spirit of Keep a Changelog.</p> <p>Each release rewinds and fast-forwards your mixtape experience\u2014just like a real cassette! \ud83c\udfa7</p> <p>\ud83d\uddd3\ufe0f 2026-01-04</p>"},{"location":"project/changelog.html#added_4","title":"\u2728 Added","text":"<ul> <li>Adaptive theming based on the mixtape cover for playback</li> </ul> <p>v0.5.1</p>"},{"location":"project/changelog.html#v050","title":"\ud83d\udce6 v0.5.0","text":"<p>Release</p> <p>\ud83d\uddd3\ufe0f 2026-01-04</p>"},{"location":"project/changelog.html#added_5","title":"\u2728 Added","text":"<ul> <li>Modal for statistics on the entire collection</li> <li>Improved details in editor for compilation albums</li> <li>Enlarged album cover in album details</li> <li>Hierarchical hints in search results</li> <li>User are alerted on broken DB and can rebuild from UI -&gt; Shared mixtapes still usable while DB is being rebuilt</li> </ul>"},{"location":"project/changelog.html#changed_2","title":"\ud83d\udd27 Changed","text":"<ul> <li>Moved the resync button to the statistics modal</li> <li>Handling of compilation albums is now uniform</li> </ul>"},{"location":"project/changelog.html#fixed_5","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Database resync corrupts database</li> <li>Watcher corrupts database when changing lots of files in the music collection</li> <li>Type 2 characters to start searching -&gt; 3 characters</li> <li>Top of search bar focus ring cut-off on top</li> <li>Title of mixtape falls off the screen</li> <li>Navbar on mobile across two rows</li> </ul> <p>v0.5.0</p>"},{"location":"project/changelog.html#v041","title":"\ud83d\udce6 v0.4.1","text":"<p>Release</p> <p>\ud83d\uddd3\ufe0f 2026-01-02</p>"},{"location":"project/changelog.html#added_6","title":"\u2728 Added","text":"<ul> <li>Fallback cover art</li> <li>Cover art for track search results</li> <li>Cover art in mobile notification</li> </ul>"},{"location":"project/changelog.html#changed_3","title":"\ud83d\udd27 Changed","text":"<ul> <li>CSS changed to sematic color system</li> <li>Cleaning up editor and player javascript</li> </ul> <p>v0.4.1</p>"},{"location":"project/changelog.html#v040","title":"\ud83d\udce6 v0.4.0","text":"<p>Release</p> <p>\ud83d\uddd3\ufe0f 2026-01-01</p>"},{"location":"project/changelog.html#added_7","title":"\u2728 Added","text":"<ul> <li>Floating buttons in editor for mobile to:<ul> <li>Jump to mixtape tracks</li> <li>Save changes</li> </ul> </li> <li>Added button for manual resyncing the music collection library</li> </ul>"},{"location":"project/changelog.html#fixed_6","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Saving existing mixtapes results in error</li> <li>Covers not available for non-admin users</li> <li>Playback notification on Mobile progress not in sync with actual playback position.</li> </ul> <p>v0.4.0</p>"},{"location":"project/changelog.html#v030","title":"\ud83d\udce6 v0.3.0","text":"<p>Release</p> <p>\ud83d\uddd3\ufe0f 2026-01-01</p>"},{"location":"project/changelog.html#added_8","title":"\u2728 Added","text":"<ul> <li>Covers included for tracks</li> <li>Composite cover generation</li> <li>Track removal toast</li> </ul>"},{"location":"project/changelog.html#changed_4","title":"\ud83d\udd27 Changed","text":"<ul> <li>Play buttons changed to cover-play buttons</li> </ul>"},{"location":"project/changelog.html#fixed_7","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Reordering tracks resulted in track added toast.</li> <li>White theme browsing mixtapes, mixtape title invisible</li> <li>No version information extracted from the repository</li> </ul> <p>Changes since previous version</p>"},{"location":"project/changelog.html#v025","title":"\ud83d\udce6 v0.2.5","text":"<p>Release</p> <p>\ud83d\uddd3\ufe0f 2025-12-30</p>"},{"location":"project/changelog.html#added_9","title":"\u2728 Added","text":"<ul> <li>Creating cached versions of FLAC's converted to lower (but listenable) quality when creating a mixtape</li> <li>Option to let the user choose higher quality playback</li> </ul>"},{"location":"project/changelog.html#changed_5","title":"\ud83d\udd27 Changed","text":"<ul> <li>Updated documentation</li> </ul>"},{"location":"project/changelog.html#fixed_8","title":"\ud83d\udc1b Fixed","text":"<ul> <li>When searching with multiple words, the results narrows down</li> <li>Player control text is illegible in the white theme</li> <li>Editor search result play preview button not in sync with player controls</li> </ul> <p>Changes since previous version</p>"},{"location":"project/changelog.html#v022","title":"\ud83d\udce6 v0.2.2","text":"<p>Release</p> <p>\ud83d\uddd3\ufe0f 2025-12-28</p>"},{"location":"project/changelog.html#added_10","title":"\u2728 Added","text":"<ul> <li>Possibility to expand the tracks/liner notes for easier reordering and writing notes.</li> <li>Duration of playlist items added</li> </ul>"},{"location":"project/changelog.html#changed_6","title":"\ud83d\udd27 Changed","text":"<ul> <li>Mixtape player page:<ul> <li>Playback track layout so it looks better on mobile devices</li> <li>Decreased space for controls on mobile devices</li> <li>More track information in the audio player</li> </ul> </li> <li>Mixtape editor page<ul> <li>Search results layout in editor looks better on mobile devices</li> <li>Removed redundant labels from artist and album search results</li> <li>Made player in editor consistent with player_mixtape</li> </ul> </li> </ul>"},{"location":"project/changelog.html#fixed_9","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Track title not rendering on playlist</li> </ul> <p>Changes since previous version</p>"},{"location":"project/changelog.html#v021","title":"\ud83d\udce6 v0.2.1","text":"<p>Release</p> <p>\ud83d\uddd3\ufe0f 2025-12-27</p>"},{"location":"project/changelog.html#fixed_10","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Mixtapes editing made more uniform and robust against network failures, resulted in fatal crash</li> <li>Fixed album sorting for artists to case insensitive</li> <li>Made cover validation more robust</li> </ul> <p>Changes since previous version</p>"},{"location":"project/changelog.html#v020","title":"\ud83d\udce6 v0.2.0","text":"<p>Release</p> <p>\ud83d\uddd3\ufe0f 2025-12-27</p>"},{"location":"project/changelog.html#changed_7","title":"\ud83d\udd27 Changed","text":"<ul> <li>OpenGraph image and added information to better comply with standards https://opengraph.dev/</li> <li>Changing the title of the mixtape is independent of the filename of the stored mixtape. This ensures keeping the old shared URL alive when changing the title.</li> <li>Navbar to sticky-top</li> </ul>"},{"location":"project/changelog.html#fixed_11","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Collapse of the navbar happens only on small screens</li> <li>Better player display on smaller screens</li> <li>Redundant JavaScript removed from editor.html</li> </ul> <p>Changes since previous version</p>"},{"location":"project/changelog.html#v018-alpha","title":"\ud83d\udce6 v0.1.8-alpha","text":"<p>Unreleased</p> <p>\ud83d\uddd3\ufe0f 2025-12-26</p>"},{"location":"project/changelog.html#added_11","title":"\u2728 Added","text":"<ul> <li>Using tags while searching<ul> <li>Artist</li> <li>Album</li> <li>Song</li> </ul> </li> <li>Track sorting for albumns by disc and track number</li> <li>Clickable search results so you can change the search to an artist or album</li> <li>Logo in social card cover image</li> <li>Cache pass-1 results between keystrokes, keep the results of Pass 1 (scored rows) around temporarily, so increasing the precision of your search  doesn't need to hit the database again.</li> <li>Added robot.txt to disallow page indexing by search engines</li> </ul>"},{"location":"project/changelog.html#changed_8","title":"\ud83d\udd27 Changed","text":"<ul> <li>Extracted JavaScript from Jinja templates to <code>static/js</code></li> <li>Liner notes before tracks list</li> <li>Remove track icon</li> <li>Removed theme label from navbar</li> <li>Documentation images update</li> <li>Large cover files are scaled down to save bandwidth</li> <li>Sped up searching</li> <li>Better mixtape editing</li> <li>When indexing the music library, Ajax reloading is used to reduce page reloads</li> </ul>"},{"location":"project/changelog.html#fixed_12","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Player does not adhere to light/dark theming</li> <li>Possible mixtape collision</li> <li>Loading at first start-up in musiclib</li> <li>Progress report on loading in musiclib</li> </ul> <p>Changes since previous version</p>"},{"location":"project/changelog.html#v017-alpha","title":"\ud83d\udce6 v0.1.7-alpha","text":"<p>Pre-release</p> <p>\ud83d\uddd3\ufe0f 2025-12-18</p>"},{"location":"project/changelog.html#added_12","title":"\u2728 Added","text":"<ul> <li>Editing previews - each track in the search-result can be played without adding them to the playlist</li> <li>Project link in navbar</li> </ul>"},{"location":"project/changelog.html#changed_9","title":"\ud83d\udd27 Changed","text":"<ul> <li>Logo and favicon</li> <li>Documentation line-up with app style</li> </ul> <p>Changes since previous version</p>"},{"location":"project/changelog.html#v016-alpha","title":"\ud83d\udce6 v0.1.6-alpha","text":"<p>Pre-release</p> <p>\ud83d\uddd3\ufe0f 2025-12-17</p>"},{"location":"project/changelog.html#changed_10","title":"\ud83d\udd27 Changed","text":"<ul> <li>Extracted CSS to dedicated files in static, and added comments with clear flow</li> </ul>"},{"location":"project/changelog.html#fixed_13","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Covers not showing for users that are not logged in</li> <li>Editor headers change according to creating or editing mixtape</li> </ul> <p>Changes since previous version</p>"},{"location":"project/changelog.html#v015-alpha","title":"\ud83d\udce6 v0.1.5-alpha","text":"<p>Pre-release</p> <p>\ud83d\uddd3\ufe0f 2025-12-17</p>"},{"location":"project/changelog.html#added_13","title":"\u2728 Added","text":"<ul> <li>Grouping search results:</li> <li>Nesting for Artists: Artists show as top-level headers with summaries (e.g., \"2 album(s)\", \"5 nummer(s)\").</li> <li>Albums are nested in an accordion below, each with their own header and collapsible tracks.</li> <li>Folding Tracks: Tracks are hidden by default under a collapsible section (accordion for nested albums, simple collapse for standalone albums). Click the header/button to expand.</li> </ul>"},{"location":"project/changelog.html#changed_11","title":"\ud83d\udd27 Changed","text":"<ul> <li>Moved from JavaScript dialogs to Bootstrap dialogs</li> </ul>"},{"location":"project/changelog.html#fixed_14","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Fixed Dockerfile by starting app from factory function</li> </ul> <p>Changes since previous version</p>"},{"location":"project/changelog.html#v014-alpha","title":"\ud83d\udce6 v0.1.4-alpha","text":"<p>Pre-release</p> <p>\ud83d\uddd3\ufe0f 2025-12-16</p>"},{"location":"project/changelog.html#added_14","title":"\u2728 Added","text":"<ul> <li>Page that informs user at first startup on the progress of music library scraping</li> </ul>"},{"location":"project/changelog.html#changed_12","title":"\ud83d\udd27 Changed","text":"<ul> <li>Music library extraction backend (Musiclib)</li> <li>Huge overhaul of back-end, making modules less interdependent</li> </ul>"},{"location":"project/changelog.html#fixed_15","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Missing standard cover art</li> <li>Theme adherence on some pages</li> <li>Favicon theme adherence</li> <li>Deleting a mixtape actually works</li> </ul> <p>Changes since previous version</p>"},{"location":"project/changelog.html#v013-alpha","title":"\ud83d\udce6 v0.1.3-alpha","text":"<p>Pre-release</p> <p>\ud83d\uddd3\ufe0f 2025-12-15</p>"},{"location":"project/changelog.html#added_15","title":"\u2728 Added","text":"<ul> <li>Creating liner notes for a mixtape</li> <li>Pop-up when adding tracks to a mixtape</li> <li>Share mixtape from play page</li> <li>Social card metadata for mixtapes</li> </ul>"},{"location":"project/changelog.html#fixed_16","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Moving away from editing a mixtape then returning would lose changes</li> </ul> <p>Changes since previous version</p>"},{"location":"project/changelog.html#v012-alpha","title":"\ud83d\udce6 v0.1.2-alpha","text":"<p>Pre-release</p> <p>\ud83d\uddd3\ufe0f 2025-12-12</p>"},{"location":"project/changelog.html#fixed_17","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Made Mixtape browser responsive</li> </ul> <p>Changes since previous version</p>"},{"location":"project/changelog.html#v011-alpha","title":"\ud83d\udce6 v0.1.1-alpha","text":"<p>Pre-release</p> <p>\ud83d\uddd3\ufe0f 2025-12-12</p>"},{"location":"project/changelog.html#added_16","title":"\u2728 Added","text":"<ul> <li>Included version information from git tags</li> <li>Docker logging</li> <li>Rate limiting for login</li> </ul>"},{"location":"project/changelog.html#changed_13","title":"\ud83d\udd27 Changed","text":"<ul> <li>Improved music path handling</li> <li>Simplified logging</li> <li>Documentation welcome page</li> </ul>"},{"location":"project/changelog.html#fixed_18","title":"\ud83d\udc1b Fixed","text":"<ul> <li>Password handling with strange characters</li> <li>Database locks when too many Watcher related updates</li> </ul> <p>Changes since previous version</p>"},{"location":"project/contributing.html","title":"Contributing","text":""},{"location":"project/contributing.html#contributing","title":"Contributing","text":"<p>Interested in helping out? Here's how things work.</p>"},{"location":"project/contributing.html#ways-to-contribute","title":"\ud83e\udd1d Ways to Contribute","text":"<p>Share ideas - Open a discussion or comment on existing ones. All suggestions welcome, even half-baked ones.</p> <p>Report bugs - Open an issue with: - What you expected to happen - What actually happened - Steps to reproduce (if possible) - Your setup (Docker/local, browser, OS)</p> <p>Improve documentation - Typos, unclear explanations, missing info\u2014all fair game. Docs live in the <code>/docs</code> folder.</p> <p>Code contributions - Check good first issue tags or the roadmap for things I'm planning to work on.</p>"},{"location":"project/contributing.html#before-you-start","title":"\ud83e\udded Before You Start","text":"<p>If you're thinking of tackling something substantial (new feature, big refactor), open an issue or discussion first. Saves everyone time if the direction doesn't fit the project.</p>"},{"location":"project/contributing.html#development-setup","title":"\ud83d\udee0\ufe0f Development Setup","text":""},{"location":"project/contributing.html#using-docker-recommended","title":"Using Docker (Recommended)","text":"<p>See Docker Development guide for full instructions.</p>"},{"location":"project/contributing.html#local-development","title":"Local Development","text":"<pre><code>git clone https://github.com/mark-me/mixtape-society.git\ncd mixtape-society\nuv sync  --group dev\n\n# Edit MUSIC_ROOT and APP_PASSWORD in .env\nuv run python app.py\n</code></pre> <p>First run will index your music library.</p> <p>See Local Development guide for more details.</p>"},{"location":"project/contributing.html#code-style","title":"\ud83c\udfa8 Code Style","text":"<p>Nothing strict, but: - Keep it readable - Add docstrings for non-obvious functions - Follow existing patterns in the codebase - Run basic tests if you're changing core functionality</p> <p>If something's unclear, just ask.</p>"},{"location":"project/contributing.html#pull-request-process","title":"\ud83d\udd00 Pull Request Process","text":"<ol> <li>Fork the repo</li> <li>Create a branch (<code>git checkout -b feature/thing</code>)</li> <li>Make your changes</li> <li>Commit with a clear message</li> <li>Push and open a PR</li> </ol> <p>I'll review when I can. Might ask for changes, might take a while\u2014this is a side project.</p>"},{"location":"project/contributing.html#project-organization","title":"\ud83d\uddc2\ufe0f Project Organization","text":"<p>The GitHub Project board tracks work in progress:</p> <ul> <li>Ideas - Things being explored (Ideas doc has more)</li> <li>Planned - Committed to building (Roadmap)</li> <li>In Progress - Currently working on</li> <li>Done - Shipped (see Changelog)</li> </ul> <p>It's pretty informal, just helps keep things organized.</p>"},{"location":"project/contributing.html#what-gets-prioritized","title":"\ud83c\udfaf What Gets Prioritized","text":"<p>Roughly in this order:</p> <ol> <li>Bug fixes (especially breaking ones)</li> <li>Features that align with project goals (see About)</li> <li>Community interest (reactions, comments on issues)</li> <li>Maintainer availability (aka: when I have time)</li> </ol>"},{"location":"project/contributing.html#testing","title":"\ud83e\uddea Testing","text":"<p>Basic manual testing is fine for most changes. If you're modifying core components (musiclib, audio caching, database), please test thoroughly:</p> <ul> <li>Try it with your own music library</li> <li>Test edge cases (empty folders, weird filenames, large libraries)</li> <li>Check it doesn't break existing features</li> </ul> <p>No formal test suite yet, but that might change.</p>"},{"location":"project/contributing.html#documentation","title":"\ud83d\udcda Documentation","text":"<p>If your PR adds a feature or changes behavior: - Update relevant docs in <code>/docs</code> - Add a brief note in <code>CHANGELOG.md</code> (I'll handle version numbering)</p>"},{"location":"project/contributing.html#questions","title":"\u2753 Questions?","text":"<p>Not sure about something? Open a discussion or comment on a relevant issue. I'll respond when I can.</p>"},{"location":"project/contributing.html#license","title":"\u2696\ufe0f License","text":"<p>By contributing, you agree your contributions will be licensed under the MIT License.</p> <p>Thanks for considering contributing to Mixtape Society. Even small fixes are appreciated. \ud83c\udfa7</p>"},{"location":"project/roadmap.html","title":"Roadmap","text":""},{"location":"project/roadmap.html#roadmap","title":"Roadmap","text":""},{"location":"project/roadmap.html#what-not-to-add-to-keep-the-soul-of-the-project","title":"\ud83d\udeab What NOT to add (to keep the soul of the project)","text":"<ul> <li>No accounts on a central server</li> <li>No Spotify/Apple Music integration</li> <li>No social features (likes, comments, following)</li> <li>No ads, analytics, telemetry by default</li> </ul>"},{"location":"project/roadmap.html#liner-notes","title":"\ud83d\udcdd Liner notes","text":"<ul> <li>Background info for artist, album or track</li> </ul>"},{"location":"project/roadmap.html#mobile-experience","title":"\ud83d\udcf1 Mobile Experience","text":"<ul> <li>Pull-to-refresh on browse page</li> <li>Add manifest.json + service worker \u2192 installable on phone/home screen</li> <li>Cache current mixtape for offline playback (big for mobile users on the go)</li> <li>Queue-based background download of mixtape tracks (zip or sequential)</li> <li>A super-light Flutter or Tauri app that just points to your self-hosted instance (basically a webview + native controls + background playback).</li> </ul>"},{"location":"project/roadmap.html#transcoding-streaming-optimisation","title":"\ud83d\udce1 Transcoding / Streaming Optimisation","text":"<ul> <li>Cache eviction policy</li> <li>Manual clear via admin endpoint.</li> <li>Implement an LRU or size\u2011based eviction strategy inside AudioCache.</li> <li>Range\u2011request optimisations</li> <li>Reads the requested slice into memory (f.read(length)).</li> <li>Stream directly from the file using werkzeug.wsgi.wrap_file for very large files.</li> </ul>"},{"location":"project/roadmap.html#smart-features-without-becoming-bloated","title":"\ud83e\udde0 Smart Features (without becoming bloated)","text":"<ul> <li>\u201cRecently added\u201d automatic mixtape</li> <li>Simple last.fm/scrobble support (optional)</li> </ul>"},{"location":"project/roadmap.html#sharing-discovery","title":"\ud83d\udcac Sharing &amp; Discovery","text":"<ul> <li>Beautiful public mixtape page with embed support (iframe)</li> <li>QR code for quick mobile sharing</li> <li>Optional short-url service (built-in or caddy/shlink integration)</li> <li>Web Share API target (native share button on mobile)</li> </ul>"},{"location":"project/roadmap.html#cover-art-visual-polish","title":"\ud83d\uddbc\ufe0f Cover Art &amp; Visual Polish","text":"<ul> <li>Cover format<ul> <li>Now Always JPEG, quality 100, max width 1200 px.</li> <li>Add a configuration option (max_width, jpeg_quality, or even support PNG/WebP).</li> </ul> </li> <li>Image validation<ul> <li>Now: Only checks that the string starts with <code>data:image</code>.</li> <li>Verify MIME type (image/jpeg, image/png) before decoding, reject oversized payloads early.</li> </ul> </li> <li>Async I/O<ul> <li>Now: All file operations are blocking.</li> <li>Switch to aiofiles + async methods if you need non-blocking behavior in a high-throughput API server.                                                                                      |</li> </ul> </li> </ul>"},{"location":"project/roadmap.html#multi-library-multi-user-support","title":"\ud83d\udc65 Multi-Library &amp; Multi-User Support","text":"<ul> <li>Support multiple MUSIC_ROOT paths (e.g., \u201cMain\u201d + \u201cVinyl Rips\u201d + \u201cLive Bootlegs\u201d)</li> <li>User roles: admin + read-only \u201cfamily\u201d accounts</li> </ul>"},{"location":"project/roadmap.html#backup-portability","title":"\ud83d\udce6 Backup &amp; Portability","text":"<ul> <li>One-click export of all mixtapes + cover art as .zip</li> <li>M3U export of mixtapes for use in other players</li> <li>Import from M3U playlists to create mixtapes</li> </ul>"},{"location":"project/roadmap.html#developerdeployment-experience","title":"\ud83d\udee0\ufe0f Developer/Deployment Experience","text":"<ul> <li>Add healthcheck endpoint for Docker/Kubernetes</li> <li>Support for Podman as well as Docker</li> <li>Optional Redis cache for very large libraries (&gt;50k tracks)</li> </ul>"},{"location":"project/roadmap.html#tiny-but-delightful-details","title":"\ud83c\udf43 Tiny but Delightful Details","text":"<ul> <li>Keyboard shortcuts (J/K for next/prev, space to play/pause, etc.)</li> <li>Click on album art in player \u2192 enlarge lightbox</li> <li>\u201cSleep timer\u201d in player</li> <li>Random mixtape button on home page</li> </ul>"},{"location":"project/roadmap.html#browser-page","title":"\ud83d\udd0d Browser page","text":"Area Current implementation Suggested enhancements Pagination All mixtapes are loaded at once (<code>list_all</code>). Add <code>limit</code>/<code>offset</code> parameters to <code>MixtapeManager.list_all</code> and UI pagination controls for large libraries. Search No search endpoint for mixtapes. Introduce <code>/mixtapes/search?q=</code> that filters by title/artist, returning JSON for a live-search UI. Bulk actions Only single-item delete. Add \u201cSelect \u2192 Delete\u201d or \u201cExport\u201d bulk actions, with a new API endpoint (<code>POST /mixtapes/bulk_delete</code>). Cover thumbnail generation Covers are served directly from the file system. Serve scaled thumbnails via a dedicated route (<code>/mixtapes/thumb/&lt;slug&gt;</code>) to reduce bandwidth on the browse page. Permissions All authenticated users can edit/delete any mixtape. Tie mixtapes to a user ID (<code>owner_id</code>) and restrict edit/delete to the owner (or admins). Progressive enhancement JavaScript is required for copy/delete. Provide graceful degradation (e.g., plain links for copy, a server-side delete confirmation page) for users with JS disabled."},{"location":"project/roadmap.html#base-page-and-app","title":"\ud83e\uddf1 Base page and app","text":"Area Current State Suggested Improvements Theme persistence Stored in <code>localStorage['theme']</code>. Add a server-side preference (user profile) so the choice survives across devices. Modular CSS One monolithic <code>base.css</code>. Split into component-level files (navbar, modals, toasts) and use a build step (e.g., PostCSS) for easier maintenance. Accessibility Basic ARIA attributes on modals/buttons. Add <code>aria-live=\"polite\"</code> to toasts, ensure focus trapping inside modals, and provide high-contrast color variants. Internationalisation Hard-coded English strings. Integrate Flask-Babel and expose a <code>gettext</code> filter in Jinja to support multiple locales. Progressive Enhancement Many features rely on JavaScript (copy, delete, theme). Provide graceful fallbacks: a plain link for copying (mailto fallback), a server-side confirmation page for delete, and a CSS-only dark-mode toggle. Testing No unit tests for <code>base.html</code> or the global JS. Add Selenium / Playwright integration tests that verify the theme switcher, indexing modal, and database-corruption flow. Security CSRF protection is handled globally by Flask-WTF (not shown). Ensure all POST endpoints (<code>/reset-database</code>, <code>/delete/&lt;slug&gt;</code>, <code>/resync</code>) validate a CSRF token. Performance All static assets are CDN-served individually. Bundle CSS/JS with a tool like Webpack or Rollup, enable HTTP/2 server push, and add integrity attributes for Subresource Integrity (SRI)."},{"location":"project/roadmap.html#qr-code-sharing","title":"\ud83d\udcf1 QR code sharing","text":"Idea Description Impact Dynamic QR size selector Add a UI dropdown (e.g., Small / Medium / Large) that updates the <code>size</code> query param on the fly. Improves UX for users who need larger codes for printing. QR colour theming Pass a color query param (hex) to <code>generate_mixtape_qr</code> and recolour the QR modules. Allows branding or better contrast on printed materials. Cache-Control header customization Make the <code>max-age</code> configurable via an environment variable (<code>QR_CACHE_MAX_AGE</code>). Gives operators control over CDN caching strategies. Rate limiting Apply Flask-Limiter to <code>/qr/*</code> endpoints (e.g., 1000/day). Prevents abuse (mass QR generation). SVG output Add an optional <code>format=svg</code> query param that returns a vector QR. Useful for high-resolution prints. QR analytics Record a lightweight hit counter (Redis or SQLite) each time a QR is generated. Lets you see how often mixtapes are shared. Fallback to Gravatar If the logo files are missing, generate a simple coloured circle with the site initials. Guarantees a logo is always present."},{"location":"project/ideas/design_implementation_map.html","title":"Design & Implementation Map","text":""},{"location":"project/ideas/design_implementation_map.html#design-implementation-map","title":"Design &amp; Implementation Map","text":""},{"location":"project/ideas/design_implementation_map.html#core-principles","title":"\ud83e\udded Core principles","text":"<ul> <li>No central-server accounts</li> <li>No Spotify / Apple Music integration</li> <li>No social features (likes, comments, following)</li> <li>No ads, analytics, or telemetry by default</li> </ul>"},{"location":"project/ideas/design_implementation_map.html#qr-card-make-it-meaningful-not-just-a-link","title":"\ud83d\uddbc\ufe0f QR card \u2013 make it meaningful (not just a link)","text":"Item Status Where it lives Notes / Proposal QR \u2192 mixtape landing page (title, personal note, tracklist, optional cover, \u201cplay order matters\u201d) \u2714\ufe0e Implemented <code>routes/play.py \u2192 public_play(slug)</code> renders <code>play_mixtape.html</code> The template already displays title, cover, dedication, and a Play button. Nothing to add. QR generation (one-time per mixtape) \u2714\ufe0e Implemented <code>routes/editor.py \u2192 qr.generate_qr(slug)</code> and <code>qr.download_qr(slug)</code> Already usable from the editor UI. QR + NFC hybrid \u2716\ufe0e Missing \u2013 Proposal: Add <code>GET /nfc/&lt;slug&gt;.json</code> returning <code>{ \"url\": \"&lt;public-share-url&gt;\" }</code>. NFC tags store this JSON (or URL). Stateless; preserves no-account rule. Contextual liner-note page (not a bare playlist) \u2714\ufe0e Implemented <code>templates/play_mixtape.html</code>, <code>static/js/player/linerNotes.js</code> Markdown liner notes already render. \u201cPlay order matters\u201d banner \u270e Partially implemented <code>play_mixtape.html</code> Proposal: Add <code>ordered: true</code> to <code>mixtape.json</code>. Template conditionally shows banner; editor exposes toggle."},{"location":"project/ideas/design_implementation_map.html#cassette-minidisc-cd-style-card-nostalgia-wins-hard","title":"\ud83c\udfb6 Cassette / MiniDisc / CD-style card (nostalgia wins hard)","text":"Item Status Where it lives Notes / Proposal Cassette-shaped card design (QR on label) \u2716\ufe0e Missing \u2013 Proposal: Printable SVG (<code>assets/templates/cassette.svg</code>) with QR placeholder. Editor offers \u201cDownload printable card\u201d. QR PNG merged server-side (Pillow). Mini-CD sleeve with liner notes \u2716\ufe0e Missing \u2013 Proposal: SVG CD-case layout + liner-note markdown \u2192 PDF (WeasyPrint / pdfkit). Embed QR via existing QR route. Polaroid-sized card, handwritten vibe \u2716\ufe0e Missing \u2013 Proposal: Handwritten font CSS option. Store <code>style</code> in <code>mixtape.json</code>; PDF generator applies CSS class. <p>All of these are offline artifacts; server interaction is limited to existing QR generation.</p>"},{"location":"project/ideas/design_implementation_map.html#nfc-instead-of-or-alongside-qr","title":"\ud83d\udcf1 NFC instead of (or alongside) QR","text":"Item Status Where it lives Notes / Proposal NFC-enabled card / coin / pick / postcard \u2716\ufe0e Missing \u2013 Proposal: Add <code>GET /nfc/&lt;slug&gt;.txt</code> returning the plain public URL. NFC tags can be written with any consumer writer. Hybrid QR + NFC fallback \u2716\ufe0e Missing \u2013 Proposal: Printable assets embed QR + printed short URL (<code>/s/&lt;slug&gt;</code>). Graceful fallback for non-NFC devices."},{"location":"project/ideas/design_implementation_map.html#the-artifact-concept-beyond-cards","title":"\ud83c\udf81 The \u201cartifact\u201d concept (beyond cards)","text":"Artifact Status Notes / Proposal Matchbox with QR inside \u2716\ufe0e Missing SVG template with QR placeholder. Bookmark \u2716\ufe0e Missing Bookmark-shaped SVG using existing QR PNG. Sticker set (each sticker = a track) \u2716\ufe0e Missing Proposal: PDF \u201csticker sheet\u201d where each track has a tiny QR pointing to <code>/share/&lt;slug&gt;#track=&lt;index&gt;</code>. Client-side fragment parsing starts playback at that track. Small zine (QR at end) \u2716\ufe0e Missing Combine liner-note markdown + QR into a single PDF (WeasyPrint). Postcard (mailed) \u2716\ufe0e Missing Same as postcard template in \u00a72 with larger personal note area. <p>All artifacts remain physical; backend usage is limited to existing QR endpoints.</p>"},{"location":"project/ideas/design_implementation_map.html#time-based-or-ritual-based-access","title":"\u23f3 Time-based or ritual-based access","text":"Item Status Where it lives Notes / Proposal Unlock 1 track per day \u2716\ufe0e Missing \u2013 Proposal: Add <code>access_policy</code> to <code>mixtape.json</code> (<code>{ type: \"daily_unlock\", start: ... }</code>). Enforced client-side via <code>playerControls.js</code> + <code>localStorage</code>. Unlock only at night \u2716\ufe0e Missing \u2013 Proposal: <code>policy: \"night_only\"</code>. Client checks local time / UTC offset. \u201cPlay in order, no shuffle\u201d \u2714\ufe0e Implemented <code>routes/play.py</code>, <code>playerControls.js</code> Shuffle disabled; order enforced. Expiring mixtape (30 days) \u2716\ufe0e Missing \u2013 Proposal: Add <code>expires_at</code>. Public page renders \u201cMixtape expired\u201d if past timestamp. \u201cListen once\u201d mode \u2716\ufe0e Missing \u2013 Proposal: <code>once: true</code>. Client disables playback after first full listen using <code>localStorage</code>. <p>All policies are client-side only \u2014 no server state, no accounts.</p>"},{"location":"project/ideas/design_implementation_map.html#playback-without-accounts-or-ads-important-constraint","title":"\ud83d\udeab Playback without accounts or ads (important constraint)","text":"Item Status Where it lives Notes / Proposal Self-hosted audio files \u2714\ufe0e Implemented <code>routes/play.stream_audio</code> Streams from <code>MUSIC_ROOT</code>. Independent / CC music \u270e Partially implemented Editor UI Proposal: License confirmation checkbox \u2192 <code>license: \"CC0\"</code> badge on public page. Bandcamp embeds \u2716\ufe0e Missing \u2013 Proposal: Optional <code>external_url</code> per track. Player renders <code>&lt;iframe&gt;</code> embed. Progressive Web App (offline caching) \u2716\ufe0e Missing \u2013 Proposal: Service Worker (<code>static/sw.js</code>) caches JSON, cover, audio files. Zero-ads, zero-tracking \u2714\ufe0e Implemented Templates No analytics or third-party scripts."},{"location":"project/ideas/design_implementation_map.html#strong-framing-sentence-this-matters","title":"\ud83e\udde0 Strong framing sentence (this matters)","text":"Item Status Where it lives \u201cThis is not a playlist. It\u2019s a mixtape you give to someone.\u201d \u2714\ufe0e Implemented <code>play_mixtape.html</code>, <code>editor.html</code>"},{"location":"project/ideas/design_implementation_map.html#top-3-suggestions","title":"\u2705 Top 3 suggestions","text":"Suggestion Already present? Beautiful physical card (cassette / CD inspired) \u2716\ufe0e Missing \u2013 see \u00a72 Mixtape landing page (not generic player) \u2714\ufe0e Implemented Intentional constraints (order matters, finite) \u2714\ufe0e Implemented"},{"location":"project/ideas/design_implementation_map.html#mvp-definition-one-sentence","title":"\ud83d\udca1 MVP definition (one sentence)","text":"<p>\u201cA creator makes a mixtape, prints a QR card, gives it to a friend. The friend scans it and listens \u2014 no account, no ads.\u201d</p> <p>All parts are functional except physical artifact generation, which is purely additive.</p>"},{"location":"project/ideas/design_implementation_map.html#mvp-flow-creator-receiver","title":"\ud83d\udd01 MVP Flow (creator \u2192 receiver)","text":""},{"location":"project/ideas/design_implementation_map.html#creator","title":"Creator","text":"Phase Implemented? Files / Routes Missing pieces Create mixtape \u2714\ufe0e <code>routes/editor.new_mixtape()</code> \u2013 Add tracks \u2714\ufe0e <code>static/js/editor/search.js</code>, <code>src/musiclib/</code> \u2013 Publish (QR generation) \u2714\ufe0e <code>routes/qr_blueprint.py</code> \u2013 Print / share physically \u2716\ufe0e \u2013 Printable SVG/PDF templates"},{"location":"project/ideas/design_implementation_map.html#receiver","title":"Receiver","text":"Phase Implemented? Files / Routes Scan QR \u2714\ufe0e <code>routes/play.public_play</code> Landing page \u2714\ufe0e <code>templates/play_mixtape.html</code> Listen (order-only) \u2714\ufe0e <code>playerControls.js</code>, <code>stream_audio</code>"},{"location":"project/ideas/design_implementation_map.html#mvp-includes-excludes-re-affirmed","title":"\ud83d\udcdd MVP includes / excludes (re-affirmed)","text":"Category Status Public mixtape page \u2714\ufe0e Audio playback \u2714\ufe0e QR-code generation \u2714\ufe0e Mobile-first UI \u2714\ufe0e No accounts \u2714\ufe0e No ads / analytics \u2714\ufe0e No social features \u2714\ufe0e No streaming-service APIs \u2714\ufe0e"},{"location":"project/ideas/design_implementation_map.html#digital-enablement-implementation-map","title":"\ud83d\uddfa\ufe0f Digital enablement (implementation map)","text":"Piece Implemented? Where it lives Next step Data model (JSON) \u2714\ufe0e <code>mixtapes/&lt;slug&gt;/mixtape.json</code> \u2013 Backend (Flask) \u2714\ufe0e <code>app.py</code> \u2013 Frontend UI \u2714\ufe0e <code>templates/</code>, <code>static/js/</code> \u2013 QR generation \u2714\ufe0e <code>routes/qr_blueprint.py</code> \u2013 NFC endpoint \u2716\ufe0e \u2013 Add <code>GET /nfc/&lt;slug&gt;.txt</code> Printable artifacts \u2716\ufe0e \u2013 Add <code>/print/&lt;slug&gt;/&lt;template&gt;</code> route Time-based access \u2716\ufe0e \u2013 Extend JSON + client checks Offline PWA \u2716\ufe0e \u2013 Add <code>static/sw.js</code> <p>All additions are stateless and preserve the four core principles.</p>"},{"location":"project/ideas/design_implementation_map.html#mvp-timeline","title":"\ud83d\udcc5 MVP timeline","text":"Week Done New tasks 1 Core app, streaming, QR, editor Design SVG templates 2 Editor save flow, QR modal <code>/print</code> route, NFC endpoint 3 \u2013 Access-policy enforcement 4 \u2013 Service Worker, UI polish, physical test <p>Success metric: A tester scans a printed card, listens fully, sees enforced constraints \u2014 no login, no ads.</p>"},{"location":"project/ideas/design_implementation_map.html#how-youll-know-it-worked","title":"\ud83e\uddea How you\u2019ll know it worked","text":"<ul> <li>Qualitative: \u201cI listened to the whole thing.\u201d</li> <li>Technical: No login redirects; no third-party network requests.</li> <li>Artifact: Printed cassette card opens the mixtape instantly on mobile.</li> </ul>"},{"location":"project/ideas/design_implementation_map.html#tldr-whats-already-there-vs-what-to-add","title":"\ud83d\udcdd TL;DR \u2014 What\u2019s already there vs what to add","text":"Feature Implemented Missing (quick win) QR \u2192 landing page \u2714\ufe0e \u2013 QR generation \u2714\ufe0e \u2013 Physical artifacts \u2013 SVG/PDF templates + <code>/print</code> NFC token \u2013 <code>/nfc/&lt;slug&gt;.txt</code> Time-based access \u2013 <code>access_policy</code> + JS Offline playback \u2013 Service Worker \u201cOrder matters\u201d flag \u270e Store in JSON Once-only / daily unlock \u2013 LocalStorage guard Bandcamp embeds \u2013 <code>external_url</code> support <p>All missing pieces are additive and do not violate any core principles.</p>"},{"location":"project/ideas/ideas.html","title":"General","text":""},{"location":"project/ideas/ideas.html#ideas-exploration","title":"Ideas &amp; Exploration","text":"<p>Things I'm considering or that have been suggested. Some will happen, some won't, some need more thought before deciding.</p>"},{"location":"project/ideas/ideas.html#what-not-to-add","title":"\ud83d\udeab What NOT to Add","text":"<p>To keep the soul of the project:</p> <ul> <li>No accounts on a central server</li> <li>No Spotify/Apple Music integration</li> <li>No social features (likes, comments, following)</li> <li>No ads, analytics, telemetry by default</li> </ul>"},{"location":"project/ideas/ideas.html#music-playback","title":"\ud83c\udfb5 Music &amp; Playback","text":"<p>Liner notes - Background info for artist, album, or track</p>"},{"location":"project/ideas/ideas.html#smart-features-without-bloat","title":"Smart features (without bloat)","text":"<ul> <li>\"Recently added\" automatic mixtape</li> <li>Simple last.fm/scrobble support (optional)</li> </ul>"},{"location":"project/ideas/ideas.html#transcoding-optimizations","title":"Transcoding optimizations","text":"<ul> <li>Cache eviction policy (manual clear, LRU strategy)</li> <li>Range-request optimizations for large files</li> </ul>"},{"location":"project/ideas/ideas.html#mobile-experience","title":"\ud83d\udcf1 Mobile Experience","text":"<ul> <li>Pull-to-refresh on browse page</li> <li>PWA manifest + service worker \u2192 installable</li> <li>Cache current mixtape for offline playback</li> <li>Queue-based background download of tracks</li> <li>Super-light Flutter/Tauri app (webview + native controls)</li> </ul>"},{"location":"project/ideas/ideas.html#sharing-discovery","title":"\ud83d\udcac Sharing &amp; Discovery","text":"<ul> <li>Beautiful public mixtape embed (iframe)</li> <li>Optional short-url service integration</li> <li>Web Share API target</li> </ul>"},{"location":"project/ideas/ideas.html#cover-art-visuals","title":"\ud83d\uddbc\ufe0f Cover Art &amp; Visuals","text":"<p>Image validation improvements:</p> <ul> <li>Better MIME checking</li> <li>Reject oversized payloads early</li> <li>Async I/O with aiofiles</li> </ul>"},{"location":"project/ideas/ideas.html#multi-library-users","title":"\ud83d\udc65 Multi-Library &amp; Users","text":"<ul> <li>Support multiple MUSIC_ROOT paths</li> <li>User roles: admin + read-only accounts</li> </ul>"},{"location":"project/ideas/ideas.html#backup-portability","title":"\ud83d\udce6 Backup &amp; Portability","text":"<ul> <li>One-click export (all mixtapes + covers as .zip)</li> <li>M3U export for other players</li> <li>Import from M3U playlists</li> </ul>"},{"location":"project/ideas/ideas.html#developer-experience","title":"\ud83d\udee0\ufe0f Developer Experience","text":"<ul> <li>Healthcheck endpoint for Docker/Kubernetes</li> <li>Podman support</li> <li>Optional Redis cache for huge libraries (&gt;50k tracks)</li> </ul>"},{"location":"project/ideas/ideas.html#tiny-delights","title":"\ud83c\udfa8 Tiny Delights","text":"<ul> <li>Keyboard shortcuts (J/K, space to play/pause)</li> <li>Click album art \u2192 enlarge lightbox</li> <li>Sleep timer in player</li> <li>Random mixtape button</li> </ul>"},{"location":"project/ideas/ideas.html#technical-improvements","title":"\ud83d\udd27 Technical Improvements","text":""},{"location":"project/ideas/ideas.html#browse-page","title":"Browse Page","text":"<ul> <li>Pagination for large libraries (currently loads all)</li> <li>Search endpoint (<code>/mixtapes/search?q=</code>)</li> <li>Bulk actions (select \u2192 delete/export)</li> <li>Thumbnail generation to reduce bandwidth</li> </ul>"},{"location":"project/ideas/ideas.html#base-ui","title":"Base &amp; UI","text":"<ul> <li>Modular CSS (split by component)</li> <li>Accessibility improvements (ARIA, focus trapping)</li> <li>Internationalization (Flask-Babel)</li> <li>Progressive enhancement (JS fallbacks)</li> </ul> <p>Got thoughts on any of these? Open a discussion or comment on issues.</p>"},{"location":"user/collection-management.html","title":"Collection Management","text":""},{"location":"user/collection-management.html#collection-management","title":"Collection Management","text":""},{"location":"user/collection-management.html#overview","title":"\ud83c\udf0d Overview","text":"<p>The Collection Management feature provides two key capabilities:</p> <ol> <li>View Statistics - Get an overview of your music library with counts of artists, albums, tracks, total duration, and information about recently added music</li> <li>Resync Library - Update your music database when you've added, removed, or modified music files</li> </ol>"},{"location":"user/collection-management.html#accessing-collection-management","title":"\ud83d\udd11 Accessing Collection Management","text":"<p>To access collection management:</p> <ol> <li>Click the Collection button in the top navigation bar</li> <li>The collection management modal will open automatically</li> </ol> <p></p> <p>Note</p> <p>You must be logged in to access collection management features.</p>"},{"location":"user/collection-management.html#collection-statistics","title":"\ud83d\udcca Collection Statistics","text":"<p>When you open the Collection Management modal, you'll see current statistics about your music library.</p>"},{"location":"user/collection-management.html#artist-count","title":"Artist Count","text":"<p>The total number of unique artists in your music collection. This includes both individual artists and bands.</p>"},{"location":"user/collection-management.html#album-count","title":"Album Count","text":"<p>The total number of unique albums across all artists. Compilation albums with various artists are counted as separate albums.</p>"},{"location":"user/collection-management.html#track-count","title":"Track Count","text":"<p>The total number of individual music tracks in your collection.</p>"},{"location":"user/collection-management.html#total-duration","title":"Total Duration","text":"<p>The combined playback time of all tracks in your library, displayed in a human-readable format:</p> <ul> <li>Days (d)</li> <li>Hours (h)</li> <li>Minutes (m)</li> </ul> <p>Example: <code>5d 3h 45m</code> means 5 days, 3 hours, and 45 minutes of total music</p>"},{"location":"user/collection-management.html#last-added","title":"Last Added","text":"<p>Shows the age of the most recently added track in your collection:</p> <ul> <li>Recent additions - Displays as relative time (e.g., \"2 hours ago\", \"3 days ago\")</li> <li>Older additions - Shows the actual date (e.g., \"Dec 15, 2024\")</li> </ul> <p>What does Last Added mean?</p> <p>This indicates when the newest file in your collection was added to your music directory (based on file modification time), not when it was added to the database. After resyncing, this reflects the youngest track in your entire collection.</p> <p>Tip</p> <p>The statistics automatically refresh each time you open the modal, ensuring you always see up-to-date information.</p>"},{"location":"user/collection-management.html#resyncing-your-library","title":"\ud83d\udd04 Resyncing Your Library","text":"<p>Resyncing is essential for keeping your music database in sync with your actual music files. You should resync whenever you make changes to your music directory.</p>"},{"location":"user/collection-management.html#when-to-resync","title":"When to Resync","text":"<p>Resync your library when you:</p> <ul> <li>Add new music - New files won't appear until after a resync</li> <li>Delete music - Removed files will still appear in the database until resynced</li> <li>Move or rename files - The database won't reflect these changes until resynced</li> <li>Update metadata - Changes to ID3 tags won't appear until after a resync</li> <li>Notice discrepancies - If statistics or search results seem incorrect</li> </ul>"},{"location":"user/collection-management.html#how-to-resync","title":"How to Resync","text":"<ol> <li>Open the Collection Management modal (click Collection in the navbar)</li> <li>Review your current statistics</li> <li>Click the Resync Library button at the bottom</li> <li>Confirm the resync operation in the confirmation dialog</li> <li>The resync process will begin in the background</li> </ol> <p>Resync Duration</p> <p>Resyncing may take several minutes depending on the size of your music collection. During this time, the application will show an indexing progress screen.</p>"},{"location":"user/collection-management.html#what-happens-during-resync","title":"What Happens During Resync","text":"<p>The resync process:</p> <ol> <li>Scans your music directory for all audio files</li> <li>Adds new files to the database</li> <li>Updates metadata for existing tracks</li> <li>Removes references to deleted or moved files</li> <li>Extracts album artwork from audio files</li> <li>Recalculates all collection statistics</li> </ol> <p>Info</p> <p>During the resync process, you'll see a progress screen showing how many files have been processed. You cannot use other features of the application until the resync completes.</p>"},{"location":"user/collection-management.html#after-resync","title":"After Resync","text":"<p>Once the resync completes:</p> <ul> <li>All statistics will reflect your current music collection</li> <li>New tracks will appear in search results</li> <li>Deleted tracks will no longer appear</li> <li>Updated metadata will be reflected throughout the application</li> </ul>"},{"location":"user/collection-management.html#understanding-the-resync-process","title":"\ud83e\udde0 Understanding the Resync Process","text":""},{"location":"user/collection-management.html#initial-indexing-vs-resync","title":"Initial Indexing vs. Resync","text":"<ul> <li>Initial Indexing - Happens automatically the first time you start the application, building the complete database from scratch</li> <li>Resync - Updates an existing database by comparing file system changes with the current database</li> </ul>"},{"location":"user/collection-management.html#what-gets-updated","title":"What Gets Updated","text":"<p>During a resync:</p> <ul> <li>\u2705 Track titles and metadata</li> <li>\u2705 Artist and album information</li> <li>\u2705 File paths and locations</li> <li>\u2705 Duration and bitrate</li> <li>\u2705 Album artwork</li> <li>\u2705 File modification times</li> </ul>"},{"location":"user/collection-management.html#what-doesnt-change","title":"What Doesn't Change","text":"<ul> <li>\u274c Mixtapes you've created (these remain intact)</li> <li>\u274c Application settings</li> <li>\u274c Your login credentials</li> </ul>"},{"location":"user/collection-management.html#common-questions","title":"\u2753 Common Questions","text":""},{"location":"user/collection-management.html#why-are-my-new-files-not-showing-up","title":"Why are my new files not showing up?","text":"<p>New files won't appear in the application until you resync your library. The database only knows about files that existed at the time of the last sync.</p>"},{"location":"user/collection-management.html#what-counts-as-a-unique-album","title":"What counts as a unique album?","text":"<p>An album is considered unique based on the combination of artist name and album title. The same album title by different artists counts as separate albums.</p>"},{"location":"user/collection-management.html#how-is-duration-calculated","title":"How is duration calculated?","text":"<p>The total duration is the sum of all track durations in your collection. If a track's duration cannot be determined from its metadata, it's excluded from the total.</p>"},{"location":"user/collection-management.html#can-i-cancel-a-resync-in-progress","title":"Can I cancel a resync in progress?","text":"<p>No, once a resync starts, it must complete. However, the process is safe and can be performed as often as needed without risk to your data.</p>"},{"location":"user/collection-management.html#will-resyncing-affect-my-mixtapes","title":"Will resyncing affect my mixtapes?","text":"<p>No, resyncing only updates the music database. Your created mixtapes and their track lists remain unchanged.</p>"},{"location":"user/collection-management.html#how-often-should-i-resync","title":"How often should I resync?","text":"<p>Resync whenever you make changes to your music collection. There's no harm in resyncing frequently - it simply ensures your database stays current.</p>"},{"location":"user/collection-management.html#troubleshooting","title":"\u26a0\ufe0f Troubleshooting","text":""},{"location":"user/collection-management.html#statistics-wont-load","title":"Statistics won't load","text":"<p>If the statistics fail to load:</p> <ol> <li>Ensure you have an active internet connection</li> <li>Try refreshing the page</li> <li>Close and reopen the modal</li> <li>Check that your music library has been indexed at least once</li> </ol>"},{"location":"user/collection-management.html#resync-fails-or-gets-stuck","title":"Resync fails or gets stuck","text":"<p>If a resync operation fails or appears stuck:</p> <ol> <li>Check the progress screen to see if it's still processing</li> <li>Wait for the current operation to complete (it may take longer for large libraries)</li> <li>Check your server logs for error messages</li> <li>Ensure your music directory is accessible and permissions are correct</li> <li>Verify you have sufficient disk space for the database</li> </ol>"},{"location":"user/collection-management.html#resync-says-already-in-progress","title":"Resync says \"already in progress\"","text":"<p>If you try to start a resync while one is already running:</p> <ol> <li>Wait for the current resync to complete</li> <li>Check the indexing progress screen</li> <li>If the process appears truly stuck, restart the application</li> </ol>"},{"location":"user/collection-management.html#statistics-seem-incorrect-after-resync","title":"Statistics seem incorrect after resync","text":"<p>If your statistics don't look right after a resync:</p> <ol> <li>Close and reopen the Collection Management modal to refresh the data</li> <li>Check that all your music files are in the configured music directory</li> <li>Verify that your audio files have valid metadata tags</li> <li>Try another resync to ensure all files were processed</li> </ol>"},{"location":"user/docker.html","title":"Production Deployment","text":""},{"location":"user/docker.html#docker-deployment","title":"Docker Deployment","text":"<p>Deploying Mixtape Society with Docker is the easiest and most reliable way to get your private mixtape server up and running\u2014perfect for home servers, VPS, or NAS devices.</p>"},{"location":"user/docker.html#why-docker","title":"\ud83d\udc0b Why Docker?","text":"Benefit Explanation Zero-setup No manual Python, <code>uv</code>, or system-package installation. The container bundles the interpreter, dependencies, and the Flask app. Portability Works on Linux, macOS, Windows (Docker Desktop) and ARM devices (Raspberry\u202fPi) without code changes. Isolation The app runs as an unprivileged user inside the container; the host file system is only accessed through explicit volume mounts. Persistence Docker volumes keep your SQLite DB, mixtape JSON files, cover images, and audio-cache across container upgrades or restarts. Automatic Updates Pull the latest image from GitHub Container Registry (<code>ghcr.io/...</code>) and restart \u2013 the new version starts instantly."},{"location":"user/docker.html#official-image","title":"\u2705 Official Image","text":"<p>The official, automatically\u2011built image lives at:</p> <pre><code>ghcr.io/mark-me/mixtape-society:latest\n</code></pre> <p>Tags are rebuilt on every release tag for <code>master</code>. Use a specific tag (e.g. <code>v1.4.2</code>) for reproducible deployments.</p>"},{"location":"user/docker.html#quickstart-oneliner","title":"\ud83d\ude80 Quick\u2011Start One\u2011Liner","text":"<p>Replace the placeholders with paths that exist on your host:</p> <pre><code>docker run -d \\\n  --name mixtape-society \\\n  --restart unless-stopped \\\n  -p 5000:5000 \\\n  -v /path/to/your/music:/music:ro \\\n  -v /path/for/data:/app/collection-data \\\n  -e APP_PASSWORD=YourStrongPassword123! \\\n  ghcr.io/mark-me/mixtape-society:latest\n</code></pre> <p>What this does</p> Flag Effect <code>-p 5000:5000</code> Exposes the Flask dev server on host port\u202f5000. <code>-v /path/to/your/music:/music:ro</code> Mounts your music library read-only (required by <code>MUSIC_ROOT</code>). <code>-v /path/for/data:/app/collection-data</code> Persists the SQLite DB, mixtapes, covers, and audio cache. <code>-e APP_PASSWORD=\u2026</code> Sets the login password (mandatory in production). <code>-e APP_ENV=production</code> Forces the <code>ProductionConfig</code> class (disables debug mode). <code>--restart unless-stopped</code> Guarantees the container restarts after crashes or host reboots. <p>After the container starts, open http://localhost:5000, log in with the password you set, and let the indexing run (check progress with <code>docker logs -f mixtape-society</code>).</p>"},{"location":"user/docker.html#full-dockercompose-setup-production-ready","title":"\ud83c\udfd7\ufe0f Full Docker\u2011Compose Setup (Production Ready)","text":"<p>Create a directory (e.g. <code>docker</code>/) and place the following two files inside it.</p>"},{"location":"user/docker.html#docker-composeyml","title":"<code>docker-compose.yml</code>","text":"<pre><code>services:\n  mixtape:\n    image: ghcr.io/mark-me/mixtape-society:latest\n    container_name: mixtape-society\n    restart: unless-stopped\n    ports:\n      - \"5000:5000\"\n    volumes:\n      - /your/music:/music:ro\n      - /data/mixtape-society:/app/collection-data\n    environment:\n      - PUID=1000\n      - PGID=1000\n      - TZ=Europe/Amsterdam\n      - PASSWORD=${APP_PASSWORD}\n      - APP_ENV=production\n      - LOG_LEVEL=INFO\n</code></pre>"},{"location":"user/docker.html#env-placed-next-to-docker-composeyml","title":"<code>.env</code> (placed next to <code>docker-compose.yml</code>)","text":"<p>Create a .env file in the same directory:</p> <pre><code># Path on the host where your music files live.\n# Example: /home/user/Music   (Linux/macOS)\n#          C:\\Users\\Me\\Music   (Windows, use forward slashes)\nMUSIC_HOST_PATH=/absolute/path/to/your/music\n\n# Strong password for the web UI (required in production!)\nAPP_PASSWORD=YourVeryStrongPasswordHere!\n</code></pre> <p>Tip</p> <p>Tip: Keep the <code>.env</code> file outside version control (<code>gitignore</code>) because it contains secrets.</p>"},{"location":"user/docker.html#starting-the-stack","title":"Starting the stack","text":"<pre><code>cd docker\ndocker compose up -d   # pulls the image, creates the volume, and starts the service\n</code></pre>"},{"location":"user/docker.html#updating-to-a-newer-version","title":"Updating to a newer version","text":"<pre><code>docker compose pull          # fetch latest image\ndocker compose up -d --no-deps --force-recreate mixtape   # restart only the app container\n</code></pre>"},{"location":"user/docker.html#configuration-mapping-env-filesystem","title":"\ud83d\udd04 Configuration Mapping (Env\u202f\u2194\u202fFilesystem)","text":"Config variable (Python) Docker-Compose / docker run mapping Default (if omitted) Where it ends up on the host APP_ENV <code>-e APP_ENV=production</code> (or in <code>.env</code>) <code>development</code> Controls which subclass (<code>ProductionConfig</code>) is used. MUSIC_ROOT Volume mount <code>-v /host/music:/music:ro</code> <code>/music</code> The container sees the music library at <code>/music</code>. DATA_ROOT Volume mount <code>-v mixtape_data:/app/collection-data</code> (named volume) <code>../collection-data</code> (relative to repo) Holds <code>collection.db</code>, <code>mixtapes/</code>, <code>cache/audio/</code>. PASSWORD (APP_PASSWORD) <code>-e APP_PASSWORD=\u2026</code> (or in <code>.env</code>) <code>dev-password</code> (dev) / <code>test-password</code> (test) Required in production; used for login. LOG_LEVEL (optional) <code>-e LOG_LEVEL=INFO</code> <code>INFO</code> Controls the verbosity of the Flask logger. TZ (optional) <code>-e TZ=UTC</code> System timezone Affects timestamps shown in the UI (<code>now</code> context variable). <p>All derived paths (<code>DB_PATH</code>, <code>MIXTAPE_DIR</code>, <code>COVER_DIR</code>, <code>AUDIO_CACHE_DIR</code>) are automatically calculated from <code>DATA_ROOT</code> inside the container, so you never need to set them manually.</p>"},{"location":"user/docker.html#volume-layout-explained","title":"\ud83d\udce6 Volume Layout Explained","text":"<p>Inside the container (<code>/app/collection-data</code>):</p> <pre><code>/app/collection-data/\n\u251c\u2500 collection.db                # SQLite DB with indexed music metadata\n\u251c\u2500 mixtapes/\n\u2502   \u251c\u2500 &lt;slug&gt;.json             # One JSON file per mixtape\n\u2502   \u2514\u2500 covers/\n\u2502       \u2514\u2500 &lt;slug&gt;.jpg          # Optional cover image for each mixtape\n\u2514\u2500 cache/\n    \u2514\u2500 audio/\n        \u2514\u2500 &lt;artist&gt;/&lt;album&gt;/&lt;track&gt;.mp3   # Cached/transcoded audio files\n</code></pre> <p>On the host, the named volume <code>mixtape_data</code> maps to a directory managed by Docker (usually under<code>/var/lib/docker/volumes/...</code>). You can inspect it with:</p> <pre><code>docker volume inspect mixtape_data\n</code></pre> <p>If you prefer a bind\u2011mount (easier to explore manually), replace the volume line with:</p> <pre><code>- /path/on/host/collection-data:/app/collection-data\n</code></pre> <p>Make sure the host directory is writable by the UID/GID the container runs as (default <code>1000:1000</code>).</p>"},{"location":"user/docker.html#running-behind-a-reverse-proxy-https-domain","title":"\ud83c\udf10 Running Behind a Reverse Proxy (HTTPS &amp; Domain)","text":"<p>For external access you\u2019ll typically terminate TLS at a reverse proxy (Traefik, Nginx Proxy Manager, Caddy, TSDProxy, etc.). Below is a minimal Traefik example that assumes you already have Traefik running on the same Docker network.</p>"},{"location":"user/docker.html#traefik","title":"Traefik","text":""},{"location":"user/docker.html#add-labels-to-docker-composeyml","title":"Add Labels to docker-compose.yml","text":"<pre><code>services:\n  mixtape-society:\n    # ... existing config\n    labels:\n      - \"traefik.enable=true\"\n      - \"traefik.http.routers.mixtape.rule=Host(`mixtape.yourdomain.com`)\"\n      - \"traefik.http.routers.mixtape.entrypoints=websecure\"\n      - \"traefik.http.routers.mixtape.tls.certresolver=myresolver\"\n</code></pre> Label Meaning <code>traefik.enable=true</code> Expose this container to Traefik. <code>rule=Host(...)</code> Route requests for your domain to this service. <code>entrypoints=websecure</code> Use the TLS entrypoint (usually port\u202f443). <code>tls.certresolver=myresolver</code> Let Traefik obtain a Let's Encrypt certificate automatically. <code>loadbalancer.server.port=5000</code> Traefik forwards to the Flask container\u2019s internal port\u202f5000. <p>Explanation</p>"},{"location":"user/docker.html#minimal-docker-composeyml-snippet","title":"Minimal docker-compose.yml Snippet","text":"<pre><code>services:\n  traefik:\n    image: traefik:v2.11\n    command:\n      - \"--api.insecure=true\"\n      - \"--providers.docker=true\"\n      - \"--entrypoints.web.address=:80\"\n      - \"--entrypoints.websecure.address=:443\"\n      - \"--certificatesresolvers.myresolver.acme.email=you@example.com\"\n      - \"--certificatesresolvers.myresolver.acme.storage=/letsencrypt/acme.json\"\n      - \"--certificatesresolvers.myresolver.acme.tlschallenge=true\"\n    ports:\n      - \"80:80\"\n      - \"443:443\"\n      - \"8080:8080\"   # Traefik dashboard\n    volumes:\n      - \"/var/run/docker.sock:/var/run/docker.sock:ro\"\n      - \"letsencrypt:/letsencrypt\"\n    restart: unless-stopped\n\nvolumes:\n  letsencrypt:\n</code></pre> <p>Now visiting <code>https://mixtape.yourdomain.com</code> will present a secure HTTPS site with a valid certificate.</p>"},{"location":"user/docker.html#tsdproxy","title":"TSDProxy","text":"<p>If you prefer to terminate TLS and handle routing with tailscale and TSDProxy (a lightweight, Docker\u2011native reverse\u2011proxy that works similarly to Traefik/Nginx Proxy Manager), you can expose the Mixtape\u202fSociety container behind it with just a few environment variables and labels.</p>"},{"location":"user/docker.html#add-tsdproxy-labels-env-vars-to-docker-composeyml","title":"Add TSDProxy Labels / Env Vars to docker-compose.yml","text":"<pre><code>services:\n  mixtape:\n    image: ghcr.io/mark-me/mixtape-society:latest\n    container_name: mixtape-society\n    restart: unless-stopped\n    ports:\n      - \"5000:5000\"                     # internal Flask port (kept private)\n    volumes:\n      - ${MUSIC_HOST_PATH}:/music:ro\n      - mixtape_data:/app/collection-data\n    environment:\n      - APP_ENV=production\n      - APP_PASSWORD=${APP_PASSWORD}\n      # ==== TSDProxy integration ====\n      - TSDPROXY_ENABLE=true                     # tell TSDProxy to watch this container\n      - TSDPROXY_HOST=mixtape.yourdomain.com    # the public hostname\n      - TSDPROXY_HTTP_PORT=5000                 # internal port to forward to\n      - TSDPROXY_TLS_EMAIL=you@example.com      # email for Let\u2019s\u202fEncrypt\n    # Alternatively you can use Docker **labels** instead of env vars:\n    # labels:\n    #   - \"tsdproxy.enable=true\"\n    #   - \"tsdproxy.host=mixtape.yourdomain.com\"\n    #   - \"tsdproxy.port=5000\"\n    #   - \"tsdproxy.tls.email=you@example.com\"\n</code></pre> <p>What each variable does</p> Variable / Label Meaning <code>TSDPROXY_ENABLE=true</code> Marks the container as a candidate for proxying. <code>TSDPROXY_HOST=mixtape.yourdomain.com</code> The DNS name that will resolve to your server\u2019s public IP. <code>TSDPROXY_HTTP_PORT=5000</code> The internal port on which the Flask app listens (the port you expose above). <code>TSDPROXY_TLS_EMAIL=you@example.com</code> Email address used by Let\u2019s Encrypt for certificate issuance and renewal notices. <p>Note</p> <p>TSDProxy reads either environment variables (as shown) or Docker labels prefixed with <code>tsdproxy..</code> Pick whichever style fits your workflow.</p>"},{"location":"user/docker.html#deploy-the-tsdproxy-container","title":"Deploy the TSDProxy Container","text":"<p>Create a separate <code>docker-compose.tsdproxy.yml</code> (or add it to the same file) that runs the proxy:</p> <pre><code>services:\n  tsdproxy:\n    image: ghcr.io/tsdproxy/tsdproxy:latest\n    container_name: tsdproxy\n    restart: unless-stopped\n    ports:\n      - \"80:80\"          # HTTP \u2192 redirect to HTTPS\n      - \"443:443\"        # HTTPS entry point\n    volumes:\n      - tsdproxy_data:/etc/tsdproxy   # persistent config &amp; cert storage\n    environment:\n      - TZ=UTC\n    networks:\n      - default   # ensure it shares the same network as the mixtape service\n\nvolumes:\n  mixtape_data:\n  tsdproxy_data:\n</code></pre> <p>Key points</p> <ul> <li>TSDProxy automatically discovers containers on the same Docker network that have <code>TSDPROXY_ENABLE=true</code> (or the matching label).</li> <li>It will request a Let\u2019s\u202fEncrypt certificate for the hostname you supplied (<code>mixtape.yourdomain.com</code>).</li> <li>Certificates are stored in the <code>tsdproxy_data</code> volume, so they survive container restarts and updates.</li> </ul>"},{"location":"user/docker.html#common-gotchas-troubleshooting","title":"\u26a0\ufe0f Common Gotchas &amp; Troubleshooting","text":"Symptom Likely Cause Fix First indexing hangs forever The music library is huge or the container lacks permission to read it. Verify the <code>/music</code> mount is read-only and the UID inside the container (default 1000) can traverse the host directory. Check logs: <code>docker logs -f mixtape-society</code>. Permission denied when writing covers DATA_ROOT volume owned by root. Ensure the named volume or bind-mount is owned by UID\u202f1000 (or set <code>PUID</code>/<code>PGID</code> env vars and adjust the Dockerfile accordingly). Cannot reach the app on port\u202f5000 Port not published or firewall blocks it. Confirm <code>-p 5000:5000</code> (or the <code>ports:</code> entry in compose) and that your host firewall allows inbound traffic. Database corruption after abrupt shutdown Container killed with SIGKILL while SQLite was writing. Use Docker\u2019s graceful stop (<code>docker stop</code>) or configure <code>restart: unless-stopped</code>. SQLite is robust, but a clean shutdown is safest. Audio cache not being created <code>AUDIO_CACHE_ENABLED</code> set to False or <code>AUDIO_CACHE_PRECACHE_ON_UPLOAD</code> disabled. Verify those env vars (defaults are True). Cover images not showing Wrong <code>COVER_DIR</code> mount or missing <code>covers/</code> sub-folder. The container automatically creates <code>covers/</code> under <code>MIXTAPE_DIR</code>. Ensure the volume is persistent and not overwritten on each <code>docker compose up</code>. SSL handshake errors behind proxy Proxy terminates TLS but forwards HTTP to Flask on the wrong port. Make sure the proxy forwards to port\u202f5000 (the Flask server) and that the <code>X-Forwarded-Proto</code> header is respected (Flask handles it automatically)."},{"location":"user/docker.html#building-a-custom-image-optional","title":"\ud83d\udee0\ufe0f Building a Custom Image (Optional)","text":"<p>If you need to add extra Python packages, modify the UI, or pin a specific commit, you can build your own image:</p> <pre><code># Dockerfile (place in project root)\nFROM python:3.11-slim\n\n# Install system deps required by Pillow, ffmpeg, etc.\nRUN apt-get update &amp;&amp; apt-get install -y \\\n    libjpeg-dev zlib1g-dev libffi-dev libssl-dev \\\n    ffmpeg &amp;&amp; rm -rf /var/lib/apt/lists/*\n\n# Set a non\u2011root user (mirrors the official image)\nARG USER_ID=1000\nARG GROUP_ID=1000\nRUN groupadd -g ${GROUP_ID} appgroup &amp;&amp; \\\n    useradd -m -u ${USER_ID} -g ${GROUP_ID} appuser\n\nWORKDIR /app\nCOPY . /app\n\n# Install uv (fast Python package manager) and dependencies\nRUN pip install --no-cache-dir uv &amp;&amp; \\\n    uv sync --no-dev --frozen-lockfile\n\n# Switch to non\u2011root user\nUSER appuser\n\nEXPOSE 5000\nCMD [\"python\", \"app.py\"]\n</code></pre> <p>Build and push:</p> <pre><code>docker build -t ghcr.io/yourname/mixtape-society:custom .\ndocker push ghcr.io/yourname/mixtape-society:custom\n</code></pre> <p>Then reference the custom tag in your <code>docker-compose.yml</code>.</p>"},{"location":"user/editor.html","title":"Editor Guide","text":""},{"location":"user/editor.html#the-mixtape-editor","title":"The Mixtape Editor","text":"<p>The Mixtape Editor lets you craft personal mixtapes by searching your music library, adding tracks, reordering them, and customizing the cover and title. It evokes the joy of making a real cassette for a friend\u2014simple, intuitive, and fun.</p> <p></p>"},{"location":"user/editor.html#features","title":"\u2728 Features","text":"<ul> <li>Live search with result highlighting</li> <li>Drag-and-drop track reordering (Sortable.js)</li> <li>Automatic cover extraction from the available cover art.</li> <li>Manual cover upload (drag or paste)</li> <li>Title auto-sanitizes to create clean URLs</li> <li>One-click \u201cSave\u201d \u2192 instantly get a public link</li> </ul>"},{"location":"user/editor.html#how-to-create-a-mixtape","title":"\ud83c\udfb6 How to Create a Mixtape","text":"<ol> <li> <p>Start a New Mixtape</p> <p>Log in and click \"New Mixtape\" on the browse page. This opens the blank editor.</p> </li> <li> <p>Search and Add Tracks</p> <p>Type in the search bar to find songs, artists, or albums. Results highlight matches live for quick scanning.</p> <p>Click a track to add it to your mixtape.</p> <p>Pro Tip</p> <p><code>Escape</code> \u2013 Clears the search field</p> </li> <li> <p>Reorder Tracks</p> <p>Drag and drop tracks in the playlist to perfect the flow (powered by Sortable.js).</p> <p>Pro Tip</p> <p>Start with a strong opener track\u2014the app uses its album art as the default cover for that perfect first impression.</p> </li> <li> <p>Customize the Cover Art</p> <p>The app auto-extracts cover art from the first track's album.</p> <p>Upload manually.</p> </li> <li> <p>Add liner notes</p> <p>What would you want your friend to know about the mixtape?</p> <p>You can refer to tracks by using <code>#1</code> or ranges of tracks <code>#1-4</code>, which will turn into track titles and artist names when viewing the mixtape.</p> </li> <li> <p>Add a Title and Save</p> <p>Enter a title (it auto-generates a clean, URL-friendly slug).</p> <p>Click Save \u2013 your mixtape saves instantly, and you get a public share link.</p> <p>Pro Tip</p> <p><code>Ctrl + Enter</code> also saves your mixtape</p> </li> </ol> <p>Once saved, head to the Sharing page to learn how to send your mixtape to friends. Enjoy the magic! \ud83d\ude80</p>"},{"location":"user/getting-started.html","title":"Overview","text":""},{"location":"user/getting-started.html#getting-started","title":"Getting Started","text":"<p>Launch your own Mixtape Society server and start crafting mixtapes from your music library.</p> <p>We recommend Docker for most users: it is the quickest and most reliable way.</p>"},{"location":"user/getting-started.html#quickest-way-one-command-docker-run","title":"\ud83d\ude80 Quickest Way: One-Command Docker Run","text":"<pre><code>docker run -d \\\n  --name mixtape-society \\\n  --restart unless-stopped \\\n  -p 5001:5000 \\\n  -v /path/to/your/music:/music:ro \\\n  -v /data/mixtape-society:/app/mixtapes \\\n  -v /data/mixtape-society:/app/collection-data \\\n  -e APP_PASSWORD=YourStrongPassword123! \\\n  ghcr.io/mark-me/mixtape-society:latest\n</code></pre> <p>\u2192 Open http://localhost:5000, log in with your password, and let it index your library (check logs if needed: docker logs -f mixtape-society).</p>"},{"location":"user/getting-started.html#prefer-docker-compose-or-more-options","title":"\ud83c\udfd7\ufe0f Prefer Docker Compose or More Options?","text":"<p>See the full Docker Deployment guide for:</p> <ul> <li>Docker Compose examples (with .env secrets)</li> <li>Persistent volumes setup</li> <li>HTTPS via reverse proxy</li> <li>Troubleshooting &amp; tips</li> </ul>"},{"location":"user/getting-started.html#local-development-for-contributors","title":"\ud83d\udee0\ufe0f Local Development (for contributors)","text":"<pre><code>git clone https://github.com/mark-me/mixtape-society.git\ncd mixtape-society\nuv sync\ncp .env.example .env  # Edit MUSIC_ROOT and APP_PASSWORD\nuv run python app.py\n</code></pre> <p>Opens at http://localhost:5000 (dev password: <code>dev-password</code>).</p> <p>First run auto-indexes your library.</p> <p>Enjoy the mixtape magic! \ud83d\ude80</p>"},{"location":"user/sharing.html","title":"Sharing & Public Links","text":""},{"location":"user/sharing.html#sharing-mixtapes","title":"Sharing Mixtapes","text":"<p>Sharing a mixtape with friends or family with Mixtape Society recreates the joy of handing over a physical cassette\u2014personal, direct, and full of heart.</p> <p>How Sharing Works</p> <ol> <li> <p>Save Your Mixtape</p> <p>In the editor, click Save (or Ctrl + Enter). The app instantly generates a unique, permanent public link.</p> </li> <li> <p>Share it</p> <p>After saving, a shareable URL or QR code appears (e.g., https://yourdomain.com/share/my-favorite-songs_20251217_abcd1234). You can copy the link or QR code or download the QR code with cover image and title.</p> </li> <li> <p>Send It Anywhere</p> <p>Share via email, text, social media, or messaging apps\u2014no app download or account required for recipients.</p> </li> <li> <p>What Recipients See</p> <p>Anyone with the link opens a beautiful, standalone player: full tracklist, cover art, play/pause, seeking, and next/previous controls. It works perfectly on desktop, phone, or tablet.</p> </li> </ol>"},{"location":"user/sharing.html#key-features","title":"\u2728 Key Features","text":"<ul> <li>No login required \u2014 Recipients stream directly in their browser.</li> <li>Permanent links \u2014 They never expire (unless you delete the mixtape).</li> <li>Privacy control \u2014 Links are unguessable; only people you share with can access.</li> <li>Mobile-friendly \u2014 Responsive design for easy listening on the go.</li> <li>QR codes \u2014 for quick scanning .</li> </ul> <p>Pro Tip</p> <p>Pair your link with a personal message: \"Made this mixtape thinking of you\u2014hope it brings back good memories! \ud83c\udfa7\"</p> <p>Sharing is the heart of Mixtape Society. Go make someone's day with your perfect playlist! \ud83d\ude80</p>"}]}